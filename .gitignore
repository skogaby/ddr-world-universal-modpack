/target
/updater/target
/tools/bot_sim/target
# bot_sim default report/JSON outputs (generated, machine-local)
/bot_sim_report.html
/bot_sim_report.json
/release
**/.DS_Store
capture/
mod_assets/
**/__pycache__/
log.txt
*.arc
# The one place arcs ARE repo content: the Background Dancers custom
# dancer/stage packs (add-on-authored models, never Konami data).
!/data_mods/custom_models/**/*.arc
/tests/

# Claude Code
.claude/settings.local.json

# Agents sometimes store build logs locally for checking the outputs later on
/.agents/**/logs

# Shader build intermediates (committed artifacts are the .gsp under data_mods/)
/shaders/build/
**/shader_dump/
