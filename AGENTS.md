# AGENTS.md

## Project Summary

Rust hook DLL (`cdylib`, `x86_64-pc-windows-msvc`) for DanceDanceRevolution World arcade. Loaded into the live game process via spice2x's `-k` flag. Installs inline function hooks against `gamemdx.dll` and sibling Konami DLLs (`libafp-win64`, `libafputils-win64`, `libavs-win64`, `arkmdxbio2`) to add mods that render through the game's own UI pipeline — widgets are native game objects, not overlays.

Almost every interesting constraint in this codebase comes from running inside someone else's process: allocator matching, FFI calling conventions, no panics across `extern "C"`, no hardcoded offsets (everything is AOB-scanned or RTTI-walked so it survives game updates).

## Directory Map

```
src/
├── lib.rs                  # DLL entry point, full init sequence, splash screen
├── core/                   # Game-agnostic infrastructure (scanner, hooks, memory, formats)
├── services/               # Game system integrations (one per game subsystem)
│   ├── avs_layeredfs/      # Transparent file replacement (5 AVS hooks + texture pipeline)
│   ├── custom_options/     # Framework for injecting mod option rows into native UI
│   └── *.rs                # widget_renderer, scene_manager, input_manager, judge_hook, etc.
├── mods/                   # Mod implementations (each implements Mod trait)
│   ├── note_types_expansion/  # Multi-file: mines, render pass, SSQ parsing
│   ├── webui_options/         # Multi-file: cosmetic options + asset discovery
│   ├── fast_bootup/           # Multi-file: loader pacing + boot analysis cache
│   └── *.rs                   # Single-file mods
├── widgets/                # TextWidget, ImageWidget — native game UI wrappers
└── types/                  # Shared constants (scenes, buttons)

scripts/
├── deploy.sh              # Build + SCP to cabinet
├── build_ddr_package/     # Python: PNG → ARC texture packs
├── convert_movies.sh      # Batch VC-1→H.264 movie transcode for Wine/CrossOver (idempotent, .bak originals, --restore)

docs/                      # RE research notes (addresses are file-relative to 0x180000000)
```

## Key Entry Points

| Task | Start Here |
|------|-----------|
| Add a mod | `src/mods/mod_trait.rs` (trait), then register in `src/lib.rs` |
| Add a service | `src/services/mod.rs`, init call in `src/lib.rs` |
| Add a signature | `src/core/signatures.rs` — then run `./scripts/validate_signatures.sh <dir-of-gamemdx-builds>` (below) before deploying |
| Cross-build signature sweep (offline, all 4 supported builds) | `scripts/validate_signatures.sh <dir>` mounts the DLL's REAL `scanner.rs` + `signatures.rs` into a host crate and runs `resolve_all` + `resolve_derived` over every `gamemdx*.dll` in `<dir>`, PE-mapped like the loader (sections at VAs, DIR64 relocs applied, libafp IAT slots emulated from ONE `libafp-win64*.dll` in the same dir — gamemdx imports libafp by ORDINAL); `scripts/sig_harness/report.py` joins the per-build `[+]/[-]` lines with the crate's consumer graph (required_signatures / get_address / require_address) → per-mod HARD/soft losses; exit 0 = every miss covered by a version alternate (`_vN` stem or `report.py::ALT_GROUPS`). `scripts/sig_harness/shape_diff.py --json … --dir …` then disassembles a 0x200 window after every match on every build (capstone; RIP/rel32/module-RVA displacements normalized) and reports the first divergence offset + consumers — **an AOB hitting on all builds proves nothing about the bytes a consumer reads at `match+N` or the struct offsets it hardcodes**; review every divergent signature against its consumer's reads. Non-address derivations (offsets, vslots, kind ids) go through `SignatureStore::publish_value`/`published_value` (the `player_option_offset` convention) so they show in the boot log as `name (derived) = 0x…`. Build set: `~/Desktop/ddr_modules` (20250805 / 20260224 / 20260721 / 20260825). Results + findings: `docs/cross_build_signature_sweep.md` |
| Build-dependent actor layouts (derived, NEVER hardcode) | **GamePlayActor**: every field ≥ ~`+0x208` sits 8 bytes LOWER on 20250805/20260224 than on 20260324+ (≤ `+0x1E9` identical) — `signatures.gameplay_actor_layout()` (from the ctor seed block; speed cluster `0x288/0x290`, gauge cluster `0x298/0x2A0`, death gate/result `0x2AF,0x2B0 / 0x2B7,0x2B8`) feeds `song_rate::real_speed`, `song_reset` (unavailable without it), `quick_restart_or_fail` fallback death. **ShutterActor**: kind fields `+0x2E0/+0x2E4` + 6 kinds + STAGE kind **1** on old builds vs `+0x310/+0x314` + 9 kinds + kind **3** — `signatures.shutter_actor_layout()` (onUpdate kind/layer lookup + stage-kind CMP) gates quick_restart_or_fail's fast path. **music-DB entry `hasChart` vslot** `+0x58` old / `+0x70` new (`+0x70` is `isShock` on old — same arg shape, silently wrong) — `signatures.entry_has_chart_vslot()` gates fast_bootup replay. Already-derived siblings: `PlayerWork` record base `0x570/0x590` (`stage_record_accessor_v1`), `ddr::player::Option` `0xF0/0xE0` (`player_option_offset`), judge foot-panel ptr `0x270/0x278` (`judge_hook` detection) |
| Understand init order | `src/lib.rs::init()` — sequence is load-bearing. Config + **LayeredFS install at step 0b, BEFORE the gamemdx wait and the AOB scan**: the game's `Application::onBoot` drains `shader.arc` (the session's only shader read — shader-fixes/theme synthesis rides it) and loads `musicdb.xml` on its own thread within a few hundred ms of gamemdx loading; a Win7 cabinet lost that race when LayeredFS still installed after the scan (2026-09-03). Never move LayeredFS back below `resolve_all`. Race detector: `overlay_draw::check_shader_arc_race` WARN + `shader_synthesis::status()` |
| LayeredFS file hooks | `src/services/avs_layeredfs/file_hooks.rs` |
| Flare-ranking exclusion for custom series (series ≥ 22 → no flare category) | `src/mods/series_expansion.rs` patch 7 — the `flare_skill_classifier` AOB (`signatures.rs`) matches the inlined classification walk in `ddr::player::Record::CalcFlareSkill` (raw series ≥ 18 GOLD / ≥ 14 WHITE / ≥ 1 CLASSIC, GOLD unbounded above). `init_flare_exclusion` copies the stock 3-entry cat/threshold tables into a near-alloc 4-entry block prepending `≥ 22 → category 0` (the game's never-summed bucket); enable rewrites the walk's two **module-base-relative** disp32s (match+20/+28 — `target − module_base`, NOT `rip_disp`) and the loop-bound imm8 (match+41, `F8`→`F4`). Fail-open: missing AOB or table validation ⇒ stock behavior, one WARN. The other 4 classification sites (score-window tab, sort comparator ×2, category label — UI only) are intentionally unpatched pending user testing. RE notes: `docs/flare_ranking_research.md` |
| Judge hook subscribers | `src/services/judge_hook.rs` |
| S-Marvelous Judgement (presentation-layer judgement above Marvelous — engine grade space untouched, all display native) | `src/mods/s_marvelous/` (id `s-marvelous`, config `s_marvelous.window_ms` clamp 1..=16 default 12 — STRICTLY below stock Marvelous ±17; also live-editable via the overlay GLOBAL SETTINGS scalar row `smarv_window_ms` under the auto S-MARVELOUS JUDGEMENT header, which persists the section back and applies NEXT song — the armed window is read only at play-scene entry) — classification in the shared `judge_submit` tap (`power_user_statistics::data_feed`, pre-original; ALL display re-drives post-original — the grade dispatch is synchronous). **Arming = play scenes GAMEPLAY ∪ ATTRACT_DEMO** (`is_play_scene`; the attract autoplay rides the same actor chain, so the demo shows the full S-Marv presentation) and `state::combo_is_all_smarv` REQUIRES `is_armed` — a disarmed side's loose-marv bit is trivially false, and the bare `!bit` painted a violet combo under a stock white word during attract (2026-09-01). Surfaces (each fail-open): gameplay flash `flash.rs` (0xF09 `in_smarvelous` on the NoteResultActor's own wrapper), combo digits `combo.rs` (repaint + violet tint via wrapper SetColor vfunc), S-MFC splash `splash.rs`, **Marvelous FAST/SLOW** `fast_slow.rs` (rule: the HIGHEST tier is exempt from FAST/SLOW — stock Marvelous, now S-Marvelous. ONE-byte patch: the NoteResultActor grade-case gate `CMP [RDI+0x94],0; JZ hide` excludes grade 0 from the `dance_fast_slow` show path — signature `note_result_fast_slow_gate`, unique/byte-identical on all four builds; imm8 at match+15 `00→FF` on enable (grade ∈ 0..=5 ⇒ never equal), restored on disable; delta==0 hide kept; respects the player's FAST/SLOW option since the clip is only created when it's on. S-Marv events re-hide the clip post-original via `hide_for_smarvelous` from `flash::on_smarvelous` — the gate can't see S-Marv), results score tab `results_score.rs` (7-row STOCK-NAME sheet swap — purged at init/disable so art and row positions move together — + translate-only row moves + the game's OWN row-write helper + exclusive-MARVELOUS glyph rewrite + **FAST/SLOW widgets (`fast_usr/num_usr`/`slow_usr/num_usr`) rewritten to stock + the Marvelous share** (`records::count_marv_fast_slow(grades, ms, window)`: grade-0 slots OUTSIDE the S-Marv window, ms<0 fast / >0 slow; `smarv + fast + slow == marv` host-tested) — the record's `+0x6C/+0x70` counters and thus the score save stay stock; counts recomputed fail-closed from record streams, judged-slots-only), judgement graph `results_graph.rs` (three detours riding the game's rebuild/append/legend fns; violet series LEADS the stack, legend reads "■MARVELOUS" in violet FIRST — maintainer art language: stock wording, violet hue 0xB05CE0FF, no "S-" prefix; its per-beat FAST/MARVELOUS/SLOW statistics box is intentionally stock — Marvelous has its own column there), FC emblems `results_emblem.rs` (`loop_smfc` clone in `result_root` with HSL-rotation records dropped + loop DoAction retargeted; total badge `scre_total_player_fc_smfc`). AFP machinery: `core/ap2/` (byte-identical parser/serializer — label tables SORTED ON WRITE, engine binary-searches them — + editing recipes incl. `SegmentCloneOpts` and the split-dictionary clone) + `core/msvc.rs` (cabinet-proven MSVC ABI structs — vector<string> elements are 0x28-STRIDE, never re-derive). Host validation: `scripts/validate_s_marvelous.sh` (Legs A–G run the DLL's ACTUAL recipes on real templates). Art sources: `data_mods/s_marvelous/` (generated `*_ifs/` dirs are enable-time — never commit). RE: `docs/s_marvelous_judgement_research.md` (+ 2026-09-01 addendum) + `.agents/planning/2026-08-29-s-marvelous-judgement/research/display-side-re.md` |
| Auto-Calibration (one-song SOUND_OFFSET auto-sync, StepMania-AutoSync style) | `src/mods/timing_offsets/calibration.rs` (sub-feature of timing-offsets; overlay GLOBAL SETTINGS enum row `timing_calibrate_next` "Calibrate next song?", first row of the section) — zero new detours. Arm is IN-MEMORY only (never persisted, OFF at boot); D16 one-rule: ANY song ending while ON flips it OFF (success/failure/refusal), via idempotent row re-registration. GAMEPLAY-entry guards: exactly-one entered side (`compute::census` over `stage_records::side_entered`; 2P ⇒ 3 s refusal toast) + rate 100 % (`clock_patch::snapshot().is_non_identity_commit()`, re-checked at apply). Measurement: a lock-free tap in `power_user_statistics::data_feed`'s `judge_submit` hook (install is IDEMPOTENT — timing_offsets' init calls it too, so calibration works with PUS disabled; grades M–Boo only — Miss excluded — of the armed side; disarmed cost = one relaxed load). Apply at GAMEPLAY exit: `compute::compute` (pure, host-tested — ≥30 samples, |mean| ≤ 500 ms, delta = mean.round() so the toast delta == written delta, clamp ±1000) → `set_offset(SOUND)` → **`refresh_overlay_row(SOUND_IDX)`** (the overlay row's displayed value only updates through menu edits — without the refresh a later edit steps from the stale value; cabinet-caught) → 5 s `CALIBRATED: old -> new (±N MS)` toast + INFO old/mean/count/stddev/new. Autoplay guard reads `score_guard::is_autoplay_tainted` (autoplay ALONE — NOT `is_stage_suppressed`, whose quick-fail/training/assist-tick/rate bits are score-policy taints with humanly-real steps; cabinet-caught misattribution). During the calibration song: pulsing "Calibrating..." toast (`services/toast` — promoted from training_mode; `flash`/`flash_with_hold`/`show_pulsing`, pure curves in `toast/curve.rs`) + ALL judgement feedback hidden (D18 `overlay_element_styling::set_calibration_hide` — opacity-0 override inside its two opacity read paths, fail-open WARN when that mod is off; D19 `power_user_statistics::set_calibration_suppress` — timing-stats widget + pacemaker→ms-error swap). Sign model cabinet-verified: `new = old + round(mean)`, positive mean (late) raises the offset. `song_reset` ⇒ counters cleared, session survives. Host tests: `scripts/validate_auto_calibration.sh` (mounts `toast/curve.rs` + `timing_offsets/compute.rs`). Planning: `.agents/planning/2026-08-26-auto-calibration/` |
| Fast Bootup + boot-analysis cache (ultrafast boot) | `src/mods/fast_bootup/` (id `fast-bootup`, single toggle) — two layers. **(1) Loader pacing:** the `update_hook` detour on `CheckStepDataActor::onUpdate` (`check_step_data_update`, vtable[6]) batch-processes ready items per frame + raises the step-data manager's per-pump open cap `mgr+0x70` 4→64 during the boot pass (restore on done-flag/disable; dev A/B env `DDR_FAST_BOOT_OPEN_CAP`; ~2.9× on cache-miss boots). Both original crash fixes stay on the miss path (bounded chunk-walk readiness gate + cursor-bounds/done-flag gate). **(2) Analysis cache:** the boot pass's entire persistent write-set (music-DB entry fields per `idx=difficulty+mode*5` — max/core/min BPM @ `+0x98/+0xC0/+0xE8`, song-wide u16 BPM @ `+0x94/+0x96`, shock/variable-BPM/`result[4]` flags @ `+0x11A/+0x124/+0x12E`, EX @ `+0x1B4`, corruption flag @ `+0x1B0`, plus the actor's **per-side** radar accumulators — side 0 `+0xA8..+0xB8`, side 1 `+0xBC..+0xCC`) is a pure function of the SSQ bytes, so it's cached to `data_mods/_cache/step_data/v1.bin`. First boot captures the game's own analyzer outputs (`result[14]`/`radar[5]`/`ret`) at the shared `services::analyze_hook` Analyze boundary; later boots inject the cached values via the derived `find_music_by_mcode` and skip the reads+analysis for unchanged charts (SSQ window collapses to the single final item — cabinet-measured 2500 ms → ~42 ms pass, 1 SSQ open). Modules: pure host-tested `cache.rs` (bin v1 + identity/merge helpers), `replay.rs` (`compute_slot`/`fold_radar`), `plan.rs` (flip-safety invariants); impure `identity.rs` (host-`std::fs` resolve + off-thread verifier → replay index) + `capture.rs` (boot-gated Analyze subscriber + store + completion writer thread, tmp+rename). Derived addrs in `signatures.rs::derive_ultrafast_boot`: `music_db_global`/`variable_bpm_threshold`/`find_music_by_mcode`/`step_data_release`. **Safety (D9):** replay writes ONLY the `+0x1B0` flag, NEVER the `ME1529`/`FILE CORRUPTION ERROR` reporter (hard boot blocker); `plan::compute` flips a record 1→6 ("complete, empty" shape) only when ALL its items replay AND it's still status 1, and the final item is always Stock so the game's completion block runs natively. Identity = game path + LayeredFS-resolved backing file + size + mtime; header invalidators = cache-format version + gamemdx PE stamp/size (NOT NTX config). Fail-open everywhere (missing bin/derivation/per-item ⇒ plain loader-acceleration, one WARN). Delete `v1.bin` to force a rebuild. Host tests: `scripts/validate_fast_bootup.sh`. RE notes: `docs/ultrafast_boot_research.md`; planning: `.agents/planning/2026-08-24-ultrafast-boot/` |
| Split SSQ Auto-Discovery (replace the game's hardcoded split-chart song table with disk discovery — old `gamemdx.dll` + newer chart data) | `src/mods/split_ssq_auto_discovery/` (id `split-ssq-auto-discovery`, no config, default ON) — ONE `GenericDetour` on `build_ssq_path` (`void(char out[0x100], const char* basename, int d 0..4)`; AOB `build_ssq_path` = prologue + first `acef` cell, unique on all four builds; the callback reads NOTHING at `match+N` — the body grows 0x3A9→0x70F with the table). All four game consumers (`CheckStepDataActor::onInit` boot pass ~7200 calls, `DancePlaySequence`/`MatchingDancePlaySequence::onSetup`, `PlayerCourseWork::prepare`) go through it. `enable()` scans stock `data/mdb_apx/ssq/` ∪ every LayeredFS mod folder for `*_[1-5].ssq` SYNCHRONOUSLY (content read via `find_first_modfile` else stock — ~40 files, header walk only) and builds the pure Rule-A index (`resolver.rs`, host-tested via `scripts/validate_split_ssq.sh`): highest `N ≤ d+1` whose file has a level-`d` type-3 chunk (either mode; level = `param2>>8` ∈ `04/01/02/03/06`), else the unsplit file — reproduces stock on all 39 installed split files (sole divergence `sabm` d=4 → `_5`, chunk-identical to stock's `_3`); the content check is the safety property (a file lacking the chart raises the boot-blocking `ME1529`). **Basename-OPAQUE — never consults musicdb** — which is what preserves the `toho` special case (play sequences randomize the basename to `toho1..4` BEFORE calling the builder). Divergence oracle: every call also runs the original into a scratch buffer and logs one INFO per distinct `(basename, d)` that differs (cap 64) — a matched binary/data boot logs `sabm d=4` + ~11 harmless `d=4` lines where stock names a `_3`/`_5` file that has NO Challenge chart and Rule A names the base (neither has it — equivalent); the target scenario adds one line per newly-discovered split. NOTE: adding a test song needs a startup.arc OVERLAY (`arc/startup_arc/data/gamedata/musicdb.xml`) — `musicdb.merged.xml` fragments never reach the game's DB (learnings 2026-09-03); fixture: `scripts/setup_split_ssq_test.sh`. Fail-open: unknown ⇒ unsplit path; stock dir unlistable ⇒ passthrough + WARN; bad args ⇒ original; disable = passthrough flag (detour never uninstalled). `chart_length.rs` (the one DLL-side path builder outside the detour) is a documented follow-up. RE: `docs/split_ssq_research.md`; planning: `.agents/planning/2026-09-03-split-ssq-auto-discovery/` |
| Anytime Speedmod Adjustment (remove the ~10 s in-song speed-mod adjust window) | `src/mods/anytime_speedmod.rs` (id `anytime-speedmod`) — zero detours, one byte patch. The whole stock mechanism lives in `sequence::dance::ControlSpeedActor` (spawned per song by GamePlayActor): its onUpdate polls the nav buttons and steps the multiplier ±0.25×, and the ONLY time limit is its msg-0x1045 self-destruct gate `CMP dword [R8+8], 10000ms; JL` — the downstream apply (msg 0x1042 → GamePlayActor lerp cluster `+0x290/+0x294/+0x298/+0x29C`) is time-agnostic. The mod rewrites the gate imm32 to `0x7FFFFFFF` on enable / restores `10000` on disable (signature `speedmod_window_gate`, unique on all four builds; init requires exactly 1 match + stock imm read-back). Song-end cleanup (msg 0x104A) untouched. Cabinet-wide, live toggle, default ON. Cosmetic side effect: the stock "speed change" footer hint stays up all song. Mid-song enable can't resurrect an already-expired window (per-song actor); next song is covered. RE notes: `docs/anytime_speedmod_research.md` |
| Music Wheel Song Length (LENGTH M:SS in the song-select header, styled like the BPM readout) | `src/mods/music_wheel_song_length.rs` (id `music-wheel-song-length`) — zero detours. Constructs ONE mod-owned `sequence::SpriteLayer` (the game's native "row of bitmaps by texture name" widget — the SAME class that renders the header's BPM digits) anchored to the card's `bpm_usr` MC but shifted via the class's built-in x/y offset fields (+0xC0/+0xC8; config `music_wheel_song_length.offset_x/offset_y/spacing/scale` — operator-authored, read once at init; the live-tuning overlay rows were retired in the overlay-menu rewrite Step 9). Glyph row = net-new `muca_card_len_t` LENGTH caption (92×24, gap baked in — NEVER add a spacer glyph: `muca_card_bpm_blank` is 19 chars and breaks the ≤15-char SSO names rule; `GameString::set` clamps oversized names to blank instead of panicking) + stock `muca_card_bpm_0..9` digits + net-new `muca_card_len_c` colon, both net-new textures injected via `atlas_cloner` FRESH mode (label doesn't fit a donor cell; donor `muca_card_bpm_question` supplies encoding only; PNGs under `data_mods/music_wheel_song_length/`, art borrowed from a friend's hex-edit pack with permission). Length source: `services/chart_length` (SSQ chart-derived, ~1-frame latency) with the `song_rate::selected_song()` audio publication as digest-matched fallback. Per-frame (`input_manager::on_frame`, scene 25 only): poll the highlighted-song weak_ptr at `*(selectmusic_model)+0x1B0` (the global holds a POINTER to the model — the missing deref caused a wild-call crash) — selection change ⇒ blank instantly + read the code (inline `music::Info+0xD`) + `chart_length::request`; then call the SpriteLayer's layout vfunc (vtable slot 0) every frame like the game's own card tick. Parent = the header-card clip found by CONTENT (`bm2d_api::find_wrapper_by_children(["bpm_usr","music_name_usr","gimmick_top_usr"])` — clip names are NOT in pool slots; slot+0x114 is the root search path). LOCKSTEP RULE: `rt.wrapper` and the sprite's parent field (+0x60) must always change together, and `apply_names` refuses a null parent — the setter's trailing layout has no null check (fast-scroll crash, 2026-08-16). Signatures `spritelayer_ctor` / `spritelayer_set_names` / `selectmusic_model_anchor` (+derivation) hit once on all four builds. RE notes: `.agents/planning/2026-08-16-music-wheel-song-length/research.md` (incl. §6 full decode of the friend's 3-cave hex-edit implementation) |
| Per-Song Judgement Offsets (per-player offsets that follow the song wheel, overriding the stock JUDGEMENT OFFSET per song) | `src/mods/per_song_judgement_offsets/` (id `per-song-judgement-offsets`) — `mod.rs` (lifecycle; FULLY INERT without `row_injection_available()`, D20), `csv.rs`/`store.rs` (pure layers: `judgement_offsets.csv` next to mod-config.json — `code,p1_offset,p2_offset`, blank=unset — and the baseline/session merge model: session maps seeded from CSV, a server load REPLACES the side's session map, card-in resets to baseline, only explicit edits touch the CSV; wire codec `code\|offset\|...` sorted/2000-capped, empty map ⇄ empty string = the server-clear signal), `musicdb_scan.rs`+`bootstrap.rs` (boot crawl appends missing basenames — DISK-based: startup.arc via `core::arc` + whole-file override + `musicdb.merged.xml` fragments; the AVS read trampolines DON'T work off game threads — plus the coalesced CSV-writer thread), `ui.rs` (rows `adjust_song_offset` bool + `current_song_offset` scalar −100..+100 `SignedUnit{"ms"}`, both `PersistMode::None`; wheel poll re-seeds per side via `set_value_silent`; edits apply per-side, or to BOTH sides + both CSV columns when the operator sets `per_song_judgement_offsets.mirror_players` in mod-config.json — last writer wins, the other side's rows silently re-seeded, never the editing side's), `override_hook.rs` (the safety-critical core: identity = wheel latch + SSQ-open observer + DANCE-BANK-CREATE observer (the course fix — courses batch-preload SSQs at course start; each stage's bank create is the authoritative identity), lazy value resolve at first judge dispatch at **`Priority::Early`** — LOAD-BEARING: assist_tick reads Option+0x24 on the same dispatch at Normal; write `Option+0x24` with stock cached, restore on `prev==GAMEPLAY` + song-select sweep + save-tree `<timing_music>` rewrite via `custom_options_persistence::replace_option_s32` — the trampoline runs AFTER the marshal so a memory restore there is too late), `persistence.rs` (first consumer of the persistence service's STRING-FIELD registry: `register_string_field`/`register_card_in_callback`; emit = ordinal-163 str-typed view (value by POINTER, kbin type 11 — ess `ghost` convention, Ghidra-verified), read = ordinal 176 type 11, ddrcode-keyed deferral drained card-in-resets→s32→strings). Applies in normal play, Training Mode, AND Course/Dan (D21 — each stage its own offset). Repo-committed pre-seeded CSV (community sync list, 1441 songs, P1=P2) via `scripts/gen_judgement_offsets_csv.py` — the community list records each song's sync ERROR, the CSV stores the COMPENSATION, so the script NEGATES every incoming value; host tests via `scripts/validate_judgement_offsets.sh` (temp-crate harness — plain `cargo test` can't compile retour on ARM hosts). Backend: `mod_judge_offsets` → bemani-buddy `opt_mod_judge_offsets TEXT` (migration 016), stored VERBATIM. RE notes: `docs/per_song_judgement_offsets.md`; planning: `.agents/planning/2026-08-17-per-song-judgement-offsets/` |
| Chart-length service (SSQ-derived song lengths at select time) | `src/services/chart_length.rs` — background worker + code-keyed cache + digest-stamped `latest()` cell. Length = last event across all step chunks through the tempo chunk (`core/ssq/` — `ssq_chunk` + `timing::TempoConverter`, moved from `note_types_expansion` which now re-exports them), rounded UP; chart ≤ audio (no outro silence). LayeredFS-aware SSQ resolution (mod folders before `data/mdb_apx/ssq/`). Requests are latest-wins; failures cache as `Failed` (consumers fall back to the audio publication). Consumers: music_wheel_song_length (drives requests from its selection poll), training_mode (seeds SONG START/END rows + everything scaled from the seeded end from `latest()`, preferred over the audio publication; falls back cleanly when the wheel mod is disabled — no requests ⇒ `latest()` never matches) |
| Native SMX Hardware (StepManiaX Dedicated Cabinet: pads as stage input, DDR lights on pads + cabinet, touchscreen overlay) | `src/services/smx/` (game-agnostic HID layer: `protocol.rs`/`input_map.rs`/`light_map.rs`/`cabinet_map.rs` pure; `device.rs` SetupAPI discovery VID 0x2341/PID 0x8037 substring-matched "StepManiaX"/"SMXArcade"; `transport.rs` worker + per-device reader threads, HOST_CMD_FINISHED flow control, serialized per-packet writes, 30 Hz lights drain) + `src/mods/smx_hardware/` (id `smx-hardware`, DEFAULT OFF). **Input:** SMX panel masks inject at the ark IO singleton's VTABLE panel-getter impls (+0x310..0x328, 6-arg — resolved from the LIVE vtable, installed lazily from `input_manager::poll`; exports miss internal consumers). **Touch overlay:** SpiceManiaX-layout buttons (menu diamonds/pinpad/insert-card/visibility) rendered TOPMOST as textured command-list quads via `overlay_draw`'s post-dispatcher append to the widget layer's private list (above the mod menu — widget z can't do this); Gold-cab-styled atlas generated by `scripts/gen_smx_overlay_atlas.py` → `data_mods/smx_hardware/smx_overlay_atlas.png` (basename MUST equal the load stem) + generated UV table; menu buttons lamp-lit from dimlamps `player*8+button` with bloom crossfade; per-cluster screen-corner-anchored scale (50–150%) applied to render AND hit-test; WndProc subclass feeds presses (mouse under CrossOver; WM_TOUCH/WM_POINTER on Windows) with per-contact release tracking, an owner-walk + console-exclusion window discovery (fullscreen: D3DProxyWindow is an owned popup — input goes to the OWNER; fullscreen-optimizations mapping via monitor rect when the outer window covers it), and IR-frame release debounce (`touch_debounce_ms`, default 150 ms: releases DEFERRED and cancelled by re-press — the SMX touchscreen's beam plane triggers above the glass, one press arrives as down/up/down flutter; edge actions like the visibility toggle/card-in fire on HELD-bit edges only); the subclass also owns X-close (transport shutdown + TerminateProcess after 1.5 s — graceful CRT exit is what wedges under Wine). **Injection seams (one-detour rule, all in `input_manager`):** menu = the ark's dormant per-player digest OVERRIDE WORDS (`DAT_180c47f50`, module-relative disp32 AOB) written pre-original in the IO-dispatcher detour (+0x28) + synthesized edge-byte pulses post-original (covers game + ark-internal + TEST MENU); pinpad = the 10-key vtable impl (+0x308), ~120 ms pulses (momentary like real pinpads), feeds the modpack poll too (0-0-0/gestures work from touch); card-in = post-dispatcher replication of stepUpdate's physical-scan writes (uid block +0x5BC/+0x5D4, scan-armed gates +0x6F8/9; `arkMDXGetEAPass` is DEAD — gamemdx never calls it). **Lights:** capture = Tapeled/Dimlamp export detours + (GOLD force) direct polls of the ark's internal buffers (test-menu LAMP CHECK bypasses exports); `cabinet_force.rs` detours GetMachineType/GetPCType→GOLD so gamemdx drives the per-LED path (config `force_gold_cabinet`); pad accent Gold/Platinum via `light_map::PadStyle`. Mod-menu SMX HARDWARE section (GLOBAL SETTINGS): opacity/scale/pad+cabinet light toggles/pad style/touch debounce, persisting the whole `smx_hardware` config section. Wine prereq: winebus `EnableHidraw REG_MULTI_SZ "2341:8037"`. RE notes: `docs/smx_hardware_research.md`; planning: `.agents/planning/2026-08-27-native-smx-hardware-support/` |
| Announcer Mute (per-player option to silence combo callouts / accolades / stage-clear cheers) | `src/mods/announcer_mute.rs` (id `announcer-mute`) — one `GenericDetour` on the announcer/voice dispatcher (AOB `announcer_dispatcher`, unique on all four builds; 32-bit analog `FUN_10047ab0`), conditional early-return when mute is effective; state read live from per-side atomics so an options toggle applies next song. Cabinet-wide effect (single dispatcher instance reads both sides' combos): entered side governs via `stage_records::side_entered`, P1 wins when both entered, `p1 \|\| p2` fallback when entered-state unavailable. Option row `announcer_mute` (bool, `PersistMode::Full` → wire `mod_announcer_mute`; server-side storage needs a bemani-buddy migration — until then the JSON cache carries it). RE notes: `docs/hex_edit_porting.md` Hack 1 |
| Classic Difficulty Adjustment (double-tap pad UP/DOWN at song select to raise/lower difficulty, pre-World behavior) | `src/mods/classic_difficulty.rs` (id `classic-difficulty-adjustment`) — zero new detours/signatures: pure gesture→pinpad translation. At scene 25 only, per-side double-tap (≤400 ms, atomics-only timestamp pairing; leaving the scene clears pending taps) of PANEL_UP ⇒ synthesized pinpad **5** (difficulty up), PANEL_DOWN ⇒ pinpad **2** (down) — the game's own pinpad handler does the change. Built on two `input_manager` extensions added for this mod: **(1) opt-in stage-panel polling** (`set_panel_polling(true)` — poll_player also reads the stable `arkMDXGetPanel*` exports `(player,*trig,*hold,*rel,*counter)` behind the singleton gate and emits `button::PANEL_*` InputEvents; currently a plain flag — promote to a refcount when a second consumer appears); **(2) generic pinpad pulses** (`request_pinpad_pulse(player, key)` — per-(player,key) deadline atomics OR'd ~120 ms into the SMX-shared 10-key vtable-impl detour, independent of the SMX provider/active gates, visible to game + modpack poll alike; `request_pinpad_injection()` at enable is a second trigger for the lazy vtable-detour install so pulses work with smx-hardware disabled). No config, no option rows, default ON |
| Realtime gameplay calories | `src/mods/power_user_statistics/calorie_feed.rs` — detours the `CalcCalorieActor` per-frame tick (`calc_calorie_tick` signature in `signatures.rs`) to cache each side's live per-stage kcal (`CalcCalorieActor+0x94`, the game's own running accumulator; created unconditionally in normal play regardless of `is_disp_weight`). Rendered as the `Cal:` line on `timing_stats_widget` under the `timing_stats` gate. RE notes: `docs/calorie_weight_profile_research.md` §3.1 |
| Pacemaker → MS ERROR display (always-show + restart/loop survival + decoupled white zone) | `src/mods/power_user_statistics/pacemaker_swap.rs` — the 11-byte patch inside `NoteResultActor`'s msg-0x1036 case swaps the pacemaker delta for the latest ms-error; its stub passes RDI (the NoteResultActor) so the callback can FORCE the display when the option is on: the visibility byte `NoteResultActor+0xC0` is only ever set by `GhostActor::onUpdate` on a successful ghost/rival download, so with no prior score the stock readout renders invisible — the callback writes it to 1 (vtable-guarded via `note_result_actor_vtable`, once per song) + re-asserts layer visibility. **White zone (`pacemaker_threshold`, clamp 0..50, 0 = always colored):** the callback returns the REAL ms error always; the white color is forced separately by redirecting the colored branch's two shared-0.5f `MOVSS [rip]` loads (content-derived: exactly-2 rule within patch+0x100, byte shape attested 20250805/0616/0721/0825) at a mod-owned float in the stub allocation — 1.0 per dispatch in the white zone degenerates both colored paths to the white branch's (1,1,1,1). NEVER zero the value for the white zone (the 2026-08 port did — it blanked the digits; derivation failure falls back to that legacy shape with one WARN). The exact-0 "sign-only, no digit" bug (resolved 2026-09-01) was NOT this mod: real-speed-fix's ported R15/R16 "logf guard" anchored INSIDE this same 0x1036 case and its R15 byte broke the zero branch's sign-slot math — the guard is retired (see `src/mods/real_speed_fix/mod.rs` + research resolved §); never re-add a patch at `pacemaker log10f-site − 0x38`. The 0x1036 case ALSO refuses once the `dance_score_compare` clip reaches its "out" frame label (msg 0x103A, sent on ANY gauge-empty/LIFE4-death — a one-way latch the in-place reset would otherwise inherit forever); `song_reset::reset_side_state` rewinds the clip to its song-start state (SetFrame 0 via `mc_op` 0xF08 + `layer_play_raw(0.0)`) on every reset/seek. All fixes fail-open. RE notes: `docs/pacemaker_display_research.md` |
| Custom options | `src/services/custom_options/mod.rs` |
| Mod Menu (overlay) — tabbed in-game modal: MODS / GLOBAL SETTINGS / PLAYER SETTINGS / APPEARANCE tabs, themes, animated shader backgrounds | `src/mods/mod_menu/` — `mod.rs` (lifecycle; pinpad 0-0-0 toggles, 1/3 tabs, menu buttons navigate; exclusive input consumer while open), pure `model.rs` (tabs/nav/rows — host-tested), `tabs.rs` (per-tab builders over the registry snapshot), `rows.rs` (the FROZEN contributed-row API `register_scalar_row`/`register_enum_row`/`remove_rows_for` — fps_unlock, shader_fixes, timing_offsets, quick_restart use it), `input.rs`, `render.rs` (widgets/chrome/layout; creates the overlay-draw EMISSION ANCHOR first in `allocate_widgets`), `chrome.rs`+`chrome_loader.rs` (synthesized gradient panel + strip via CacheHasher/asset_loader; `overlay_menu` config owner), `theme.rs` (12 palettes: bubbles/BUBBLES + the Shadertoy theme pack terminal/TERMINAL, waveform/WAVEFORM, spectrum/SPECTRUM, tunnel/TUNNEL, xmb/XMB, squares/SQUARES, card_swirl/CARD SWIRL, blobs/BLOBS, ps2/PS2, prime_cube/PRIME CUBE (ports with attribution headers in `shaders/src/themes/`; `.agents/planning/2026-08-25-shadertoy-themes/`) + static minimal/MINIMAL; BUBBLES is the default; the retired arrows/wavefield/mandelbulb ids degrade via the unknown-id WARN path; theme shaders must keep dynamic flow control SHALLOW — deep-nested dynamic loops fail D3DMetal's buildPipelineState under CrossOver and freeze the game in software rasterization). Animated backgrounds: `src/services/overlay_draw/` — pure `encode.rs` record encoders + the IDENTITY-GATED ANCHOR emitter (quad emitted at the menu's own anchor wrapper's render into the ACTIVE command list — mid-walk z: above earlier layer content incl. loading-screen art, below the panel/text; "same list ≠ same z", see learnings). Theme shaders `shaders/src/themes/` appended LAST to the synthesized default container (`shader_layout.rs`, persp stays index 1; indices published via `publish_theme_programs`). Host tests: `scripts/validate_mod_menu.sh` (model/chrome/theme) + `scripts/validate_overlay_draw.sh` (encode + shader_layout) + `scripts/validate_custom_options.sh` (framework + display-string lint). RE: `docs/overlay_draw_research.md`; planning: `.agents/planning/2026-08-24-overlay-menu-rewrite/` |
| Options-menu texture localization (ENG/JPN/KOR generated sets) | `scripts/gen_option_labels.py` (renderer: fonts/metrics/CJK kinsoku wrap) + `scripts/option_strings.py` (data-only en/ja/ko string tables — translation edits happen HERE, then regenerate) + `scripts/templates/` (baked art crops + verbatim masters) + `scripts/check_option_takeover.py` (layout-regression harness, `--reference <dir>`). Runtime: `asset_gen.rs::OPTION_LANGS` drives per-language donor-clone atlas builds (fail-open per language, prefixes `copt_mods_lang_<code>`/`copt_prev_<code>`); `preview_gen.rs` derives per-language template/chrome dirs from the same table (chrome per-language; marker geometry language-invariant, eng-first lookup). Output: `data_mods/custom_options/select_music_option_lang_{eng,jpn,kor}_v3_ifs/tex/` (91 files each). NEVER hand-edit a generated PNG — edit strings/templates and regenerate. Planning: `.agents/planning/2026-08-17-options-texture-localization/` |
| On-demand customizer textures | `src/services/asset_loader.rs` (FileManager→ResourceManager loader) |
| WebUI preview overlay | `src/mods/webui_options/preview_overlay.rs` (+ `preview_gen.rs` template utils) |
| WebUI cosmetic pickers (scalar rows, no per-value textures) | `src/mods/webui_options/discovery.rs` (every runtime-count category is a scalar row; the former `RenderMode` enum was retired when VIDEO SIZE moved out — see Movie Size Customization below) + the registration loop in `mod.rs` (calls `preview_gen::generate_chrome` before registering; `ScalarFormat::OffsetInteger { display_offset: 1 }` shows the 1-based position while the stored value stays the 0-based asset index). The former `EnumIndexed` mode + its 150 shared `seop_op_item_<NNN>` ribbon chips were retired 2026-08-11 — they dominated the CAUTION (scene 21) preload (138 of 208 slow-path texture opens). If re-adding assets to the cabinet, never reintroduce `seop_op_item_*.png` under `data_mods/custom_options/` (LayeredFS texture injection would pack any stray PNG back into the served atlas). Load-path analysis: `docs/scene_load_analysis.md` |
| Movie Size Customization (standalone VIDEO SIZE row) | `src/mods/movie_size_customization.rs` (id `movie-size-customization`) — split out of webui_options 2026-08-26 so the video-size control survives disabling the cosmetics mod. Same option id `customize_movie_size` (wire `mod_customize_movie_size` → bemani-buddy `cust_movie_size`, same shipped `seop_*` textures, same `option_menu_settings` key). Fixed 3-value enum (index 0/1/2 = FULLSCREEN/ON/OFF ↔ game/wire 1/2/3), `PersistMode::SaveOnly` + `+1` save transform; on_change writes `Customize+0x30`, scene-25 seed reads it back via `set_value_silent`. Shares the `player_work_table`/`customize_offset` derivations with webui_options. RE notes: `docs/player_customization_system_research.md` |
| WebUI workout-profile rows (WEIGHT + DISPLAY BURNED CALORIES) | `src/mods/webui_options/profile_fields.rs` — registers the `is_disp_weight` parent toggle + conditional `weight` (kg) child, writes `PlayerWork+0x28`/`+0x24` on edit, seeds from them at scene-25. `PersistMode::SaveOnly` → framework auto-emits wire fields `mod_is_disp_weight` / `mod_weight`; the bemani-buddy backend writes its native `is_disp_weight` / `weight` profile columns (save-path applier in `crates/game-server/src/handlers/ddr_world/playdata.rs`). RE notes: `docs/calorie_weight_profile_research.md` |
| Animated background previews | `src/mods/webui_options/bg_preview_overlay.rs` (AFP-layer overlay for the two BACKGROUND rows) + `src/services/bm2d_package.rs` (on-demand package load/lookup/release) + the AFP-layer wrapper set in `src/services/bm2d_api.rs` |
| BM2D package / AFP internals | `docs/bm2d_background_preview_research.md` (RE notes: data manager, create recipe, deferred-destroy crash class, alias trick) |
| Score-submission policy (suppress / sanitise) | `src/services/score_guard.rs` (taint state: per-song `is_stage_suppressed`, session-sticky `logout_taint`, `was_logout_sanitised`) + `custom_options_persistence.rs` `save_sender` trampoline (enforcement). Per-stage saves (`savekind==2`) of a tainted (Autoplay/Quick-Fail) side are suppressed outright; the card-out logout save (`savekind==3` — the ONLY save carrying the profile/customize write-back) is **sanitised, not suppressed**: an EAM_EXIT (0-idx 34) scene callback virginises the tainted side's 5 play records + course record (`mcode = -1`, the marshal's skip key) via `stage_records`, then the trampoline strips `<data><league>` (libavs Ordinal 164 = `property_node_remove`, resolved non-fatally) and forwards. Fail-closed: either piece missing ⇒ full suppression. RE notes: `docs/quick_logout_research.md` §13 |
| Premium Free (frozen stage counter = unlimited stages; shared bool option) | `src/mods/premium_free/` (id `premium-free`) — `mod.rs`: bool row `premium_free` whose APPLIED freeze is resolved from the ENTERED side(s) (`effective_freeze`: P1 governs when both in; `p1 \|\| p2` when entered-state unknown; re-resolved on every option change AND scene change) and versus-MIRRORED via `services/versus_mirror` (P1 seeds at song select) — a profile load never overwrites the other side's preference (the pre-2026-09-01 unconditional cross-side `set_value` made the freeze follow whichever 2P profile loaded LAST); 3-byte NOP of the WaitSequence stage bump (`premium_free_stage_inc`+3, 0-idx scene 31 — AFTER results, so results read the just-played record) + the **stale-record fix** (SONG_SELECT-entry write of `mcode=-1` into `record[frozen]` for both sides, forcing the song-select commit's `new_mcode != rec->mcode` guard open — that guard ALSO covers the display difficulty `PlayerWork+0x5C`/mcode `+0x54` writer `FUN_1800fcf40`). `ghost_cache.rs` (World port of the DDR A3 `pfree_ghost` hook): the game's same-credit PB pacemaker ghost is a LOCAL-SLOT copy (`GhostActor` init `ghost_actor_init`: negative score-DB ghost id ⇒ copy `record[(side,stage)]+0xB8` into `actor+0x98`), and under the freeze that slot is the virginised/re-prepared current record ⇒ empty ghost ⇒ pacemaker target 0. Fix = post-original `result_commit` detour snapshots `rec+0xB8` keyed `(side, mcode, style, diff)` (keep-if-better), post-original `ghost_actor_init` detour injects it via the game's own `ghost_vec_copy` (derived from `ghost_local_slot_copy_site`+25) when the vector came back empty and no network load is in flight (A3 rule); state 2 + ready byte `*(actor+0x88)+0xC0` like the stock branch; injection freeze-gated, cache session-scoped, fail-open. `diag.rs`: WARN-only signature taps (silent in healthy play — two clean 1P + 2P cabinet runs 2026-09-01; WARN lines DO reach spice2x's log.txt, the level is only a tag inside the OutputDebugString text): `BUG-2 SIGNATURE` = `rec[frozen]` header ≠ `PlayerWork+0x54/+0x5C` at GAMEPLAY entry; `BUG-1 SIGNATURE` = streams non-empty at GAMEPLAY entry, result-commit early-out (`actor+0x280`/`+0x288`) inside a play session, or a commit that left empty streams. A field log carrying either line pinpoints the stale record without another repro round-trip. Results GraphTab facts: ctor stamps `tab+0x14C = GameWork+0xC`; ingest reads numeric stats from `rec+0xD8` and the graph window/series from `rec+0x98` of `record[tab_stage]`; the result commit REPLACES all three streams + the gauge map at the same index (early-outs `actor+0x280`/`+0x288` skip it). RE: `docs/premium_free_stale_record_bug.md` (+ 2026-09-01 addendum) |
| Quick Logout (triple-9 at song select ends the session) | `src/mods/quick_logout.rs` (id `quick-logout`) — zero new detours; one new AOB (`sequence_finish` = `agcs::Sequence::finish(this, sceneId_1INDEXED)` — note the **1-indexed** id; everything else in the DLL is 0-indexed). Trigger: per-side triple-9 GestureBuffer at scene 25, four gates (FIRED latch, side-entered via `stage_records::player_work+0x4`, live TS, live child `*(TS+0x58)` with `flags@+0x20 & 0x24 == 0`), then `scene_manager::add_redirect_once(30, 32)` + `finish(child, 30₁ᵢₙdₑₓ)` → the 0-idx 29 loader (the ONLY loader that makes TOTAL RESULTS' `scene_result` package resident — never jump to 32 directly, and never pre-close the shutter) → 32 → 33 → 34 (logout save; cabinet-confirmed) → 35 → attract. Enable-gated on `scene_manager::redirect_repair_available()` (without the `advance_to_scene` m_currentID repair the tail would stage-bump back to song select). No lock held across `finish` (it re-enters the scene hook synchronously). Tail diagnostics WARN if 34 is skipped or exits <500 ms. Shared record layout: `src/services/stage_records.rs` (fail-closed decode from `stage_record_accessor` incl. course record @ +36 imm32; also consumed by `premium_free`). RE notes: `docs/quick_logout_research.md`; planning: `.agents/planning/20260727-quick-logout/` |
| Quick Restart / Quick Fail (single-press 1 / 3 during gameplay) | `src/mods/quick_restart_or_fail.rs` (id `quick-restart-or-fail`) — **bannerless fast path: dismiss the stage shutter, then `finish`** (v4, 2026-08-12). **SKIP RESULTS ON FAST EXIT** (Mods-tab bool `skip_results_fast_exit`, default ON, `PersistMode::Full` → wire `mod_skip_results_fast_exit`, bemani-buddy `opt_mod_skip_results_fast_exit` migration 017): the PRESSING side's value governs the fail — OFF takes the natural fail flow (`fail_song(None)`: fade + FAILED banner + stage results screen with the partial score + natural tail; no predicate needed, score taint still applies) instead of any skip shape; a direct results-loader `finish(DPS, 0x1E)` was RE-investigated and REJECTED (the play record is only written by the result commit — GamePlayActor vtable +0x28 — during the natural song-end machinery; a mid-song jump shows an all-zero results screen; research §13). Root cause of BOTH earlier `finish` limbos (cabinet-confirmed by the diag sampler: `loader exit gate=1`, `shutter=6`): the kind-3 stage jacket "READY?" panel parks at **shutter state 6** for the whole song, and both the loader's mask-apply gate (state ∈ {0,4}) and a fresh DPS's state-1 gate reject it forever — the FAILED banner request is the game's own drain mechanism, which is why skipping it hung. The fix is the ShutterActor's built-in bannerless dismiss, **msg `0x100c`** (force state 7 iff active kind 3 → drain 7→8→0, pending untouched ⇒ no banner art loads; no stock sender — verified at runtime by the synchronous state-7 read-back). Fast path: gates (live TS, live/not-dying child at `TS+0x58`, ≥1 GamePlayActor, shutter idle-or-dismissed, every shutter read range-validated) → **restart** `finish(DPS, 0x1C₁ᵢₙdₑₓ)` (0-idx 27 stage loader → `getNextID 0x1D` fresh gameplay; idle shutter accepted at DPS state 1 ⇒ READY panel skipped, counter untouched) / **fail** `finish(DPS, 0x19₁ᵢₙdₑₓ)` (0-idx 24 select loader → `getNextID 0x1A` song select) behind the session-continues predicate `!course && event==0 && override==-1 && stage<max_stage` (skipping ResultSequence skips the game's ONLY session-over decision; via `stage_records`: `final_stage_probe` AOB + derived `max_stage_global`). Resolution: `sequence_finish` + `shutter_actor_global` (derived from the `shutter_close_request` AOB — the msg-0x1007 imm pins it; unique on 3 builds). Latency (cabinet-measured, research §12): the scene machinery is < 1 s; fail's former 5 s was the select loader re-loading packages gameplay entry evicts — fixed by the **select-residency patch** (`gameplay_loader_masks` AOB, unload imm 0x32000→0x30000 at enable, one-way/checked/fail-open; also speeds the natural post-results select return); restart's remaining ~6 s is the fresh DPS init — cabinet sampler proved the song bank prepares by ~1.2 s and the rest was a hardcoded **5.0 s "READY?" ready-dwell** (`DAT_18035a8b4`, state-5 gate `5.0 <= DPS+0x130`); the restart driver seeds `DPS+0x130` past the threshold every frame during pre-song init (states 0..=5) to clear the gate the instant the bank is ready (~1.2 s), leaving only ~1 s of state-0 layout-arc load. Can't patch the 5.0 constant (13 shared readers) so it's a per-DPS field poke. Full in-place reset for Training Mode planned in §12.2. Fallback for ANY gate failure: natural death (force `STEP_GAME_OVER` + death flags, fade + FAILED banner) + one-shot redirects 29→28 (restart, proven since 2026-05) / 29→24 (fail skip-results; predicate + `redirect_repair_available()` gated, else full natural tail) — cabinet-validated 2026-08-12. **Pre-song READY window (DPS init steps 0..=6): the fallback SOFT-LOCKS** (death flags only consulted in-song; DPS never reaches its state-8 banner request — cabinet-observed 2026-08-31), so `dps_pre_song()` gates it: fail takes ONLY the fast `finish` (the `0x100c` dismiss also works from shutter states 4/5 — the handler checks only `active kind == 3`), any refusal = gesture ignored (taint set on success only); restart pre-song is a no-op; `fail_song` refuses pre-song as the structural backstop (research §14). **Early-press drain stall (§14.1, the limbo actually observed):** on shutter art whose `shutter_play` clip has no labels (stock-data CrossOver install), the drain's state-7 `"out"` play silently fails and state 8's wait (`current ≥ max(frame("out_end"), frame("end"))`) never passes when the clip is still near frame 0 (early in the song; masked mid-song where the clip already sits at its final frame) — fixed by `unblock_shutter_drain` after every verified dismiss: SetFrame (`mc_op` 0xF08, layer obj `shutter+0x88+kind*0x10`, mc id `+0x110`) to the wait's own target; a no-op on healthy art (state 7's `"out"` play re-seeks anyway), fail-open. Validated 2026-08-31, all timings. A silent post-`finish` watchdog stays permanently: destination scene within 20 s or ONE gate sample + LIMBO WARN. No lock held across `finish` (re-enters the scene hook synchronously). Shared record layout: `src/services/stage_records.rs`. RE notes: `docs/quick_restart_fail_speedup_research.md` (§4a root cause + ShutterActor state machine, §4b fast path, §4c fallback, §13 skip-results toggle); planning: `.agents/planning/20260812-quick-restart-fail-speedup/`, `.agents/planning/2026-08-20-skip-results-toggle/` |
| Non-native OS (CrossOver/Wine) support — background-movie crash/stall fix + native VC-1 decode | `src/mods/non_native_os_support.rs` — one mod (best-effort resolve in `init`, self-disable, `is_active()` = installed). **Movie-graph stub/fallback:** the shared `services/movie_policy` detour on gamemdx `DShowPlayer::BuildGraph` (AOB `movie_build_graph` in `signatures.rs` — the binary's only `CLSID_FilterGraph` user; `0x18023AE40` on 20260616), two modes via `non_native_os_support.movie_mode` (read at enable): **`"suppress"`** (default) never calls the original and fakes the success epilogue's one observable side effect (player state dword `+0x8 = 3`, "opened"), return 0 — crash-safe, all movies absent; **`"fallback"`** calls the original first (after `absolutize_request_path` — the game passes `.\data\...` relative paths and Wine quartz's source-filter byte-pattern probe needs the file openable) and only fakes the epilogue when it FAILED (high-bit hr) — playable movies play, unplayable files degrade to no-movie instead of soft-locking (per-failure INFO names the file's hr). Two distinct Wine failure modes (diagnosed 2026-08-19): the CRASH is spice2x's audio hooks (its process-wide `CoCreateInstance` IAT patch wraps the `MMDeviceEnumerator`/`IAudioClient` that Wine's builtin `winmm` consumes internally during quartz's devenum audio-renderer enumeration; codec/container-independent — spice2x **`-audiohookdisable`** removes it at the source, game audio is WASAPI and unaffected); the STALL is the missing VC-1 decoder → `RenderFile` fails → `BuildGraph`'s error path never writes state 3 → the song waits forever on the movie-ready gate. `"fallback"` therefore requires `-audiohookdisable` under Wine. **Native VC-1 decode (Path B, WORKING 2026-08-19):** with Microsoft's x64 WM runtime installed in the bottle (qasf/wmvcore/wmasf/wmvdecod/wmadmod + mfperfhelper/wmidx, `native,builtin` overrides, `regsvr32`, the ASF Media-Type byte-pattern→WM-ASF-Reader mapping — full recipe in `docs/native_wm_runtime_bottle_setup.md`), stock VC-1 movies decode natively; the last blocker was a genuine Wine bug — builtin mfplat's `MFInitMediaTypeFromVideoInfoHeader(subtype=NULL)` derives the subtype from `biBitCount` only, so wmvdecod's own WVC1 input converts to "RGB24" and the decoder rejects it (0x80040205) — fixed in-process by `services/mfplat_vih_fix.rs` (Wine-gated `GenericDetour` on the mfplat export; injects the `biCompression` FOURCC subtype exactly like Windows, installed by the mod's enable in fallback mode, fail-open, never touches real Windows). This supersedes the July "never build the graph" conclusion; mass transcode (`scripts/convert_movies.sh`, "Path A") remains a no-runtime alternative — H.264 conversions decode via winegstreamer and compose with fallback mode. NEVER add `Media Type\Extensions\.wmv` to the bottle (kills Wine's byte-pattern probe → async source → winegstreamer). The state-3 write is load-bearing (an error-returning stub soft-locks the attract demo); the `opened` byte (`+0x14`) stays 0 in faked paths so per-frame code keeps to its guarded early-return. `SongRate` suppression always wins over fallback (never builds — the DirectShow clock can't follow the XACT rate). Fail-open, config-gated (default on, mode default suppress). **Removed — networking fixes:** earlier revisions also promoted `arkmdxbio2!arkGetNetworkStatus` CHECKING→ONLINE (boot online) and libavs `ea3_get_status` DOWN→ONLINE (PASELI); both worked around Wine's inability to create the raw ICMP socket AVS keepalive needs, and are now redundant — spice2x's **`-icmphook`** flag fakes that keepalive game-agnostically at the socket layer (the game boots fully online, PASELI included, with no hook DLL). RE records: `docs/native_wm_runtime_bottle_setup.md` (Path B recipe + root-cause trail); retained (superseded): `.agents/planning/20260721-raw-socket-network-fix/` (network), `.agents/planning/20260721-non-native-os-support/` (movie), `.agents/planning/20260722-eacoin-paseli-online-cascade/` (PASELI) |
| Overlay element styling (per-player combo/judge/pacemaker scale & opacity) | `src/mods/overlay_element_styling/` — `mod.rs` (lifecycle + option rows + per-side value atomics), `capture.rs` (CMovieClip::Create + SetPosition detours, 64-slot clip registry, side binding, scale/opacity one-shots), `color_hook.rs` (SetColor +0x90/+0xB0 opacity compose detours). Signatures + color-twin IAT resolver in `src/core/signatures.rs` (`cmovieclip_*` + `derive_cmovieclip_color_twins`); raw game-owned-layer setters in `src/services/bm2d_api.rs` (`layer_set_scale_raw`/`layer_set_color_raw`/`layer_color_available`). RE notes: `docs/gameplay_overlay_elements_research.md` |
| Playfield styling (per-player arrow/receptor/guideline/mine scale & opacity) | `src/mods/playfield_styling/` — `mod.rs` (option rows, per-song latch at GAMEPLAY entry, all-or-nothing enable gate), `fill_hook.rs` (detour on `render_sprite_final` — every lane quad incl. mine_render's; 16-slot renderer registry, RTTI-vtable classification, presence/posX side binding, per-side receptor-row anchor `posY@+0x34` exposed as `side_anchor_y`), `services/cull_window.rs` (PROMOTED from this mod's former `cull_patch.rs`: verified disp32 redirects of the collector + guideline 720.0 cull loads → mod-owned float; int3-cave slot, `alloc_near` fallback; never unpatch; multi-contributor — this mod contributes `min(scale)`, player_perspective contributes its draw distance; effective = `max(720,distance)/min(scale,1)`, multiplicative because the fill's scale and the perspective map STACK), `guideline_hook.rs` (guideline-draw capture detour with `Ybase/s` pre-scale + single-caller bulk-emitter record transform; SHARED with player_perspective via `acquire/release` refcount — installs when either mod enables; pass state carries scale+opacity+optional hallway params, emitter applies scale-then-perspective), `lane_hook.rs` (lane FILTER band + lane COVER + DANGER low-life flash + receptor HIT-FLASH (`dance_effect`) sizing — AFP clips that bypass the fill; capture via `cmovieclip_pool_create` (filter/cover/danger: `dance_filter_*`/`hidden_cover_*`/`sudden_cover_*`/`danger_single|double`, layer id at +0x08) + `note_result_setup` (receptor hit flash: walk `NoteResultActor`'s `vector<CMovieClip*>` @ +0xE8..+0xF0, layer id at clip+0x08, mode at +0x90); apply DEFERRED to the first fill call (HUD builder reads lane-clip marker positions at build) with `ApplyOutcome::Retry` until the side's renderer binds; lane bands via `layer_get_matrix_raw`+`layer_set_matrix_raw` RMW `{s·a,b,s·c,d,tx,ty}` (shrink-in-place, horizontal-only), hit flash via translation-only RMW toward the fill's fixed points `{a,b,c,d, Cx+s·(tx−Cx), posY+s·(ty−posY)}` (Cx = per-actor flash-tx centroid; posY = receptor-row top from the fill registry) plus uniform component scale on the clip's root MC (`layer_find_child(layer,"/")` + MC param 0x1003 — matrix a/d scaling displaces the art by its internal offset); best-effort/non-gated. The visible lane BACKGROUND needs no handling (scales via fill+filter alone; the `1p_lane_usr`-style find-child clips are HUD layout containers with transient handle ids — that path was removed)). Derivations in `src/core/signatures.rs::derive_playfield_styling` (collector identified by cull-load content, NOT call order — first-CALL is a helper). Mine integration = `mine_render` reads `playfield_styling::cull_bound()` (mine quads inherit the fill transform automatically). RE notes: `docs/playfield_styling_research.md` |
| Assist tick (pre-mixed clap track at each arrow's chart timestamp, per-player) | `src/mods/assist_tick.rs` (id `assist-tick`) + `src/services/se_bank_synth/` (pure-CPU format layer: mono MS-ADPCM encoder, SE-profile XSB writer, fixed-header one-entry XWB writer exposing the rewritable sample segment, sample-exact clap mixer — zero crate deps, host-validatable via `scripts/validate_se_bank_synth.sh` against the sibling `ddr-chart-tools`) + `src/services/game_audio.rs` (the XACT game-ABI surface; tick-bank API: `register_tick_bank` / `rewrite_tick_wave` / `play_tick_track` / `stop_cue`, all game-thread-only). Ticks are delivered as ONE pre-mixed whole-song mono waveform played as a single cue — sample-exact spacing on the music's own mixer clock (the per-tick `se_play` path was retired: the engine's cue starts are ~10 ms packet-grid quantized with NO sample-offset primitive, and `Play(timeOffset)` is NOT a seek — it only fast-forwards the cue event timeline; both live-confirmed). The immortal tick bank (internal name `astk` — NOT `asti`, the engine pairs banks globally by name; and NO manager slot — the slots' only readers are the `se_play` façade, slot 5 is the per-song bank) is registered once, entry declared at `TICK_CAPACITY_MS` = **1200 s wall** (~28.9 MB, lazily allocated — D15: keeps 300 s of chart content covered at the slowest song rate); per song ONLY the sample bytes are rewritten, after an immediate `SoundBank::Stop` (vt+0x28, flags=1). Normal/late start and clock rewinds are ONE mechanism: the rewrite copies `encoded[skip..]` + silence-fills the tail (`shift_bytes_for_ms`, 2.90 ms ADPCM-block granularity; blocks are self-contained so shifted concatenation is exact). Timing (FR-3, rate-aware per song-rate design req 30): `wall_ms = content_to_wall_ms(t_i + JUDGMENT_TIMING − m0) − SOUND_OFFSET` — the clap marks the JUDGEMENT MOMENT; the conversion (and the commit/rewind `mc − m0` skips) ride the AwaitAnchor-latched committed `RateSnapshot` through the pure host-tested `song_rate::tick_domain` (identity/uncommitted reproduces the legacy arithmetic bit-identically; `sound_offset` is wall-domain, unscaled); `SOUND_OFFSET` = chosen actor `+0x16c`, `JUDGMENT_TIMING` = per-side `ddr::player::Option+0x24` via the derived `player_option_table` (`*(*(table+side*8))+0xE0`; sign +1 ear-validated; the derivation validates its anchor's `LEA R12` == module base, fail-closed — it's in `required_signatures`, mod absent without it). NO latency knob: `assist_tick.offset_ms` is retired (parse-but-ignore, one INFO). Synthesis runs on a background thread per song (generation-tokened against restarts); engine calls game-thread-only; per-song failure ⇒ silent song + one WARN, no fallback path. Tick list unchanged: FR-2 predicate (`kind==0`, engine's own 4-per-side TRG shock test, live panel, `mc>=0`; NEVER reads `length[]` — breaks under FREEZE ARROW: OFF), 4 ms coalesce, FR-5 side choice via the sibling walk, enables latched at GAMEPLAY entry. Clap asset: `data_mods/assist_tick/clap_44k_mono.pcm` (raw mono i16 LE 44.1 kHz; regenerate: `ffmpeg -i source/clap.ogg -ac 1 -ar 44100 -f s16le clap_44k_mono.pcm`); missing ⇒ mod disabled. Option rows versus-MIRRORED via `services/versus_mirror` since 2026-08-31 (one cabinet-wide tick track — P1 seeds, last writer wins): `assist_tick` (bool, `PersistMode::Full`) + child scalar row `assist_tick_volume` ("TICK EFFECT VOLUME", 25–175 / fine 5 / coarse 10 / default 100, `ShowWhen::Equals` on the parent, clamp+snap `load_transform`, fail-open to unity if the scalar machinery is unavailable): linear-amplitude clap gain, latched per-side at GAMEPLAY entry beside the enables, the CHOSEN side's value baked into the track at synthesis via `se_bank_synth::scale_pcm` (i32 headroom, i16 saturate) — 100 skips the call entirely (byte-identical track), applies next song, rewinds/in-place restarts reuse the encoded track. Backend: `mod_assist_tick_volume` → bemani-buddy `opt_mod_assist_tick_volume`, stored VERBATIM (migration 013). RE notes: `docs/xact_audio_research.md`; planning: `.agents/planning/20260729-assist-tick-premixed-track/` (supersedes `20260725-assist-tick`); volume child: `.agents/planning/20260812-assist-tick-volume/` |
| Shader fixes (runtime-synthesized lane shader containers: index-aware bilinear AA + perspective programs) | Synthesis: `src/services/avs_layeredfs/shader_synthesis.rs` — runs lazily inside `arc_handler::handle_arc` when the game opens `data/arc/shader.arc`; slices the game's OWN stock VS/PS blobs, packs extended GSPW containers (Rust packer byte-compatible with `scripts/gsp_pack.py`; FNV hash copied from stock) per the minimal-overlay rule (arrow+judge iff AA∨persp, default iff persp; prog 0 = stock VS + (AA ? AA PS : stock PS), prog 1 = persp VS + AA PS (arrow) / stock PS (default, must output `o3=c23`) / same-PS-as-prog0 (judge — reuses the ARROW persp VS blob, stock judge VS ≡ stock arrow VS byte-identically); fingerprint-cached (`v2` prefix) at `data_mods/_cache/shader_synthesis/`. Committed inputs: `data_mods/shader_fixes/blobs/*.d3dbc` ONLY (4 mod blobs; no Konami bytecode in the repo). Thin mod `src/mods/shader_fixes.rs` (id `shader-fixes`): ARROW ANTI-ALIASING overlay enum row → `shader_fixes.anti_aliasing`, next-launch semantics; mod disabled ⇒ no synthesis ⇒ literal stock bytecode. HLSL: `shaders/src/gs_screencommand_{arrow,judge,default}.hlsl` (4-tap palette-aware bilinear PS: blend COLORS never indices, 1:1 collapses to stock; `vs_persp_main` hallway VS). Built by `scripts/build_shaders.sh` — **fxc 9.29 under the CrossOver bottle is the golden path** (`tools/fxc/`, ~4.4× smaller PS code than the Docker/vkd3d fallback). Kill switches: `mods["shader-fixes"]=false` or `layeredfs.blocklist: ["shader_fixes"]`. RE notes: `docs/shader_replacement_research.md` |
| Player perspective (StepMania lane-view family, per-player) | `src/mods/player_perspective/` — `mod.rs` (PERSPECTIVE enum row OVERHEAD/HALLWAY/DISTANT, `PersistMode::Full`, per-side atomics + GAMEPLAY latch, the shared `compute_constants` preset table + `PerspConstants::map_point` (the ONE CPU copy of the VS map — guidelines and the receptor hit flash both consume it), per-frame publication of each side's resolved constants for cross-mod CPU consumers, cull-window distance contribution whenever any side is non-OVERHEAD, guideline + flash acquires) + `pass_rewrite.rs` (the C1 mechanism: pre @ Normal on `services/render_notes_hook` emits a tag-0x14 SetVSConstantF record — c48={anchorY,cx,k,dir}, c49={d_min, z0, ty} — and snapshots the CommandList write ptr; post @ Late walks the pass window and flips tag-0x13 SetShader records for the arrow/default shader objects from program 0→1 **behind the mandatory ≥2-programs gate** (`*(u32*)(shaderObj+4) ≥ 2` — the engine's SetShader handler has NO bounds check); plus a `spot_render`-detoured receptor pass rewrite so receptors foreshorten like a note at the row; plus a `judge_effect_render`-detoured pass rewrite for `screen::JudgeEffectRenderer` — the tap hit-burst AND freeze-hold glow (arrow-sheet cells at the receptor row, own SetShader @ this+0x98 binding the JUDGE container) — side bound presence-first (versus → posX split, NO cross-side fallback; single/doubles → whichever side published constants), consuming the published per-side block). Presets are CONSTANT PRESETS against ONE parameterized VS program (design decision — never add per-perspective programs): HALLWAY anchors at the receptor row receding toward the entrance; DISTANT anchors mid-field receding past the receptors with base zoom `z0` (c49.y — **load-bearing: never emit 0**; the persp VS multiplies by it) for stock-lane-rectangle containment, plus a rigid realignment shift `ty` (c49.z) putting the shrunken receptor row back at its stock height (exactly 0 for HALLWAY). The skewed SM presets (INCOMING/SPACE) were implemented then REMOVED after live evaluation (unpleasant to play + filter-band spill in versus; old persisted values 3/4 clamp to DISTANT; `cx` stays a free constant so re-adding is a table change). The perspective VS reconstructs pixels from NDC (1280×720), applies `s = z0·k/(k+d)` about the preset's anchor + the `ty` shift, and outputs REAL `w` → rasterizer-side perspective divide + perspective-correct UVs (single-quad freeze bodies exact). Guidelines get the identical map CPU-side via playfield_styling's shared `guideline_hook`; the receptor HIT FLASH (an AFP clip the VS never touches) gets it via playfield_styling's `lane_hook` — the `note_result_setup` capture is consumer-refcounted like the guideline detours (`flash_acquire/flash_release`), the apply composes playfield scale first then `map_point`, and the perspective lane pass doubles as a drain site so it works with playfield_styling config-disabled. Shocks/mines ride the default container's prog 1. Dispatcher: `services/render_notes_hook.rs` (judge_hook model; mine_render migrated to post @ Normal; perspective's window rewrite at Late so mine records fall inside). Reverse scroll: dir from the renderer/guideline objects' own vb reverse flags (presets keep their meaning relative to scroll direction). Config: `player_perspective` {hallway_focal, hallway_draw_distance (contributed for ANY perspective side — DISTANT's `ty` shift pulls content past the stock 720 bound on screen), distant_focal, distant_zoom (SM's 0.9 compensation)}. Planning docs: `.agents/planning/20260721-player-perspective-hallway/` (mechanism POC) + `.agents/planning/20260731-perspective-expansion/` (DISTANT + z0/ty + flash tracking); feasibility RE: `docs/custom_arrow_renderer_research.md` |
| Song Playback Speed (per-player rate-adjusted songs, 25–175 % in 5 % steps, score-contained, STREAMED) | `src/mods/song_playback_speed.rs` (id `song-playback-speed`) — the `song_speed` scalar row (step 5 / coarse 10, default 100, `PersistMode::Full` → wire `mod_song_speed`; `lifecycle::snap_rate_percent` clamp+snap as the framework's first `load_transform` user; gated on `custom_options::set_option_available` + strict `row_injection_available()` AND `song_rate::runtime::integration_ready()`) feeding per-side desired-percent atomics (`song_rate::runtime::set_desired_percent` — 100/100 ⇒ identity boots keep ZERO footprint); the mod also owns the rate-aware Real Speed recompute lifecycle (`song_rate::real_speed::init/activate/deactivate` — see the dependent-features note below) and the **versus mirror** (2026-08-31): while both sides are entered, the three rows (`song_speed`/`preserve_pitch`/`sync_movie`) are MIRRORED through the shared `services/versus_mirror` service (see its row below); persistence is pull-based at save time so BOTH profiles save the shared value at logout. Engine `src/services/song_rate/` — **streaming, NO disk cache** (the 2026-08-05 cache/worker model is retired; D9 dropped `cache_limit_gib` outright): pure DSP/format in `src/core/xact/` (`stretch::StretchState` resumable pull-driven WSOLA — pitch-preserved, exact reduced rates, byte-identical to the whole-buffer reference, 5-word checkpoints; `adpcm::BlockCachePcm` on-demand decode view; `virtual_bank::plan_virtual_bank/resolve` — the virtual XWB the engine reads; `xwb::stream_pre_data` emission follows the PARSER's layout rule — stock-shaped durations inside the final block are legal, fixtures must be honest; **`xwb::parse_song_bank` accepts BOTH stock shapes — with the segment-3 entry-name table (flags `0x0009_0001`) and WITHOUT it (flags `0x0008_0001`, segment 3 = 0/0; 33 World song banks incl. `acef`/`neut`/`dais`/`rhyz` — the 2026-09-02 "song speed does nothing for specific songs" report) — main/preview identity comes from `SongBank::main_entry_index()` / `preview_entry_index()` (names when present, else the strictly LONGER entry is the main wave; never compare `entry.name() == bank.name()` yourself), and the writers reproduce whichever shape the stock bank had; **the profile is 2..=8 entries** — `goru` (GOLD RUSH, selectable) ships `goru_cs`/`goru`/`goru_ac`/`goru_s` and the stock XSB maps cue `goru` → wave 1, `goru_s` → wave 3, so `VirtualBankLayout.entries`/`entry_offsets` are `Vec`s, `resolve` walks N regions, and the binding serves EVERY non-target entry verbatim (`entry_source_offsets`) — wave indices in the served header must stay stock or the XSB's cues point at the wrong wave; also any ADPCM tail remainder is legal as long as `ceil(duration/128) == complete_blocks` (10 DDR-era previews carry 103–137-byte tails)**). Runtime: `binding.rs` (qualify/preflight → private source copy → `Binding` with 16 MiB ring (absolute virtual offsets, seqlock rewinds) + **side buffer two-region serving — PREVIEW PASSTHROUGH**: the non-main `<code>_s` entry keeps stock header values and is served VERBATIM from the resident copy (WSOLA at 47 kHz is only ~2.4× realtime under CrossOver; stretching the preview cost 23–25 s of loading — live-diagnosed twice, maintainer-approved deviation from the design's "both entries stretched"); 4 SPSC pending slots; pure allocation/log-free serve+poll dispatch), `generator.rs` (producer thread, capacity/2 pacing, behind-window regeneration, catch_unwind → silence-fill), `io_callback_hook.rs` (the XACT readFile/getOverlappedResult `GenericDetour` pair, pair-or-neither install; `OVERLAPPED.Internal` = completion accumulator), `transaction.rs` (exactly-once commit order score→movie→snapshot→**Q31 LAST** via `clock_patch`; wall-driven clock anchor patched at `+0x5CD6A` on 20260721), `lifecycle.rs` (scene-26 arm: ordinary solo/doubles arm the entered side; VERSUS arms the SHARED rate since 2026-08-31 — P1 governs, `participant_mask=0b11` so score containment covers both sides, TRAINING arms included since the same-day versus-training lift; course/unknown fail closed to identity), `runtime.rs` (the one permanent scene callback + 250 ms drain: binding reclamation sweep, per-generation metrics INFO, coalesced refusal WARN). Live-proven 2026-08-11: ~5 s loading at 25 %/175 %, a full 8.5-min 25 % song with 0 deferrals. Score containment: `score_guard` pending rate-save ledger — per-stage saves of rate-played songs SUPPRESSED, card-out logout save score-stripped, 100 % literally stock. Dependent features (plan Step 6): assist-tick positions/skips convert content→wall via `song_rate::tick_domain` (see the Assist tick row); **rate-aware Real Speed** — `song_rate::real_speed` recomputes the normalized multiplier as `Core BPM × source/output` at each side's FIRST judge dispatch (post-commit), writing the GamePlayActor multiplier cluster `+0x290/+0x294/+0x29C` (the per-frame renderer-speed source) + `Option+0x10`, Real-Speed-mode (type 0) sides only, independent of the Real Speed Fix toggle, fail-open (RE: `docs/song_playback_speed.md` §Rate-aware Real Speed); PUS CSV rate columns via `RateSnapshot::csv_rate_cells` (exact `source/output` fraction, latched with the song identity). Failures fail open to stock 100 % (EarlyFailed) with one bounded WARN; `DDR_SONG_RATE_FAULT` env injects failures (dev mode; incl. `mid-song-failure` → live silence-fill). Host validation: `scripts/validate_song_playback_speed.sh` (schema `song-rate-validation/v1`; synthetic/streaming/corpus/identity_runtime sections + 50 %/175 % engine-replay legs; needs a sibling `ddr-chart-tools` checkout; fast iteration via a temp-dir `#[path]` harness). **Preserve Song Pitch sub-option** — `preserve_pitch` bool child row ("PRESERVE SONG PITCH", default ON, `PersistMode::Full` → wire `mod_preserve_pitch`), registered right after `song_speed` and shown only while that side's speed ≠ 100 via `ShowWhen::NotEquals` (the framework variant added for this feature — 4 touch points: api.rs enum, registry.rs parent validation, rows.rs evaluator + `update_children_visibility`; live per-side show/hide rides the existing remask). OFF ⇒ the main entry streams through a **plain resampler** instead of WSOLA — pitch follows the rate (record-player style): `src/core/xact/resample.rs` (frozen whole-buffer reference + streaming `ResampleState`; shared piecewise-Q32 `PositionMap` (direct per-frame multiplication, loop segment maps `output_start ↦ source_start`) + half-away linear interpolation; O(1) `positioned_at` seeks — no checkpoints), swapped in at the `DspState::{Wsola, Resample}` seam inside the generator's `Feed` (mode read once from `Binding.preserve_pitch`; regen in resample mode = direct seek). Flag carriage mirrors the percent: `runtime::DESIRED_PRESERVE_PITCH` atomics (default true) → scene-26 `EligibilityInputs.desired_preserve` → entered side's flag into `ArmRequest.preserve_pitch` → `LifecycleState` atomic (arm log line shows `preserve_pitch=`) → `prepare_binding` param → `Binding`; NOT in `RateSnapshot` (every rate consumer keys off `effective_rate`, identical in both modes — same plan, same block-quantized length, same clock/ticks/Real-Speed/score-containment). Identity/course never consult it (versus consults P1's, like the percent); failures fail open to pitch-preserved. Validation: `resample_*` host tests (reference/streaming byte identity, chunking, seek, loop map) + `resample_mode_*` generator tests (byte-equal to a resample oracle through the full serve dispatch) + the harness's `resample` report section (INVERTED pitch expectation `f_out ≈ f_src·S/O`, measured by zero-crossing fundamental — the autocorrelation `estimate_frequency` folds subharmonically and is only valid for ratio-invariant checks). Textures: `seop_item_preserve_pitch` + `seop_image_preserve_pitch_{off,on}` via `scripts/gen_option_labels.py`. Backend: bemani-buddy `mod_song_speed` → nullable `opt_mod_song_speed` and `mod_preserve_pitch` → `opt_mod_preserve_pitch` (migration 014), stored VERBATIM. Config: NONE (retired `cache_limit_gib` dropped outright; `diagnostic` parse-but-ignore, one INFO). RE notes: `docs/xact_streaming_research.md`; planning: `.agents/planning/2026-08-08-song-rate-streaming/` (supersedes `2026-08-05-song-playback-speed/`, whose policy/score/clock/option/backend sections remain accurate); preserve-pitch sub-option: `.agents/planning/2026-08-12-preserve-pitch-toggle/` |
| Song-select preview rate (previews obey SONG SPEED / PRESERVE PITCH, live-edit restart) | `src/services/song_rate/preview.rs` — the Song Playback Speed feature's preview half (no separate mod, no config: on with the `song-playback-speed` mod, D11). Two mechanisms, ZERO new hooks. **(1) Wheel-settle binding:** every slot-5 dance-bank create at scene 25 already flows through the detoured `wavebank_create`; after the gameplay path resolves Stock, the preview branch (`maybe_bind_preview`) qualifies (pure host-tested `qualify`: a governed side desiring a supported ≠ 100 % rate — single entered side, or P1 in versus (2026-08-31, gameplay-classifier parity); none/unreadable ⇒ stock, D3) and publishes a `StretchTarget::Side` virtual bank (stretched `_s` entry, verbatim main — the INVERSE of the gameplay plan; `virtual_bank::plan_virtual_bank` grew the 3rd param + `target_entry_index`, `prepare_binding` grew `target`) into the registry's dedicated PREVIEW slot (`publish_preview`/`with_preview`/`retire_preview`; both-slot `retire_by_file` means the unregister prelude retires previews on every natural teardown; own monotonic generation counter, R15; refusal mailbox → drain WARN). Preview bindings NEVER touch Q31/score/movie/lifecycle/XactSlots (R8); the io miss path checks the preview slot after an active-slot miss. **(2) Live-edit restart:** the option rows' `on_change` stamps `RefreshCell` (atomics-only); a per-frame executor (`input_manager::on_frame` — NEW frame-callback API dispatched at the top of `poll()` before the ark gate, panic-contained) fires 150 ms after the last tick (D4; superseded if the `selected_song` seqlock generation advanced — a settle already re-qualified), re-validates the loader chain `TS→*(TS+0x58) live→View(+0xB8, vftable identity)→AudioPlayer(+0xC8)→loader(+0x08, vftable identity)` + `loader_sane` (slot 5/mode 1/ids ≥ 0) + rows ∈ {0,5,6,8} + cue `*_s`, then runs the stock sequence `cue_handle_stop(handle)` → unregister XSB→XWB via the PATCHED entries (prelude retires the binding; `GenericDetour::call` would BYPASS the detour) → `sound_bank_create_router(xwb→xsb)` (the XWB create re-qualifies through the preview branch — or stays stock at 100 %, R5 free) → `loader.handle = −1; failed = 0` (the game's own tick replays the cue). The restart can NOT use the game's request façade (dedupes on cue; acquires-before-releases refs — a same-song request never re-creates the bank). **Preview play watchdog** (deploy-#1 fix, "slightly late but reliable"): the loader's tick fires `se_play` without waiting for XACT stream prepare; a WSOLA preview's first 64 KiB packet takes ~0.6 s (output-frame-bound), a Play landing in the window fails and latches `failed` forever — the same frame executor re-arms (`failed=0`, `handle=−1`) once the binding's produced watermark covers `min(target_data_start+0x10000, target_data_end)`, ONE retry per preview generation (engine RE ruled out short completions — the completion poll discards the byte count — and smaller initial reads). Fail-open EVERYWHERE (one latched WARN per class; stock preview at worst); scene-exit force-retire (R7) + cabinet-proven fresh-file-id confirm flow keep preview headers out of gameplay. Signatures: `audio_loader_ctor` / `selectmusic_view_ctor` (→ the two derived vftable identity gates; note the View vftable is the ctor's SECOND LEA — the first is an inner interface vftable at +0x28) / `cue_handle_stop` / `sound_bank_create_router`, all four validated exactly-once on all four builds, declared through the all-or-nothing `preview::init_restart` (5th pointer: the patched `song_rate_wavebank_unregister` entry) — NEVER `required_signatures` (a miss degrades to wheel-settle-only previews, gameplay rate untouched). RE notes: `docs/song_preview_pipeline_research.md`; planning: `.agents/planning/2026-08-15-song-preview-rate/` (research §9 = the four-build byte authority) |
| Versus option mirror + 2P training sessions | `src/services/versus_mirror.rs` — the shared "one value governs both players" driver for rows whose backing mechanism is cabinet-global (one clock, one dance bank, one timeline). Consumers register row ids at enable (`register`/`unregister`); a per-frame driver ENGAGES at the first SONG-SELECT frame with both sides entered (loads resolved by then — P1 genuinely seeds) seeding P2 ← P1 via `custom_options::set_value`, each row's `on_change` tail calls `mirror_edit` (set_value cross-side sync, unchanged-value check terminates recursion, last writer wins), and it DISENGAGES the instant either entered flag drops. Derived side effects (training's silent sibling nudge) propagate by RE-EXECUTION on the other side, never by copying. Consumers: song_playback_speed (`song_speed`/`preserve_pitch`/`sync_movie`), premium_free (`premium_free` — one cabinet-wide stage counter), training_mode (`training_start_time`/`training_end_time`/`training_loop_song`/`training_progress_pos` — one strip, so placement mirrors too), assist_tick (`assist_tick`/`assist_tick_volume` — one cabinet-wide tick track). **2P training (2026-08-31 lift):** `classify_scene26` arms versus training sessions (P1 governs, mask 0b11 — `IdentityReason::VersusTrainingArm` retired); both players share ONE training session — gestures/scrubs/loops move the one shared timeline and `taint_entered_sides` taints BOTH sides at every alteration site (resolution latch, marker sets, scrubs — the no-reset truncation paths were the score-integrity gap). Per-side correctness rework that made versus safe: CMA end-threshold stash/convert/restore is PER-SIDE (versus sides play different charts; `song_reset::set_chart_end_thresholds_per_side` all-or-nothing writer), the loop death-bypass gate stash is PER-SIDE (`death_gate_for_side`/`set_death_gate_for_side` — sides can run different gauge classes), the loop fire bound folds MIN across both sides' thresholds (any-side conversion failure degrades the whole display term), and the strip HUD pins its once-per-song snapshot to P1's actor in versus (`SNAPSHOT_SIDE_FILTER`; solo/doubles keep accept-any). NOT mirrored: `autoplay` (genuinely per-side). Loop-in-versus semantics: EITHER side's death revives BOTH into the next pass (shared grind, `any_actor_dead`) |

| Background Movie Sync (movie follows every song-timeline jump; optional rate-locked movies at non-100 % SONG SPEED) | `src/services/movie_sync.rs` — always-on service (no separate mod; init at lib.rs §5a). **Position sync (any rate, incl. 100 %):** captures the live `DShowPlayer` from `movie_policy`'s BuildGraph detour (`Passthrough` hr==0 only — suppressed/faked paths never reach it; capture dropped on every scene change), and every timeline alteration (quick/delayed restart, training scrub/loop/SONG START — the `song_reset::on_song_reset` subscription + a capture-time sync for late graph builds) queues a pending seek drained by an `input_manager::on_frame` driver ONLY while the player is RUNNING (paused-graph seeks break Wine — probe v1 post-mortem in the module docs). The drain classifies against the LIVE music count (pure host-tested `drain_action` + `is_advancing` stability gate) and EVERYTHING executes as a **park**: seek to the destination via the game's own null-guarded absolute seek (player vtbl +0x58), measure restart latency from the frame-deposit slot `*(player+0x78)+0x250` (NEVER the IMediaSeeking position readback — on Wine it tracks the graph clock, not presentation), PAUSE on the frame's arrival (game command byte via vtbl +0x18/+0x20), RUN at the live-count crossing. Jumps (scrubs) park at `live + Δ` (Δ = 2× the per-direction per-capture estimate — NO cross-song state, NO hardcoded sync offsets, both maintainer directives; 14 cabinet deploys settled this). `map_position` honors the loop flag; non-loop clamps to `duration − 500 ms` (seeking AT the end fires the stock EC_COMPLETE stop — unrecoverable, live-observed). Delayed quick restarts are covered by the same park (frozen first frame through the countdown, run at the 0-crossing). **Rate sync:** the `sync_movie` bool child row of SONG SPEED in `src/mods/song_playback_speed.rs` ("SYNC BACKGROUND VIDEO", 2nd child after `preserve_pitch`, default OFF, `ShowWhen::NotEquals(100)`, `PersistMode::Full` → wire `mod_sync_movie`, bemani-buddy `opt_mod_sync_movie` migration 018): ON ⇒ the non-identity arm/commit SKIPS the `MovieSuppressor::SongRate` set (carriage mirrors preserve_pitch: `runtime::DESIRED_SYNC_MOVIE` → `EligibilityInputs.desired_sync` → `ArmRequest.sync_movie` → `LifecycleState.sync_movie`; the commit-side skip lives in `transaction.rs::finish_create`, host-tested) and the graph open consumes `runtime::movie_rate_directive()` → installs the **scaled reference-clock proxy** (`movie_sync::rate_clock` — a single-instance `IReferenceClock` COM object wrapping the graph's own sync source: `GetTime = T0 + (inner − t0)·(source/output)` with a fetch_max monotonic floor, advise deadlines/periods inverse-mapped and forwarded to the wrapped clock) via `IMediaFilter::SetSyncSource` on the still-paused graph (adaptive install — see the platform note below) — filters pace streaming by timestamp vs the graph clock, so the movie plays at the scaled rate with zero chain cooperation. `IMediaSeeking::SetRate` is NOT used: the game's WMV chain (its own custom renderer '0001' {8313E2A7-…} + WMVideo Decoder DMO + WM ASF Reader) refuses it categorically (E_INVALIDARG at every rate, paused AND running, per-filter probes — Windows cabinet tests #1–#3 2026-08-23; FR-3 amended, maintainer-approved). Install verified by **GetSyncSource POINTER READBACK — never the HRESULT alone**; any failure degrades PRE-RUN via `degrade_to_suppressed`: direct `IMediaControl::Stop` (player+0x50, vtbl +0x48) + **zero the `opened` byte (+0x14)** — the suppress path's `fake_opened` shape so the game shows its static no-movie background (post-Run degradations strand a black plane (`opened=1`) or a frozen first frame (actor latches movie mode) — both live-observed; direct COM is legal there because get-frame never dispatches the command byte again once `opened` drops) + one WARN, song unaffected. The proxy's inner-clock ref is held until the NEXT install (graph threads may call GetTime between scene change and teardown). **Platform-uniform (D14 SUPERSEDED 2026-08-24):** the capability gate is `movie_sync::is_available()` alone — the clock proxy works under Wine too (its install is ADAPTIVE: in-place `SetSyncSource` where the FGM permits a paused-graph swap — Wine; escalated stop→swap→re-pause on `VFW_E_NOT_STOPPED` — Windows; Wine's cue/stop state machine WEDGES on the unnecessary dance, CrossOver trial #1 — never stop/re-pause a Wine graph that accepted the in-place swap). Under Wine movies exist at all only in `non_native_os_support.movie_mode="fallback"` (suppress mode never builds a graph). D14's dead ends stay dead: builtin quartz SetRate is a triple-confirmed silent no-op, native quartz in the bottle hard-locks (do NOT retry), seek-based drift correction rejected as stuttery — the clock proxy simply needs none of them. Cabinet-VALIDATED both platforms 2026-08-24 (Windows test #5: 50%/80%/175%; CrossOver trial #2: all rates + off-rate scrubs in sync); a per-song consumption diagnostic (`clock proxy consumption at Run+2s — N GetTime call(s)...`) plus a 15 s never-reached-RUNNING wedge WARN make "verified but ignored/wedged" outcomes self-diagnosing; `DDR_MOVIE_SYNC_PROBE` (dev) dumps the live graph + per-filter SetRate evidence; `DDR_MOVIE_SYNC_FAULT=set-rate\|seek` (dev-mode-gated) exercises the failure ladder. NOTE: the option row's label textures ship as `data_mods/custom_options/.../seop_item_sync_movie*` PNGs — a DLL-only deploy leaves the row invisible (blank label, `get_bitmap_info[seop_item_sync_movie] can not find` in the log). RE notes: `.agents/planning/2026-08-20-background-movie-sync/research/movie-sync-re.md` (note: the earlier non-native-os movie RE had player vtbl +0x48/+0x58 swapped — +0x48 is a deferred volume setter, +0x58 is the seek); planning: `.agents/planning/2026-08-20-background-movie-sync/` |

## Workflow

Solo-maintainer repo: sole owner, no code-review system, no CI gate. The maintainer
manages `git commit` / push themselves. When working through a multi-step plan or task
list, don't stop and hand back between tasks waiting for a CR or approval handoff —
continue through the plan in the same session, running each step's validation as you
go. Commit/push only when the maintainer asks.

### Git rules (agents)

- **NEVER run `git commit` (or push) unless the maintainer explicitly asks in the
  current session.** This includes commits mandated by skills/SOPs (e.g. code-assist's
  Commit step): skip that step, leave the work staged or unstaged, and report what
  would have been committed. Where a workflow tracks completion by commit hash, record
  `Status: Complete (uncommitted — maintainer commits manually)` instead.
- When the maintainer DOES authorize a commit, write a plain conventional-commit
  message with **no attribution trailers** — no `🤖 Assisted by …`, no
  `Co-authored-by`, no tool footers.

## Build

```bash
cargo check --target x86_64-pc-windows-msvc   # fast type check
cargo test                                      # host tests (pure layers: song_rate, core/xact, …)
./scripts/validate_signatures.sh ~/Desktop/ddr_modules   # offline 4-build signature/derivation sweep (see Key Entry Points)
cargo fmt                                       # auto-format (run before commit)
./build.sh                                      # release build (cargo-xwin)
./scripts/deploy.sh                             # build + SCP to cabinet
```

Nightly toolchain required (pinned in `rust-toolchain.toml` — needed by `retour`'s
static-detour feature; do not switch to stable). Pure modules (the `song_rate` /
`core/xact` families, format/DSP layers, etc.) carry extensive host tests run via
`cargo test`; everything engine-facing has no test harness — validation is live
cabinet deployment + log observation.

Readiness gates before handing a build to the maintainer (or committing):
`cargo check` clean → `cargo fmt` (whole crate — never pass file args; a
targeted `cargo fmt -- <file>` still formats the whole crate and produces
unrelated churn) → `./build.sh` clean → **if `signatures.rs` or any
consumer-side fixed offset changed: `./scripts/validate_signatures.sh
~/Desktop/ddr_modules` ALL GREEN** (and `shape_diff.py` for anything that
reads `match+N`).

## Code Navigation (LSP)

This project is configured with a **rust-analyzer LSP** — prefer it over `grep`/`rg`
for code intelligence: `goToDefinition`, `findReferences`, `hover` (types/signatures),
`documentSymbol`, `workspaceSymbol` (find a symbol by name across the crate), and the
call-hierarchy ops. It resolves `static`/trait/`extern "C"` symbols and gives exact
types where text search guesses. Note: rust-analyzer indexes the whole crate on first
use, so the first query in a session may return "server is starting" — retry after a
moment. Keep `grep` for non-symbol scans (AOB byte patterns, comments, log strings).

## Patterns That Deviate From Defaults

- **`static mut` for hook state** — Intentional. Hook callbacks are `extern "C"` and can't capture. Accessing via `std::ptr::addr_of!` with null guards.
- **`unsafe impl Send`** — Raw pointers to game memory are valid for process lifetime. Required for `Arc<Mutex<State>>`.
- **`#![allow(dead_code)]`** — Crate-wide. Many public items are only called at runtime by hook callbacks or reserved for future mods.
- **No `println!`/`eprintln!`** — All logging through `log_info!`/`log_warn!`/`log_error!`/`log_debug!` → `OutputDebugStringA`.
- **One detour per target function** — When multiple mods need the same hook, use a shared dispatcher (see `judge_hook`). Never install two `GenericDetour` handles on the same address.
- **Three allocator heaps** — CRT heap (`game_malloc`), AGCS app heap (`agcs_heap_malloc`), VirtualAlloc (`memory::alloc_zeroed`). Mismatch = crash in `RtlFreeHeap`. See `data_models.md` for the table.

## Rust Quality Rules

Shaped by being an in-process hook DLL where the engine-facing code has no test harness:

1. **Panics must not cross FFI.** Hook callbacks are `extern "C"` — a panic unwinding into game code is undefined behavior. Wrap fallible callback bodies in `std::panic::catch_unwind` (see `scene_manager` for the pattern) or keep them strictly panic-free. No `unwrap()` / `expect()` / indexing / `unreachable!()` inside a hook callback, render-thread closure, or input callback; reserve those for init-time code, and prefer graceful degradation even there.
2. **Graceful degradation over hard failure.** If a signature doesn't resolve or a service fails to init, log a warning and continue — other mods may still work. Most service functions return `Option<T>`; callers check `is_available()`. Use `require_address()` (panicking) only when a mod literally cannot function without that address, and declare it in `required_signatures()` so `ModRegistry` can skip the mod cleanly.
3. **Respect thread boundaries.** Widget creation/mutation and texture resolution happen on the game's render thread — use `widget_renderer::run_on_render_thread()`. Background threads can read but not mutate. Do not hold a state `Mutex` across a `run_on_render_thread` schedule (the closure deadlocks against your own lock).
4. **Keep hot-path callbacks tight.** `judgeNotes`, the render hook, and `input_manager::poll` run every frame. Work over ~1 ms stutters the game — stash a flag and defer heavy work to another thread or a later render-thread closure.
5. **Use the scanner primitives.** `core/scanner.rs` provides `decode_rip_relative`, `decode_call_rel32`, `scan_first_call_rel32`, `scan_xrefs_to`. Don't reimplement RIP-relative decoding inline; add a new primitive to `scanner.rs` once it has two call sites.
6. **No hardcoded offsets in hook code.** Addresses come from AOB signatures (`core/signatures.rs`), RTTI walks, or RIP-relative derivation from a scanned landmark. Absolute addresses are acceptable only in `docs/` research notes, written file-relative to the module base.
7. **Stay inside the module layout.** New functionality belongs in `core/` (game-agnostic), `services/` (game-system integrations), `widgets/`, `mods/`, or `types/`. Mods that outgrow a single file get a subdirectory (`mods/note_types_expansion/` is the reference).
8. **`unsafe` is expected — discipline isn't optional.** Keep `unsafe` blocks narrow (scoped to the operation, not the function) and structure the surrounding code so the soundness argument is obvious.
9. **Verify after changing a hook.** "It compiles" is a weak signal. After touching `signatures.rs`, a hook callback, or a memory layout, run `cargo check` *and* plan a deploy test — observe the relevant logs and visual behavior. When investigating apparent runtime bugs, ship a diagnostic build with one-shot WARN/INFO logs on every fallback branch before rewriting (see `.agents/learnings/learnings.md`).

## Config

`mod-config.json` — single file for all configuration:
- `mods`: enable/disable map
- `layeredfs`: verbose, dev mode, mod folder path
- `series_expansion`: custom series definitions
- `folder_expansion`: custom folder definitions
- `custom_options`: persistence gates (`persist_network`, `persist_json`, both
  default true) plus the offline `p1`/`p2` value cache for every registered
  `PersistMode::Full` option (Premium Free, Autoplay, PUS toggles, etc. — all
  mods). The WebUI cosmetics are `PersistMode::SaveOnly`: network-save-only
  (no network load, no JSON cache) — the server stores them in its native
  customize columns, the game's own `<customize>` load applies them, and the
  DLL seeds its menu by reading the game's `Customize` object at scene-25
  entry. Legacy `webui_options` blocks
  are auto-migrated into here on first boot. Also holds
  `lane_gamma_correction` (optional `f32`, default `6.0`): the Photoshop-convention
  gamma (`>1` brightens) applied to the WebUI lane-skin preview art, read at
  `webui_options` enable time and threaded through `preview_overlay`'s
  pre-brightened lane arc cache (which auto-regenerates when the value changes).
  And `preview_window` (optional `i32`, default `3`, clamped 0..=10): the
  half-width N of the WebUI preview prefetch window — the overlay keeps
  `[cur-N, cur+N]` of the FOCUSED cosmetic category's assets resident while the
  options modal is open (one category at a time; a wider prefetch overwhelmed
  the engine's serialized file queue). `preview_window` is ALSO the window for
  the animated BACKGROUND previews (each slot is a full background AFP package).
  And `animate_backgrounds` (optional `bool`, default `true`): whether the two
  BACKGROUND rows' previews animate; `false` shows a static first frame
  (`bg_preview_overlay` calls `layer_play(0.0)` instead of `1.0`).
  And `option_menu_settings` (optional array of `{id, overlay?, in_game?}`,
  default absent → registration order, both menus): operator-defined option
  placement + display order for BOTH the in-game MODS tab and the overlay menu.
  Array order = display order (listed ids first, unlisted appended in
  registration order; unknown ids → one latched WARN, ignored); the optional
  booleans override each option's registration-time `MenuPlacement`. Read once
  at `custom_options::init` into `custom_options::ordering` (settings store +
  `placement_override_for`). Registry indices / `OptionHandle`s are never
  reordered (handles stay stable). Cabinet-wide; edits apply next launch.
  Operator-authored only — the DLL never writes it. (Replaced the retired
  `row_order` key — old configs were migrated in Step 5 of the overlay-menu
  rewrite.)
- `overlay_menu`: the DLL-owned appearance section for the overlay mod menu —
  `theme` (`"arrows"` | `"bubbles"` | `"wavefield"` | `"minimal"`, default
  arrows; unknown ⇒ one WARN + arrows), `animate_background` (default `true`),
  `opacity` (25..=100, clamp+snap 5, default 80). Unlike most sections the DLL
  WRITES this one: every APPEARANCE-tab change persists the whole section via
  `save_json_key`. Theme/opacity drive the synthesized panel chrome
  (`mod_menu/chrome_loader.rs`) and the animated shader background. Also holds
  the last-position memory `last_tab` (tab id `mods` / `global_settings` /
  `player_settings` / `appearance`) + `last_row` (the selected row's stable
  key), written at every menu close and restored at open (stale ids/keys
  degrade to the tab top via `clamp_after_rebuild`).
- `timing_offsets`: the four cabinet-wide timing offsets (sound/input/render/bomb),
  written on overlay change; missing keys use the game's stock defaults. The
  auto-calibration apply writes `sound_offset` through the same path; the
  "Calibrate next song?" arm itself is in-memory only and never persisted.
- `fps_unlock`: `presets` (editable FPS list, default `[60,120,144,165,240,360]`) +
  `selected` (raw FPS value, default `60`). The `fps-unlock` mod AOB-byte-patches the
  display-refresh imm32 in `Application::onBoot` during `early_apply` (consumed once at
  boot → applies next launch). Normalized in-memory; only `selected` is written back
  (operator's `presets` array preserved). Overlay shows it as an `Enum` row.
- `quick_restart`: `restart_delay_ms` (default `0`, clamped 0..=10000) — countdown
  between the press-1 restart gesture and the restarted song's start. Consumed by the
  in-place song reset (`services/song_reset.rs` `request_reset(t_ms, delay_ms, …)`):
  the field resets IMMEDIATELY with the timing anchor future-dated by the delay (music
  count runs negative = the engine's own pre-song approach), the cue replay is issued
  `REPLAY_LEAD_MS` before the countdown ends, and an idempotent second `0x1044`
  re-anchor fires when it is prepared (the same prepared→anchor adjacency as the
  instant path, so audio/chart sync is identical). Live-adjustable via the
  `RESTART DELAY` overlay enum row under the mod's toggle (fps_unlock pattern:
  presets 0–10000 ms in 500 ms steps, out-of-list config values auto-added; row
  callback persists the whole `quick_restart` section via `save_json_key`);
  effective at the next gesture.
- `assist_tick`: holds only the RETIRED `offset_ms` key (the old per-tick path's latency
  knob). Parsed-but-ignored — one INFO at enable when present, never reinterpreted, never
  written back. The pre-mixed tick track derives its timing from game state (cabinet
  `sound_offset` + per-side JUDGMENT TIMING); there is no latency knob.
- `per_song_judgement_offsets`: `mirror_players` (optional `bool`, default `false`) —
  when true, an options-menu offset edit by EITHER side applies to BOTH sides in-sync
  (both session maps, both `judgement_offsets.csv` columns, the other side's rows
  silently re-seeded; last writer wins, no contention policy). For solo home players
  without a persisting backend who swap pads. Operator-authored only — the DLL never
  writes it; read once at mod enable (next-launch semantics).
- `non_native_os_support`: `movie_mode` (optional `string`, default `"suppress"`) —
  background-movie graph handling for the `non-native-operating-system-support` mod.
  `"suppress"` never builds the DirectShow graph (crash-safe, movies absent);
  `"fallback"` builds the real graph and only fakes success when it FAILS
  (requires spice2x `-audiohookdisable` under Wine — with audio hooks enabled the
  real graph build crashes in Wine's builtin `winmm`). In fallback mode movies
  play natively when the bottle can decode them: stock VC-1 via the native WM
  runtime + the mod-installed `mfplat_vih_fix` detour (recipe:
  `docs/native_wm_runtime_bottle_setup.md`), H.264 conversions via
  winegstreamer; anything undecodable degrades to no-movie. Unknown values warn
  once and use `"suppress"`. Operator-authored only — the DLL never writes it;
  read at mod enable.

## Detailed Documentation

- `.agents/summary/` — generated architectural docs. Start at `.agents/summary/index.md` for the decision tree: `architecture.md` (layers, init sequence, thread model), `components.md` (every module), `interfaces.md` (Mod trait, hooks table, APIs), `data_models.md` (memory layouts, allocator table, config schema), `workflows.md` (build/deploy + runtime flows), `dependencies.md` (crates, toolchain).
- `.agents/steering/rust-hooking.md` — allocator rules (CRT heap vs. app heap), hook callback patterns, shared_ptr layout, instruction-decoding primitives, and design rationale (why `retour`, why `static mut` on hot paths).
- `.agents/steering/reverse-engineering.md` — target modules, address-space conventions, derivation anchors, `docs/` research-note structure.
- `.agents/learnings/learnings.md` — hard-won, project-specific traps captured from prior sessions. Check here when something feels subtle.
- `.agents/planning/<feature>/` — per-feature PDD docs (rough-idea → idea-honing → research → design → implementation → summary) for in-flight features. `.agents/planning/_archive/` holds completed features' docs (historical reference).
- `docs/` — reverse-engineering research notes (addresses are file-relative to `gamemdx.dll`'s `0x180000000` base). `README.md` is user-facing documentation for operators installing the DLL.

## Custom Instructions

<!-- This section is maintained by developers and agents during day-to-day work.
     It is NOT auto-generated by codebase-summary and MUST be preserved during refreshes.
     Add project-specific conventions, gotchas, and workflow requirements here. -->

### PDD feature progress tracking

For features developed under `.agents/planning/<date>-<name>/`, maintain a `progress.md`
in that feature directory throughout implementation. It is the live resume point — written
so a fresh agent with zero context can pick up the work after a context-window reset by
reading it once. It complements `implementation/plan.md` (the stable plan + step checklist),
it does not replace it.

- **Top of file:** an `Updated:` date, a `Status:` line (`Step N of M — in progress|blocked|
  done`), and a single **`NEXT ACTION:`** line stating the exact next thing to do (file /
  function / command), plus a one-line resume protocol pointer to the plan/design/research.
- **Sections:** `Done` (one line per completed step + outcome), `In flight` (current edit /
  uncommitted work), `Deploy & test log` (each cabinet deploy → observed result; this repo's
  only real validation), `Deviations & open questions`, and a condensed `Key facts for a cold
  resume`.
- **Update it after each implementation step and before any long pause / handoff.** Keep it
  tight — it's a resume aid, not a journal.
- **Per-task working documents belong to the skills that create them.** Skills like
  `code-assist` (invoked directly or via `code-task-generator`) keep their own per-task
  artifacts — context/plan/progress/logs — in their own default working directories and
  use markers like a `Status: Complete <hash>` line in the task's `progress.md` to track
  task/step completion. Let them: those markers are how the tooling decides whether a
  plan step's tasks are done. The artifacts are task-scoped working records; the
  feature-level `progress.md` (planning dir) plus the ticked checklist in
  `implementation/plan.md` remain the cross-session resume points. Don't duplicate one
  into the other — summarize and link instead.
- **Task files** produced by `code-task-generator` live under
  `.agents/tasks/<feature>/step<NN>/`. They are inputs to implementation, not a progress
  record — never track status in them.

### Shared movie hook ownership

- `src/services/movie_policy.rs` owns the sole `DShowPlayer::BuildGraph` detour. `non_native_os_support` only toggles `MovieSuppressor::NonNativeOs`; song-rate suppression has its own contributor, set tentatively at a non-identity scene-26 arm and confirmed at commit (background movies are suppressed for rate-played songs — the DirectShow graph clock cannot follow the XACT rate).
