# Components

> Machine-generated by the codebase-summary workflow. Do not hand-edit; regenerate instead.
> Mechanism detail lives in each module's `//!` doc and in the `docs/` RE note named in the tables. The hook-ownership map and cross-mod seams are in `interfaces.md`.

## src/lib.rs

`DllMain` (spawns `init()`), the init sequence (see `architecture.md`), mod construction in registration order, and the boot splash.

## src/core/ — game-agnostic infrastructure

| Module | Responsibility |
|---|---|
| `scanner.rs` | AOB scan (wildcards, Aho-Corasick batch); instruction-decode primitives: `decode_rip_relative`, `decode_call_rel32`, `scan_first_call_rel32`, `scan_xrefs_to`, `find_function_entry` |
| `signatures.rs` | The signature registry. `SIGNATURES` table (entries carry annotated RE notes), `resolve_all` (one batch scan), `resolve_derived` (the `derive_*` / `find_*` family, fail-closed), `get_address` / `require_address`, typed layout accessors (e.g. `gameplay_actor_layout()`, `shutter_actor_layout()`), `find_vtable_by_rtti`, `publish_value` / `published_value` for derived non-address values |
| `hooks.rs` | `install_enabled` (store-then-enable), `HookManager` |
| `hook_transaction.rs` | All-or-nothing multi-hook install with reverse rollback |
| `memory.rs` / `memory_patch.rs` | `is_readable` (VirtualQuery), `alloc_zeroed`, `alloc_near(_rel32)`, protection helpers, checked patch transactions with rollback |
| `frame_pump.rs` | One queued batch per engine frame with reentrancy guard and panic containment; backs `run_on_render_thread` |
| `deferred_work.rs` | `Deadline`, `PendingPump` (coalesced, cancellable render-thread continuations) |
| `msvc.rs` | MSVC ABI layouts: `MsvcString`, `MsvcVec<T>`, `SharedPtrPair` |
| `module_resolver.rs` | Locate gamemdx / ark / libavs modules |
| `logger.rs`, `crash_handler.rs`, `profiling.rs`, `platform.rs` | `log_*!` macros (OutputDebugString), panic hook, SEH → `ddr_hook_crash.log`, init timing, Wine detection |
| `arc.rs`, `ifs.rs`, `afp.rs`, `ap2/`, `geo.rs` | Konami containers and AFP: ARC parse/write, IFS extract, AFP descramble/patch, AP2 byte-identical parse/edit/write, GE2D geo labels |
| `ssq/` | Chart chunk walker + engine-exact tempo converter |
| `xact/` | Pure XACT2 layer: MS-ADPCM, WSOLA stretch, resampler, XWB, virtual bank planning, rate math, MD5 |
| `anm/` | Pure ANM/CAMANM/B2IT/MRL0/KTMDL codecs, pose + camera sampling (background dancers) |

## src/services/ — game-system integrations

Each service exposes `init()` / `is_available()` and degrades gracefully.

**Event backbones and frame**

| Service | Role |
|---|---|
| `scene_manager.rs` | Current scene (0-indexed), scene-change callbacks, one-shot/persistent redirects + redirect repair |
| `input_manager.rs` | Ark IO polling and button events, `on_frame` callbacks, exclusive consumer, panel polling, pinpad/card/menu injection |
| `judge_hook.rs` | `judgeNotes` dispatcher (pre/post, `Priority::{Early, Normal, Late}`) |
| `render_notes_hook.rs` | `ArrowRenderer::render_notes` dispatcher + command-list access |
| `analyze_hook.rs` | `IStepReader::Analyze` post-subscriber dispatcher |
| `widget_renderer.rs` | Native widget lifecycle, `run_on_render_thread`, `bring_to_front`, `dispatch_frame` |
| `overlay_draw/` | Layer-dispatcher detour (the frame seam), DLL-owned command-list quads (menu backgrounds, SMX topmost) |

**Rendering, UI and assets**

| Service | Role |
|---|---|
| `avs_layeredfs/` | File replacement hooks + texture/arc/xml/atlas pipelines + `shader_synthesis` / `shader_layout` |
| `texture_resolver.rs`, `asset_loader.rs` | Texture name → id/UV; on-demand arc texture loads |
| `bm2d_api.rs`, `bm2d_package.rs`, `afp_patcher.rs` | MovieClip/AFP-layer wrappers; package load/lookup/release; AFP template patching at stream create |
| `custom_options/` | Option-row framework (see `interfaces.md`) incl. label-atlas generation (eng/jpn/kor) |
| `options_scroll.rs`, `series_filter_scroll.rs` | Overflow scrolling for options tabs / VERSION filter |
| `toast/` | Shared bottom-centre toast |
| `hud_layout_hooks.rs` | Single owner of the LayoutActor marker builder/setter |
| `combo_hooks.rs` | Single owner of every ComboActor detour |
| `call_voice_hooks.rs` | Single owner of the announcer dispatcher (mute + override slots) |
| `bottom_text.rs` | Bottom status text hide (OR of reasons) |
| `shutter.rs` | ShutterActor snapshot / `0x100c` dismiss / drain unblock |
| `selectmusic_highlight.rs` | Wheel-highlighted song + difficulty |
| `cull_window.rs` | Multi-contributor note-cull window widening (byte patch) |
| `scene3d/` | 3D scene service: arc sets/mounts, render items, nodes, frame board, camera, viewport passes |

**Audio and timing**

| Service | Role |
|---|---|
| `game_audio.rs` | XACT ABI surface: mod banks, cues, tick bank, versus pan |
| `se_bank_synth/` | Pure MS-ADPCM/XSB/XWB writers + clap mixer |
| `song_rate/` | Streamed rate engine: binding, generator, IO/wavebank hook pairs, clock patch, transaction, lifecycle, preview, real-speed, tick domain |
| `audio_clock/` | DAC-authority clock: pure `fit`/`onset`/`seqpub`, engine observers, game tick pairing |
| `audio_sync_diag/` | Boot-gated timing diagnostics + owner of the XACT engine detours |
| `chart_length.rs` | SSQ-derived song lengths (background worker + cache) |
| `movie_policy.rs`, `movie_sync.rs`, `mfplat_vih_fix.rs` | BuildGraph suppress/fallback policy; movie position/rate sync (clock proxy); Wine VC-1 fix |

**Gameplay state and policy**

| Service | Role |
|---|---|
| `stage_records.rs` | Fail-closed decode of GameWork/PlayerWork/record layout |
| `score_guard.rs` | Score taint state + rate-save ledger |
| `custom_options_persistence.rs` | ess save/load bridge, string fields, `/data` node producers, score-policy enforcement |
| `song_reset/` | In-place restart/seek + DPS/GamePlayActor probes |
| `foot_panel_swap/` | Single owner of the judge-time foot-panel slot swap (`Off/Perfect/Bot`) |
| `versus_mirror.rs` | One value governs both players for cabinet-global rows |
| `ident_override.rs` | Soft-id revision override at `ea3_boot` |
| `smx/` | StepManiaX HID layer (protocol, maps, device discovery, transport threads) |
| `ntdll_state_shim.rs` | Retained but uncalled (pattern reference) |

## src/mods/ — features

The trait is in `src/mods/mod_trait.rs` and registration order is in `src/lib.rs`. `src/mods/config.rs` reads `mod-config.json` and writes it back section by section.

**Default** is ON unless the mod is listed in `DEFAULT_OFF_MODS`. **Harness** means the matching `scripts/validate_*.sh` script. **RE note** names the relevant file under `docs/`.

| id | Entry | Purpose | Harness | RE note |
|---|---|---|---|---|
| `song-limit-expansion` | `song_limit_expansion.rs` | XML read buffers 1 MB → 8 MB (`early_apply`) | – | `omnimix_song_limit_research.md` |
| `fps-unlock` | `fps_unlock.rs` | Boot-time refresh target override | – | – |
| `custom-resolution` | `custom_resolution/` | Native 16:9 output sizes or 4:3 SD output; next launch | `custom_resolution` | `custom_resolution.md` |
| `fast-bootup` | `fast_bootup/` | Loader pacing + boot-analysis cache | `fast_bootup` | `ultrafast_boot_research.md` |
| `skip-intros` | `skip_intros.rs` | Redirect WARNING → TITLE | – | – |
| `timer-freeze` | `timer_freeze.rs` | Freeze + hide countdown timers | – | – |
| `premium-free` | `premium_free/` | Frozen stage counter + ghost cache | – | `premium_free_stale_record_bug.md` |
| `quick-restart-or-fail` | `quick_restart_or_fail.rs` | Pinpad 1 restart / 3 fail | – | `quick_restart_fail_speedup_research.md` |
| `quick-logout` | `quick_logout.rs` | Triple-9 at song select ends the session | – | `quick_logout_research.md` |
| `classic-difficulty-adjustment` | `classic_difficulty.rs` | Double-tap UP/DOWN at song select changes difficulty | – | – |
| `autoplay` | `autoplay.rs` | Per-player autoplay (watermarked, score-suppressed) | – | `autoplay.md` |
| `multiplayer-bot` | `multiplayer_bot/` | 1P versus against a bot (levels / ghost replay) | `multiplayer_bot` (+ `tools/bot_sim`) | `multiplayer_bot_research.md` |
| `announcer-mute` | `announcer_mute.rs` | Silence the announcer | – | `hex_edit_porting.md` |
| `anytime-speedmod` | `anytime_speedmod.rs` | Remove the in-song speed-mod window | – | `anytime_speedmod_research.md` |
| `hide-bottom-text` (OFF) | `hide_bottom_text.rs` | Hide bottom status readouts | – | `hex_edit_porting.md` |
| `split-ssq-auto-discovery` | `split_ssq_auto_discovery/` | Disk-discovered split charts | `split_ssq` | `split_ssq_research.md` |
| `series-expansion` | `series_expansion.rs` | Custom VERSION filters + flare exclusion | – | `flare_ranking_research.md` |
| `folder-expansion` | `folder_expansion.rs` | Custom genre folders (late-binding) | – | `folder_system_research.md` |
| `note-types-expansion` | `note_types_expansion/` | Note-type framework; mines | – | `ssq_mine_chunk_format.md`, `mine_render_architecture.md` |
| `real-speed-fix` | `real_speed_fix/` | Real Speed from Core BPM | – | `binary_modpack_research.md` |
| `power-user-statistics` | `power_user_statistics/` | Timing-stats widgets, pacemaker→ms-error, CSV, calories | `power_user_statistics` | `pacemaker_display_research.md` |
| `webui-options` | `webui_options/` | WebUI cosmetics in-game with previews (late-binding) | – | `player_customization_system_research.md` |
| `movie-size-customization` | `movie_size_customization.rs` | VIDEO SIZE row | – | `player_customization_system_research.md` |
| `center-arrows-single` | `center_arrows_single.rs` | Centre the solo playfield | – | `hex_edit_porting.md` |
| `timing-offsets` | `timing_offsets/` | Cabinet timing offsets + one-song auto-calibration | `auto_calibration` | `hex_edit_porting.md` |
| `overlay-element-styling` | `overlay_element_styling/` | Per-player combo/judge/pacemaker scale+opacity | – | `gameplay_overlay_elements_research.md` |
| `playfield-styling` | `playfield_styling/` | Per-player playfield scale+opacity | – | `playfield_styling_research.md` |
| `shader-fixes` | `shader_fixes.rs` | Shader synthesis toggle (AA, perspective, 3D style variants) | `overlay_draw` | `shader_replacement_research.md` |
| `player-perspective` | `player_perspective/` | HALLWAY / DISTANT lane perspective | – | `custom_arrow_renderer_research.md` |
| `non-native-operating-system-support` | `non_native_os_support.rs` | Wine/CrossOver movie suppress/fallback | – | `native_wm_runtime_bottle_setup.md` |
| `assist-tick` | `assist_tick.rs` | Pre-mixed clap track | `se_bank_synth` | `xact_audio_research.md` |
| `gameplay-timing-fixes` | `gameplay_timing_fixes/` | DAC-locked clock + tick alignment; boot-only | `audio_clock` | `audio_clock_research.md` |
| `song-playback-speed` | `song_playback_speed.rs` | SONG SPEED / PRESERVE PITCH / SYNC VIDEO rows over `services/song_rate` | `song_playback_speed`, `movie_sync` | `song_playback_speed.md`, `xact_streaming_research.md` |
| `training-mode` | `training_mode/` | Loop, A/B markers, scrub, timeline HUD | `training_mode` | `training_mode_research.md` |
| `decorative-option-headers` | `decorative_option_headers.rs` | Heading rows in the options MODS tab | `custom_options` | `option_header_rows_research.md` |
| `music-wheel-song-length` | `music_wheel_song_length.rs` | LENGTH M:SS in the song-select header | – | – |
| `per-song-judgement-offsets` | `per_song_judgement_offsets/` | Per-song per-player offsets (CSV + profile) | `judgement_offsets` | `per_song_judgement_offsets.md` |
| `s-marvelous` | `s_marvelous/` | Display-layer judgement above Marvelous (+ server upload, lamps; per-era art on DDR SELECTION's legacy skins) | `s_marvelous` | `s_marvelous_judgement_research.md` |
| `two-player-bpl-mode` | `two_player_bpl_mode/` | In-shop battle HUD in local versus | `two_player_bpl` | `in_shop_battle_local_versus_research.md` |
| `background-dancers` (OFF) | `background_dancers/` | A3 3D stage + dancers, custom models, previews | `background_dancers` | `background_dancers_research.md`, `3d_model_format_research.md` |
| `ddr-selection` | `ddr_selection/` | A3-era legacy gameplay skins per song (inert until a player's row picks an era) | `ddr_selection` | `ddr_selection_research.md` |
| `smx-hardware` (OFF) | `smx_hardware/` | StepManiaX pads, lights, touch overlay | – | `smx_hardware_research.md` |
| `mod-menu` (always) | `mod_menu/` | The overlay menu | `mod_menu`, `overlay_draw` | `overlay_draw_research.md` |

## src/widgets/ and src/types/

- `widgets/text_widget.rs` wraps `kt::BmpfontSimpleString`.
- `widgets/image_widget.rs` wraps `agcs::Sprite`.
- `widgets/bounce.rs` holds pure bounce/rainbow math.
- `types/scenes.rs` holds 0-indexed scene ids.
- `types/buttons.rs` holds players, bitmasks and `InputEvent`.
- `types/game_note.rs` holds the 0x60-byte note record.

## Outside the DLL crate

| Path | Role |
|---|---|
| `updater/` | Standalone auto-updater. It resolves the game dir, downloads the GitHub release, verifies sha256, extracts to a stage, merges user-owned files, and applies a journaled transaction with rollback. It self-updates. Separate crate; see `workflows.md`. |
| `tools/blender_ddr_addon/` | Blender import/export for KTMDL/B2IT/ANM/CAMANM, characters and stages; porting examples |
| `tools/bot_sim/` | Offline bot simulator mounting the DLL's pure bot files; HTML report |
| `tools/fxc/` | Pinned fxc 9.29 + D3DCompiler_43 (run under CrossOver) |
| `shaders/src/` | Lane shaders (`gs_screencommand_*`), model style shaders (`mdl_*`), menu themes (`themes/`) |
| `data_mods/` | Shipped runtime assets: option label sets, series/folder textures, shader blobs, clap PCM, custom models, dancer cameras/badges, SMX atlas, training icons, S-Marvelous art (World's in `s_marvelous/`, the legacy skins' in `ddr_selection/s_marvelous/`) |
