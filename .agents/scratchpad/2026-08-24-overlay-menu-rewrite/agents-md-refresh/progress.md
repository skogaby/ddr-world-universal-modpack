# Progress — step09 task-03 agents-md-refresh

- [x] Config section: `row_order` bullet REPLACED with `option_menu_settings`
      ({id, overlay?, in_game?}; array order = display order both menus;
      migration note); new `overlay_menu` bullet (theme/animate_background/
      opacity; DLL-written section)
- [x] Key Entry Points: new "Mod Menu (overlay)" row (mod_menu module family,
      overlay_draw anchor emitter + "same list ≠ same z" pointer, theme
      shaders/synthesis, 3 harnesses, research + planning pointers)
- [x] music_wheel_song_length row: "X/Y live-tunable from the mod-menu overlay
      scalar rows" clause replaced with config-only + retirement note
- [x] Greps clean: 1× option_menu_settings, row_order only in the migration
      note, no stale live-tunable claims; Custom Instructions untouched
      (README untouched per maintainer — full docs overhaul is a separate
      upcoming session)

Status: Complete (uncommitted — maintainer commits manually)
