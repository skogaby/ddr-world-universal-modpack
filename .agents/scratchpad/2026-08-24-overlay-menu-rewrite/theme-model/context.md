# Context — task-01 theme-model (Step 7, overlay-menu rewrite)

## Task
.agents/tasks/2026-08-24-overlay-menu-rewrite/step07/task-01-theme-model.code-task.md

## Approval chain (verified)
- Task carries `Generated By: code-task-generator 2026-08-25` (breakdown
  approved in-session by the maintainer 2026-08-25).
- Source plan `.agents/planning/2026-08-24-overlay-menu-rewrite/implementation/plan.md`
  — `Status: Approved 2026-08-24`.
- Design `.agents/planning/2026-08-24-overlay-menu-rewrite/design/detailed-design.md`
  — `Status: Approved 2026-08-24` (§4.6 read).
Mode: auto (approved-planning descent stands in for plan approval).

## Build & test commands (from repo root)
- Host tests: `./scripts/validate_mod_menu.sh` (temp-crate harness; plain
  `cargo test` cannot compile retour on this ARM host)
- Gates: `cargo check --target x86_64-pc-windows-msvc` → `cargo fmt`
  (bare) → `./build.sh`

## Requirements (from the task)
1. New pure `src/mods/mod_menu/theme.rs`: `Palette` (text `[f32;3]` ×11,
   tint `[u8;3]` ×3, panel stops `[u8;4]` ×2, `gradient()` →
   `super::chrome::PanelGradient`), `Theme { id, display, palette,
   background }`, `Background::Static`, `THEMES` ×4
   (arrows/bubbles/wavefield/minimal, authored values in the task),
   `DEFAULT_THEME_INDEX = 0`, `resolve_theme_index(Option<&str>) ->
   (usize, bool)`, `theme(index)` clamped.
2. model.rs: `TabId::Theme` appended (ALL order M/G/P/T, label "THEME");
   update `tab_labels_stable` + `tab_nav_memory_and_wrap`.
3. model.rs: `build_theme_tab(theme_index, theme_labels, animate,
   animate_greyed, opacity) -> Vec<Row>` — keys `theme`/`animate_bg`/
   `opacity`, kinds Enum/Boolean/Scalar(formatted "NN%"), all
   `RowSource::Theme`.
4. tabs.rs temporary `TabId::Theme => Vec::new()` arm.
5. Harness MODULES += theme.rs.
6. Tests: table integrity, resolution, builder, updated tab tests.

## Facts verified in-repo
- model.rs:19-47 TabId (`ALL` :26, label :28); tests `tab_labels_stable`
  (asserts len 3) and `tab_nav_memory_and_wrap` (3-tab wrap walk).
- `build_player_tab` (model.rs:246-289) is the builder pattern model.
- chrome.rs:73-83 `PanelGradient` public, stop alphas ignored;
  `OPACITY_MIN/MAX/STEP` 25/100/5.
- tabs.rs:70-77 exhaustive match over `TabId::ALL` — new variant forces
  an arm (temporary `Vec::new()` here).
- Harness (scripts/validate_mod_menu.sh:27 `MODULES=(model.rs chrome.rs)`)
  mounts modules as SIBLINGS at the temp-crate root ⇒ `super::chrome`
  resolves identically in harness and real crate.

## Assumptions (auto mode)
- Red-first via compile-error red is impractical for a static table
  module; implement + tests together, verify green via the harness, and
  verify test honesty by review (assertions written from the task spec,
  not from the code).
- `Background` gets `#[derive(Clone, Copy, PartialEq, Eq, Debug)]` and a
  doc note that `Shader { program }` arrives with Step 8.
