# Context — restructure-mod-menu-module

Task: .agents/tasks/2026-08-24-overlay-menu-rewrite/step01/task-01-restructure-mod-menu-module.code-task.md
Mode: auto (maintainer authorized autonomous run through steps).

## Approval chain (verified)

- Task carries `Generated By: code-task-generator 2026-08-24`.
- Source plan `.agents/planning/2026-08-24-overlay-menu-rewrite/implementation/plan.md`:
  `Status: Approved 2026-08-24`. Step: "Step 1: Module restructure + widget-pool diagnostic".
- Design `.agents/planning/2026-08-24-overlay-menu-rewrite/design/detailed-design.md`:
  `Status: Approved 2026-08-24` (§4.1 read).

## Project facts

- Build/test commands (repo root): `cargo check --target x86_64-pc-windows-msvc`,
  `cargo fmt` (whole crate, never file args), `./build.sh`. Host tests `cargo test`
  exist only for pure layers (none touched here).
- Git: NEVER commit — maintainer commits manually (AGENTS.md). Commit step will be
  skipped; completion recorded as `Complete (uncommitted)`.
- Instruction files read: AGENTS.md (authoritative; Custom Instructions incl. PDD
  progress tracking), no CODEASSIST.md (noted absent).

## Requirements (from task)

1. Split `src/mods/mod_menu.rs` into `src/mods/mod_menu/{mod,rows,input,render}.rs`.
2. Zero behavior change; function bodies byte-identical modulo paths/imports/visibility.
3. Public API paths preserved (re-exports): `ScalarRowSpec`, `EnumRowSpec`,
   `RowChangeCallback`, `register_scalar_row`, `register_enum_row`, `remove_rows_for`,
   `MOD_MENU_STATE`, `ModMenuState`, `ModMenuMod` (+ `MenuRow`, `RowKind`,
   `ToggleCallback`, `EntriesCallback` for completeness).
4. External users compile unmodified: `src/lib.rs`, `src/mods/timing_offsets.rs`,
   `src/mods/fps_unlock.rs`, `src/mods/quick_restart_or_fail.rs`,
   `src/mods/music_wheel_song_length.rs`, `src/mods/shader_fixes.rs`.

## Split map

- `mod.rs`: module docs, submodule decls + re-exports, gesture constants,
  `ModMenuState` (+`unsafe impl Send`), `MOD_MENU_STATE`, `ModMenuMod` + `Mod` impl,
  `on_zero_pressed`, `open`, `close`.
- `rows.rs`: callback type aliases, `RowKind`, `MenuRow`, `ScalarRowSpec`,
  `EnumRowSpec`, `register_scalar_row`, `register_enum_row`, `remove_rows_for`,
  `row_value`, `enum_index_of`, `rebuild_rows`, `rebuild_visible`, `clone_row`,
  `apply_row_value`, `set_row_value_and_refresh`, `toggle_registry_mod`
  (`super::config::save_mod_states` becomes `crate::mods::config::save_mod_states`).
- `input.rs`: repeat constants + `REPEAT_THREAD_GEN`, `handle_exclusive_input`,
  `navigate_up/down`, `activate_selected`, `coarse_held`, `adjust_dir_held`,
  `selected_repeats`, `start/stop_repeat_thread`.
- `render.rs`: layout constants, `Slot`, `adjust_scroll`, `selected_row_index`,
  `allocate_widgets`, `destroy_widgets`, `refresh_slots`, `show_all_widgets`,
  `hide_all_widgets`.

Visibility: children read the parent module's private items freely (Rust descendant
visibility); cross-module (parent→child, sibling→sibling) items get `pub(super)`.
`ModMenuState` fields stay private except the already-`pub` `toggle_callback` /
`entries_callback`.

## Ambiguities

None — mechanical split; target layout named in the task.
