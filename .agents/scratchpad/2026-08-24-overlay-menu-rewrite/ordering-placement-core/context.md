# Context — ordering-placement-core (Step 5 task-01)

## Provenance (verified)
- Task: `.agents/tasks/2026-08-24-overlay-menu-rewrite/step05/task-01-ordering-placement-core.code-task.md`
  (`Generated By: code-task-generator 2026-08-24`; breakdown user-approved in
  session 2026-08-24 incl. 4 recommendations).
- Plan + design both `Status: Approved 2026-08-24`. Mode: auto. Never commit.

## Requirements (task file §Technical Requirements 1–8)
Config schema swap (row_order → option_menu_settings), ordering.rs rework
(OptionMenuSetting + placement overrides + preserved order semantics),
mod.rs init read, doc/string sweep, shipped mod-config.json migration,
new scripts/validate_custom_options.sh (log stub macros + once_cell dep).

## Verified facts
- ordering.rs (243 lines): CONFIGURED_ORDER OnceCell (lowercased at store),
  UNKNOWN_WARNED latch, pure `compute_order(registered, is_header,
  configured) -> (Vec<usize>, Vec<String>)` with 8 in-file tests,
  `display_order_for` shell (crate::log_warn at :33/:234), sole caller
  builder_hook.rs:208.
- mod.rs:137-145 = the only row_order config read.
- config.rs:55 row_order field (+ doc 45-53) in CustomOptionsConfig.
- decorative_option_headers.rs strings: :12/:14/:30 docs, :60 description,
  :103 enable log.
- api.rs:50 UiKind::Header doc; builder_hook.rs:192/199 comments.
- mod-config.json:72-114 ships a 44-entry row_order array + `p1`/`p2` blocks
  after it (JSON edit must preserve them).
- Harness template: scripts/validate_mod_menu.sh; `#[macro_export]
  macro_rules! log_warn` in the generated lib.rs makes `use crate::log_warn;`
  resolve in the mounted file.

## Interpretations (auto mode record)
- `OptionMenuSetting` duplicates: first entry wins for BOTH ordering (existing
  behavior) and placement lookup (consistency).
- Placement query is case-insensitive like ordering; returns
  `(in_game: Option<bool>, overlay: Option<bool>)`; `(None, None)` when
  unconfigured/unlisted.
- Pure core keeps `configured` as a parameter (tests construct settings
  directly); the OnceCell shell stays thin.
- Red checkpoint: harness run against CURRENT ordering.rs first (proves the
  macro-stub mount), then rework.

## Gates
`./scripts/validate_custom_options.sh` → `cargo check --target
x86_64-pc-windows-msvc` → `cargo fmt` (bare) → `./build.sh`; autonomous boot
regression (in-game menu order preserved via the migrated config).
