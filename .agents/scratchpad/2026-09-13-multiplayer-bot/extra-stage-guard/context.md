# Context — extra-stage-guard (Step 5, task-01)

Task: `.agents/tasks/2026-09-13-multiplayer-bot/step05/task-01-extra-stage-guard.code-task.md`
Mode: auto. Approval chain verified (task `Generated By: code-task-generator 2026-09-13`; plan and
design both `Status: Approved 2026-09-13`). No `CODEASSIST.md`. Commit step SKIPPED per AGENTS.md.

## Build / test
- `./scripts/validate_signatures.sh ~/Desktop/ddr_modules` (offline 4-build sweep — the gate)
- `cargo check --target x86_64-pc-windows-msvc` → `cargo fmt` → `./build.sh` → `./scripts/validate_multiplayer_bot.sh`
- Consumer classification check: `python3 scripts/sig_harness/report.py --src src --logs target/sig_harness/out --json <tmp>`

## Requirements
1. `SignatureDefinition { name: "extra_stage_grant", … }` appended to `SIGNATURES` (soft consumer).
2. `extra_stage_guard.rs`: `init(&SignatureStore)` (get_address → AtomicPtr, WARN on miss),
   `enable()` (install once via `hooks::install_enabled`), `disable()` (passthrough), `is_installed()`,
   `grant_hook(arg)` clearing `PW[bot]+0x4` around the original under a scope guard when
   `impersonation::active_bot_side()` is Some and the guard is enabled.
3. `mod.rs` wiring (`init` → guard init non-gating; `enable` → guard enable; `disable` → guard disable).
4. Sweep ALL GREEN, 4/4 hits.

## Patterns
- `src/mods/announcer_mute.rs` (detour shape), `src/core/hooks.rs::install_enabled`.
- `signatures.rs` BPL block for the description style.

## Files
- `src/core/signatures.rs`, NEW `src/mods/multiplayer_bot/extra_stage_guard.rs`, `src/mods/multiplayer_bot/mod.rs`.
