# Plan — extra-stage-guard

Status: Approved 2026-09-13 (auto mode — approval inherited from the approved plan/design + the
`Generated By` task line, per code-assist Step 3)

## Test scenarios

| AC | Check |
|---|---|
| 1 signature attested | `./scripts/validate_signatures.sh ~/Desktop/ddr_modules` → `[+] extra_stage_grant` in all four `target/sig_harness/out/<build>.log`, `RESULT: ALL GREEN`; independent raw byte scan = exactly 1 match per build; `report.py --json` classifies `extra_stage_grant → mod:multiplayer_bot ['get']` |
| 2 guard semantics | Code review: bot side read from `impersonation::active_bot_side()`; `PW+0x4` cleared → `hook.call` → scope-guard restore; disabled/idle ⇒ passthrough, silent |
| 3 fail-open | `init` WARN + `false` on miss; `enable` returns silently without a target; install failure WARNs; `CAPABLE` never gated on the guard |
| 4 cabinet | maintainer (§7.3 item 6) |
| 5 gates | check / fmt / build / harness / sweep |

## Implementation shape
- Signature first (attest before the consumer exists), then `extra_stage_guard.rs` (announcer_mute
  shape + `EnteredRestore` drop guard), then `mod.rs` wiring.
