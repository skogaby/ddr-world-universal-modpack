# Context — extract-foot-panel-swap-service

Task: `.agents/tasks/2026-09-13-multiplayer-bot/step01/task-01-extract-foot-panel-swap-service.code-task.md`
Mode: auto (maintainer approved the plan in conversation; continuing through the step).

## Approval chain (verified)

- Task: `Generated By: code-task-generator 2026-09-13` — present.
- Source plan `.agents/planning/2026-09-13-multiplayer-bot/implementation/plan.md` — `Status: Approved 2026-09-13`.
- Design `.agents/planning/2026-09-13-multiplayer-bot/design/detailed-design.md` — `Status: Approved 2026-09-13`.

## Project instructions read

`AGENTS.md` (project summary, workflow/git rules — NEVER commit unless asked; build; patterns;
Rust quality rules), no `CODEASSIST.md`. The code-assist Commit step is therefore SKIPPED: work
is left uncommitted and `progress.md` closes with
`Status: Complete (uncommitted — maintainer commits manually)`.

## Build / test commands (run from repo root)

- Type check: `cargo check --target x86_64-pc-windows-msvc`
- Format: `cargo fmt` (whole crate — never file args)
- Release build: `./build.sh`
- Pure-module host tests: plain `cargo test` cannot compile `retour` on this ARM host; pure
  files are mounted into a temp crate via `#[path]` (see `scripts/validate_two_player_bpl.sh`).
  For this task a throwaway temp-crate mount of `layout.rs` is used; the permanent harness
  (`scripts/validate_multiplayer_bot.sh`) is plan Step 2.

## Files the change touches

- NEW `src/services/foot_panel_swap/layout.rs` (pure) + `mod.rs` (engine-facing)
- `src/services/mod.rs` — `pub mod foot_panel_swap;`
- `src/lib.rs` — `foot_panel_swap::init(&signatures)` after step 6b `judge_hook::init`
- `src/mods/autoplay.rs` — slimmed to a client
- `src/mods/note_types_expansion/mine_render.rs:93` — comment references `mods::autoplay` for
  the swap; retarget to the service (comment only)

## Existing patterns to match

- `judge_hook` API: `register_pre(Priority, fn(actor, mc)) -> Option<CallbackHandle>`,
  `register_post`, `unregister(handle)`, `foot_panel_offset() -> Option<usize>`,
  `is_available()`. Dispatcher wraps every callback in `catch_unwind`.
- Other `Late` pre subscriber: `training_mode/strip_hud.rs::on_judge_tick` (snapshot only; no
  foot-panel interaction ⇒ no ordering hazard). Early pre: `per_song_judgement_offsets`,
  `note_types_expansion`. Normal pre: `real_speed`, `assist_tick`. Late post:
  `note_types_expansion`. Early post: autoplay (→ service).
- Service shape: `static AVAILABLE: AtomicBool`, `init(&SignatureStore) -> bool` with soft
  `get_address` + WARN, `is_available()`, `static mut` slots read via `addr_of!` only where a
  plain-`fn` callback needs them; `log_info!/log_warn!` only.
- Memory helpers: `memory::alloc_zeroed(size)` (RWX VirtualAlloc), `write_ptr`, `read_ptr`,
  `read_i32`.
- Pure module convention: no `crate::` imports, `#[cfg(test)] mod tests`, dependency-free so a
  `#[path]` mount compiles on the host.

## Requirements (from the task file, restated)

R1 new service module with `layout.rs` (Controller, effective_controller, constants,
BotPanelFlags) + `mod.rs` (init/is_available/set_perfect/arm_bot/disarm_bot/controller,
BotFillFn).
R2 `init`: soft-resolve 3 signatures + fp offset; alloc 0x58 object with vtable; register pre
Late + post Early once; both-or-neither.
R3 callbacks: side from `actor+0x84`; Off ⇒ return; Perfect ⇒ stash+write+update; Bot ⇒ one
rate-limited WARN + behave as Off (this step only); post restores non-null stash.
R4 arbitration statics; `arm_bot` while PERFECT ⇒ one INFO; `disarm_bot` clears; `set_perfect`
never touches BOT_ARMED.
R5 `lib.rs` init after 6b with INFO/WARN + profiling tick.
R6 autoplay slimmed: no statics/callbacks/registrations; `required_signatures() -> &[]`; `init`
= service availability; on_change → `set_perfect` + taint; watermark asks the service; disable
clears both sides.
R7 priorities unchanged (pre Late / post Early).
R8 conventions (logging macros, narrow unsafe, no local paths).

## Acceptance criteria → verification

AC1 service init + single pre/post pair: grep (`register_pre|register_post` absent from
autoplay.rs) + cabinet log `FootPanelSwap started`.
AC2 autoplay unchanged (Perfect): cabinet.
AC3 autoplay OFF silent: code review (Off arm returns before any log) + cabinet.
AC4 arbitration: host test (`effective_controller` truth table) + code review of `arm_bot` INFO.
AC5 fail-open init: code review — every miss path WARN + `false`; `AutoplayMod::init` returns
service availability.
AC6 constants: host test.
AC7 build gates: cargo check / fmt / build.sh / git grep.

## Ambiguities / decisions (auto mode)

- Where `BotPanelFlags` lives: `layout.rs` (pure, so plan Step 2/3 host tests can round-trip it
  against the planner's `PanelFlags`). Re-exported from the module root.
- `BOT_FILL` storage: `AtomicPtr<()>` holding the `fn` pointer cast — lock-free on the judge
  path; `Option<BotFillFn>` reconstructed via transmute of a non-null pointer. Chosen over a
  Mutex to keep the hot path allocation- and lock-free.
- The Bot-armed-but-unwired WARN in `swap_in` is rate-limited by a per-side `AtomicBool`
  "warned once" latch cleared on `disarm_bot` (simplest correct shape; replaced in Step 3).
- `AutoplayMod` keeps `unsafe impl Send` (the watermark `Arc<Mutex<…>>` holds a raw widget).
