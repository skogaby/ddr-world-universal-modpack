# Context — option-rows-textures-menu (Step 4, task-01)

Task: `.agents/tasks/2026-09-13-multiplayer-bot/step04/task-01-option-rows-textures-menu.code-task.md`
Mode: auto. Approval chain verified: task carries `Generated By: code-task-generator 2026-09-13`;
`Source Plan` `.agents/planning/2026-09-13-multiplayer-bot/implementation/plan.md` resolves with
`Status: Approved 2026-09-13`; required design `design/detailed-design.md` resolves with
`Status: Approved 2026-09-13`. No `CODEASSIST.md` in the repo (AGENTS.md is the project
instruction file). Commit step SKIPPED per AGENTS.md (maintainer commits manually).

## Build / test
- `cargo check --target x86_64-pc-windows-msvc` (repo root)
- `cargo fmt` (whole crate) + `cargo fmt --manifest-path tools/bot_sim/Cargo.toml`
- `./build.sh` (release, cargo-xwin)
- `./scripts/validate_multiplayer_bot.sh` (= `cargo test` in `tools/bot_sim`, mounts the pure files)
- `python3 scripts/gen_option_labels.py` (label/preview textures)
- `python3 -m json.tool mod-config.json` (JSON validity)

## Requirements (from the task)
1. `mod.rs`: `OPTION_ON: [AtomicBool;2]`, `LEVEL: [AtomicI32;2]` (default 5), accessors
   `option_on(side)`/`level(side)`; ids `bot_opponent` / `bot_opponent_level`; panic-free
   `on_change` fns; `register_rows()` in `enable` (parent on `is_available`, child on
   `row_injection_available`, `Duplicate` ⇒ reseed from `get_value` + re-show); `disable`
   hides both rows via `set_option_available(id, false)`.
2. `scripts/option_strings.py`: `LABELS` for both ids (en/ja/ko) + WIDE text-only previews;
   regenerate; only additions under the three `tex/` dirs.
3. `mod-config.json` `option_menu_settings`: both ids right after `autoplay`.
4. No config section, no DLL writes, no signatures.

## Acceptance criteria → checks
- AC1 rows registered → boot log INFOs (cabinet); code review of registration order.
- AC2 atomics follow rows, clamped → unit-style reasoning (engine-facing; `clamp_level` is
  host-tested in `eligibility.rs`).
- AC3 re-enable idempotent → `Duplicate` arm reseeds + `set_option_available(true)`.
- AC4 textures shipped → `ls` the three `tex/` dirs; `git status` shows only additions.
- AC5 fail-open → WARN paths present.
- AC6 build gates.

## Patterns found
- `src/mods/assist_tick.rs:1531-1558` parent bool + `Duplicate` reseed; `:1091-1133` child
  scalar with `ShowWhen::Equals` + `persist_transform`.
- `src/mods/song_playback_speed.rs:280-282, 342-344` re-show on `Duplicate` / hide on disable.
- `src/services/custom_options/api.rs`: `RegisterSpec::bool_toggle`, `::scalar(id,min,max,step,fmt)`,
  `.display_name/.description/.default_value/.show_when/.persist_transform/.on_change`;
  `RegisterError::{Duplicate, UnknownParent, NotInitialized, …}`.
- `scripts/option_strings.py`: `LABELS` dict (~64), `PREVIEWS` list with `PreviewSpec(option,
  value|None, WIDE|SPLIT, {lang: [paragraphs]}, art=None)`; `assist_tick` off/on + `_volume`
  are the text-only WIDE precedents (~491–545).
- `mod-config.json:169-173` = the `autoplay` entry.

## Files to touch
- `src/mods/multiplayer_bot/mod.rs`
- `scripts/option_strings.py` (+ generated PNGs under
  `data_mods/custom_options/select_music_option_lang_{eng,jpn,kor}_v3_ifs/tex/`)
- `mod-config.json`

## Assumptions (auto mode)
- ja/ko label wording per the task file (proposals; maintainer may edit + regenerate).
- Previews: three WIDE text-only panels (off/on for the bool, one for the level) — optional per
  design, included because every shipped row has one.
- Description strings (overlay tooltip): parent "Play VERSUS against a computer opponent on the
  empty pad", child "1 = beginner, 10 = expert".
