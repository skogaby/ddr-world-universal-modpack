# Context — pure-cores (Step 2, task-01)

Task: `.agents/tasks/2026-09-13-multiplayer-bot/step02/task-01-pure-cores.code-task.md`
Mode: auto. Approval chain verified: task `Generated By: code-task-generator 2026-09-13`; plan +
design both `Status: Approved 2026-09-13`. Commit step skipped per AGENTS.md (maintainer commits).

## Build / test
- Pure modules: temp-crate `#[path]` mount (throwaway here; permanent = task-02's
  `scripts/validate_multiplayer_bot.sh`). Nested mount `multiplayer_bot::{eligibility,skill,planner}`
  so `planner.rs`'s `use super::skill` resolves.
- Crate: `cargo check --target x86_64-pc-windows-msvc`, `cargo fmt`.

## Files
- NEW `src/mods/multiplayer_bot/{mod.rs (stub), eligibility.rs, skill.rs, planner.rs}`
- `src/mods/mod.rs` + `pub mod multiplayer_bot;`

## Requirements → task file TR1–TR6; AC1–AC11 map 1:1 to the tests listed in plan.md.

## Decisions (auto)
- `planner::Plan` resolution: on the first emitted frame the plan is rewritten to
  `Hit { d_ms: E − music_count }` so E is stable and `tally` can recompute the grade from the
  plan alone. Pre-emission, the raw `d` is kept (blocked_until/last_event may still move E).
- Tally bookkeeping: `Vec<bool> tallied` sized with `plans`.
- `gaussian`: Marsaglia polar, no cached second value.
- 1e6-sample tests run in debug under the harness (~1 s); acceptable.

## Anomaly
- The first scaffolding write (stub `mod.rs`, empty `skill.rs`/`planner.rs`, the `mods/mod.rs`
  declaration, these three docs) was reverted between two shell cells by something outside the
  session; gitignored `logs/` survived. Re-created; every write now verified with `ls`/`rg`.
