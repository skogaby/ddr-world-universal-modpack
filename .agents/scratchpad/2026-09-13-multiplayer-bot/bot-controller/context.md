# Context — bot-controller (Step 3, task-01)

Task: `.agents/tasks/2026-09-13-multiplayer-bot/step03/task-01-bot-controller.code-task.md`
Mode: auto. Approval chain verified (task `Generated By`, plan/design `Status: Approved 2026-09-13`
incl. the same-day revisions). Commit step skipped per AGENTS.md.

## Build / test
- Host tests: `./scripts/validate_multiplayer_bot.sh` (tools/bot_sim mounts `layout.rs`).
- Crate: `cargo check --target x86_64-pc-windows-msvc`, `cargo fmt` (both crates), `./build.sh`.

## Files
- `src/services/foot_panel_swap/layout.rs` (+BotFootPanel, image builder), `mod.rs` (clone + Bot arm)
- NEW `src/mods/multiplayer_bot/filler.rs`, `self_test.rs`; `mod.rs` gains the `Mod` impl
- `src/lib.rs` (register), `mod-config.json` (`"multiplayer-bot": true`)

## Decisions (auto)
- Per-side filler state in `Mutex<Option<SongCtx>>`, `try_lock` on the judge path.
- The vtable image + both BotFootPanel objects live in ONE `alloc_zeroed` region:
  `[COL][slot0..6][BotFootPanel side0][BotFootPanel side1]` (0x40 + 2·0x58).
- Self-test seed = `skill::seed(qpc, 0, 0, level)`; `qpc` via a local `QueryPerformanceCounter`
  extern (the repo has three private copies; no shared helper).
