# progress — random-badge (Step 6 task-01)
Task `.agents/tasks/2026-09-21-background-dancers-selection-options/step06/task-01-random-badge.code-task.md`
(`Generated By: code-task-generator 2026-09-21`; plan + design `Status: Approved 2026-09-21`) — auto mode.
- [x] `scripts/gen_preview_random_badge.py` → `data_mods/background_dancers/tex/preview_random.png` (170×150 RGBA: dark
      panel in the `BACKDROP_ARGB` colour, five-pip die, geometric "RANDOM" caption — no TTF dependency; visually checked).
- [x] `preview/badge.rs`: `Badge { widget, texture, shown, rect, widget_failed }`, `set_visible(side, on, rect)` (asset
      requested once per process via `asset_loader::load(TEX_PATH, TEX_STEM)`, `resolve` polled, lazy `ImageWidget` at the
      box rect, position/size refreshed on a rect change, show/hide idempotent), `destroy()`; one-shot WARNs for loader /
      load / widget failures.
- [x] `preview/mod.rs`: `PreviewSlot.{focused_kind, badge}`; `focused_kind` recorded in `on_preview_request`, cleared on
      close / scene exit / shutdown; `drive_slot` step 4 = `badge_on = modal_open ∧ focused ∧ wanted None ∧ live None ∧
      !menu_open` → `badge.set_visible(side, badge_on, box_rect(side, marker_for(kind)))`; the live-window step factored
      into `drive_live_window`; `marker_for(kind)` caches the template marker per kind (one PNG decode); `is_idle` keeps
      the driver ACTIVE while a badge is shown or a row is focused with the modal open; `shutdown` destroys the badges.
- [x] `cargo check` 0 warnings (`logs/cargo-check.log`), `./build.sh` clean (`logs/build.log`), `cargo fmt`.
## Deviations
- The marker lookup is cached per kind (the task suggested factoring it; the cache keeps the badge path free of PNG decodes).
- [x] Cabinet 2026-09-22: badge renders over the box for RANDOM (PASS). Feedback: top/bottom margins unbalanced (die at 0.42 H,
      caption at 0.78 H ⇒ 32 px over / 11 px under) — the generator now centres the die + 14 px gap + caption block
      (`DIE_SIZE`/`GAP`/`CAPTION_*` constants; ≈ 26 px each side); PNG regenerated.
Status: Complete (uncommitted — maintainer commits manually); cabinet-confirmed
