# context — catalog-and-choice-resolution

Task: `.agents/tasks/2026-09-21-background-dancers-selection-options/step01/task-02-catalog-and-choice-resolution.code-task.md`
Approval chain verified (task Generated By code-task-generator 2026-09-21; plan + design Approved 2026-09-21). Mode: auto.

## Build / test
- `./scripts/validate_background_dancers.sh` (temp-crate harness; mounts pure files at the crate root —
  `catalog.rs` must reach `selection` via `super::selection`, no `crate::` imports)
- `cargo check --target x86_64-pc-windows-msvc`; `cargo fmt`

## Requirements (from the task)
- `catalog.rs`: `RANDOM`, `MAX_LABEL_BYTES`, `Kind`, `CatalogEntry`, `Catalog{dancers,stages}` +
  `label/key/count`, `split_key`, `label_for`, `build_catalog`, `clamp_to_catalog`.
- `selection.rs`: `resolve_choice(rng, stages, dancers, stage_key, dancer_keys)`.
- Mount `catalog.rs` in the harness; `pub mod catalog;` in `background_dancers/mod.rs`.

## Interpretation / assumptions
- `split_key`: prefix = the maximal leading run of ASCII alphanumerics minus the trailing digit run;
  digits = the trailing run (`emi01` → EMI, 2). A key with an inner digit (`st001x2`) is not in either
  rlist; documented behaviour: prefix = everything before the trailing digits, upper-cased.
- Variant counting: `#k` appended iff the family (same prefix) has > 1 entry IN THE CATALOG (distinct keys).
- `resolve_choice` RANDOM legs call `pick_stage` / `pick_dancers`-equivalent draws so the all-None case
  reproduces `make_pick`'s picks under the same seed (pick_stage first, then one `below(len)` per dancer).

## Files
- src/mods/background_dancers/catalog.rs (new)
- src/mods/background_dancers/selection.rs
- src/mods/background_dancers/mod.rs
- scripts/validate_background_dancers.sh
