# progress — option-rows-registration

context: task `.agents/tasks/2026-09-21-background-dancers-selection-options/step01/task-03-option-rows-registration.code-task.md`;
approval chain verified (task Generated By code-task-generator 2026-09-21; plan + design Approved 2026-09-21); auto mode.
Build/test: `cargo check --target x86_64-pc-windows-msvc`, `./scripts/validate_background_dancers.sh`,
`./scripts/validate_custom_options.sh`.

- [x] `selection.rs`: `PickSource { Random, Option, Pin }` + `tag()` (pure).
- [x] `session.rs`: `Pick.source_stage` / `source_dancers`, `Pick::with_sources`, `summary()` renders
      `stage=<key>[row]{src}` and `<dancer>(S) x=±d.d{src}`; `assemble_pick` defaults every element to `Random`.
- [x] `options.rs` (new): `OPT_DANCER` / `OPT_STAGE`, `OnceLock<Catalog>`, per-side atomics, `label`
      (the `DynamicLabelFn`), `on_dancer_change` / `on_stage_change` (+ `versus_mirror::mirror_edit`),
      `clamp_load`, `register` (chrome first, `row_injection_available` gate, empty-catalog refusal,
      `Duplicate` ⇒ re-seed + re-show, `versus_mirror::register(&[OPT_STAGE])`), `set_available`,
      `rows_live`, `stage_choice(side)` / `dancer_choice(side)`, `value(kind, side)`.
- [x] `lifecycle.rs`: `tables_snapshot()`, `entered_side_list()` (replaces the count-only helper),
      `window_entry` order pin → `option_pick` → `make_pick`; `option_pick` validates keys element by
      element (unknown ⇒ WARN naming it + RANDOM for that element) before `resolve_choice`.
- [x] `mod.rs`: `pub mod options;`, `enable()` registers over `catalog::build_catalog(tables_snapshot())`,
      `disable()` calls `options::set_available(false)`; module doc gains the player-choice paragraph.
- [x] `webui_options::preview_gen::generate_chrome` → `pub(crate)`.
- [x] `cargo check`: clean. `validate_background_dancers.sh`: 117 passed. `validate_custom_options.sh`:
      57 passed + display-string lint OK.

## Deviations
- `stage_choice(side)` takes the side (the design's `stage_choice()` had none): the entered side's value is
  read (the FIRST entered side at window entry — in versus the mirror keeps both equal).
- `Pick` provenance = two fields + `with_sources` (the task allowed "an equivalent minimal shape");
  `{random}` is rendered too (not only option/pin) so a normal song's line is self-describing.
- No host test for `summary()` — `session.rs` is engine-facing (`crate::` imports) and not harness-mountable;
  the rendering is a format string over the pure `PickSource::tag()` which IS tested. Cabinet demo covers it.

Status: Complete (uncommitted — maintainer commits manually)
