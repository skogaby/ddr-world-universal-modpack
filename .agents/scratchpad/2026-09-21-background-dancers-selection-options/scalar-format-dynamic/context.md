# context — scalar-format-dynamic

Task: `.agents/tasks/2026-09-21-background-dancers-selection-options/step01/task-01-scalar-format-dynamic.code-task.md`
Approval chain: task `Generated By: code-task-generator 2026-09-21`; source plan
`implementation/plan.md` `Status: Approved 2026-09-21`; design `Status: Approved 2026-09-21`. Verified.
Mode: auto (maintainer's autonomy grant). No `CODEASSIST.md` in the repo; AGENTS.md read.

## Build / test
- `cargo check --target x86_64-pc-windows-msvc` (repo root)
- `./scripts/validate_custom_options.sh` (host harness: api.rs + registry.rs + scalar_format_tests.rs)
- `cargo fmt` whole crate

## Requirements
- `DynamicLabelFn = fn(&str, i32) -> Option<String>`; `ScalarFormat::Dynamic(DynamicLabelFn)` (Copy+Debug stay).
- `format_scalar_value(id, value, format)` / `format_scalar_value_utf8(id, value, format)`;
  `Dynamic(f) => f(id, value).unwrap_or_else(|| value.to_string())`.
- Call sites: `rows.rs::push_scalar_value_text` (has `opt.id`), `registry.rs::overlay_row` (has `opt.id`).
- Tests: `scalar_format_tests.rs`, `api.rs` tests, `registry.rs` overlay tests.

## Files
- src/services/custom_options/api.rs
- src/services/custom_options/rows.rs (line ~1956)
- src/services/custom_options/registry.rs (line ~493, tests ~712/746)
- src/services/custom_options/scalar_format_tests.rs
