# progress — preview-driver (Step 5 task-02)
Task `.agents/tasks/2026-09-21-background-dancers-selection-options/step05/task-02-preview-driver.code-task.md`
(`Generated By: code-task-generator 2026-09-21`; plan + design `Status: Approved 2026-09-21`) — auto mode. Engine-facing:
no host harness for the driver itself; validation = gates + the cabinet demo.
- [x] `preview/scene.rs`: `PreviewWindow { identity, side, scene: SceneWindow, built_at, seed }` (+ `t()`), `build_pick`
      (stage → `Pick::stage_only` capped at `MAX_PREVIEW_INSTANCES` parts with one WARN; dancer → `Pick::dancer_only` with
      the LayeredFS-aware `arc_exists`), `make_session` (`Session::new(.., TempoOptions::REAL_TIME, style::effective().style,
      HullPlan::none(), SLOT_BASE[side], Some(FILTER_BIT[side])).with_schedule(synthetic_schedule(9.0))`).
- [x] `preview/camera.rs`: `dancer_frustum`, `fallback_frustum`, `frustum_for` (stage: `director::camera_frame` sample →
      `crop_to_aspect(|t|, aspect)` keeping eye/target/up/near/far; no camera set ⇒ cropped fallback), `apply` (view_proj →
      `PassSet::set_camera`).
- [x] `preview/mod.rs`: `init` (subscribe once; compositor WARN once), `shutdown` (render thread: neutralise windows, detach
      passes — blocks wait for a later reap, documented), `on_scene_change` (leaving 25 ⇒ clear both sides),
      `on_menu_open/close`, `on_preview_request` (records into `SlotState` only), `on_frame` → `drive_slot`: retiring windows
      driven + finished; `poll` ⇒ Teardown (begin_teardown "preview exit" → retiring; passes disabled) / Start (only while
      nothing is retiring; `StartOutcome::{Started, Failed ⇒ mark_started "attempted", Retry ⇒ dims unreadable}`); live:
      `drive_assets(make_session)` → on first built `built_at` + INFO → `park_engine_destroyed`, `publish(t, true)`,
      camera → `set_camera`, `retry_textures`, `attached_diagnostics`; passes enabled iff `has_built && !mod_menu::is_open()`.
      `start_preview`: compositor / graph-enabled / tables gates (one WARN each), box (dims FIRST, then the template marker
      — a PNG decode kept out of the retry loop; `FALLBACK_MARKER` + one INFO when unreadable), lazy `PassSet::create`
      (`ClearSpec { depth, color: BACKDROP_ARGB }`, disabled until built) / `set_rect`, seed, pick (+ `pick.summary()` INFO
      with the RT rect + aspect), `load_arcs(PREVIEW)`, `SceneWindow::start("BackgroundDancers: preview P{n}", "this preview")`.
- [x] `options.rs`: `kind_for_option(id)`, `choice_key(kind, side)`. `mod.rs`: `preview::init()` after the rows,
      `preview::on_scene_change` / `preview::on_frame` (before the single `reap()`), `preview::shutdown()` in `disable`.
- [x] Gates: `cargo check` 0 warnings (`logs/cargo-check.log`), harness 152 ✓ (`logs/harness.log`), custom_options 57 ✓,
      `./build.sh` clean (`logs/build.log`), `cargo fmt`.
## Deviations
- Colour clear (`BACKDROP_ARGB`) for BOTH kinds (the design left the stage's colour clear optional) — covers the chrome
  where a cropped stage has no geometry.
- The "attempted" semantics for a hard start failure (`mark_started` without a window) so a bad key is not retried every
  frame; a value change resets it.
- `shutdown` leaks the detached pass blocks until a later `reap()` (the frame callback is gone at disable) — documented.
## Cabinet demo (maintainer) — plan §Step 5
- Focus BACKGROUND DANCER, pick `EMI #2` → after ~150 ms Emi dances in the box over a dark backdrop; log:
  `BackgroundDancers: preview P1 -- stage=none{random} parts=0 dancers=[emi01(F) x=+0.0{option}] … (box rt=(…) aspect 1.133)`,
  `viewport_pass: attached clear@0x68 opaque@0x69 trans@0x6a filter=0x8 …`, `BackgroundDancers: preview P1: FileManager::Load
  accepted …`, `… parsed in …`, `… built …`, `BackgroundDancers: preview P1 -- visible N ms after request`.
- Scrub values fast → one restart after the last change (`… dancer "emi01" ends` / `preview exit -- N node(s) disabled` /
  `preview teardown -- destroy(s) queued` / `… destroyed by the engine flush` / `preview N arc handle(s) freed`).
- BACKGROUND STAGE `BOOM #3` → the stage animates under its own camera cuts (cropped view).
- P1 + P2 simultaneously (P2 uses `clear@0x6b … filter=0x20`); 0-0-0 menu hides both; close modal / leave song select ⇒
  teardown lines; the next song plays normally with its own dancers.
## Cabinet 2026-09-22 (CrossOver)
- Deploy #1: badge OK, NO 3D — `preview::init()` called `viewport_pass::is_available()` at mod enable, before the engine's
  render-graph boot constructed the MODEL pass objects (null globals) ⇒ the identity gate "failed" and latched
  `Unavailable` for the boot. Fix: `viewport_pass::availability()` tri-state (`NotYet` for null globals / unreadable
  RENDER_2D list — one INFO, not latched), `preview::init` checks `derivation_present()` only, `start_preview` ⇒ `Retry`
  on `NotYet`.
- Deploy #2: dancer + stage previews render — PASS.
Status: Complete (uncommitted — maintainer commits manually); cabinet-confirmed
