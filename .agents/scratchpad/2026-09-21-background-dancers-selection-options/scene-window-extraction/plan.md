# plan — scene-window-extraction (Step 4 task-03)

Status: Approved 2026-09-21 (auto mode: approved plan Step 4 + design §4.6 "preview/window.rs" stand in)

Context: task `.agents/tasks/2026-09-21-background-dancers-selection-options/step04/task-03-scene-window-extraction.code-task.md`
(`Generated By: code-task-generator 2026-09-21`; plan + design both `Status: Approved 2026-09-21`). Engine-facing — no host
harness; validation = `cargo check`, the log-string inventory diff (`logs/log-strings-before.txt` vs after), `./build.sh`,
then the maintainer's 1P + 2P cabinet regression.

## Shape
- `scene_window.rs`: constants, `AssetPhase`, `ScenePhase`, `LoadedArcs` + `load_arcs(pick, opts)`, `SceneWindow` with
  `start`, `drive_assets(make_session) -> has_built`, `park_engine_destroyed`, `publish(t, visible)`, `retry_textures`,
  `attached_diagnostics`, `begin_teardown(exit_label) -> bool`, `drive_teardown(label) -> bool`, `finish(label)`,
  `neutralise() -> leaked`, accessors. `tag` prefixes every line (`BackgroundDancers` for gameplay); `scope` = "this song".
- `lifecycle.rs`: `Window { generation, scene: SceneWindow, clock, tempo*, one-shot latches }`, `State.orphan: Option<SceneWindow>`.

## Checks
1. `rg -o '"BackgroundDancers: [^"]*'` over lifecycle.rs + scene_window.rs (with `{}` for the factored prefix) covers the
   before-list one-to-one.
2. Constants 20_000 / 5_000 / 2 / 180 / 900 / 1200.
3. `cargo check` clean, harness green, `cargo fmt`, `./build.sh` clean.
