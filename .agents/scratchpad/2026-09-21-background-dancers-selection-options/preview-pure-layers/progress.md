# progress — preview-pure-layers (Step 5 task-01)
Task `.agents/tasks/2026-09-21-background-dancers-selection-options/step05/task-01-preview-pure-layers.code-task.md`
(`Generated By: code-task-generator 2026-09-21`; plan + design `Status: Approved 2026-09-21`) — auto mode.
- [x] `pick.rs`: `stage_only(rng, stages, camera_rows, key)` (uniform row of the key, no dancers, source Option) +
      `dancer_only(rng, dancers, key, arc_exists)` (stage None, one dancer, source Option); 2 tests (fixture camera rows
      now cover boom00's rows 0 AND 32 — the first run picked row 32 and found no camera set, correctly).
- [x] `preview/layout.rs` (pure): `CHROME_ORIGIN`, `FALLBACK_MARKER`, `SETTLE_MS`, `SLOT_BASE`, `MAX_PREVIEW_INSTANCES`,
      `STAGE_CUT_PERIOD_S`, dancer / fallback camera constants, `AUTHORED_ASPECT`, `BACKDROP_ARGB`, `CanvasRect` +
      `box_rect`, `Extents` + `crop_to_aspect` / `dancer_extents` / `fallback_extents`; 4 tests.
- [x] `preview/state.rs` (pure, generic `SlotState<I>`): `on_request(focused_ours, wanted, now)`, `on_clear`, `poll ->
      Action::{None, Teardown, Start}`, `mark_started/mark_torn_down`, `is_idle`; 5 tests (settle, re-arm vs repeat,
      RANDOM / other row, clear, retarget while live).
- [x] `preview/mod.rs` (declares the two pure modules), `pub mod preview;` in `background_dancers/mod.rs`;
      `viewport_smoke.rs` imports `CHROME_ORIGIN` / `FALLBACK_MARKER` from `preview::layout`.
- [x] Harness mounts `preview_layout` + `preview_state` → 152 ✓ (`logs/harness.log`); `cargo check` 0 warnings
      (`logs/cargo-check.log`); `cargo fmt`.
## Deviations
- `SlotState::on_request` takes `(focused_ours, wanted, now)` as the task's simplification suggested.
- `layout.rs` also carries `DANCER_UP`, `FALLBACK_NEAR`, `AUTHORED_ASPECT` (needed by task-02's camera glue).
Status: Complete (uncommitted — maintainer commits manually)
