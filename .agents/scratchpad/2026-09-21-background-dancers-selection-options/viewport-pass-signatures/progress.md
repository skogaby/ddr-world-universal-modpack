# progress — viewport-pass-signatures (Step 2)

Task `.agents/tasks/2026-09-21-background-dancers-selection-options/step02/task-01-viewport-pass-signatures.code-task.md`;
approval chain verified (task Generated By code-task-generator 2026-09-21; plan + design Approved 2026-09-21); auto mode.
Build/test: `cargo check --target x86_64-pc-windows-msvc`; `./scripts/validate_signatures.sh ~/Desktop/ddr_modules`;
`scripts/sig_harness/shape_diff.py --json … --dir … --names <the nine>`.

- [x] Ghidra (gamemdx_20260825): byte-exact anchors read for all nine sites (boot attach 3D +0x2E5 / 2D +0x3AA,
      shutdown detach +0x100, pass ctor store block +0x159 and tail +0x2C1, manager tick memcpys +0xAD, setup +0x5A,
      worker +0x187, target-list render +0x07); attach/detach/render-fn/setup prologues read for the identity gates.
- [x] `signatures.rs`: nine `SignatureDefinition`s; `Scene3dViewportSites` (30 fields) + `Scene3dSites.viewport`;
      `scene3d_resolve_viewport` (every cross-check in the task table) + `publish_viewport` + `scene3d_viewport_sites`;
      wired into `derive_scene3d` as the third OPTIONAL sub-group (`SCENE3D_VIEWPORT` names removed on any miss).
- [x] `scene3d::init` logs `scene3d viewport pass: available (…)` / `unavailable`.
- [x] Sweep iterations: (1) `viewport_detach` matched twice → anchored on the preceding `ADD RDX,imm32` block (+30 skip);
      (2) `model_pass_enable_tail` matches twice by design (SceneGraphManager ctor twin) → accept 1..=2, all must agree;
      (3) setup prologue is `40 53 …` (REX PUSH RBX). Final: ALL GREEN, 25/25 values + 8/8 addresses on
      20250805 / 20260224 / 20260721 / 20260825 / 20260915, every value == the design table
      (0x38/0x28/0x40/0x38/0x14/0x16/0xF8/0x30/0x28/0x2C/0x38/0x48/0x4C/0x50/0x54/0x58/0x98/0xE0/0xE8/0xF0/8/0x24/0x218/0x140000/0x14).
- [x] `shape_diff.py` (ref 20260825, window 0x100): all nine byte-shape-identical on every build.
- [x] `cargo fmt`, `cargo check` clean.

## Deviations
- NINE AOBs instead of the design's seven: `render_graph_2d_attach` (the RENDER_2D list offset comes from the 2D-list
  attaches, a different shape from the pass attaches) and `model_pass_enable_tail` (the DISTANTVIEW global for the
  free-bit check). Each adds cross-checks; nothing is hardcoded.
- Published names use the `scene3d_vp_*` prefix (the design's examples were `viewport_*`).

Status: Complete (uncommitted — maintainer commits manually)
