# plan — instance-plan-and-session-knobs (Step 4 task-02)

Status: Approved 2026-09-21 (auto mode: approved plan Step 4 + design §4.8/§5.3 stand in for plan approval)

Context: task `.agents/tasks/2026-09-21-background-dancers-selection-options/step04/task-02-instance-plan-and-session-knobs.code-task.md`
(`Generated By: code-task-generator 2026-09-21`; plan + design both `Status: Approved 2026-09-21`). Build/test commands as
in `../pick-generalisation/context.md`.

## Test scenarios (pure, `instance_plan.rs` + `schedule.rs`, harness-mounted)
Fixture `PlanInput`: stage parts `gm_boom00_bg` (prio −2, 3 bones) + `gm_boom00_stage` (no prio, 2 bones); dancers
`pl_emi01` (40 bones, parts `pl_emi01_head00`, `pl_emi01_forearm00` mirrored; ground) + `pl_rage00` (38 bones, part
`pl_rage00_hips00`; ground); shadow (1 bone).
1. `gameplay_plan_order_and_slots` — base 0 / budget 32 / 0 layers / None ⇒ kinds
   `[Stage(0){lowprio,−2}, Stage(1){stage,0}, Dancer(0), Dancer(1), Part{0,0}, Part{0,1}, Part{1,0}, Shadow(0), Shadow(1)]`,
   slots 0..=8, masks stage/lowprio/dancer as today, `children == [[4,5,7],[6,8]]`, `max_bones == 40`, `truncated == 0`,
   `mirror` true only for Part{0,1}.
2. `slot_base_shifts_owner_slots` — base 16 ⇒ slots 16..=24; with 1 hull layer every hull's slot == its body's.
3. `mask_override_covers_every_instance` — Some(0x20), 2 layers ⇒ all `pass_mask == 0x20`; hull count = 2 × 6 (stage
   part `_stage`, 2 dancers, 3 parts — never the `_bg` part, never a shadow); hull twins grouped per body in body order.
4. `budget_truncates_the_tail` — budget 4 ⇒ owners 4..=8 `Skipped` + `NO_SLOT`, `truncated == 5`; with 1 layer no hull
   whose `of` is a skipped body.
5. `restyle_allowed_cases` — Shadow false; StagePart `_bg` false / other true; Dancer/Part true; Hull of `_bg` false,
   of `pl_shadow00` false, of a body true.
6. `no_shadow_when_skeleton_missing` — `shadow_bone_count: None` ⇒ no Shadow instances even with ground bones.
7. `schedule::synthetic_schedule(9.0)` ⇒ `cut_times(23.0) == [7.5, 15.0, 22.5]`, `dancer_count() == 1`;
   `synthetic_schedule(0.0)` ⇒ `segment_len(0) == MIN_SEGMENT`.

## Implementation
1. `instance_plan.rs`: move `InstanceKind`/`InstanceStatus`/`Instance`/`restyle_allowed`; add `NO_SLOT`, `PassMasks`,
   the `*Spec`/`PlanInput`/`Plan` types, `plan_instances` (line-for-line port of `Session::new`'s loops, slot =
   `slot_base + owner_index`, budget check on the owner count, mask override applied at construction time so hulls copy it).
2. `session.rs`: re-export; `const _` NO_SLOT pin; `Session::new(.., slot_base, item_pass_mask)` builds `PlanInput` from
   `parsed`, consumes `Plan`, keeps the WARN text; `with_schedule`.
3. `schedule.rs`: `synthetic_schedule`.
4. `lifecycle.rs`: `Session::new(.., 0, None)`. Harness mounts `instance_plan`.
