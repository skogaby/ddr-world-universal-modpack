# context — pick-generalisation (Step 4 task-01)

Task: `.agents/tasks/2026-09-21-background-dancers-selection-options/step04/task-01-pick-generalisation.code-task.md`
(`Generated By: code-task-generator 2026-09-21`). Source plan
`.agents/planning/2026-09-21-background-dancers-selection-options/implementation/plan.md` — `Status: Approved 2026-09-21`;
design `design/detailed-design.md` — `Status: Approved 2026-09-21` (read: §4.6, §4.8, §7). Approved-planning lineage
verified ⇒ auto mode runs to completion. No `CODEASSIST.md`; AGENTS.md governs (no commits, `~`-relative paths only).

## Requirements
- Pure `src/mods/background_dancers/pick.rs` (std + `super::selection`): `Pick` (stage `Option<StageCandidate>`),
  `make_pick`, `assemble_pick` (wraps `Some`), `assemble_pick_opt`, `ParseOptions { shadow }` (+ `GAMEPLAY`/`PREVIEW`),
  `Pick::arcs()` / `arcs_for(opts)` / `summary()` handling `None` + empty dancers.
- `session.rs` re-exports; `parse_pick(pick, opts)` skips stage / motion / shadow / camera work accordingly.
- `lifecycle.rs` passes `ParseOptions::GAMEPLAY`.
- Harness mounts `pick`.
- Gameplay output byte-identical (arcs list, summary line, parsed bundle).

## Build / test
- `cargo check --target x86_64-pc-windows-msvc` (repo root)
- `./scripts/validate_background_dancers.sh` (pure harness; baseline 127 ✓ in `logs/harness-baseline.log`)
- `cargo fmt` (whole crate)

## Files
- new `src/mods/background_dancers/pick.rs`; edit `session.rs`, `lifecycle.rs`, `mod.rs`, `scripts/validate_background_dancers.sh`.
- Fixtures: `selection::fixtures::{rows, real_map_rows, real_chara_rows}`; candidates via `selection::stage_candidates` /
  `dancer_candidates` with an `exists` closure.

## Assumptions
- `Pick.camera_row` stays a `Vec<String>` (empty for a `None` stage) — `assemble_pick_opt` only looks the row up when a
  stage is present.
- `summary()` for `None`: `stage=none{<src>} parts=0` (the design gives no wording; the `{source}` token is kept so the
  line stays greppable).
