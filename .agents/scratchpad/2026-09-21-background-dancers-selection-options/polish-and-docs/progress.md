# progress — polish-and-docs (Step 6 task-02)
Task `.agents/tasks/2026-09-21-background-dancers-selection-options/step06/task-02-polish-and-docs.code-task.md`
(`Generated By: code-task-generator 2026-09-21`; plan + design `Status: Approved 2026-09-21`) — auto mode.
- [x] WARN audit: `preview/mod.rs` classes (compositor / graph disabled / tables / unknown key / marker) are `Warn` bits;
      `badge.rs` loader / widget WARNs are `AtomicBool` latches; `scene.rs`'s > 16-parts truncation WARN now once per stage
      key (`TRUNCATION_WARNED`); `viewport_pass::create` refusals reached ≤ once per side per boot via the "attempted" rule.
- [x] `docs/background_dancers_research.md` §5 (5.1 dispatch, 5.2 pass object + clone, 5.3 ClearViewport + ARGB, 5.4 camera
      math, 5.5 what the graph does not do, 5.6 tag-0x10 correction, 5.7 signatures, 5.8 preview scene shape).
- [x] AGENTS.md Background Dancers row: player choice + previews, compositor one-liner, driver, fail-open rule, D3DCOLOR
      note, dev knobs, harness mounts, RE §5 + planning pointers.
- [x] README: the two options + live preview paragraph under Background Dancer Revival; the mods table entry.
- [x] `progress.md` validation matrix (design §7 items 1–6 × platforms × resolutions) with expected visuals + log lines;
      Step 6 ticked in `implementation/plan.md` (matrix pending).
- [x] Gates: `cargo check` 0 warnings, harness 152 ✓, custom_options 57 ✓, `./build.sh` clean, `cargo fmt`; scratchpad logs
      sanitised (`<repo>` / `/<home>/`); `git grep` for `/Users|/home` adds no new tracked hits.
Status: Complete (uncommitted — maintainer commits manually)
