# Context — theme-policy-and-trigger

Task: `.agents/tasks/2026-09-25-ddr-selection-a-a3-themes/step01/task-01-theme-policy-and-trigger.code-task.md`
Mode: auto (maintainer: autonomous through the first cabinet smoke test).

## Approval chain (verified 2026-09-25)
- Task carries `Generated By: code-task-generator 2026-09-25` (breakdown pre-approved by the
  maintainer's autonomy instruction).
- Source plan `.agents/planning/2026-09-25-ddr-selection-a-a3-themes/implementation/plan.md`:
  `Status: Approved 2026-09-25`.
- Design `.agents/planning/2026-09-25-ddr-selection-a-a3-themes/design/detailed-design.md`:
  `Status: Approved 2026-09-25`.

## Build / test
- Host tests: `./scripts/validate_ddr_selection.sh` (repo root) — mounts the pure files; baseline
  142 passed.
- Crate type check: `cargo check --target x86_64-pc-windows-msvc` (baseline clean).
- No `CODEASSIST.md`; AGENTS.md rules apply (no commits; `cargo fmt` whole crate; harness files
  `crate::`-free, `super::` cross-refs allowed — `banner_logic` already uses `super::panel_logic`).

## Files
- `src/mods/ddr_selection/trigger.rs`, `policy.rs`, `marker_keys.rs`, `song_info_logic.rs`.
- Consumers of the changed policy API (task-02): `package_helper.rs`, `intro.rs`, `mod.rs`.

## Requirements (from the task)
1. Row values 7..=9 → skins 6..=8; labels; clamp 0..=9; dev knob 1..=8; AUTO unchanged.
2. Theme table, `is_era` / `is_theme` / `theme`, `tex_number`, `engine_skin`, `skin_name` = labels.
3. `Entry.skins: u16`, `Naming` / `ThemeArc`, `Decision::Legacy { naming }`, one `package_name`.
4. Theme rows for judge / fast_slow / fullcombo / game_over / danger / score_compare / stage /
   song_info only.
5. `root_name(6..=8)`, `mode_for_skin(6..=8)`.

## Interpretation / assumptions
- `skin_name` returns the row labels; a test asserts `skin_name(s) == super::trigger::row_label(s+1)`.
- `package_name` signature: `(arc_base, skin, naming)`; `Naming::Theme` needs the skin's suffix, so
  the function consults `theme(skin)`; a `Theme` naming with a non-theme skin falls back to
  `Suffixed` (unreachable by table construction; tested).
- `legacy_name` kept (still used for `Suffixed`).
- Theme danger row: adapter None (skin-0 placement at `filter`).
