# Context — theme-gameplay-name

Task: `.agents/tasks/2026-09-25-ddr-selection-a-a3-themes/step06/task-01-theme-gameplay-name.code-task.md`
Mode: auto. Approval chain: task generated by code-task-generator; plan and design `Status: Approved
2026-09-25`.

Build / test: `cargo check --target x86_64-pc-windows-msvc`, `cargo fmt`, `./build.sh`,
`./scripts/validate_ddr_selection.sh` (baseline 181), `./scripts/validate_signatures.sh
~/Desktop/ddr_modules`, `scripts/sig_harness/shape_diff.py`.

RE (recorded in `src/mods/ddr_selection/score_name.rs`'s module doc): World's screen graph is
A3's (`DAT_1806f2d20`, slot 7 `+0xB0` = the mod's widget list, slot 8 `+0xC8` = the gameplay list
World still pushes onto); ScreenRoot draws children sorted by the `+0xC` key; the difficulty frame
is BM2D group 1 (`0x7FFFFFFA`) in both games, so A3's key `0x7FFFFFFB` is right; font 6 is loaded
at boot (`font_by_id` = A3 `FUN_18014f310`); A3 `PlayerWork+1` = World `+5` (profile).

Engine design: two widgets created once on the render thread (font 6, native style, slot-8 list,
key `0x7FFFFFFB`), reused; `score.rs` init POST binds a side (text / style); a self-requeuing
render-thread job re-binds from the placeholder each frame while the difficulty layer lives and
follows its visibility; GAMEPLAY exit / disarm / disable hide.
