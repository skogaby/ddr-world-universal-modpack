# Context — theme-panel-score-sets

Task: `.agents/tasks/2026-09-25-ddr-selection-a-a3-themes/step05/task-02-theme-panel-score-sets.code-task.md`
Mode: auto. Approval chain: task generated by code-task-generator; plan and design `Status: Approved
2026-09-25`.

Build / test: `cargo check --target x86_64-pc-windows-msvc`, `cargo fmt`, `./build.sh`,
`./scripts/validate_ddr_selection.sh` (baseline 164), `./scripts/validate_signatures.sh
~/Desktop/ddr_modules`, `scripts/sig_harness/shape_diff.py`.

RE: `docs/ddr_selection_theme_score_sets.md`.

Engine design: the pure `score_set_logic::fill_side` turns per-side inputs (entered, difficulty,
name bytes, area texture, record state, target state, package readiness) into `Write {path,
texture, visible}`; `score_set.rs` builds those inputs from World's memory; `panel.rs` requests the
packages with the theme session (tickets join the existing release queue) and applies the writes
in `fill_theme` with its own `set_texture` / `set_visible` helpers. `signatures.rs` (shared with
other in-flight work) only gains `derive_ddr_sel_score_set` + its accessor.
