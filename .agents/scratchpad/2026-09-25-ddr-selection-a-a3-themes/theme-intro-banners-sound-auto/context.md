# Context — theme-intro-banners-sound-auto

Task: `.agents/tasks/2026-09-25-ddr-selection-a-a3-themes/step03/task-01-theme-intro-banners-sound-auto.code-task.md`
Mode: auto. Approval chain: task generated by code-task-generator; plan and design `Status: Approved
2026-09-25`.

Build / test: `cargo check --target x86_64-pc-windows-msvc`, `cargo fmt`, `./build.sh`,
`./scripts/validate_ddr_selection.sh` (baseline 155; its real-bank leg builds every manifest cue
from the stock `_n` banks when `$DDR_WORLD_INSTALL` is set).

Research (stock install + Ghidra A3 `gamemdx_20240402`):
- `dance_message_v{0,1,2}`: `00_ready` (labels in/loop/loop_end/out/end, embeds `vo_ingame_ready`) and
  `00_here` — the era packages' shape.
- `common_shutter_v{0,1,2}`: `shutter_clear`, `shutter_failed`, `00_cleared`, `00_failed`,
  `00_prayforall` with in/loop/loop_end/out/end; `_v0`'s `shutter_failed` embeds `ACE_TEPPAN3`, the
  only theme cue the `dsel` manifest lacked (present in `se_normal_n.xsb`, not in World's loaded
  banks) — added. A full sweep of every theme package's embedded cues found no other gap (the
  savior-only `ACE_shutter_choice_savior` is event art, not normal play).
- A3 `FUN_1800369e0` skin-0 crowd (transcribed): `g > 0.7` (`DAT_1802888ac` = 0x3F333333) →
  `STG_APP02` + guarded `vo_ingame_cheer`; `g ≤ 0.4 ∧ c < 13` → nothing if `g ≥ 0.2`, else `STG_BOO`
  + guarded `vo_ingame_boo`; else `STG_APP03` + guarded cheer. Guarded voices share `+0x84` with the
  state voice (`FUN_180037140`), skipped while `c % 100 ≥ 91`.
