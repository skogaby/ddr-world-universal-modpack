# Context — theme-arming-and-engine-wiring

Task: `.agents/tasks/2026-09-25-ddr-selection-a-a3-themes/step01/task-02-theme-arming-and-engine-wiring.code-task.md`
Mode: auto. Approval chain as in `../theme-policy-and-trigger/context.md` (plan + design approved
2026-09-25; task generated by code-task-generator).

Build / test: `cargo check --target x86_64-pc-windows-msvc`, `cargo fmt`, `./build.sh`,
`./scripts/validate_ddr_selection.sh`. No signatures changed (no sweep needed).

Files: `mod.rs`, `package_helper.rs`, `intro.rs`, `stage_frame.rs`, `movie_sel.rs`, `song_info.rs`,
`options.rs` (docs / logs).

Assumptions:
- Themes have no panel / banner / intro / announcer path yet: `panel_logic::packages`,
  `banner_logic::art`, `rules::step` return `None` for 6..=8, and `dance_message` has no theme row,
  so those surfaces stay World's (verified by reading the call sites).
- S-Marvelous stands down on 6..=8 (`targets::target_skin` → `None`): plain Marvelous.
- Theme package exports verified against the stock install: judge / fast_slow / fullcombo /
  game_over / danger / score_compare export lists identical to skin 1's; `dance_song_info0000_v{0,1,2}`
  = `dance_song_info` with `music_name_usr` / `artist_name_usr`; `dance_stage_frame0000_v{0,1}` =
  `stage_frame` with `stage_frame_usr` / `stage_number_usr`, textures `stage_frame0000_stage_*`.
