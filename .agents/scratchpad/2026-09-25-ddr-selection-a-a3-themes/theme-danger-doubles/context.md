# Context — theme-danger-doubles

Task: `.agents/tasks/2026-09-25-ddr-selection-a-a3-themes/step07/task-01-theme-danger-doubles.code-task.md`
Mode: auto. Approval chain: task generated by code-task-generator; plan and design `Status: Approved
2026-09-25`.

Build / test: `cargo check --target x86_64-pc-windows-msvc`, `cargo fmt`, `./build.sh`,
`./scripts/validate_ddr_selection.sh` (baseline 184), `./scripts/validate_signatures.sh
~/Desktop/ddr_modules --json $TMPDIR/opencode/sweep_s7.json`, `scripts/sig_harness/shape_diff.py`.

Verified in Ghidra (20260825): DanceDangerActor RTTI vtable at `0x1803616a8` (COL at -8), slot 4 =
`FUN_180068ce0` (init). Site A at init + 0xAC (`0x180068d8c`): `83 BF B4 00 00 00 00` (a dword
compare: `CMP dword [RDI+0xB4],0`, the record skin copied from the record's `+0x28`), `48 8D 35`
→ `"danger_single"`, `4C 8D 3D` → `"danger_double"`, `4C 8B C6`, `75 07` (+0x18), `45 84 ED`,
`4D 0F 45 C7`. `R13B` = `*(actor+0x58) == 1` (the doubles style). The clip create result is
dereferenced unchecked, so the export must exist.

Offline AOB (`83 BF B4 00 00 00 00 48 8D 35 ?? ?? ?? ?? 4C 8D 3D ?? ?? ?? ?? 4C 8B C6 75 07 45 84 ED
4D 0F 45 C7`): one hit per build at RVA 20250805 `0x6573c`, 20260224 `0x6478c`, 20260721 `0x68dac`,
20260825 `0x68d8c`, 20260915 `0x6955c`, which is the design's file offsets + 0xC00.

Stock install: `dance_danger0000_v0.arc` (unpacked fresh) lists `danger_single`, `danger_double`
and their `_failed` variants.

Engine design: `danger.rs` holds `Sites { jnz }` and an `APPLIED` flag under a lock. `sync(want)`
is idempotent. The package helper calls it for every `dance_danger` request: a theme's registered
decision ⇒ `true`; an era's registered decision, a stock request or a probe miss ⇒ `false`.
