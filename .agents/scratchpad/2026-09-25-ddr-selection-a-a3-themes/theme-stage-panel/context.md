# Context — theme-stage-panel

Task: `.agents/tasks/2026-09-25-ddr-selection-a-a3-themes/step04/task-01-theme-stage-panel.code-task.md`
Mode: auto. Approval chain: task generated by code-task-generator; plan and design `Status: Approved
2026-09-25`.

Build / test: `cargo check --target x86_64-pc-windows-msvc`, `cargo fmt`, `./build.sh`,
`./scripts/validate_ddr_selection.sh` (baseline 162).

Verified against the stock install (`common_choice_v{0,1,2}` / `shutter_choice_hd_root`): labels
`in`, `loop`, `loop_end`, `out`, `end`, `frame_out`, `data_release`, `ready_loop[_end]`; children
`choice_stage_usr` (+ `scene_choice_stage_usr`), `choice_stage_usr2`, `choice_background_usr`,
`choice_jacket_usr` (`jacket_root_usr/jacket_usr`), `fullcombo_challenge_usr`, the score-set
children; `caution_usr` only in `_v1` / `_v2`. Textures `scene_choice_stage_{1st,2nd,3rd,4th,final,
extra}` in each package. The root's default `choice_stage` has only `in` / `end` labels (no `voice`).
Embedded cues `Plate_spin3_st`, `STG_APP02`, `banner_in`, `se_shutter_in/out` — in the era bank.

Engine design: the session carries its variant and its static root-package name; the row patch
writes that pointer and remembers it (`ROW_PKG`) for the checked restore; a theme session holds no
tickets (`art_ready` is then trivially true), so it never conflicts with the end banner's by-name
load of `common_shutter_vN`.
