# Context — theme-hud-adapters

Task: `.agents/tasks/2026-09-25-ddr-selection-a-a3-themes/step02/task-01-theme-hud-adapters.code-task.md`
Mode: auto. Approval chain: task generated by code-task-generator; plan and design `Status: Approved
2026-09-25`.

Build / test: `cargo check --target x86_64-pc-windows-msvc`, `cargo fmt`, `./build.sh`,
`./scripts/validate_ddr_selection.sh` (baseline 151).

Verified against the stock install (theme packages vs era packages):
- `dance_gauge0000_v{0,1,2}` export `00_dance_gauge` with `fill _usr`, `fill _2_usr`,
  `damage_1..8_usr`, `gauge_frame_usr`; `_v1` carries every FLARE label, `_v0` none (as the eras).
- `dance_combo0000_v{0,1}` export `dance_combo` with `combo_usr`, `number_usr/0001..1000_usr`;
  textures `dance_combo0000_{marvelous,perfect,great,good}_{0..9,combo}`.
- `dance_score0000_v{0,1,2}` exports `frame_score` (7 places, commas, `ex_tex`),
  `frame_difficulty_{1,2}p[_reverse]` (`difficulty_level_usr`, `level_tex`, `level_base_tex`, plus
  `name_usr`), `difficulty_level_base` (the name stand-in); textures `dance_score0000_*` incl.
  `lv00..20`, `score_num_0_gray`, `score_comma[_gray]`.
- Theme layout roots `dance_common0000_v{0,1,2}`: every marker the post-pass reads is present.
