# Context + Plan: task-01 preview registry slot (step 02)

Status: Approved 2026-08-15 (auto mode — same verified chain: task
`Generated By: code-task-generator 2026-08-15`, plan + design
`Status: Approved 2026-08-15`)

Build/test commands: as step-01 records (validator script = host tests,
222 baseline; windows check; fmt; build.sh). Repo-root `cargo test`
pre-existing-broken on macOS (retour).

## Test scenarios (binding_tests.rs, mounted by the validator)

- T1 preview slot lifecycle: publish → with_preview sees it; re-publish
  retires the previous onto the retired list (retired_count 1);
  retire_preview empties (true, then false when already empty).
- T2 both-slot retire: active file 5 + preview file 7;
  retire_by_file(7) → preview gone, active intact; retire_by_file(5) →
  active gone; retire_by_file(9) → false.
- T3 routing: with_bound_for_file(5) visits active, (7) visits preview,
  (9) None; any_bound truth table (empty/active-only/preview-only/both).
- T4 cycle: publish_preview(Side-target binding via prepare_binding Side)
  → serve first verbatim-main packet through with_bound_for_file →
  retire_by_file → sweep with cooldown loop → reclaimed once, count 0.
- T5 preview mailbox: interleave note_refusal + note_preview_refusal;
  each take_* reports its own (identity, count); no cross-masking.
- T6 next_preview_generation monotonic ≥ 1, strictly increasing.

## Implementation shape

- Registry: `preview: AtomicPtr<Binding>` + `preview_refusal_{code,file,
  count}` atomics; `publish_preview` / `with_preview` / `retire_preview`
  mirror the active-slot code (share `push_retired`); `retire_by_file`
  gains a preview leg (factor the CAS-retire into a helper over a slot
  ref); `any_bound`; `with_bound_for_file` (active first);
  `note_preview_refusal` / `take_preview_refusal`;
  `next_preview_generation` static counter fn.
- io_callback_hook: `bound_verdict` = `any_bound` gate → one lookup →
  `with_bound_for_file`.
- Doc comment updates: registry struct doc + io hook header note that the
  song-select preview player's bank IS served when a preview binding is
  published (the old "everything else takes the trampoline" sentence).

## Progress

- [x] Registry slot + methods + mailbox + counter (`publish_preview` /
      `with_preview` / `retire_preview`; factored `publish_into` /
      `with_slot` / `retire_slot`; both-slot `retire_by_file`;
      `any_bound` + `with_bound_for_file`; preview mailbox;
      `next_preview_generation`)
- [x] io hook rewire (`bound_verdict` = any_bound gate → one lookup →
      with_bound_for_file; module header updated)
- [x] Tests T1–T6 (5 tests + monotonicity helper; validator 222 → 227)
- [x] Gates: validator 227/0, windows check clean, fmt applied,
      build.sh release DLL clean

Deviations: none. (Registry tests use identity-passthrough bindings for
slot mechanics — no producer threads — and one real Side-target
prepare_binding for the full cycle test.)

Status: Complete (uncommitted — maintainer-managed commits per the
step-01 precedent; all readiness gates green)
