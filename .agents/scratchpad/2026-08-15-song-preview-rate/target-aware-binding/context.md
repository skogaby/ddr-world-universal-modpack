# Context: task-02 Target-aware Binding runtime

Date: 2026-08-15. Mode: auto (same verified approval chain as task-01:
task `Generated By: code-task-generator 2026-08-15`; plan + design both
`Status: Approved 2026-08-15`).

## Build / test commands (repo root)

Same as task-01 (see `../stretch-target-planner/context.md`), including the
recorded fact that repo-root `cargo test` is pre-existing-broken on this
macOS host (retour) — the host-test runner is
`./scripts/validate_song_playback_speed.sh` (218 tests after task-01).

## Requirements (task file)

1. `prepare_binding(..., target: StretchTarget)`; plan through the target;
   rate from `layout.entries[target_entry_index].rate`; percent==100 keeps
   the identity path + `debug_assert!(target == Main)`.
2. `Binding::build` derives ring base / verbatim entry from
   `layout.target_entry_index`; fields renamed `target_*`/`verbatim_*`.
3. Serve dispatch keys on target vs verbatim entry; gap/pre-data/EOF
   unchanged.
4. Regen targets, pace limit, silent blocks, content mapping follow the
   target entry.
5. `generator.rs` consumes target-relative accessors.
6. Existing callers pass `StretchTarget::Main`; observable behavior
   unchanged (incl. identity passthrough + fault legs).
7. Both cfg targets compile.

## Code inventory (verified by grep, post-task-01 tree)

### binding.rs surfaces to rename/redirect
- `rate` selection: line ~246 → target entry (already parameterized site).
- `active_content_grid` (~601): grid = ring-served entry → target.
- Struct fields (~969–977): `side_entry` → `verbatim_entry`,
  `side_source_offset` → `verbatim_source_offset`,
  `main_source_offset/len` → `target_source_offset/len`.
- `build` (~1082–1141): derive `verbatim_entry = 1 − target_entry_index`;
  ring base `entry_offsets[target_entry_index]`; verbatim length
  debug_assert; identity-mismatch guard on the target entry (identity sets
  target == main, semantics unchanged).
- `ms_to_blocks` (~1235): `formats[target_entry_index]`.
- `check_spans` (~1574): `entry != verbatim_entry` gate.
- `copy_spans` (~1616/1628): verbatim arm + `copy_mapped_main` →
  `copy_mapped_target`.
- `copy_spans_silent` (~1723): verbatim arm.
- `main_data_start/end` (~1758/1764) → `target_data_start/end`;
  `pace_limit` (~1811) follows.

### generator.rs
- Lines 438/486 (`main_data_start`), 500/542/558 (`main_data_end`),
  457/479/525 (`layout().main_entry_index`), 693 (regen guard), doc at 516.

### prepare_binding call sites (12)
- `wavebank_hook.rs:211` (production — `StretchTarget::Main`)
- `binding_tests.rs`: 137, 180, 202, 233, 296, 313, 383, 613, 646, 649, 661.

### Untouched on purpose
- The validator script's own `main_entry_index` report/oracle logic
  (gameplay-plan validation; target == main there).
- `xact_runtime.rs`, `transaction.rs`, `io_callback_hook.rs`,
  `clock_patch.rs` — no main/side entry knowledge.

## Test harness facts (generator_tests.rs)

- `replay_fixture(preview_first)` gives BOTH entries full-entry loops and
  an honest preview duration (2000 < 2048-frame payload) — Side-target
  WSOLA at 50 % has the loop it needs.
- `transform_bank_oracle_mode(source, percent, preserve_pitch)` — the
  whole-buffer oracle to generalize with a target parameter.
- `make_binding(source, percent, preserve_pitch, ring_capacity)` — ditto.
- `replay_via_serve(binding, core)` reads the header, BOTH entries'
  packets, and a defensive EOF read — covers AC3/AC4 as part of AC2's
  byte-identity comparison.
- Existing `side_entry_prepare_read_completes_without_main_production` is
  the shape for the inverted no-production assertion.
- `retire_cancels_pending_and_waits_for_reader_quiescence` is the AC5
  shape.

## Ambiguities

None — the breakdown decisions (approved) settle naming, the identity
corner, and mapping semantics.
