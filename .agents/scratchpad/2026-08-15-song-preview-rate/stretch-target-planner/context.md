# Context: task-01 StretchTarget parameterization of the virtual-bank planner

Date: 2026-08-15. Mode: auto (approval chain verified — see below).

## Approval chain (Step-1 verification)

- Task: `.agents/tasks/2026-08-15-song-preview-rate/step01/task-01-stretch-target-planner.code-task.md`
  carries `Generated By: code-task-generator 2026-08-15`. VERIFIED.
- Source plan `.agents/planning/2026-08-15-song-preview-rate/implementation/plan.md`:
  `Status: Approved 2026-08-15`. VERIFIED.
- Design `.agents/planning/2026-08-15-song-preview-rate/design/detailed-design.md`:
  `Status: Approved 2026-08-15`. VERIFIED.
- Conclusion: verified approved planning — auto mode may stand this in for
  plan approval per the sop.

## Build / test commands (from AGENTS.md, run at repo root)

- `cargo test` — host tests (the pure layers this task lives in)
- `cargo check --target x86_64-pc-windows-msvc` — cross type check
- `cargo fmt` — whole crate, never file args
- `./scripts/validate_song_playback_speed.sh` — song-rate validator
  (temp-dir `#[path]` harness; embeds Rust that CALLS `plan_virtual_bank` —
  must be updated with the new parameter)
- `./build.sh` — release build (not required for a pure-layer task per the
  task file; run at Step-1 close-out by task-02)

## Requirements (from the task file)

1. `StretchTarget { Main, Side }` enum in `src/core/xact/virtual_bank.rs`.
2. `plan_virtual_bank(source, percent, target)` — stretched plan to the
   target entry, verbatim to the other; `EntryRate` identity follows the
   stretched entry.
3. `VirtualBankLayout.target_entry_index`; Main ⇒ == `main_entry_index`,
   Side ⇒ == `1 − main_entry_index`.
4. `plan_identity_bank` behaviorally untouched; sets
   `target_entry_index = main_entry_index`.
5. All callers pass `StretchTarget::Main`; both cfg targets compile.
6. Side fixtures honest (durations inside the final block).
7. Side-entry loops map through `map_loop` when targeted.
8. No behavior change to `plan_entry`/`plan_entry_values`/`map_loop`.

## Implementation paths

- `src/core/xact/virtual_bank.rs` — the module under change (fully read).
- Callers of `plan_virtual_bank` (all must gain `StretchTarget::Main`):
  - `src/services/song_rate/binding.rs:238` (`prepare_binding`)
  - `src/core/xact/tests.rs` (~13 call sites)
  - `src/services/song_rate/generator_tests.rs:168,217,928`
  - `src/services/song_rate/binding_tests.rs:176,277,448`
  - `scripts/validate_song_playback_speed.sh:1049,1802` (embedded harness Rust)
- `plan_identity_bank` callers need no signature change (field addition only).

## Existing patterns to match

- Test fixtures: `src/core/xact/tests.rs` — `build_bank(preview_first,
  tail_remainder)`, `build_bank_with_data_lengths`, `build_bank_bytes`
  (explicit formats/durations/loops — the tool for honest Side fixtures),
  `partial_tail_bank` (the honest-fixture exemplar), `layout_payload`,
  `streamed_bank_bytes` (serializer oracle), `serve_virtual_bank`
  (region-walk assembler). Constants `BLOCK_ALIGN = 140`,
  `SAMPLES_PER_BLOCK = 128`. Both physical entry orders always exercised
  (`preview_first` loops).
- Absolute-value pinning style: `plan_targets_match_the_retired_transformer_vectors`
  (in-module tests of `virtual_bank.rs`).
- Doc-comment style: paragraph rationale with design/research citations.

## Non-functional notes

- Pure module: no unsafe, no game deps, no logging. Failure = typed
  `PlanError` only.
- The existing oracle tests (`virtual_bank_pre_data_matches_streaming_serializer`,
  `virtual_bank_resolve_reconstructs_serializer_layout`) already pin the
  Main path against the serializer; the task adds an explicit value-level
  pin as well.

## Ambiguities

- None material. The task's breakdown decisions (approved in conversation)
  cover the `percent==100 + Side` corner (debug_assert + fall-through —
  actually that corner lives in `prepare_binding`, task-02; for THIS task
  the planner treats `target` uniformly at any percent) and naming.
