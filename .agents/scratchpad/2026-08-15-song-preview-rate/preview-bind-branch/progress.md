# Progress: task-01 preview bind branch (step 03)

Updated: 2026-08-15
Status: Host half COMPLETE — awaiting cabinet deploy #1 (the step's demo
gate). Uncommitted (maintainer-managed commits).

Approval chain: verified (task Generated By 2026-08-15; plan + design
Approved 2026-08-15). Mode: auto.

## Checklist

- [x] preview_tests.rs first (qualify matrix red against absent module)
- [x] `preview.rs`: pure `qualify` + `PreviewBindRequest` +
      `QualifyInputs`; feature gate; `init` (scene-exit force-retire via
      scene_manager callback); `request_refresh` stub; windows glue
      `maybe_bind_preview` (gate order: feature atomic → scene atomic →
      both-identity early exit → path/song-code → entered flags →
      qualify → SourceView → `prepare_binding(Side)` with
      `next_preview_generation()` → `publish_preview` →
      `ensure_maintenance_drain`; refusals → preview mailbox)
- [x] `wavebank_hook.rs`: bind closure restructured — preview branch runs
      on every Stock gameplay outcome (incl. the identity-phase fast
      exit); `create_path`/`create_source`/`file_table_state` accessors
- [x] `runtime.rs`: `ensure_maintenance_drain`; drain preview-publish
      INFO latch + preview refusal WARN
- [x] `song_playback_speed.rs`: enable → `preview::init` +
      `set_feature_active(true)`; disable → gate off + `retire_now`;
      both change callbacks stamp `request_refresh` (inert until Step 5)
- [x] Harness: preview.rs + preview_tests.rs in the file-check list
      (mounted automatically through song_rate/mod.rs)
- [x] Gates: validator 227 → 232 (5 new preview tests), windows check
      clean, fmt applied, `./build.sh` clean
      (DLL md5 2f757e76bae22e2f5f9f785f3dbe4bd0)
- [ ] CABINET DEPLOY #1 — the step's demo (see below)

## Cabinet deploy #1 — what to validate (design matrix subset)

Deploy `./scripts/deploy.sh`. With a single side carded in:

1. C1 zero footprint: boot + browse at 100% — NO `preview binding live` /
   `preview bind refused` lines; previews sound stock.
2. C2 (settle half): set SONG SPEED 75% in the options modal, close it,
   move the wheel — every settle logs `song_rate: preview binding live —
   preview generation N, file_id F, rate a/b, pitch-preserved` and the
   preview PLAYS SLOWED pitch-preserved with sub-second start. (No live
   restart yet — an edit applies at the NEXT wheel settle; that is
   expected for this deploy.)
3. C3: PRESERVE SONG PITCH OFF → next settle logs `resample` and sounds
   record-player.
4. C5 header safety: confirm a song from a stretched preview → gameplay
   arms/commits/plays EXACTLY as shipped (armed/open-redirect/exposed/
   committed lines; audio at rate; arrows in sync; per-stage save
   suppressed as before).
5. C6: versus (both carded) → no preview lines, stock previews.
6. C7 spot: 25% and 175% preview start latency (~sub-second),
   `preview binding live` + later `binding reclaimed` lines per settle
   (reclaim confirms the sweep works — watch for leak growth).

## Deviations

- None affecting the design. The qualify matrix red phase was real (tests
  written before preview.rs existed).

## Notes for the resume

- Preview branch runs inside the bind closure pre-original — the header
  read is served from the virtual bank, exactly like gameplay binds.
- The gameplay transaction never sees preview binds (outcome stays Stock).
- Natural teardown coverage came free from Step 2's both-slot
  `retire_by_file` in the unregister prelude.
