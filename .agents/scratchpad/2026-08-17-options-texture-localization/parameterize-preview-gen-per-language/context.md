# Context: parameterize-preview-gen-per-language

Task: .agents/tasks/2026-08-17-options-texture-localization/step01/task-02-parameterize-preview-gen-per-language.code-task.md

## Approval provenance (verified)

Same chain as task 01: `Generated By: code-task-generator 2026-08-17`; source
plan + design both `Status: Approved 2026-08-17`. Auto mode proceeds.

## Session constraints

- NO git commits by the agent (maintainer commits).
- Windows-target tests run via `cargo xwin test --no-run` + CrossOver wine
  (`CX_BOTTLE=bemani`); host-native `cargo test` broken by pre-existing
  retour/aarch64 failure (see task 01 context).

## Requirements (from task file)

1. `PREVIEW_OUT_DIR` → per-language dir helper derived from
   `custom_options::asset_gen::OPTION_LANGS` (do NOT duplicate the code list).
2. `generate_chrome(option_id)` loops languages: per-language skip-if-exists;
   per-language missing-template warn+skip; return true per current caller
   contract (base image exists for languages whose templates exist).
3. `template_path_for` becomes language-explicit or internal (no external
   callers — verified: only preview_gen uses it).
4. `marker_rect_for(option_id, color)` resolves eng → jpn → kor, first clean
   hit wins; log once per option on fallback past eng.
5. `find_marker`, `apply_gamma`, `find_asset_arc*`, `MarkerRect` unchanged.
6. Module doc comment updated.

## Consumers (verified by grep — signatures must stay source-compatible)

- `src/mods/webui_options/mod.rs:184` → `generate_chrome(option_id)`
- `src/mods/webui_options/preview_overlay.rs:429` → `marker_rect_for(id, color)`
- `src/mods/webui_options/bg_preview_overlay.rs:310` → `marker_rect_for(id, MarkerColor::Green)`
- `template_path_for`: no callers outside preview_gen.rs

## Access path

`asset_gen` is `pub mod` inside `custom_options`; `OPTION_LANGS` is
`pub(crate)` → reachable as
`crate::services::custom_options::asset_gen::{OptionLang, OPTION_LANGS}`.

## Build & test commands

Same as task 01 (context.md there): check/fmt/build.sh gates; wine-run tests.

## Ambiguities & resolutions (auto mode)

- "log once per option on fallback": implemented as a log line at fallback
  time (marker lookups happen at overlay build, not per frame — bounded call
  rate; a dedup set would be over-engineering). Recorded as a judgment call.
- Scaffold copies (task 01) put eng templates in all three dirs, so
  chrome generation is exercisable for all languages now.
