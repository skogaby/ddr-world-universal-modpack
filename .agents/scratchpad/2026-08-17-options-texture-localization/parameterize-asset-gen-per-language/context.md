# Context: parameterize-asset-gen-per-language

Task: .agents/tasks/2026-08-17-options-texture-localization/step01/task-01-parameterize-asset-gen-per-language.code-task.md

## Approval provenance (verified)

- Task carries `Generated By: code-task-generator 2026-08-17` ✓
- Source Plan `.agents/planning/2026-08-17-options-texture-localization/implementation/plan.md`
  → `Status: Approved 2026-08-17` ✓
- Design `.agents/planning/2026-08-17-options-texture-localization/design/detailed-design.md`
  → `Status: Approved 2026-08-17` ✓

⇒ Approved-planning descent confirmed; auto mode may proceed without a plan
approval stop.

## Maintainer-set constraints for this session

- **NO git commits by the agent** — the sop's Commit step is replaced by
  "leave the work staged-ready and report"; the maintainer commits.

## Project instruction files

- No `CODEASSIST.md` (noted; not created in auto mode).
- `AGENTS.md` — authoritative (already in session context): build gates
  (`cargo check` → `cargo fmt` whole-crate → `./build.sh`), Rust quality
  rules (graceful degradation, narrow unsafe, no panics across FFI — not
  applicable here, this is init-time code), module layout rules.
- `.agents/learnings/learnings.md` — checked; the donor-clone vs auto-inject
  trap is the design's motivation.

## Build & test commands (from repo root)

- Fast check: `cargo check --target x86_64-pc-windows-msvc`
- Host tests: `cargo test` (pure layers only; asset_gen has no host tests —
  engine-facing, validated by cabinet deploy)
- Format: `cargo fmt` (whole crate, never file args)
- Release build: `./build.sh` (cargo-xwin)

## Requirements (from task file)

1. `OptionLang` struct + `OPTION_LANGS: [OptionLang; 3]` (eng/jpn/kor),
   longhand consts, `pub(crate)`, fields: ifs_code, arc_path, ifs_name,
   ifs_mod_path, atlas_prefix (+ preview prefix `copt_prev_<code>`).
2. Table exposed `pub(crate)` for preview_gen (task 02).
3. `flush_label_atlas()` loops languages; per-language texturelist load +
   rebuild sourced from that language's mod folder.
4. Per-language WARN+skip; return true if ≥1 language's merged texturelist
   present.
5. Registration surfaces unchanged (language-agnostic).
6. `AVAILABLE_PREVIEWS` stays single eng pass + language-invariance comment.
7. Scaffold: verbatim copy of eng tex dir → jpn/kor tex dirs.
8. Per-language log lines.

## Implementation paths

- `src/services/custom_options/asset_gen.rs` — the whole change.
- `src/services/custom_options/mod.rs` — re-export surface if needed
  (`pub use asset_gen::flush_label_atlas;` at line 47 today).
- `src/lib.rs:486` — call site (`custom_options::flush_label_atlas();`) —
  signature unchanged, no edit expected.
- `data_mods/custom_options/select_music_option_lang_{jpn,kor}_v3_ifs/tex/` —
  scaffold copies (93 files each incl. .DS_Store — exclude .DS_Store).

## Existing structure facts (read 2026-08-17)

- Constants at asset_gen.rs:37-54: MOD_ROOT, CACHE_ROOT, LANG_ENG_ARC/IFS/
  IFS_MOD_PATH/ATLAS_PREFIX, PREVIEW_ATLAS_PREFIX ("copt_prev").
- `flush_label_atlas()` (line ~234): loads eng texturelist, logs counts,
  delegates to `rebuild_lang_eng_atlas(&xml)`.
- `rebuild_lang_eng_atlas` (line ~257): clones registration vecs, `tex()`
  closure over MOD_ROOT+LANG_ENG_IFS_MOD_PATH, populates AVAILABLE_PREVIEWS
  (exists-on-disk pass), builds base AtlasSet (donor-slot mode: tab title +
  labels + ribbons) + fresh AtlasSet (previews), calls
  `generate_cloned_atlases_cached(xml, LANG_ENG_IFS_MOD_PATH, CACHE_ROOT,
  MOD_ROOT, &batch)` → `BatchResult`.
- `generate_static_tab_assets` (base IFS) untouched per task.
- Module doc comment narrates lang_eng-only design — must be updated.

## Non-functional expectations

- Boot cost: 3× texturelist load + 3× (cached) atlas build on warm boots;
  cache guard in `generate_cloned_atlases_cached` makes unchanged languages
  ~free. Cold boot pays 3 builds once.
- The `copt_prev` → `copt_prev_<code>` rename invalidates the existing eng
  preview cache once (expected; noted in feature summary watch items).
- No panics: all failure paths log_warn + continue (init-time code, but
  graceful-degradation rule applies).

## Ambiguities & resolutions (auto mode)

- Task says preview prefix "`copt_prev_<code>` (or derive it consistently)":
  resolved as a `preview_atlas_prefix` field on OptionLang, longhand.
- `.DS_Store` files exist in the eng tex dir; scaffold copy excludes them
  (junk, not textures).
- `flush_label_atlas` return semantics: today `true` = merged texturelist
  present (incl. cache-served). Per-language aggregate = logical OR.
