# Context + Plan — task-02 data-feed-tap

Status: Approved 2026-08-29 (auto mode — verified upstream approval chain:
task Generated By code-task-generator 2026-08-29; Source Plan + design both
`Status: Approved 2026-08-29`.)

Task: .agents/tasks/2026-08-29-s-marvelous-judgement/step01/task-02-data-feed-tap.code-task.md

## Facts (from src/mods/power_user_statistics/data_feed.rs, read this session)
- Hook `judge_submit_hook` (line ~235): computes `grade_index =
  judge_code - 0x1028`, accepts `<= 6`; `player_side = *(actor+0x84)`
  validated `<= 1`; `has_ms_error = judge_code != OPCODE_OK &&
  !scratch.is_null()`; `ms_error = *(scratch+4)` read INSIDE the
  has_ms_error branch (line ~257).
- The S-Marv block must run for ALL grades 0..=6 (incl. O.K. with ms None),
  so it sits at the TOP of the `player_side <= 1` branch, before the
  has_ms_error split — reading the ms value itself (duplicate read of
  scratch+4 is fine: armed-only, one aligned load).
- Combo counter: GamePlayActor +0x1DC (docs/s_marvelous_judgement_research.md
  §2.1) — read only when armed.

## Plan
1. Add `ACTOR_COMBO_OFFSET: usize = 0x1DC` beside `ACTOR_PLAY_SIDE_OFFSET`.
2. Insert the block right after the `player_side <= 1` check, before the EX
   computations:
   ```
   if s_marvelous::state::is_armed(player_side) {
       let combo = *(actor.add(ACTOR_COMBO_OFFSET) as *const i32);
       let ms = if judge_code != OPCODE_OK && !scratch.is_null() {
           Some(*(scratch.add(4) as *const i32))
       } else { None };
       s_marvelous::state::on_judge_event(player_side, grade_index, ms, combo);
   }
   ```
   (import path `crate::mods::s_marvelous`.)
3. Extend the module doc comment + the calibration section comment to name
   the S-Marvelous tap as a second consumer.
4. Validate: `cargo check --target x86_64-pc-windows-msvc`; state semantics
   already host-tested (task-01).

## Test scenarios
- Covered by task-01's host suite (the block is pure plumbing of
  already-validated values). Verification here = type check + code review
  against the four acceptance criteria (disarmed single load; armed
  classification; all grades reach the machine; zero change to existing
  consumers).
