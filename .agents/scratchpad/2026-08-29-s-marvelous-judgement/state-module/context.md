# Context — task-01 state-module

Task: .agents/tasks/2026-08-29-s-marvelous-judgement/step01/task-01-state-module.code-task.md
Approval chain (verified 2026-08-29): task carries
`Generated By: code-task-generator 2026-08-29`; Source Plan
`.agents/planning/2026-08-29-s-marvelous-judgement/implementation/plan.md`
carries `Status: Approved 2026-08-29`; required design
`.agents/planning/2026-08-29-s-marvelous-judgement/design/detailed-design.md`
carries `Status: Approved 2026-08-29`. → auto mode approval stands.

## Project facts
- Rust cdylib hook DLL; nightly toolchain; host `cargo test` cannot compile
  `retour` on this ARM machine — pure modules are validated via temp-crate
  harness scripts (`scripts/validate_auto_calibration.sh` is the template).
- Build/test commands: `cargo check --target x86_64-pc-windows-msvc`
  (type check), `./scripts/validate_s_marvelous.sh` (new, this task),
  `cargo fmt` (whole crate), `./build.sh` (release, only for deploy handoff).
  All run from repo root.
- Module registration: `src/mods/mod.rs` declares modules alphabetically;
  the full Mod impl is task-03 — this task creates
  `src/mods/s_marvelous/mod.rs` as a shell (`pub mod state;`) + the module
  declaration so the crate compiles.

## Requirements (from task file)
1. `src/mods/s_marvelous/state.rs`: pure transition core (std-only, no crate
   imports — harness-mountable) + atomic wrapper.
2. Semantics (design §4.3): combo-restart reset before classify (combo <= 1);
   grade 0 & |ms| <= window (inclusive) ⇒ count++; grade 0 & |ms| > window ⇒
   degrade bit; grades 1..=3 ⇒ degrade; 4/5 ⇒ nothing (next combo start
   resets); 6 (O.K., ms None) ⇒ neutral.
3. API: `arm(side, window_ms)`, `disarm_all()`, `reset_song_state()`,
   `smarv_count(side)`, `combo_is_all_smarv(side)`, `is_armed(side)`,
   `on_judge_event(side, grade_index, ms: Option<i32>, combo: i32)`;
   `clamp_window` (1..=17).
4. `scripts/validate_s_marvelous.sh` mounting state.rs.

## Patterns to match
- Atomics arrays `[AtomicX; 2]`, relaxed orderings, side masked `& 1`
  (data_feed.rs calibration tap; playfield_styling latch).
- Single-writer hot path (game thread) ⇒ load→pure-transition→store keeps ONE
  implementation of the semantics.
- Doc-comment style: module header `//!` explaining role + design pointer.

## Non-functional
- No panics anywhere (called from a hook path); `unsigned_abs` for the window
  compare (i32::MIN safe); no allocation; no logging in this module.
