# Context — Task 02: Add logging and the game-folder safety gate

## Provenance (verified 2026-09-13)
- Task: `.agents/tasks/2026-09-13-auto-updater/step01/task-02-logging-and-game-folder-gate.code-task.md`
  (`Generated By: code-task-generator 2026-09-13`); plan + design both
  `Status: Approved 2026-09-13`. Approved chain complete → auto mode proceeds.
- Depends on Task 01 (`Status: Complete (uncommitted)` in its progress.md).
- No `CODEASSIST.md`; authoritative: `AGENTS.md` (no commits, no machine paths).

## Build / test (from repo root)
- `cargo test --manifest-path updater/Cargo.toml`; `(cd updater && cargo fmt)`
- Win7: `(cd updater && cargo xwin build --release --target x86_64-win7-windows-msvc -Z build-std=std,panic_abort)`

## Requirements (task → see file for full text)
1. `gamedir.rs`: `GameDir { root, work }` + accessors download_dir/stage_dir/
   backup_dir/journal_path/manifest_path/log_path; `resolve(Option<&Path>)`
   (override else `current_exe().parent()`, canonicalise, gate on
   `spice64.exe` | `ddr_world_hook.dll`); injectable `exists` probe; `Refusal`
   with one-line message naming the path (+ variant for unknown exe location).
2. `log.rs`: levels INFO/WARN/ERROR; line `[+12.34s] LEVEL  msg` (level padded
   to 5); console-only until `attach_file`; file truncated; console+file lines
   identical; `header(version, argv, game_root)`; process-wide logger behind
   `log_info!/log_warn!/log_error!`; file write errors swallowed after one WARN.
3. `main.rs` wiring: parse → resolve → refusal (console only, exit 0) → attach
   log → header → placeholder (also logged).
4. Transcript: `DDR World Hook updater <ver>`, `Game folder: <path>`; refusal
   line names path + expected anchors.
5. fmt/test/build/xwin clean. 6. No machine paths; no commit.

## Assumptions
- Elapsed prefix pinned as `^\[\+\d+\.\d{2}s\] (INFO |WARN |ERROR) ` (task's
  implementation note) — i.e. `format!("[+{:.2}s] {:<5} {}")`.
- INFO → stdout, WARN/ERROR → stderr (console); file gets all.
- The refusal message goes to stdout (it is informational, exit 0).
- `header` writes at INFO level so it appears in both sinks.
- `--help`/`--version`/usage errors do not resolve the game folder (nothing to
  log; matches "console only" edge behaviour and keeps `--version` output exact).
- e2e tests point `--game-dir` at temp folders; exe-relative resolution is
  covered by a unit test with an injected probe (the test binary's own folder
  is not a game folder).

## Implementation paths
- `updater/src/gamedir.rs`, `updater/src/log.rs` (new), `updater/src/main.rs`,
  `updater/tests/cli_e2e.rs` (extend with gate/log e2e).
