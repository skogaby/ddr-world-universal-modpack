# Context — Step 2 Task 02: manifest + plan
Provenance: task Generated By code-task-generator 2026-09-13; plan/design Approved; Task 01 Complete. Auto mode (maintainer-authorised autonomous Step 2).
Build/test: `cargo test --manifest-path updater/Cargo.toml`.
Requirements: task file. Assumptions: `installed_at_unix` from SystemTime; `updater_version` = CARGO_PKG_VERSION; manifest JSON pretty + trailing newline; `hash_tree` reads files under the stage root; plan ordering writes → prunes/keeps → merged → manifest; the updater exe is skipped (Step 5 adds SelfUpdate); merged files written only when `MergedFiles` carries bytes.
Paths: `updater/src/manifest.rs`, `updater/src/plan.rs`, `Cargo.toml` (+sha2).
