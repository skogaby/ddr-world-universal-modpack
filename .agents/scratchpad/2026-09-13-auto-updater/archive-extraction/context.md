# Context — Step 2 Task 01: archive extraction
Provenance: task `Generated By: code-task-generator 2026-09-13`; plan + design Approved 2026-09-13; Step 1 tasks all Complete. Auto mode (maintainer authorised autonomous Step 2 in conversation, 2026-09-13).
Build/test: `cargo test --manifest-path updater/Cargo.toml`; Win7 xwin recipe from `updater/`.
Requirements: see task file. Assumptions: symlink detection via `unix_mode() & 0o170000 == 0o120000`; total-size cap uses declared `size()` (a lying header cannot exceed it because we count bytes actually written too); stage root emptied with `remove_dir_all` then recreated.
Paths: `updater/src/relpath.rs`, `updater/src/archive.rs`, `updater/Cargo.toml`.
