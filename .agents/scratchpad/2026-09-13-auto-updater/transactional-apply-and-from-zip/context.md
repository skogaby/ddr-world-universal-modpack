# Context — Step 2 Task 03: transactional apply + --from-zip
Provenance: task Generated By code-task-generator 2026-09-13; plan/design Approved; Tasks 01–02 Complete. Auto mode (maintainer-authorised autonomous Step 2).
Build/test: `cargo test --manifest-path updater/Cargo.toml`; Win7 xwin recipe; CrossOver smoke on `$DDR_WORLD_INSTALL` with `--from-zip <release zip>`.
Requirements: task file (journal, backups, execute, rollback, summary, --from-zip/--force wiring, DDR_UPDATER_FAULT dev injection, e2e harness, drop allow(dead_code)).
Assumptions:
- Same-volume renames: stage/backup live under `<game>/.ddr_world_hook_updater/`.
- `Write{existed:false}` whose parent dir is a regular FILE → create_dir_all fails → rollback (the e2e injected failure).
- Fault env `DDR_UPDATER_FAULT`: `apply-after:N` → error after N executed actions; `rollback` → before restoring, delete the backup of the first restorable action so its restore fails (RollbackFailed).
- Journal deleted after manifest write; `download/` + `stage/` removed on success; `backup/` kept.
- MergedFiles in this step: release copy iff user file absent.
- Real-install smoke: the release zip contains the updater exe → skipped by plan (Step 5); the DLL will be overwritten with the zip's copy (same build as the one installed today).
