# Context — Task 03: Build and ship the updater from the release archive script

## Provenance (verified 2026-09-13)
- Task: `.agents/tasks/2026-09-13-auto-updater/step01/task-03-release-script-integration.code-task.md`
  (`Generated By: code-task-generator 2026-09-13`); plan + design `Status: Approved 2026-09-13`.
- Depends on Task 01 (Complete, uncommitted). Task 02 also Complete.
- Authoritative docs: `AGENTS.md` (no commits; no machine paths; the release
  script is the one packaging path).

## Facts about the current script (`scripts/build_release_archive.sh`, 51 lines)
- `set -euo pipefail`; `cd "$REPO_ROOT"`; DLL = `target/x86_64-win7-windows-msvc/release/ddr_world_hook.dll`;
  `cargo xwin build --release --target x86_64-win7-windows-msvc -Z build-std=std,panic_abort`;
  existence check; `STAGE=$(mktemp -d)` + `trap rm`; `cp` DLL + 3 files;
  `rsync -a --exclude '.DS_Store' data_mods "$STAGE/"`; `zip -r -q`; `unzip -l | tail -3`.
- Stale comment (lines 38–41) claims `_cache/`, `*_ifs/`, `*.arc` exclusions.

## Build/test
- `bash -n scripts/build_release_archive.sh`
- Full run: `./scripts/build_release_archive.sh` (rebuilds the DLL — minutes)
- Guard proof: build the updater for the default msvc target
  (`cargo xwin build --release --target x86_64-pc-windows-msvc` in `updater/`) — that
  binary imports `ProcessPrng` — and run the guard function against it.

## Requirements → task file items 1–8.

## Assumptions
- `strings` is available on the maintainer's macOS (binutils-less `strings`
  ships with Xcode CLT) and on Linux; the guard uses it. If `strings` is
  missing the guard FAILS CLOSED (a missing tool must not silently pass).
- The guard greps for the literal `ProcessPrng` (the import name); the DLL's
  Win7 build has been verified free of it since the Win7 recipe was adopted.
