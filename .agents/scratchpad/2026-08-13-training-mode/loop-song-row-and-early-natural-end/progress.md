# Progress: LOOP SONG row + LOOP OFF early natural end

Task: step04/task-02. Mode: auto (verified upstream approval).

## Checklist

- [x] `apply_action` transition-table test written (section_math), failing
- [x] `ApplyAction` + `apply_action` implemented, tests green
- [x] bounds.rs: loop atomics/latch/accessors + apply/stash/restore state
- [x] `try_resolve_row_bounds` latches loop (both digest paths) +
      session-active + apply call
- [x] Gesture arms (B-set, triple-5) drive the apply
- [x] `rows_engaged` treats loop-ON as engaged
- [x] mod.rs row registration (Session, default OFF) + enable/disable
- [x] Textures generated (label + off/on previews)
- [x] Gates: harness → check → fmt

## Log

- Setup complete; baseline 247/247.
- Cycle 1 (pure apply table): `apply_action_transition_table` test added
  to `src/mods/training_mode/section_math.rs` — failed E0425; implemented
  `ApplyAction {Write{b_ms}, Restore, Nothing}` + `apply_action(policy,
  thresholds_written)`. 248/248.
- Cycle 2 (bounds.rs, engine-facing — cabinet-validated via the task-03
  demo per the task text):
  - `ROW_LOOP_SONG` per-side atomics + `set_row_loop_song`/
    `row_loop_song`; `LOOP_LATCHED` + public `loop_latched()` (the
    task-03 driver gate).
  - Apply state: `THRESHOLDS_WRITTEN`, `STASH_VALID`/`STASH_DISPLAY_MS`/
    `STASH_RAW_MS`, `END_APPLY_WARNED` — all cleared by
    `clear_session_state`, which first restores a written stash when the
    scene is still GAMEPLAY (the mid-song mod-disable edge; song-boundary
    clears skip it — the write gates refuse without a live run anyway).
  - `try_resolve_row_bounds`: loop latch taken from the ENTERED side
    BEFORE the digest gate (the loop row is not song-scoped — it must
    survive the stale-rows path), loop-ON sets `SESSION_ACTIVE` + one
    INFO; `apply_end_policy()` called on BOTH completion paths.
  - `apply_end_policy()`: `end_policy(loop_latched, B_MS)` →
    `apply_action` dispatch. Write arm (`write_end_thresholds`):
    stash-once via `chart_end_thresholds` → `decoded_notes` →
    `display_for_raw` → `set_chart_end_thresholds`; every failure ⇒
    `warn_end_apply_once` (one WARN per song), thresholds untouched.
    Restore arm: stash write-back + `THRESHOLDS_WRITTEN` clear (a refused
    restore WARNs once and leaves the truncated end).
  - Gesture arms: `set_marker('B')` tail + the triple-5 restore branch
    call `apply_end_policy()`.
  - GAMEPLAY-entry `rows_engaged` gains `|| row_loop_song(side)` (a loop
    session must arm the driver/resolution even with no bounds set).
- Cycle 3 (mod.rs row): `OPT_LOOP_SONG` +
  `on_loop_song_change`; registered in `register_bound_rows` after the
  bound rows (`bool_toggle`, default 0, `PersistMode::Session`,
  Duplicate-is-success); registry re-seed on the enable path;
  `set_option_available` true/false in enable/disable; disable zeroes the
  atomics. NOT part of the highlight seeder / digest stamp.
- Textures: `scripts/gen_option_labels.py` gained the OPTIONS entry
  ("LOOP SONG") + two WIDE previews (off/on, the preserve_pitch shape);
  script run — exactly 3 new PNGs
  (`seop_item_training_loop_song`, `seop_image_training_loop_song_{off,on}`)
  under `data_mods/custom_options/select_music_option_lang_eng_v3_ifs/tex/`;
  all pre-existing PNGs regenerated byte-identical. **Deploying the 3
  PNGs to the cabinet's `data_mods/` is a maintainer step (flagged in the
  Step-4 demo handoff).**
- Validate: harness 248/248, `cargo check --target
  x86_64-pc-windows-msvc` clean, `cargo fmt` (whole crate).

## Deviations

- None from the task text. The pure apply table (`apply_action`) is the
  "apply-state transition table that factors purely" the task invited;
  its defensive `ArmLoop`+written ⇒ Restore arm is unreachable in
  practice and documented as such.

Status: Complete (uncommitted — the maintainer commits)
