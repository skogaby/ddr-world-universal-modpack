# Progress: score_guard training + assist-tick taint sources (Step 5, task-01)

Updated: 2026-08-14
Status: Complete (uncommitted — the maintainer handles all git; see Deviations)

## Checklist

- [x] Setup: working dir, approval chain verified (task Generated By +
      plan/design Approved), build/test commands recorded in context.md
- [x] Explore: score_guard source model + test conventions + consumer sites
- [x] Plan: test scenarios + implementation approach (plan.md)
- [x] Tests written and failing for the expected reason (E0425 on
      `set_training_taint` / `set_assist_tick_taint` — logs/test-red.log)
- [x] Implementation: statics + setters + predicate + resets + docs
- [x] Harness suite green: **255 passed / 0 failed** (249 pre-existing + 6 new;
      logs/test-green.log)
- [x] cargo check (x86_64-pc-windows-msvc) clean (logs/check.log)
- [x] cargo fmt (whole crate)
- [x] Validate + review pass (diff reviewed; house style matched; no findings)
- [x] Commit deferred to maintainer

## TDD cycles

1. **All six scenarios in one red/green cycle** (they exercise one tiny API
   surface; splitting per-test would have been ceremony):
   - RED: six new tests in `src/services/score_guard_tests.rs` referencing the
     absent setters → compile failure E0425 (the expected failure mode for a
     missing API).
   - GREEN: `TRAINING_TAINT` + `ASSIST_TICK_TAINT` statics, `set_training_taint`
     / `set_assist_tick_taint`, `is_stage_suppressed` OR-extension,
     `reset_song_taint` clears TRAINING (both sides), `reset_session` clears
     both new sources. 255/255.
   - REFACTOR: none needed; doc comments written with the implementation.

## New tests

- `training_taint_suppresses_only_its_side` (AC1 + idempotence)
- `assist_tick_taint_is_level_driven` (AC2)
- `reset_song_taint_clears_training_but_not_assist_tick` (AC3 first half)
- `reset_session_clears_both_new_sources` (AC3 second half)
- `out_of_range_taint_sides_are_no_ops` (reqs 1–3 bounds guards)
- `new_sources_compose_with_existing_ones` (req 3: quick-fail + autoplay
  composition; AC4 rides the untouched pre-existing suite)

All serialize on a new `TAINT_TEST_LOCK` (`Mutex<()>`, poison-tolerant via
`PoisonError::into_inner`) and restore a clean taint baseline — the suite
stays order-independent; the pre-existing rate-ledger global test touches
disjoint state and was left alone (its "only global test" module-doc claim
updated to name both groups).

## Deviations

- **Commit step**: the sop's Step 6 commit is deferred to the maintainer per
  the repo's AGENTS.md Workflow ("The maintainer manages git commit / push
  themselves") and the Step-5 handoff instruction. Changed files staged-ready:
  `src/services/score_guard.rs`, `src/services/score_guard_tests.rs`.
- None in the implementation itself — matches the task text exactly; zero
  behavior change verified by the untouched pre-existing 249 tests.

## Files changed

- `src/services/score_guard.rs` — 2 statics, 2 setters, predicate + both
  resets extended, module doc updated.
- `src/services/score_guard_tests.rs` — 6 tests + shared test lock + module
  doc note.
