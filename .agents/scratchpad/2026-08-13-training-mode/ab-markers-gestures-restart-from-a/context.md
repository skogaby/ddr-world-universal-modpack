# Context: ab-markers-gestures-restart-from-a (Training Mode v1, Step 2, task-04)

## Task provenance (verified)
Task carries `Generated By: code-task-generator 2026-08-13`; plan + design
`Status: Approved 2026-08-13` (verified in task-01's setup, same session). Auto mode.

## Build & test commands
Same as tasks 01–03. Harness baseline **210/210**. Engine-facing (gestures/restart) —
cabinet demo closes the step; host coverage n/a beyond existing suites.

## Requirements (acceptance criteria)
1. Markers set/clear: triple-7 A / triple-9 B / triple-5 clear, latched from the live
   raw music count (clamped), cleared at song change, one INFO each.
2. Restart-from-A: triple-1 with A set ⇒ in-place reset resuming at A after the silent
   approach lead (combo/score/gauge reset, claps aligned) at 100/75/125 % — the demo.
3. Fallback: seek refusal ⇒ shipped restart-at-0 + one bounded WARN.
4. Zero footprint: mod disabled / other scenes ⇒ nothing fires; quick_logout's
   triple-9 at SONG SELECT (scene 25) unaffected (gameplay = scene 28).

## Key facts (explored)
- GestureBuffer precedent: `quick_logout.rs` (per-side `[GestureBuffer; 2]`,
  1500 ms window / 3 presses, scene-gated in the input callback, buffers cleared
  on scene exit and disable).
- Buttons: `button::NUM_5/NUM_7/NUM_9`; `InputEvent { player, button, event_type }`;
  `input_manager::on_input_event(Arc<dyn Fn>)`; scene ids `scene::GAMEPLAY = 28`,
  `scene::SONG_SELECT = 25`.
- Live position: GamePlayActor `+0x178` = raw-ms music count (research §3.1; the
  rewind worker zeroes it) — new `song_reset::current_raw_music_count()` accessor
  (song_reset owns the actor layout + walkers).
- Marker clamp: `song_reset::chart_end_raw(side)` (task-03) minus the same
  1000 ms margin class; markers block-quantize via the seek's own composition
  (wall → `seek::quantize_seek` on `active_content_grid` → content) so the stored
  value equals what a seek to it produces.
- `trigger_restart` (quick_restart_or_fail:1177): clears song taint, then the
  in-place reset with `RESTART_DELAY_MS`; cross-mod pub-fn reads are precedented
  (mine_render → playfield_styling::cull_bound).
- Step-1 skeleton: `training_mode/mod.rs` — standing arm + TEMPORARY
  `DDR_TRAINING_TEST_SHIFT_MS` knob (REMOVED this task; `disable()`'s
  `set_initial_content_mapping_ms(0,0)` clear stays — Step 3 uses the API).

## Interpretation & assumptions (auto mode)
- **Lead composition** (task text "TRAINING_LEAD_MS + the operator's
  restart_delay_ms policy per design"; design §4.3 only fixes TRAINING_LEAD_MS
  as the approach): implemented as `max(TRAINING_LEAD_MS, restart_delay_ms)` —
  the training lead is the MINIMUM approach window; an operator delay
  ≥ 2500 ms keeps their calibrated repositioning time exactly (a sum would
  inflate it). Default delay 0 ⇒ the demo's 2.5 s lead either way. Recorded
  as a deviation note.
- Markers per-song: cleared on gameplay entry AND exit (defensive both ways).
- B marker: stored + one INFO (consumed by Steps 4–6); no other effect.
- A == 0 sentinel = none (design data model); a quantized-to-0 gesture target
  is meaningless (the song start) and treated as none.
- chart_end_raw unavailable at gesture time ⇒ marker stored unclamped (the
  seek's own gates still protect; degraded path per the §6 ladder).
- Score containment deliberately absent until Step 5 (plan-approved): a
  seek-practiced song's score WILL submit — flagged in the demo notes.

## Files to touch
- `src/services/song_reset/mod.rs`: `current_raw_music_count()` accessor.
- `src/mods/training_mode/bounds.rs` (new): marker state + gestures.
- `src/mods/training_mode/mod.rs`: callback registration, knob removal,
  `active_section_start()` re-export.
- `src/mods/quick_restart_or_fail.rs`: restart-from-A consultation + fallback.
