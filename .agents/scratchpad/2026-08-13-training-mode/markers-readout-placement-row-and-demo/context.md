# Context: markers, readout, placement row, backend + Step-6 demo (task-03)

Task file: `.agents/tasks/2026-08-13-training-mode/step06/task-03-markers-readout-placement-row-and-demo.code-task.md`
Mode: auto (approval chain verified in task-01's record — same plan/design;
Generated By: code-task-generator 2026-08-14).
Working dir: `.agents/scratchpad/2026-08-13-training-mode/markers-readout-placement-row-and-demo/`

## Maintainer-approved visual plan (in-session, 2026-08-14)

Bottom-to-top: strip texture (its edges ARE song start/end; reverse flips)
→ **active-section veil** (translucent white over `[A or 0 … B or end]`)
→ A marker (bright green 3px line, overhang) → B marker (bright red 3px)
→ cursor (white 2px, dark outline baked into the marker PNG, ~3px
overhang) → `m:ss / m:ss` readout fixed under the strip.

**Amendment (maintainer)**: the veil shows whenever a PROPER sub-section
is defined — either marker set — INDEPENDENT of LOOP (supersedes the
task text's "while loop_latched, [a, fire bound]"). Verified:
`bounds::active_section_start()`/`section_end()` reflect rows AND
gestures (the resolution stores rows into A_MS/B_MS, bounds.rs:454;
gestures refine); a row END at the natural end normalizes to none — so
`a.is_some() || b.is_some()` is exactly "not playing the whole song".
Veil span end = B or chart_end (no fire-bound involvement).

Whole-song grind (loop ON, no markers): no veil (agreed).

## Key facts from tasks 01/02 (this session)

- Strip is BAR MODE (R7 second amendment); `StripLayout` carries the
  REVERSE flip — all overlay y goes through `layout.y_for_ms` /
  `fraction_for_ms` so reverse scroll follows with zero special cases.
- strip_hud owns a per-frame RENDER PUMP (toast-style, generation-token)
  — the overlay loop lives THERE (no second loop; the task text's "the
  strip's driver may host it" option). The pump is already render-thread
  + O(1).
- Strip geometry: COLUMN_PX 14, STRIP_HEIGHT_PX 620, right edge margin 8,
  y centered (50). Widget rect set at poll_resolve per song.
- `song_reset::current_raw_music_count()` = live cursor time (engine
  read, game-thread — the pump qualifies).
- `format_mss` in strip_synth (pure).

## Requirements distilled

1. Marker/readout loop: ≤4 widget position writes/frame + occasional
   text update; gated on session-active + GAMEPLAY; generation-tokened
   (the pump's token). Mid-song activation works (per-frame poll).
2. Placement row `training_progress_pos` "TIMELINE PLACEMENT" enum
   LEFT(1)/RIGHT(0 default), `PersistMode::Full` (builder default) →
   wire `mod_training_progress_pos`; registered after LOOP SONG;
   registration failure ⇒ default RIGHT, one WARN. Per-side atomics;
   the HUD uses the ENTERED side's value (stage_records::side_entered,
   P1 fallback), LATCHED per song at gameplay entry ("applies at the
   next song without restart").
3. Strip + markers + readout all honor the placement side (mirrored x).
4. Textures via scripts/gen_option_labels.py (label seop_item_… +
   LEFT/RIGHT ribbons; NO seop_op_item_*). Maintainer deploys.
5. bemani-buddy (sibling checkout): migration
   `015_ddr_world_training_progress_pos.sql` (nullable INT, no default —
   the 012–014 convention) + playdata.rs verbatim plumbing
   (`mod_training_progress_pos` ↔ `opt_mod_training_progress_pos`).
   SEPARATE repo — separate commit handling (maintainer commits).
6. Fail-open ladder: strip absent ⇒ markers+readout on a plain track
   (the marker asset stretched, low alpha); marker asset missing ⇒
   readout only; nothing blocks the session.
7. Host tests only for new pure math (layout covers most; veil-span
   normalization if it lands as pure math).
8. Full gates + the Step-6 demo (closes the plan step).

## Marker asset decision (documented per the task text)

A repo-shipped 4×4 PNG `data_mods/training_mode/tex/training_marker.png`:
rows 0/3 dark (40,40,40,200), rows 1/2 white — stretching bakes the
cursor's outline for free; tint via `ImageWidget::set_color` (white
center × tint = tint, dark rows stay dark). Loaded ONCE at activate via
`asset_loader::load` (the mine-texture pipeline; never released —
process-lifetime, the mine loader's model). Chosen over a stock resident
asset: no dependency on which scene's atlases happen to be resident
during gameplay.

## Widget inventory (all created lazily once, reused)

- ImageWidgets: fallback track (stretched marker asset, low alpha),
  veil, A line, B line, cursor. (+ the existing strip widget.)
- TextWidget: readout (white, outline, small scale), text updated only
  when the displayed second changes.

## Implementation paths

- `src/mods/training_mode/strip_hud.rs`: placement latch + overlay
  widgets + pump extension (overlay_update), marker-asset load at
  activate.
- `src/mods/training_mode/mod.rs`: TIMELINE PLACEMENT row registration
  (player_perspective's RegisterSpec::enum_values precedent) + change
  callback → strip_hud setter.
- `scripts/gen_option_labels.py`: label + ribbon entries; run it.
- `data_mods/training_mode/tex/training_marker.png`: generated (hand-
  rolled PNG writer — committed asset, no Pillow dependency).
- bemani-buddy: `migrations/015_ddr_world_training_progress_pos.sql` +
  `crates/game-server/src/handlers/ddr_world/playdata.rs`.

## Ambiguities & resolutions (auto mode)

1. Veil decision: maintainer amendment above (supersedes task text).
2. Placement read cadence: latched per song at GAMEPLAY entry (the task
   text's "each frame" and "next song" conflict resolves to the latter —
   the strip's own x is bind-time; a mid-song placement flip moving only
   the markers would tear the ensemble).
3. Overlay host: strip_hud's existing pump (option sanctioned by the
   task text) — no new module.
4. Readout placement: centered under the strip (y ≈ strip bottom + 8),
   scale ~0.8; final tune at the demo.
