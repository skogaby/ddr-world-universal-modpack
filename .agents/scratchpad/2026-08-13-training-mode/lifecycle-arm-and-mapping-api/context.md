# Context — task-02 lifecycle arm + mapping API

Task file: `.agents/tasks/2026-08-13-training-mode/step01/task-02-lifecycle-arm-and-mapping-api.code-task.md`
Approval chain: same as task-01 (Generated By code-task-generator; plan +
design both `Status: Approved 2026-08-13`) — auto mode proceeds.
Depends on task-01 (complete: identity plan, ServeMode, mapping storage).

## Build / test commands

Same as task-01. Host tests via the temp-dir harness at
`/var/folders/31/yq10yrk557l1q0wyb1nx4vg40000gp/T/opencode/ddr-host-harness`
(`cargo test --quiet`). Baseline after task-01: 189/189.

## Requirements

1. runtime: `set_training_arm(requested: bool)` atomic (read at scene-26 arm)
   + `set_content_mapping(shift, lead) -> bool` (false when no live binding).
2. lifecycle: armed generation may resolve to identity WHEN the training-arm
   request is set; eligibility gates unchanged. Identity arms commit with Q31
   identity and WITHOUT movie suppression.
3. prepare/bind path: identity arms plan via `plan_identity_bank` →
   `Binding::new_identity_passthrough`, NO producer spawn. Bind-time mapping
   plumbed (default {0,0} — driven by Step 3).
4. RateSnapshot semantics unchanged: identity commits read as identity to
   every consumer (`is_non_identity_commit()` == false — the existing
   `committed && percent != 100` predicate already handles it).
5. Fail-open: identity bind refusal ⇒ EarlyFailed exactly like rate binds;
   new `DDR_SONG_RATE_FAULT` leg `identity-bind-refused`.
6. No training request ⇒ bit-for-bit today's behavior (existing tests green).

## Key findings from code reading

- `classify_scene26` (lifecycle.rs): identity-rate early return sits AFTER
  course/entered checks — gate it on `!training_arm` and the full eligibility
  chain (course/versus/stage) applies to identity training arms for free.
- `on_scene26` Arm branch unconditionally `set_movie_suppressed(true)` —
  make it conditional on a non-identity percent (an identity arm emits
  `Movie(false)`, which also clears stale suppression).
- Transaction commit leg (`finish_create`) appends the rate-save ledger +
  session taint + confirm_movie for every commit — must be skipped for
  identity commits (arm is not taint, design 4.1: an armed-but-untouched
  100 % song must submit normally). `publish_committed` with IDENTITY rate
  writes Q31 = identity — numerically unchanged, safe.
- `prepare_binding` percent==100 is unreachable today (identity never arms),
  so the split inside it cannot affect shipped behavior.
- runtime.rs is `#[cfg(windows)]` — the atomics/wrappers there are glue; the
  host-testable seam is `EligibilityInputs.training_arm` +
  `BindingRegistry::set_active_content_mapping`.
- `runtime::on_scene_event` skips session reads when both sides desire 100 —
  must also read them when the training arm is requested.
- EligibilityInputs literal sites needing the new field: lifecycle_tests'
  `eligible_inputs()` (all others use struct-update) + runtime.rs:396.
- wavebank_hook_tests' `Fixture` is the composition harness for the
  end-to-end identity-commit test (taint probe + ledger + movie confirm
  closure + Q31 factor all observable).

## Assumptions / decisions (auto mode)

- Identity arm's movie effect: emit `set_movie_suppressed(false)` (rather
  than no call) — clears stale suppression from a prior LateFailed hold,
  same slot in the effect order as the rate arm's `Movie(true)`.
- `identity-bind-refused` refuses via the existing `BindRefusal::Injected`
  wire code (it IS an injected refusal; the mailbox WARN shows `Injected`).
- Identity path skips the `generator-start` and `mid-song-failure` faults
  (no producer exists); `bind-refused`/`source-read`/`header-synth` still
  apply (shared sites).
- Bind-time mapping parameter: NOT added to `prepare_binding`'s signature —
  the binding is registry-published inside `bind_for_create`; Step 3's
  pre-shift will call `set_content_mapping` after publication (the runtime
  wrapper). "Plumbed" = the runtime API exists and works at bind time.
