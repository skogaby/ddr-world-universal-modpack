# Task: Re-sync the playdata_3 protocol model with the generated code

## Description
Restore `models/ddr_world/playdata_3.json` (in the sibling bemani-buddy
repository, `../bemani-buddy` relative to this repository root) as the single
source of the generated protocol code. Two fields were hand-added to the
generated `crates/bemani-protocol/src/ddr_world/playdata_3.rs` by earlier
commits without updating the model; add them to the model, regenerate, and
prove the regeneration is a no-op.

## Background
bemani-buddy generates its e-amusement protocol structs from JSON models
(`cargo run -p codegen -- <input> <output-dir>`). Commits `6e426cc`
(`mod_skip_results_fast_exit`) and `e835036` (`mod_sync_movie`) edited the
generated file directly. A dry-run regeneration from the current model differs
from the committed file in exactly: those two fields in `PlayerdataLoadOption`
(appended after `mod_judge_offsets`) and `PlayerdataSaveOption` (inserted after
`mod_judge_offsets`, before `mod_customize_appeal_board`), plus one
rustfmt-only line (`pub struct MusicdataLoadRequest {}`). The later plan step
that adds `smarv_scores` to the load response must go through the model, so the
model has to be authoritative first.

## Reference Documentation
**Required:**
- Design: `.agents/planning/2026-09-12-s-marvelous-server-upload/design/detailed-design.md` (§4.6, FR13)

**Additional References (if relevant to this task):**
- `.agents/planning/2026-09-12-s-marvelous-server-upload/research/orientation.md` §2 (bemani-buddy survey — codegen trap)

**Note:** Read any document listed above before beginning implementation.

## Technical Requirements
1. All edits are in the bemani-buddy repository; run every command with the
   working directory set to it.
2. `PlayerdataLoadOption` in the model gains `"mod_skip_results_fast_exit": "s32?"`
   then `"mod_sync_movie": "s32?"` as its last two keys.
3. `PlayerdataSaveOption` gains the same two keys immediately after
   `"mod_judge_offsets"` (before `"mod_customize_appeal_board"`).
4. Regenerate with `cargo run -p codegen -- models/ddr_world/playdata_3.json crates/bemani-protocol/src/ddr_world/`
   and run `cargo fmt`.
5. The generated file must have no field-level difference from the committed
   version; the only acceptable diff is the rustfmt normalisation of
   `MusicdataLoadRequest {}`.
6. Do not hand-edit any generated `.rs` file.
7. Do not commit (maintainer commits manually).

## Dependencies
- None (first step of the plan).

## Implementation Approach
1. Edit the two shapes in `models/ddr_world/playdata_3.json` (inferred: the
   `#NAME`-tagged objects inside the `playerdata_load` output shape and the
   `playerdata_save` input shape).
2. Regenerate, `cargo fmt`, and inspect `git diff -- crates/bemani-protocol/src/ddr_world/playdata_3.rs`.
3. If anything besides the formatting line differs, fix the model or the
   generator — never the output.
4. `cargo clippy --workspace --all-targets` and `cargo test -p game-server`.

## Acceptance Criteria

1. **Model carries the two fields in both shapes**
   - Given the edited `models/ddr_world/playdata_3.json`
   - When the `PlayerdataLoadOption` and `PlayerdataSaveOption` shapes are inspected
   - Then both contain `mod_skip_results_fast_exit` and `mod_sync_movie` typed `s32?`, in the positions matching the committed struct order

2. **Regeneration is a no-op**
   - Given the edited model
   - When codegen runs into `crates/bemani-protocol/src/ddr_world/` followed by `cargo fmt`
   - Then `git diff --stat -- crates/bemani-protocol/src/ddr_world/playdata_3.rs` shows at most the `MusicdataLoadRequest {}` formatting normalisation and no added/removed fields

3. **Workspace stays clean**
   - Given the regenerated code
   - When `cargo clippy --workspace --all-targets` and the existing tests run
   - Then both pass with no new warnings

## Metadata
- **Complexity**: Low
- **Labels**: bemani-buddy, codegen, protocol-model, hygiene
- **Required Skills**: Rust workspace tooling, JSON editing
- **Generated By**: code-task-generator 2026-09-12
- **Source Plan**: `.agents/planning/2026-09-12-s-marvelous-server-upload/implementation/plan.md`
- **Plan Step**: Step 1: bemani-buddy protocol model re-sync (codegen diff empty)
