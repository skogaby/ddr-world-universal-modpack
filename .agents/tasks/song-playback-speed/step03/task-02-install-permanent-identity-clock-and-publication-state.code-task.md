# Task: Install Permanent Identity Clock and Publication State

## Description
Install the central gameplay-clock redirect permanently at exact identity and implement coherent lock-free publication/reset state. This proves the patch and state machinery without permitting any non-100% activation.

## Background
Task 1 provides verified sites and checked patch transactions. The clock stub must replay the overwritten instructions, preserve live state, and use the exact identity Q31 multiplier until a later diagnostic step is explicitly allowed to commit a transformed bank.

## Reference Documentation
**Required:**
- Design: `.agents/planning/2026-08-05-song-playback-speed/design/detailed-design.md`
- Runtime research: `.agents/planning/2026-08-05-song-playback-speed/research/runtime-integration.md`

**Additional References:**
- `src/core/xact/rate.rs`
- `src/core/memory.rs`
- `src/services/song_rate/`

## Technical Requirements
1. Add the permanent clock stub under `src/services/song_rate/`, replaying both overwritten instructions and returning to the following `MOVZX`.
2. Scale the complete signed `music_count` using an aligned Q31 slot, symmetric signed rounding, and final i32 saturation; initialize and retain exact identity `1 << 31` throughout Step 3.
3. Preserve every live register required by verified disassembly and use only proven-dead scratch state or explicit saves.
4. Implement a pure Rust reference model and byte/disassembly validation for emitted stub instructions, jump range, slot alignment, and return address.
5. Implement the seqlock-published `RateSnapshot`, writer acquisition protocol, identity-first reset, and deferred-reset flag needed for later XACT completion.
6. Stress commit/reset overlap and prove readers never observe torn generations or a stale committed flag paired with new fields.
7. Expose readiness only after stub population/flush, target patch/readback, and identity-state validation all succeed; rollback and remain unavailable otherwise.
8. Do not expose a public path that writes non-identity Q31 or `committed=true` in this task.
9. Update canonical progress with Task 3 as the next action after all gates pass.

## Dependencies
- Step 3 Task 1 verified clock site and patch transactions.
- Step 1 `RateRatio`/Q31 arithmetic as the authoritative reference.
- No dependency on generated XWBs, LayeredFS redirects, score policy, or UI.

## Implementation Approach
1. Write reference-model and state-publication tests first.
2. Build and validate the position-independent clock stub.
3. Install through the checked transaction and publish identity readiness last.
4. Add overlap/fault tests and run repository gates.

## Acceptance Criteria

1. **Identity Stub Equivalence**
   - Given negative lead-in, zero, ordinary, and i32-boundary `music_count` values
   - When the Rust reference and emitted identity stub execute
   - Then both return the original signed value exactly and preserve required registers

2. **Permanent Checked Installation**
   - Given the verified redirect site and an in-range stub allocation
   - When installation succeeds
   - Then the eight-byte site redirects once, readback/flush pass, the Q31 slot is identity, and readiness becomes true

3. **Fail-Closed Rollback**
   - Given any injected stub/patch transaction failure
   - When initialization runs
   - Then original game bytes remain or are restored, readiness stays false, and no partial clock state is published

4. **Coherent Publication and Reset**
   - Given concurrent snapshot readers and identity commit/reset writers
   - When operations overlap under stress
   - Then readers observe only complete snapshots and a definitive reset cannot be followed by a late stale write

5. **Identity-Only Enforcement**
   - Given any Step 3 caller input
   - When publication APIs are invoked
   - Then externally observable Q31 remains identity and no non-100% committed snapshot can be created

## Metadata
- **Complexity**: High
- **Labels**: rust, x86-64, q31, seqlock, clock-patch, step-3
- **Required Skills**: code-assist, sdd-reverse-engineer, verification, self-documenting-code
- **Generated By**: code-task-generator 2026-08-06
- **Source Plan**: `.agents/planning/2026-08-05-song-playback-speed/implementation/plan.md`
- **Plan Step**: Step 3: Install identity-only runtime transaction infrastructure
