# Task: Verify Runtime Signatures and Code-Patch Transactions

## Description
Establish the verified address and patching foundation required by the identity-only runtime transaction. Cross-version signature evidence and rollback-capable memory helpers must fail closed before any permanent clock patch or audio detour can publish readiness.

## Background
Step 2 is complete and remains host-only. The approved Step 3 design requires one central-clock redirect site, `wavebank_create`, and wave-bank-unload target across four supported game binaries, plus stronger patch transactions than the current unchecked memory helpers.

## Reference Documentation
**Required:**
- Design: `.agents/planning/2026-08-05-song-playback-speed/design/detailed-design.md`
- Runtime research: `.agents/planning/2026-08-05-song-playback-speed/research/runtime-integration.md`

**Additional References:**
- `src/core/signatures.rs`
- `src/core/memory.rs`
- `src/core/hooks.rs`
- `docs/song_playback_speed.md`

## Technical Requirements
1. Add structural signatures/derivations for the central clock anchor, `wavebank_create`, and wave-bank-unload target, requiring exactly one verified match and bounded derivation.
2. Cross-verify each target on the 2026-03-24, 2026-04-21, 2026-06-16, and 2026-07-21 binaries; record repository-relative evidence without committing binaries or game-derived dumps.
3. Validate the clock redirect window against literal bytes `44 8D 34 18 4C 8D 67 58` and validate complete function-entry/ABI evidence for both audio targets.
4. Add checked memory-patch transactions covering expected-byte comparison, protection changes, near-allocation/jump range, write, instruction-cache flush, readback, protection restoration, and rollback after every injectable failure.
5. Return typed failures and leave target bytes/readiness unchanged after any failed transaction; never panic or partially publish a patch.
6. Keep all new signature and memory APIs inert until later tasks explicitly install the identity clock/audio hooks.
7. Add synthetic memory/fault tests and cross-platform compilation seams without dereferencing game addresses on the host.
8. Update canonical progress with Task 2 as the next action only after focused tests and repository gates pass.

## Dependencies
- Completed Step 1 exact-rate arithmetic and Step 2 host cache/worker.
- Access to the four supported binaries for read-only signature verification.
- No dependency on LayeredFS, XACT runtime calls, UI, or score behavior.

## Implementation Approach
1. Model verified derived sites and typed patch-transaction outcomes.
2. Add signatures and cross-version verification evidence.
3. Implement injected memory/protection/flush seams and rollback tests before production Win32 calls.
4. Run the song-rate validator and normal repository gates.

## Acceptance Criteria

1. **Cross-Version Target Verification**
   - Given each of the four supported binaries
   - When the three signatures and derivations run
   - Then each target resolves exactly once with the expected bytes, bounds, and ABI evidence

2. **Transactional Patch Success**
   - Given a synthetic writable target and in-range near allocation
   - When a checked patch transaction completes
   - Then write, flush, readback, and protection restoration all succeed and the requested bytes remain installed

3. **Failure Rollback**
   - Given injected protection, allocation, range, write, flush, readback, or restoration failure
   - When the patch transaction runs
   - Then it returns a typed error, restores original bytes/protection where modification began, and publishes no readiness

4. **Identity-Only Scope**
   - Given the completed foundations
   - When the DLL initializes without later Step 3 tasks
   - Then no new clock redirect or audio hook is installed and ordinary behavior is unchanged

5. **Build Readiness**
   - Given the verified signature/patch layer
   - When focused tests, `./scripts/validate_song_playback_speed.sh`, `cargo check --target x86_64-pc-windows-msvc`, `cargo fmt`, and `./build.sh` run
   - Then all pass and canonical progress identifies Step 3 Task 2

## Metadata
- **Complexity**: High
- **Labels**: rust, reverse-engineering, signatures, code-patching, rollback, step-3
- **Required Skills**: code-assist, sdd-reverse-engineer, verification, self-documenting-code
- **Generated By**: code-task-generator 2026-08-06
- **Source Plan**: `.agents/planning/2026-08-05-song-playback-speed/implementation/plan.md`
- **Plan Step**: Step 3: Install identity-only runtime transaction infrastructure
