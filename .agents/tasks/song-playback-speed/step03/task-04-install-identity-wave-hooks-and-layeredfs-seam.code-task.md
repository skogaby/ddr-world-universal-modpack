# Task: Install Identity Wave Hooks and LayeredFS Seam

## Description
Install the exactly-once wave-bank create/unload detours and make LayeredFS readiness transactional while preserving stock path behavior. The new conversion seam must remain inert: no generated song-rate path or token can be exposed in Step 3.

## Background
Tasks 1-3 provide verified targets, identity clock state, and bounded transaction primitives. This task connects them to the runtime call chain without enabling non-100% arming or changing ordinary static LayeredFS replacements.

## Reference Documentation
**Required:**
- Design: `.agents/planning/2026-08-05-song-playback-speed/design/detailed-design.md`
- Runtime research: `.agents/planning/2026-08-05-song-playback-speed/research/runtime-integration.md`

**Additional References:**
- `src/services/avs_layeredfs/file_hooks.rs`
- `src/services/avs_layeredfs/mod.rs`
- `src/core/hooks.rs`
- `src/services/song_rate/` from Step 3 Tasks 1-3

## Technical Requirements
1. Install the sole `wavebank_create(i32) -> u8` and verified wave-bank-unload detours through `install_enabled`, with one owner per target.
2. Ensure each detour calls its original exactly once under normal operation, nested calls, injected pre/post failures, panic containment, token mismatch, and identity-only calls.
3. Create/pop call-nonced TLS frames around `wavebank_create`; stock/static LayeredFS success or failure without an exact generated token must preserve the original result and identity state.
4. Restrict post-original audio work to atomic slot transitions and bounded maintenance enqueue; do not allocate, lock, perform file I/O, serialize JSON, or call game APIs beyond the original.
5. Drain unload/release maintenance outside the audio detour and release Step 2 leases only after original unload completion.
6. Make the LayeredFS hook set transactional: any partial install failure disables hooks installed in that attempt, clears broad availability, and reports strict conversion/source-read readiness separately.
7. Add the qualifying dance-XWB conversion seam after effective static-source resolution, but always return ordinary stock/static replacement behavior while non-100% arming is unavailable.
8. Preserve ARC/XML/texture/AFP and existing direct replacement behavior byte-for-byte at semantic identity.
9. Integrate song-rate service initialization in load-bearing order and expose readiness only when clock, slot, wave-hook, unload, and LayeredFS prerequisites are all complete.
10. Update canonical progress with Task 5 as the next action after host/build gates pass.

## Dependencies
- Step 3 Tasks 1-3.
- Existing LayeredFS five-hook ownership and static replacement behavior.
- No scene-26 arming, generated XWB exposure, score policy, or player UI.

## Implementation Approach
1. Add exactly-once detour tests using injected original functions.
2. Refactor LayeredFS installation into rollback-capable staged ownership.
3. Add inert conversion/source-read readiness APIs and service initialization.
4. Run host regression/fault tests and repository gates before live validation.

## Acceptance Criteria

1. **Exactly-Once Create and Unload**
   - Given normal, nested, failing, and panic-injected audio calls
   - When each detour executes
   - Then its original is called exactly once, returned stock result is preserved at identity, and TLS/slots are cleared or conservatively retained

2. **Inert Conversion Seam**
   - Given stock and static LayeredFS dance XWBs plus unrelated file types
   - When open/lstat/convert-path flows run
   - Then existing replacements are unchanged, no dynamic song-rate path/token appears, and the clock remains identity

3. **Transactional LayeredFS Installation**
   - Given failure at every hook-install position
   - When LayeredFS initializes
   - Then all hooks installed by that attempt are disabled, readiness remains false, and a complete attempt publishes conversion/source-read readiness only at the end

4. **Hot-Path Discipline**
   - Given allocation, lock, I/O, and maintenance probes
   - When create/unload detours run
   - Then only original calls, atomic/TLS operations, and bounded enqueue occur on the hot path

5. **Integrated Identity Readiness**
   - Given all prerequisites resolved versus any missing prerequisite
   - When song-rate service initialization completes
   - Then full identity readiness is true only for the complete set and non-100% arming remains unavailable in every case

## Metadata
- **Complexity**: High
- **Labels**: rust, hooks, xact, layeredfs, transactions, identity, step-3
- **Required Skills**: code-assist, sdd-reverse-engineer, verification, self-documenting-code
- **Generated By**: code-task-generator 2026-08-06
- **Source Plan**: `.agents/planning/2026-08-05-song-playback-speed/implementation/plan.md`
- **Plan Step**: Step 3: Install identity-only runtime transaction infrastructure
