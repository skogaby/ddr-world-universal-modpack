# Task: Restructure fast_bootup into a module directory

## Description
Convert `src/mods/fast_bootup.rs` into `src/mods/fast_bootup/mod.rs` with zero
behavior change, creating the directory that Step 1's pure layers (cache.rs,
replay.rs, plan.rs) and later steps' modules land in.

## Background
The ultrafast-boot feature refactors the existing `fast-bootup` mod (same id,
same toggle). Per repo convention (AGENTS.md: "Mods that outgrow a single file
get a subdirectory"), the single 355-line file becomes a directory. The
existing file carries two load-bearing crash fixes (buffer-settled chunk walk;
done-flag + cursor-bounds gates) whose code and doc comments must survive
byte-for-byte.

## Reference Documentation
**Required:**
- Design: .agents/planning/2026-08-24-ultrafast-boot/design/detailed-design.md (§Components and Interfaces → 2)

**Note:** Read the design section before beginning implementation.

## Technical Requirements
1. `git mv src/mods/fast_bootup.rs src/mods/fast_bootup/mod.rs` (preserve history)
2. No code changes beyond what the move itself requires (module resolution is
   path-implicit in Rust; expect zero edits)
3. `src/mods/mod.rs` declaration unchanged (`pub mod fast_bootup;` resolves either layout)

## Dependencies
- None (first task of the feature)

## Implementation Approach
1. Move the file; run `cargo check --target x86_64-pc-windows-msvc`
2. Verify the public surface (`FastBootupMod`) still resolves from `src/lib.rs`

## Acceptance Criteria

1. **Zero behavior change**
   - Given the moved file
   - When `cargo check --target x86_64-pc-windows-msvc` and `./build.sh` run
   - Then both pass with no source diffs other than the file move

## Metadata
- **Complexity**: Low
- **Labels**: refactor, fast-bootup
- **Required Skills**: Rust module system
- **Generated By**: code-task-generator 2026-08-24
- **Source Plan**: .agents/planning/2026-08-24-ultrafast-boot/implementation/plan.md
- **Plan Step**: Step 1: Module restructure + pure layers (cache format, replay arithmetic, boot plan)
