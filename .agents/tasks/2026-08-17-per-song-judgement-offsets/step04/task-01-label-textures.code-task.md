# Task: Option label strings and texture generation

## Description
Add the localized label strings and preview-panel specs for the two new
option rows (`adjust_song_offset` parent bool, `current_song_offset` child
scalar) to `scripts/option_strings.py`, and regenerate the option label
textures for all three languages.

## Reference Documentation
**Required:**
- Design: .agents/planning/2026-08-17-per-song-judgement-offsets/design/detailed-design.md
  (section: Components → Labels and textures)

## Technical Requirements
1. `LABELS` entries (en/ja/ko):
   - `adjust_song_offset` — "ADJUST OFFSET FOR CURRENT SONG"
   - `current_song_offset` — "CURRENT SONG OFFSET (MS)"
2. `PREVIEWS` entries: parent `off`/`on` panels, child single (`None`) panel,
   WIDE, wording explaining: overrides the stock JUDGEMENT OFFSET for the
   highlighted song only; saved per song; 0 is a valid override; OFF means
   the stock value applies.
3. Run `scripts/gen_option_labels.py`; expect 5 new PNGs in each of the three
   `data_mods/custom_options/select_music_option_lang_*_ifs/tex/` dirs.
4. Optionally run `scripts/check_option_takeover.py` if a reference dir is
   available; otherwise skip (regression harness is optional per AGENTS.md).

## Acceptance Criteria
1. **Generation** — Given the updated strings — When the generator runs —
   Then `seop_item_{adjust_song_offset,current_song_offset}.png`,
   `seop_image_adjust_song_offset_{off,on}.png`, and
   `seop_image_current_song_offset.png` exist in all three language dirs.

## Metadata
- **Complexity**: Low
- **Labels**: python, textures, localization
- **Required Skills**: Python
- **Generated By**: code-task-generator 2026-08-18
- **Source Plan**: .agents/planning/2026-08-17-per-song-judgement-offsets/implementation/plan.md
- **Plan Step**: Step 4
