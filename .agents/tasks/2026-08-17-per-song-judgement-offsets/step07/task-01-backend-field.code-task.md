# Task: bemani-buddy mod_judge_offsets backend field

## Description
Store and echo the `mod_judge_offsets` string on DDR World profiles in the
bemani-buddy server (separate repository, sibling checkout `bemani-buddy`).
Standard add-a-field change set with one deviation: the column is TEXT.

## Reference Documentation
**Required:**
- Design (this repo): .agents/planning/2026-08-17-per-song-judgement-offsets/design/detailed-design.md
  (Components → Backend: bemani-buddy)

## Technical Requirements
All paths relative to the bemani-buddy repository root:
1. `migrations/016_ddr_world_judge_offsets.sql`:
   `ALTER TABLE ddr_world_profiles ADD COLUMN opt_mod_judge_offsets TEXT NULL DEFAULT NULL;`
   with the standard header comment block (stored verbatim; nullable, no
   default, omitted-when-NULL — copy the 013/014 wording, noting TEXT).
2. `crates/db/src/models/ddr_world/profile.rs`: `pub opt_mod_judge_offsets: Option<String>`.
3. `crates/db/src/mysql/ddr_world/profile.rs`: `row_to_profile!` entry,
   UPDATE SET list, bind list; regenerate `.sqlx` offline metadata
   (`cargo sqlx prepare` per the repo's workflow — check its README/justfile).
4. `models/ddr_world/playdata_3.json`: `"mod_judge_offsets": "str?"` in BOTH
   the load-option shape and the save-request shape; **also backfill the
   missing `mod_training_progress_pos": "s32?"` field** (known desync with
   the generated .rs).
5. `crates/bemani-protocol/src/ddr_world/playdata_3.rs`:
   `PlayerdataLoadOption` field with `#[serde(skip_serializing_if = "Option::is_none")]`.
6. `crates/game-server/src/handlers/ddr_world/playdata.rs`:
   - save parse in the `mod_*` block:
     `if let Some(v) = option.child("mod_judge_offsets").and_then(|e| e.text()) { profile.opt_mod_judge_offsets = Some(v); }`
     (string analog of `child_i32`; empty string is a VALID value — stored);
   - load emit line; `None` in `build_new_player_response`;
   - tests mirroring the `mod_song_speed` family + empty-string round-trip.
7. `cargo test` green in the bemani-buddy workspace; `cargo fmt`.

## Acceptance Criteria
1. **Present-is-parsed / absent-is-none / none-omitted-on-load /
   empty-string-stored** — unit tests as in requirement 6.
2. **Verbatim storage** — no validation/decoding of the string server-side.

## Metadata
- **Complexity**: Medium
- **Labels**: rust, backend, migration, protocol
- **Required Skills**: Rust, sqlx, bemani-buddy conventions
- **Generated By**: code-task-generator 2026-08-18
- **Source Plan**: .agents/planning/2026-08-17-per-song-judgement-offsets/implementation/plan.md
- **Plan Step**: Step 7
