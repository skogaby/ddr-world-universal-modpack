# Task: Cell-level merge for `judgement_offsets.csv`

## Description
Add `merge/csv.rs`: `merge_csv(user: &mut CsvDoc, release: &CsvDoc) -> CsvReport`
on top of the DLL's own pure CSV layer, mounted into the updater crate with
`#[path = "../../src/mods/per_song_judgement_offsets/csv.rs"]` so grammar
parity holds by construction (the module is documented dependency-free and is
already host-tested through a `#[path]` mount by `scripts/validate_judgement_offsets.sh`).

## Background
Design §4.8 / R16–R18: fill BLANK user cells from the release, never overwrite a
non-blank cell, append rows for codes the user lacks (release order), rewrite
only when something changed; output = the DLL's `serialize` (header, LF,
trailing newline).

## Reference Documentation
**Required:**
- Design: `.agents/planning/2026-09-13-auto-updater/design/detailed-design.md` (§4.8, §2.5)
- `src/mods/per_song_judgement_offsets/csv.rs` (the shared grammar — read it; do not modify it)

## Technical Requirements
1. `updater/src/merge/csv_grammar.rs` = `#[path]` re-export of the DLL module (`pub use` of `CsvDoc`, `CsvRow`, `ParseStats`, `parse`, `serialize`, `HEADER`). The DLL file must keep compiling without any crate dependency — if it ever gains one, this mount breaks the updater build loudly (desired).
2. `merge/csv.rs`: `pub struct CsvReport { pub cells_filled: usize, pub rows_appended: usize }`; `merge_csv` per §4.8 using `CsvDoc::get` / `upsert` only; `changed()` = sum > 0.
3. Tests: blank filled; non-blank kept; per-side independence; missing rows appended in release order (incl. blank release rows); user row order preserved; no-change report zero; the repo's `judgement_offsets.csv` merged with itself → zero report and `serialize` byte-identical to `serialize(parse(file))`; grammar fixtures from the DLL module run as part of the mount.

## Dependencies
- Step 2 complete (independent of Tasks 01–02).

## Acceptance Criteria
1. **Fill only blanks** — Given user `a,,5` and release `a,3,9` — When merged — Then user row is `a,3,5`, report 1 cell.
2. **Append missing** — Given release rows not in user — When merged — Then they follow the user's rows in release order.
3. **Self-merge no-op** — Given the committed CSV — When merged with itself — Then `cells_filled + rows_appended == 0`.

## Metadata
- **Complexity**: Low
- **Labels**: updater, rust, merge, csv, step-3
- **Required Skills**: Rust, `#[path]` module mounts
- **Generated By**: code-task-generator 2026-09-13
- **Source Plan**: `.agents/planning/2026-09-13-auto-updater/implementation/plan.md`
- **Plan Step**: Step 3: Configuration merges — `mod-config.json`, `option_menu_settings`, `judgement_offsets.csv`
