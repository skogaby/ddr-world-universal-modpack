# Task: `tools/bot_sim` offline simulator + `scripts/validate_multiplayer_bot.sh`

> Superseded in scope 2026-09-13 (maintainer-approved in conversation): the temp-crate
> harness became a persistent native crate `tools/bot_sim/` that mounts the DLL's pure
> modules, simulates the bot over a real SSQ corpus through a model of the judge / freeze
> judge / `judge_submit` / NORMAL gauge (RE'd for this task:
> `docs/gauge_and_judge_scoring_research.md`), and writes a self-contained HTML report with
> per-level overviews, heatmaps and DDR-styled scorecards. `scripts/validate_multiplayer_bot.sh`
> is a thin `cargo test` over that crate; `scripts/bot_sim.sh` runs the simulator.
> Implemented as part of Step 2 — see `implementation/plan.md` Step 2 (as built) and the
> design §7.1. Status record: `.agents/scratchpad/2026-09-13-multiplayer-bot/pure-cores/progress.md`.

## Metadata
- **Generated By**: code-task-generator 2026-09-13
- **Source Plan**: `.agents/planning/2026-09-13-multiplayer-bot/implementation/plan.md`
- **Plan Step**: Step 2: Pure cores — `eligibility`, `skill`, `planner` + `tools/bot_sim` offline simulator
