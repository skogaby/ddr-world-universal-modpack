# Task: Theme gameplay player name (A3's BmpString in the difficulty frame)

## Description
Draw the dancer name in each theme difficulty frame the way A3's ScoreActor init did (design R9,
§4.11, §6.2): an `agcs::BmpString` in font 6 (`2d_font_player`), yellow, A3's scale, bound to the
frame's `name_usr` placeholder with A3's `FUN_180100480(…, 1, 3, 1.0)` maths. RE first (design
Appendix C row 5), then the code.

## Background
A3 (`FUN_180055390`) creates the string at ScoreActor init, gated on `FUN_18014f310(6)` (font 6
loaded), pushes it onto the screen graph's slot-8 render list (`*(DAT_1802eee58) + 0xC8`) with
node sort key `wrapper+0xC = 0x7FFFFFFB`, sets scale x = 0.8·(1.16 | 1.6 by `PlayerWork+1`),
y = 0.8·0.8 (SD: 0.576 base), colour `0xFFFFEB08`, binds once and hides the placeholder. The
placeholder is already hidden by `score.rs` (Step 2).

## Reference Documentation
**Required:**
- Design: .agents/planning/2026-09-25-ddr-selection-a-a3-themes/design/detailed-design.md (§4.11, §6.2, Appendix A.4, C row 5)
- .agents/planning/2026-09-25-ddr-selection-a-a3-themes/research/name-and-score-sets.md (§1, §5.2)

**Additional References (if relevant to this task):**
- src/services/widget_renderer.rs, src/widgets/text_widget.rs

**Note:** Read any document listed above before beginning implementation.

## Technical Requirements
1. RE (recorded in `score_name.rs`'s `//!` doc): World's counterpart of the slot-8 list and key,
   the font-6 getter and residency, World's equivalent of A3's `PlayerWork+1`.
2. Signatures (additions only) for what the RE pins, swept on all five builds.
3. `widget_renderer::create_text_widget_with_font(font, WidgetStyle, RenderList)`;
   `create_text_widget()` unchanged. `TextWidget` setters for the box (`desc+0x68/+0x6C`), the
   vertical alignment (`desc+0xAC`) and the box flag (`desc+0xB4`).
4. `score_name_logic.rs` (pure): the text rule, A3's binding maths, the style constants.
5. `score_name.rs`: two lazily created, reused widgets (render thread), a per-frame re-bind while
   the difficulty layer lives, hidden at the GAMEPLAY exit, disarm and disable; font gate.
6. `score.rs`: the theme init POST hands the placeholder to `score_name`.
7. Readiness gate incl. the signature sweep and `shape_diff.py`.

## Dependencies
- Plan Steps 2 (theme score adapter) and 5 (World's name rule in `score_set_logic`).

## Implementation Approach
1. RE in Ghidra (A3 `gamemdx_20240402`, World `20260825`, byte checks on all five builds).
2. Failing tests in `score_name_logic.rs`; then the pure core, the service change, the engine.
3. Build, harness, sweep, record the cabinet procedure.

## Acceptance Criteria

1. **Name**
   - Given a theme song with a profile player
   - When gameplay starts
   - Then the yellow dancer name shows centred in the difficulty frame, both sides in versus

2. **Guest**
   - Given a guest
   - When gameplay starts
   - Then `PLAYER1` / `PLAYER2` shows (narrower, A3's guest scale)

3. **Layering**
   - Given the stage panel or the end shutter
   - When they cover the HUD
   - Then the name is covered too

4. **Scope**
   - Given an era or stock song, or any scene after GAMEPLAY
   - When it plays
   - Then no name widget shows; 20+ songs create no new render-list nodes

## Metadata
- **Complexity**: High
- **Labels**: ddr-selection, hud, text-widget
- **Required Skills**: Rust, Ghidra, in-process hooking conventions of this repo
- **Generated By**: code-task-generator 2026-09-26 (breakdown pre-approved: maintainer asked for autonomous execution between cabinet tests)
- **Source Plan**: .agents/planning/2026-09-25-ddr-selection-a-a3-themes/implementation/plan.md
- **Plan Step**: Step 6: Gameplay player name (RE first)
