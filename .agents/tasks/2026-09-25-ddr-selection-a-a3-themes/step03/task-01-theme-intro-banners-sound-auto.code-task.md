# Task: Theme READY, end banners, announcer / crowd and AUTO

## Description
Complete a theme song outside the stage panel (design R3, R7, R11, R12): A3's READY from
`dance_message_vN`, the theme's CLEARED / FAILED / PRAY FOR ALL banners from `common_shutter_vN`,
A3's skin-0 announcer and crowd, the theme stage call rule (used by plan Step 4), and AUTO reaching
DDR A and A3 (Gold on a gold cabinet, White otherwise) through a new shared cabinet query.

## Background
Design §4.1, §4.2, §4.6–§4.8. Verified in this task's research:
- `dance_message_v{0,1,2}`: `00_ready` / `00_here` with the era packages' labels, `00_ready` embeds
  `vo_ingame_ready`.
- `common_shutter_v{0,1,2}`: `shutter_clear` / `shutter_failed` / `00_cleared` / `00_failed` /
  `00_prayforall` with the era packages' labels; embedded cues all in the `dsel` manifest except
  `ACE_TEPPAN3` (DDR A's `shutter_failed`; present in `se_normal_n.xsb`) — added to the manifest.
- A3 `CallVoiceActor::onUpdate` (`FUN_1800369e0`) skin-0 crowd, when due:
  `g > 0.7` (`DAT_1802888ac` = 0x3F333333) → `STG_APP02` + guarded `vo_ingame_cheer`;
  else `g ≤ 0.4 ∧ c < 13` → nothing when `g ≥ 0.2`, else `STG_BOO` + guarded `vo_ingame_boo`;
  else `STG_APP03` + guarded `vo_ingame_cheer`. The guarded voices share the state voice's handle
  (`+0x84`) and are skipped while `combo % 100 ≥ 91`. Combo callouts and state voices are the
  skins 4–5 rules.

## Reference Documentation
**Required:**
- Design: .agents/planning/2026-09-25-ddr-selection-a-a3-themes/design/detailed-design.md

**Additional References (if relevant to this task):**
- .agents/planning/2026-09-22-ddr-selection/research/sounds-options-folder.md §A.1 (announcer table)

**Note:** Read any document listed above before beginning implementation.

## Technical Requirements
1. `policy.rs`: theme row `dance_message` (`ThemeArc::Message`, ReadyGo).
2. `banner_logic.rs`: `art` / `package` for 1..=`SKIN_MAX`; `has_pray_for_all` = skin 4 or a theme;
   a single static NUL-terminated package table the engine uses; `banner.rs` on it.
3. `sound/rules.rs`: `step` for 1..=`SKIN_MAX`; the skin-0 crowd arm for themes; `plays` grows to 4
   slots (combo, state voice, crowd SE, crowd voice); `all_cues` gains `STG_BOO`, `vo_ingame_cheer`,
   `vo_ingame_boo`. `sound/cues.rs` gains `ACE_TEPPAN3`.
4. `panel_logic::stage_voice`: themes take the `vo_stage_*` branch.
5. `src/services/cabinet.rs` (new): `machine_type()` / `is_gold_cabinet()` over the ark export
   (called each time, so the SMX GOLD force detour is honoured); `custom_resolution/debug_ui.rs`
   uses it.
6. `trigger.rs`: `Inputs.gold_cabinet`; `auto_skin(series, gold)` — 14–16 → 5, 17 → 6, 18–20 → 8
   (gold) / 7, else as today; `mod.rs::resolve_song` fills `gold_cabinet` for AUTO rows only.
7. Readiness gate: `cargo check`, `cargo fmt`, `./build.sh`, `scripts/validate_ddr_selection.sh`.

## Dependencies
- Plan Steps 1–2 (themes armed; HUD) — done.

## Implementation Approach
1. Failing tests: AUTO table incl. the cabinet; `dance_message_vN` names; banner packages / PRAY FOR
   ALL on themes; theme crowd branches, guarding, 4-slot order, callouts; `all_cues` ⊆ manifest;
   theme stage voices.
2. Implement pure changes; then the cabinet service, `resolve_song`, banner engine table.
3. Build, harness, record the cabinet procedure.

## Acceptance Criteria

1. **AUTO**
   - Given AUTO and series 17 / 19 on a white cabinet / 19 on a gold cabinet / 15 / 21
   - When the trigger resolves
   - Then the skin is 6 / 7 / 8 / 5 / 0; an explicit row ignores the cabinet

2. **READY**
   - Given a theme skin with ReadyGo available
   - When `dance_message` is decided
   - Then the package is `dance_message_vN`

3. **Banners**
   - Given a theme skin and Tohoku EVOLVED cleared
   - When the banner art is chosen
   - Then the overlay is `00_prayforall` from `common_shutter_vN`; other clears `00_cleared`,
     fails `00_failed`

4. **Crowd**
   - Given a theme skin and a crowd due at gauge 0.8 / 0.5 / 0.3 with combo 5 / 0.1 with combo 5
   - When `step` runs
   - Then it plays `STG_APP02` + cheer / `STG_APP03` + cheer / nothing / `STG_BOO` + boo (voices
     guarded, skipped near a combo callout)

5. **Eras unchanged**
   - Given skins 1..=5
   - When the existing tests run
   - Then they pass unchanged (except the plays array length)

## Metadata
- **Complexity**: Medium
- **Labels**: ddr-selection, intro, banner, sound, auto, cabinet
- **Required Skills**: Rust
- **Generated By**: code-task-generator 2026-09-25 (breakdown pre-approved: maintainer asked for autonomous execution between cabinet tests)
- **Source Plan**: .agents/planning/2026-09-25-ddr-selection-a-a3-themes/implementation/plan.md
- **Plan Step**: Step 3: READY, end banners, announcer / crowd, AUTO with the cabinet read
