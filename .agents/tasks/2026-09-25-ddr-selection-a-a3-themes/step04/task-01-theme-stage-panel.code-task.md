# Task: Theme stage panel (A3's skin-0 fill)

## Description
Show A3's own skin-0 stage panel before every theme song (design R4, R5): the theme's
`common_choice_vN` / `shutter_choice_hd_root` hosted in World's ShutterActor stage kind through the
existing session / row patch / READY dismissal / release machinery, filled the skin-0 way — the
band texture on the root's own `choice_stage_usr`, `choice_stage_usr2` hidden, the root's default
background and jacket frame with the song jacket, the `vo_stage_*` call at once, no cut-in. The
score sets stay hidden (plan Step 5).

## Background
Design §4.9 and Appendix A.1 (A3 `FUN_180030d10`). Verified against the stock install: all three
theme roots carry `choice_stage_usr` (`scene_choice_stage_usr` inside), `choice_stage_usr2`,
`choice_background_usr`, `choice_jacket_usr/jacket_root_usr/jacket_usr`, the score-set children and
the labels the machine uses (`in`, `loop`, `out`, `end`, `frame_out`, `data_release`); the six
`scene_choice_stage_{1st,2nd,3rd,4th,final,extra}` textures are in each root package; the root's
default `choice_stage` has no `voice` label (the call plays at once); `common_choice_v0` has no
`caution_usr` (hiding it is a no-op). The embedded cues (`Plate_spin3_st`, `STG_APP02`, `banner_in`,
`se_shutter_in/out`) are in the era bank.

## Reference Documentation
**Required:**
- Design: .agents/planning/2026-09-25-ddr-selection-a-a3-themes/design/detailed-design.md

**Additional References (if relevant to this task):**
- .agents/planning/2026-09-25-ddr-selection-a-a3-themes/research/orientation.md (A3's skin-0 fill)

**Note:** Read any document listed above before beginning implementation.

## Technical Requirements
1. `panel_logic.rs`: `Variant::{Era, Theme}` / `variant(skin)`; `root_package(skin)` /
   `root_package_cstr(skin)` (static NUL-terminated: `common_choice_v2` for eras and White,
   `common_choice_v0` DDR A, `common_choice_v1` Gold); `special_stage(ctx)`;
   `theme_stage_texture(ctx)`.
2. `panel.rs`: the row patch writes the session's root package (restore uses the pointer it wrote);
   a theme session requests no packages and no cut-in and skips the era-only shutter-held check;
   adoption hides per variant (theme: `choice_stage_usr2`, `caution_usr`, `fullcombo_challenge_usr`,
   `pN_score_set_mc`; `choice_stage_usr` stays); the theme fill writes the band texture into
   `choice_stage_usr/scene_choice_stage_usr`, sets `Jacket::Song`, takes the stage call from
   `choice_stage_usr` (voice at once), logs the fill.
3. The eras' behaviour is unchanged.
4. Readiness gate: `cargo check`, `cargo fmt`, `./build.sh`, `scripts/validate_ddr_selection.sh`.

## Dependencies
- Plan Steps 1–3 (theme arming, READY — the panel needs the legacy intro — and the theme stage-call
  rule).

## Implementation Approach
1. Failing tests: `variant` / `root_package(_cstr)` per skin; `theme_stage_texture` over stages
   0..=4 in 3- and 5-stage sessions, the final-stage override, the extra stage, the override's own
   stage and past `max + 1` (special → `extra`), event chains; the machine's theme sequence (no
   cut-in: `Adopt, Fill` → `SetJacket` at the swap → voice on the first Showing frame).
2. Implement pure changes, then the engine variant.
3. Build, harness, record the cabinet procedure.

## Acceptance Criteria

1. **Root package**
   - Given skins 6 / 7 / 8 and an era skin
   - When the root package is chosen
   - Then it is `common_choice_v0` / `common_choice_v2` / `common_choice_v1` / `common_choice_v2`

2. **Band**
   - Given a theme on stage index 0 / 1 / 2 / 3 of a 5-stage session, its last stage, the extra
     stage, and a special stage
   - When the band texture is chosen
   - Then it is `scene_choice_stage_1st` / `_2nd` / `_3rd` / `_4th` / `_final` / `_extra` / `_extra`

3. **Panel flow**
   - Given a theme song
   - When World loads the stage panel
   - Then A3's theme root is adopted, filled without packages or a cut-in, shows the song jacket,
     calls the stage at once and closes at READY

4. **Eras unchanged**
   - Given an era song
   - When the panel runs
   - Then the era packages, cut-in and fill are as before

## Metadata
- **Complexity**: Medium
- **Labels**: ddr-selection, stage-panel, shutter
- **Required Skills**: Rust, in-process hooking conventions of this repo
- **Generated By**: code-task-generator 2026-09-25 (breakdown pre-approved: maintainer asked for autonomous execution between cabinet tests)
- **Source Plan**: .agents/planning/2026-09-25-ddr-selection-a-a3-themes/implementation/plan.md
- **Plan Step**: Step 4: Stage panel: theme variant and A3's skin-0 fill
