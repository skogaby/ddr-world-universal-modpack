# Task: Theme panel score sets — RE (design Appendix C rows 1–4)

## Description
Pin everything the theme stage panel's per-player score sets need from World before any code:
the best-record lookup anchor, the target resolver and its name / area getters, whether World
keeps a per-player area (and the region / language rules), and A3's no-record display and glyph
handling. Record the results in `docs/ddr_selection_theme_score_sets.md` (and later the new
module's `//!` doc).

## Background
Design §4.10 ports A3 `FUN_180032240(root, set, name, area, record)` over World's data; Appendix C
lists four open questions owned by plan Step 5. Each unresolved answer fails open (its field is
hidden).

## Reference Documentation
**Required:**
- Design: .agents/planning/2026-09-25-ddr-selection-a-a3-themes/design/detailed-design.md (§4.10, §5.4, §5.5, Appendix A.2 / A.3 / C)
- .agents/planning/2026-09-25-ddr-selection-a-a3-themes/research/name-and-score-sets.md (§2, §3, §5.1)

**Additional References (if relevant to this task):**
- docs/multiplayer_bot_research.md §12 (the rival / ranking sets and their getters)

**Note:** Read any document listed above before beginning implementation.

## Technical Requirements
1. An anchor for World's best-record lookup (`FUN_1801e2c40`) that sweeps on all five builds,
   plus the build-dependent `PlayerWork` score-db offset.
2. The target resolver's convention and record shape; the name and area getters (or a derivation
   when World has none).
3. World's per-player area field, its validity flag, the region rule and the package language.
4. A3's no-record display, `FUN_1800ffe00`'s handling of unmapped characters and unused slots,
   `FUN_1800ff9d0`'s digit visibility.
5. Every package / texture / child the fill will touch verified against the stock install.

## Dependencies
- Plan Step 4 (the theme panel session and fill).

## Implementation Approach
1. Ghidra on A3 `gamemdx_20240402`, World `gamemdx_20260825`, cross-checks on `20250805`.
2. Install checks with `scripts/unpack_arc.py` + `ifstools` + bemaniutils `afputils`.
3. Write `docs/ddr_selection_theme_score_sets.md`.

## Acceptance Criteria

1. **Anchors**
   - Given the five supported builds
   - When the best-record lookup is located
   - Then it is reachable from an already-swept signature's pinned bytes (or a new AOB), with a
     prologue gate

2. **Open rows closed**
   - Given Appendix C rows 1–4
   - When the RE note is written
   - Then each row has an answer or an explicit fail-open, with addresses

## Metadata
- **Complexity**: Medium
- **Labels**: ddr-selection, stage-panel, reverse-engineering
- **Required Skills**: Ghidra, x64 MSVC, AFP / IFS tooling
- **Generated By**: code-task-generator 2026-09-26 (breakdown pre-approved: maintainer asked for autonomous execution between cabinet tests)
- **Source Plan**: .agents/planning/2026-09-25-ddr-selection-a-a3-themes/implementation/plan.md
- **Plan Step**: Step 5: Stage-panel score sets (RE first)
