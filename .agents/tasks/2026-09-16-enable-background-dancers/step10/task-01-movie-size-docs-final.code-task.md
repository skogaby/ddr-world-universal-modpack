# Task: movie-size override, registration semantics, documentation, final validation

## Description
Finish the user-facing contract of the Background Dancers mod: `movie_size.rs` (design §4.3.7 — at window
entry every entered side whose `Customize + 0x30` reads 0/1 is switched to 2 so a movie song renders its
movie as the sized thumbnail marker over the 3D scene; restored synchronously at window exit and at mod
disable), `is_active()` = "CAN work", `DEFAULT_OFF_MODS` entry (already there), `disable()` restoring
hide + movie state, the mod's module doc at its final shape, an AGENTS.md Key-Entry-Points row, a README
operator note, and the final gates.

## Background
`world-background-and-movie.md` V2: World's `DancePlaySequence::onUpdate` step 2 reads
`Customize(PlayerWork[GameWork+8] + customize_offset)->movie_size()` (the getter maps 0 → 1); ONLY the value
1 selects `movie_fullscreen_usr`; 2/3 use the sized marker rect. The scene-26 callback fires before
`createNextSequence`, so a write at window entry lands before that read. The VIDEO SIZE row
(`movie_size_customization.rs`) re-seeds from the field at SONG_SELECT entry and the logout customize
write-back happens at EAM_EXIT — both after the window-exit restore. Maintainer rule: never suppress the
movie, only resize it.

## Reference Documentation
**Required:**
- Design §4.3.7, §4.3.8, §6; Plan Step 10
- `src/mods/movie_size_customization.rs` (the field's value model + the PlayerWork → Customize walk)

**Note:** Read any document listed above before beginning implementation.

## Technical Requirements
1. `movie_size.rs`: `init(signatures)` stashes `customize_offset` (optional; INFO when missing),
   `is_available()`, `apply([bool;2]) -> [Option<u32>;2]` (probed field pointer via
   `stage_records::player_work(side) + customize_offset + 0x30`; 0/1 → 2), `restore(saved) -> usize` (only
   where the field still reads 2), pure `override_for(v)` + test.
2. `lifecycle.rs`: `apply_movie_size()` after the pick INFO at window entry (INFO with the overridden
   values); `restore_movie_size()` synchronously in the scene callback's exit branch and in
   `teardown_on_disable`.
3. `mod.rs`: `init` calls `movie_size::init`; module doc = final shape.
4. Docs: AGENTS.md Key Entry Points row; README Full Feature List row + operator note; RE doc already
   carries §3.4; progress.md final.
5. Gates: `cargo check` (+ `--tests`) → `cargo fmt` → `./build.sh` → `validate_background_dancers.sh` →
   `validate_signatures.sh` (signatures.rs changed this session) → cabinet run (movie song, non-movie song,
   2P, rate, restart, scrub, course, attract untouched, mod toggled OFF live).

## Acceptance Criteria
1. **Cabinet**: a movie song shows its movie in the thumbnail marker with the 3D scene around it; the VIDEO
   SIZE row still reads the player's own value at the next song select; toggling the mod OFF mid-session
   restores stock behaviour at the next song; boot with the mod OFF = stock.
2. **Gates**: all green.

## Metadata
- **Complexity**: Low
- **Labels**: background-dancers, step-10, docs
- **Generated By**: code-task-generator 2026-09-16
- **Source Plan**: `.agents/planning/2026-09-16-enable-background-dancers/implementation/plan.md`
- **Plan Step**: Step 10: Movie-size override, diagnostics, registration, documentation, final validation
