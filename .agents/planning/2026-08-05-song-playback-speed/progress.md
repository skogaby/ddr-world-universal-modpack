# Song Playback Speed Progress

Updated: 2026-08-08
Status: PIVOT — the file-cache generation model is REJECTED for release (maintainer decision 2026-08-08); the feature is being REDESIGNED as STREAMING-ONLY via a fresh PDD cycle. Steps 5+6 remain UNTICKED (the cabinet matrix was deliberately halted mid-run). Tasks 01–04 agent work is checkpoint-committed in the modpack (`ee0368f`); the bemani-buddy task-03 backend work is uncommitted in that repo and SURVIVES the redesign unchanged. The option row (task 02), backend field (task 03), and all Step-4 policy/score/clock machinery are KEEPERS; the Step-2 cache/worker machinery and the Task-01 two-stage file redirect are slated for REMOVAL by the redesign.
NEXT ACTION: Run the PDD process for the streaming-only redesign (rough idea + constraints recorded in `.agents/planning/2026-08-05-song-playback-speed/streaming-pivot-brief.md`... if absent, the handoff prompt of 2026-08-08 is the rough idea). The redesign REPLACES the audio-generation internals behind the unchanged option/policy surface: detour the game's XACT file-read callback (gamemdx `FUN_1801aa250`) to serve a synthesized stretched XWB header + on-the-fly WSOLA-generated ADPCM blocks; NO on-disk cache, NO cache invalidation, NO whole-song up-front generation (both explicitly rejected — streaming-only, no fallback to the old model).
Resume protocol: Read this file (esp. the 2026-08-08 pivot entries), then the redesign's own planning docs once they exist; the retired file-cache delivery's records live under `implementation/step05-task-0{1,2,3,4}-*/`.

## Done

- Completed the light project orientation pass without ingesting the full documentation tree.
- Verified the source design and implementation plan are approved as of 2026-08-05.
- Recorded sibling `ddr-chart-tools` source revision `fa3500bd65ca0350411ef5113070055340eb5a6d` for the focused XWB/MS-ADPCM port.
- Completed Step 1 Task 1: added strict borrowed XWB v43 parsing, checked identity-preserving streaming serialization, and deterministic arbitrary-channel MS-ADPCM under `src/core/xact/`.
- Migrated Assist Tick's compatible mono encoding calls to the shared codec after byte-parity proof; its fixed XSB and in-memory XWB policy remain service-local.
- Added synthetic malformed-profile, stock-tail, logical-trim, serializer, streaming-write, determinism, channel-order, and SNR coverage for 1/2/3/6 channels.
- Completed Step 1 Task 2: added exact reduced rate/Q31/content-time math and deterministic integer joint-channel WSOLA-like stretching with explicit loop context.
- Added fixed endpoint anchoring, terminal non-hop placement, cyclic source windows, seam diagnostics, checked allocations, typed boundary failures, and byte-identical equal-length behavior.
- Completed Step 1 Task 3: added a pure sequential song-bank transformer with MD5 identities, exact loop/rate metadata, direct block encoding, streaming XWB output, hard 128 MiB admission, cancellation checkpoints, and mandatory output reparse.
- Added post-encode ADPCM seam validation, both physical entry orders, stock-tail compatibility, deterministic output, near-limit sequential memory modeling, and cancellation through both entries and every required phase.
- Completed Step 1 Task 4: added `scripts/validate_song_playback_speed.sh`, stable schema-v1 JSON, target-only 75%/125% demo banks, sibling parser/codec comparison, metric thresholds, CLI failure handling, optional external-corpus validation, and direct Windows compilation of the pure harness code.
- At the Step 1 closure point, only Step 1 was checked in `implementation/plan.md`; Steps 2-8 were still open.
- Generated and approved five Step 2 implementation tasks under `.agents/tasks/song-playback-speed/step02/` before Step 2 implementation began.
- Completed Step 2 Task 1: added the host-only `services::song_rate` boundary with canonical full-MD5 cache keys, strict immutable manifests, separate normalized LRU metadata, quarantine identities, and bounded cache-limit normalization.
- Added strict typed validation for versions, lowercase full digests, lengths, two-entry shape, reduced rates, main index, loop bounds, key/report/manifest agreement, and injected timestamps.
- Extended the existing host harness to compile and run song-rate service tests without installing hooks or runtime integration.
- Completed Step 2 Task 2: added the filesystem-injected cache store with same-directory temporary files, XWB-first/manifest-last publication, full hit validation, valid-winner/corrupt-eviction outcomes, and root-contained paths.
- Added independent atomic LRU and quarantine stores plus bounded startup recovery for orphan temp/XWB files, invalid/stale manifests, malformed tombstones, and missing/corrupt LRU metadata.
- Added typed write/flush/read/rename/remove failures and ordered manifest-before-XWB deletion; permission/disk-full/short-write/interruption tests use synthetic temporary directories only.
- Completed Step 2 Task 3: added one background generation worker, per-key build states/epochs, duplicate waiter sharing, explicit supersession/cancellation, manual-clock deadlines, and typed immutable host results.
- Threaded Step 1 cancellation checkpoints through source hash/decode/stretch/encode/write/digest/validation, added guarded XWB/manifest publication boundaries, and prevented stale epochs from committing a manifest.
- Added panic containment plus RAII completion, failure-time store recovery, unconditional waiter notification, metadata-lock probes, bounded coordinator diagnostics, and source-XWB request/key derivation.
- Completed Step 2 Task 4: added a fixed 64-slot consuming lease table, lease transfer/release, active-XACT protection, late-failure process pinning, and exact quarantine identity handling under the coordinator metadata lock.
- Added Ready-to-Evicting claims with deletion outside the lock, Evicting prepare waits, true inactive-LRU selection, committed-limit enforcement, exact final+temporary+64 MiB free-space admission, and Windows volume free-space queries.
- Added separate recency persistence, bounded lifecycle diagnostics, deletion-failure fail-closed state, and stopped-game purge that refuses active jobs/leases/evictions and removes manifests before XWBs/tombstones.
- Completed Step 2 Task 5: extended `scripts/validate_song_playback_speed.sh` without changing schema `song-rate-validation/v1` or removing Step 1 evidence; the stable report now includes a complete top-level `cache` section.
- Added a real temporary-cache demo covering one shared concurrent cold build, a transform-free warm hit, source/rate/frame/version key invalidation, cancellation, timeout, panic continuation, stale-epoch refusal, startup recovery, lease-protected LRU eviction, quarantine invalidation, and stopped-game purge.
- Closed only Step 2 in `implementation/plan.md`; Steps 3-8 remain unchecked and no Step 3 task files or runtime code were created.
- Generated five Step 3 task specifications under `.agents/tasks/song-playback-speed/step03/`: verified signatures/patch transactions, identity clock/publication, XACT slot/TLS primitives, identity wave hooks/LayeredFS seam, and movie-policy/identity validation.
- Cross-checked the Step 3 breakdown against the approved plan/design; all Step 3 guidance and demo requirements are covered without enabling generated paths, non-identity clock state, UI, or score behavior.
- Maintainer approved the five-task Step 3 breakdown on 2026-08-06.
- Completed Step 3 Task 1: added unique structural signatures for the central clock anchor, `wavebank_create`, and manager-level wave-bank unregister target, with exact boot-time count and redirect-byte validation.
- Cross-verified all three signatures in Ghidra on 2026-03-24, 2026-04-21, 2026-06-16, and 2026-07-21; expanded `runtime-integration.md` with patterns, wildcard rationale, addresses, callback/unregister evidence, and the non-authoritative constant delta.
- Added generic checked patch transactions plus a Win32 process adapter covering expected bytes, protection, near allocation/rel32 range, writes, instruction-cache flush, readback, protection restore, and best-effort rollback with typed primary/rollback failures.
- Completed Step 3 Task 2: added a position-independent clock stub that replays the exact eight displaced bytes, preserves RAX/RCX/RDX, applies signed Q31 rounding/saturation, replays `LEA R12`, and jumps over an aligned adjacent factor slot.
- Added checked stub/site installation with global readiness published only after factor/publication initialization, plus permanent startup installation at exact Q31 identity after derived signatures resolve.
- Added identity-only seqlock snapshots, writer CAS, identity-first reset, deferred reset repair, and stress-tested readers; no public API can publish non-identity or `committed=true` state.
- Completed Step 3 Task 3: added four fixed atomic XACT slots, full atomic redirect-token fields/digests, strict phase/identity validation, exact exposed-slot recovery, release/quarantine transitions, and conservative overflow behavior.
- Added a four-frame call-nonced TLS stack with nonallocating guards and deterministic out-of-order cleanup, plus a fixed sequence-numbered MPMC maintenance queue that is bounded, FIFO, lock-free, and overwrite-free.
- Corrected slot recycle ordering so a claimant can never observe `Free` before stale ownership/token fields are cleared.
- Completed Step 3 Task 4: installed the sole identity-only `wavebank_create` and manager unregister detours with exactly-once original calls, call-nonced TLS, panic-contained post work, and bounded release enqueue.
- Made the five AVS hook installation transactional with reverse rollback, removed the service-lock/install callback deadlock, and published broad/conversion/source-read readiness only after full success.
- Added the inert generated-XWB conversion seam after effective static-source resolution; it always returns no dynamic replacement in Step 3 and preserves existing LayeredFS behavior.
- Completed Step 3 Task 5 host work: promoted `DShowPlayer::BuildGraph` ownership to `services::movie_policy`, migrated Non-Native OS to its independent atomic contributor, and kept the song-rate contributor false.
- Extended stable schema-v1 validation with seven `identity_runtime` checks covering identity Q31/snapshot reset, empty TLS/slots/queue, exactly-once wave calls, no dynamic redirect, hook rollback, and movie-policy combinations.
- Added the shared movie-hook ownership override to `AGENTS.md`; no song-rate UI, arming, generated path, non-identity clock, or score behavior exists.
- Closed only Step 3 in `implementation/plan.md`; Steps 4-8 remain unchecked.
- Generated three consolidated Step 4 task specifications: host-only diagnostic policy/score safety, host-only pre-generated transaction integration, and one final combined cabinet diagnostic/closure pass.
- Per maintainer direction, Step 4 Tasks 1–2 explicitly prohibit deployment; all manual validation is concentrated in Task 3 so the planned success path requires one deployment.
- Maintainer approved the three-task Step 4 breakdown on 2026-08-06 and switched implementation agents (GPT 5.6 Sol → Claude Fable 5) citing consistency problems; artifacts from Steps 1–3 are treated with verify-against-source rigor going forward.
- Completed Step 4 Task 1: added the pure song-rate lifecycle (`song_rate/lifecycle.rs` — all nine phases, lock-free CAS transitions, XactInFlight supersession refusal, Task 2 phase entry points) with scene-26 eligibility classification (solo/P2-doubles arm; course/local-versus/no-side/unknown-session/unknown-stage fail closed) and strict 75%-only diagnostic-spec validation.
- Added the per-side eight-entry pending rate-save ledger to `score_guard` with exact oldest-first (side, stage) claim/consume, generation-idempotent appends (Quick Restart dedup), Consumed-tombstone duplicate suppression, sticky ring-overflow fail-closed state, and positive-match-only per-side card-in reset; `is_stage_suppressed` now includes the pending/overflow backstop.
- Added `score_guard::is_full_sanitization_available()` (save detour + decoded stage/course records + scene manager + registered EAM_EXIT sanitiser + league strip), latched from lib.rs and `custom_options_persistence`; the league-strip flag migrated into score_guard as the single source of truth.
- The save trampoline now runs the rate election BEFORE any side default: unknown side/stage fails closed (suppress without consuming, never default P1) while rate state exists; the legacy default-to-0 path is preserved when no rate state exists anywhere.
- `strip_league_node` returns tri-state (absent/removed/removal-failed) and a removal failure fails the sender closed (return 0) instead of forwarding a league-bearing logout request.
- Added ddrcode-deferred per-side rate reset: successful profile loads capture the ddrcode (independent of persistence gates) and the SONG_SELECT drain resets exactly the positively matched side's ledger.
- Hoisted a non-fatal stage-counter decode into `stage_records` (`stage_counter()`, validated `FF 41` INC bytes + range-checked disp8 from `premium_free_stage_inc`; decode happens at init before premium_free can NOP the site).
- Added `song_playback_speed.diagnostic` config (song_code/requested_percent/xwb_path, honored only in LayeredFS developer mode) and `song_rate::runtime` — the one permanent scene callback (atomics/raw validated reads only; game-memory reads skipped entirely when no diagnostic is configured) driving arm/complete/abandon/reset transitions through a production sink (movie contributor + clock publication); tentative `MovieSuppressor::SongRate` at accepted arm, identity-reset-before-movie-clear at gameplay exit, EarlyFailed/LateFailed retention semantics.
- Step 4 Task 1 made no deployment; Q31 stays identity, `identity_conversion_path` still returns `None`, and no score taint can be produced (nothing appends to the ledger until Task 2's commit).
- Completed Step 4 Task 2: added the exactly-once wave-bank transaction (`song_rate/transaction.rs` — exact token consumption with slot recovery, commit ordering score→movie→snapshot→Q31-last via the new `RatePublication::publish_committed` with `RESET_PENDING` deferral, late-fail slot quarantine + fixed maintenance records, recovery-failure return override with conservative both-side taint, panic containment, boot-only `DDR_SONG_RATE_FAULT` selector).
- Added the diagnostic conversion pipeline (`song_rate/conversion.rs`): exact dance-path gating, Step 1 source validation/rate math, pre-generated bank validation against the live source (names/order/durations/shared loop mapping/representable Q31), idempotent cache import through `publish_checked`, per-request quarantine tombstone identity, consuming lease transfer into the fixed XACT slot, TLS attach, reload re-exposure of the same committed generation, and a reload-refusal clock reset (identity FIRST when a committed Q31 would outlive its audio).
- Wired the windows glue: real create-detour transaction body (identity-protocol fallback when pieces are absent; detours stay allocation-free and log-free), lazy store/coordinator singletons under `<mod_folder>/_cache/song_playback_speed` (identity boots never create the directory; startup recovery at creation), a 250 ms maintenance drain thread (unregister lease release + manifest-derived quarantine tombstones; failures stay pinned), wine/windows platform + PE-header module-digest tombstone identity, the LayeredFS seam (`convert_streaming_xwb` + stock-native resolution through the original AVS conversion), gameplay-entry committed logging, and the gameplay-entry LateFailed taint policy.
- Step 4 Task 2 made no deployment; one identity-restorable 75% diagnostic release build exists (`target/x86_64-pc-windows-msvc/release/ddr_world_hook.dll`).
- MAINTAINER DECISION (2026-08-07): Steps 5 and 6 are MERGED into one delivery to hasten the feature (the hard physics — DSP transform, two-stage audio redirect, clock/score commit — are cabinet-proven; the remaining load-bearing unknowns are only U1 live cold-build blocking behavior and U2 live 125%). There is NO interim dev-forced rate config: the `song_speed` option row is the only production rate source (host tests inject `ArmRequest` directly; the `DDR_SONG_RATE_FAULT` env selector stays). The arm-scope movie-suppression gap dissolves under this model — a session-selected rate means every armed song genuinely commits, and tentative suppression through a failed attempt is accepted design behavior.
- Generated the four-task merged Steps 5+6 breakdown under `.agents/tasks/song-playback-speed/step05/` (01 wire on-demand generation into the live transaction; 02 player-facing SONG SPEED option incl. `set_option_available`/`row_injection_available` framework APIs; 03 bemani-buddy `mod_song_speed` via the JSON-model codegen pipeline + nullable `opt_mod_song_speed` migration + sqlx offline cache; 04 validator extension + the single cabinet pass closing both plan steps). Maintainer approved the breakdown 2026-08-07 with one added constraint baked into task 03: bemani-buddy wire-format Rust is codegen'd from `models/*.json` — edit the JSON and re-run `cargo run -p codegen`, never hand-edit `@generated` output.
- MAINTAINER DESIGN CHANGE (2026-08-07, supersedes the approved design's 75/100/125 enum): `song_speed` is a SCALAR row from 25 to 175 percent, granular step 5, coarse step 10, default 100, `ScalarFormat::Integer` — same style as the playfield/overlay scaling rows (`RegisterSpec::scalar(...).step_coarse(...)`, precedent `src/mods/playfield_styling/mod.rs::register_rows`). Only the item label texture is needed (scalar values render numerically). Ripple effects reflected in the specs: `rate::target_for_percent`'s `75|100|125` predicate and `lifecycle::validate_diagnostic` widen to multiples of 5 in 25..=175; the clock stub needs NO change (64-bit imul factor slot + i128 product — 175% factor ≈ 3.76e9 fits; boundary Q31 vectors at 25/175 added to tests); the 128 MiB memory ceiling and 28-bit XWB duration field become REACHABLE at extreme slow rates on long songs and must resolve via the early-failure→stock leg; backend boundary validation accepts the same domain; the cabinet matrix gains an extreme-rate observation leg (worst-case U1; admission refusal is an accepted documented outcome).

- MAINTAINER DESIGN CHANGE (2026-08-08, in-session during task 03): the backend performs NO range validation on `mod_song_speed` — the value is stored VERBATIM like every sibling `mod_*` field (only-when-present; malformed non-numeric values still skip via parse failure). Rationale: widening/narrowing the acceptable range in the client DLL must never require a server change. Supersedes task 03's TR-5 boundary-validation requirement and the invalid-value test legs; the client DLL's load transform (`snap_rate_percent`) remains the sole domain authority.
- Completed Steps 5+6 task 03 (SIBLING bemani-buddy repo, uncommitted; migration + new .sqlx query git-staged per its convention): `mod_song_speed` end to end — `"mod_song_speed": "s32?"` in both `PlayerdataLoadOption`/`PlayerdataSaveOption` model shapes + codegen regen (exact expected +4-line diff, skip_serializing_if on load / serde(default) on save); migration `012_ddr_world_song_speed.sql` (nullable `opt_mod_song_speed INT NULL DEFAULT NULL`, no server default — un-hooked clients never see the element); entity + `row_to_profile!` + UPDATE list/params threading; only-when-present save ingestion (no validation, policy comment in code); load echo + new-player `None`; 5 handler tests (present ×2 values / absent / malformed / None-skipped serialization / Some-echoed serialization); `.sqlx/` regenerated against local MySQL. Working record: `implementation/step05-task-03-mod-song-speed-backend/`.
- Completed Steps 5+6 task 04 AGENT HALF (2026-08-08, no deployment — the maintainer owns the single deployment): (1) validator `on_demand` section (TDD red first: the python schema check required the section before it existed; the run failed on only that). The section drives the REAL Task-1 pipeline against a temp cache — per rate (75/125): cold build via arm→`prepare_open_redirect` (builds +1, RedirectReady, bank on disk + digested), the exposure seam in a live TLS frame (`Exposed`, injected convert status 7 forwarded verbatim, zero new builds), warm replay on a fresh generation (zero builds, ≤ warm limit); rate-key invalidation (125's cold build against the warm 75 cache) + source-content invalidation (new `build_source_with_frequencies` variant bank ⇒ new cold build at a warm rate); records the folded-in Step-4 report extension (song/source/generated/module/platform digests + exact rates 64/85 and 64/51 + cold/warm latencies). (2) Req-24 diagnostics: the `open-redirect` log line now carries `thread <tid>, build wall <W> ms` (refusal WARN too) — non-detour side only. (3) Docs: AGENTS.md key-entry-points row; mod-config.json `song_playback_speed.cache_limit_gib: 10` example; README feature-table row + Complete Example entries + `song_playback_speed` config reference section. Gates: song-rate validator (156 tests, all sections incl. on_demand), Assist Tick validator, Windows check (0 warnings), whole-crate fmt, release build (DLL md5 fce4d53859d40e66173fa600d1296026). Run sheet with staging cleanup + legs a–j + expected log vocabulary: `implementation/step05-task-04-validation-cabinet-closure/runsheet.md`.

## In flight

- Steps 5+6 task 01 COMPLETE (host-only, uncommitted working tree — maintainer commits personally): the diagnostic import in `conversion.rs` is replaced by `coordinator.request()` (warm hits skip the transform; cold builds run under the 30 s deadline/cancellation/admission on the FileManager-open waiting site); `DiagnosticSpec`/`validate_diagnostic`/`song_playback_speed.diagnostic` retired (config key parse-but-ignore with one INFO); rate domain widened to multiples of 5 in 25..=175 (`rate::target_for_percent`, `lifecycle::is_supported_rate_percent`; clock stub unchanged — 64-bit factor slot, boundary Q31 vectors at 25/175); arming is song-agnostic driven by per-side desired-percent atomics (`runtime::set_desired_percent`, task 02's hook point; defaults 100 ⇒ identity boots keep zero footprint — no cache dir, drain thread + bank timeline latch on FIRST non-identity arm via `rate_recording_active()`); `cache_limit_gib` parsed/normalized/threaded into `DiagnosticCoordinator::new_with_limit`; fault-selector source-read/validation/conversion legs re-wired to the on-demand path. NEW since the specs: **generation-to-song BINDING** (`LifecycleState::bind_song` at first open redirect; the seam and the runtime cached-path branch refuse any other dance bank as `OpenNotRedirected`) — the song-agnostic model would otherwise have let the seam expose a song whose RAM copy was never redirected, recreating the rate-against-stock-audio class. Host validation: 153 tests green; all five gates green; zero warnings. Working record: `implementation/step05-task-01-on-demand-generation/`.
- AWAITING MAINTAINER: superseded by the 2026-08-08 pivot (see below). The cabinet matrix was deliberately halted mid-run after the 25 % admission-refusal finding; legs d/g/h/i/j were never completed and are NOT owed — re-validation happens against the reworked streaming feature.
- PIVOT — partial cabinet session evidence (2026-08-08 boot 15:20, log reviewed): 50 % cold build END-TO-END PROVEN on `blli` (~129 s song) — `open-redirect ... rate 5694337/11388672, thread 896, build wall 22427 ms` → exposed → `committed (50%, q31 1073742013)` → maintainer-confirmed slowed pitch-correct audio → `completed at gameplay exit` → rate-tainted stage saves SUPPRESSED (+ Duplicate fail-closed repeats); 50 % warm replay 77 ms with the identical committed rate; a clean 100 % song's save allowed (`stage_taint=false`); 25 % REFUSED up front — `generation 3 fell back to stock 100% (open redirect: Generation(Transform) (thread 896, wall 152 ms))`, stock playback, save allowed, no taint. Root cause: the 128 MiB transform admission ceiling (whole-song workspace ≈ 148 MB at 4× output for a 5.69 M-frame song; ≈ 89 MB at 2× passes). Independently, the 30 s worker deadline would also bite at 25 % (~45 s DSP extrapolated from the 22.4 s 50 % build) — and real cabinets are slower than the test MacBook.
- MAINTAINER DECISION (2026-08-08, supersedes the merged Steps 5+6 release path): the whole-file up-front generation model is NOT RELEASABLE and NOT ACCEPTABLE even as a fallback — the feature must work for arbitrary song length, the loading-screen stall is unacceptable on weaker cabinet hardware (4–8 GB RAM, much slower CPUs), and the on-disk cache has been an unwanted design element all along. The feature is being REDESIGNED STREAMING-ONLY via a fresh PDD cycle: serve the stretched bank through the game's own incremental XACT read-callback consumption (header synthesized up front from rate math alone; ADPCM blocks WSOLA-generated on the fly), REMOVING the disk cache, cache invalidation, eviction/lease/quarantine machinery, `cache_limit_gib`, the 30 s deadline, and the memory ceiling. Feasibility basis already pinned: XACT consumption is already incremental through gamemdx `FUN_1801aa250` (memcpys each requested (offset,length) from the FileManager RAM copy); measured DSP throughput ≈ 11× realtime under Wine on the MacBook; no retail seek path (Prepare→Play from 0 + loop start). KEEPERS: option row + persistence (task 02), bemani-buddy backend (task 03, still uncommitted in that repo), Step-4 lifecycle/policy/score-containment/Q31-clock machinery, rate math + ADPCM codec + the WSOLA core (needs an incremental reformulation). Modpack checkpoint commit: `ee0368f`. Handoff prompt for the PDD redesign produced 2026-08-08.

## Deploy & test log

- No cabinet deployment is required for host-only Step 1 work.
- Task 1 TDD red: direct `rustc --test` failed on the intentionally absent shared modules before implementation.
- Task 1 host tests: 9 passed, 0 failed after implementation and formatting.
- `./scripts/validate_se_bank_synth.sh`: all checks passed before and after migration, including shared/sibling mono+stereo encode parity, stereo decode parity, encoded silence, container framing, and sample-exact clap placement.
- `cargo check --target x86_64-pc-windows-msvc`: passed.
- `cargo fmt`: passed.
- `./build.sh`: passed; release DLL produced.
- Task 2 TDD red: host compile failed on intentionally absent `rate` and `stretch` modules before implementation.
- Task 2 host tests: 16 passed, 0 failed after review fixes and formatting; pitch error stayed within 0.25% at 75/100/125, stereo relationships remained coherent, and loop/boundary cases passed.
- Task 2 regression/build gates: Assist Tick validator, Windows target check, format, and release build all passed.
- Task 3 TDD red: host compile failed on intentionally absent `digest` and `transform` modules before implementation.
- Task 3 host tests: 23 passed, 0 failed after review fixes and formatting.
- Task 3 regression/build gates: Assist Tick validator, Windows target check, format, and release build all passed.
- Task 4 TDD red: the mandatory validator command initially failed because the script did not exist.
- `./scripts/validate_song_playback_speed.sh`: passed in development mode; 23 pure tests passed, Windows harness compilation passed, and schema-v1 report `overall_pass` is true.
- Synthetic 75% demo: exact rate `64/85`, pitch error approximately `0.0004%`, codec SNR approximately `41.49 dB`, zero clipping/lag change, deterministic output, preserved identity, and measured additional working set below 1 MiB.
- Synthetic 125% demo: exact rate `64/51`, pitch error below `0.0001%`, codec SNR approximately `41.49 dB`, zero clipping/lag change, deterministic output, preserved identity, and measured additional working set below 1 MiB.
- Task 4 failure paths passed: unknown option, missing sibling, missing/invalid release platform/corpus, and generated output-tree corpus all fail actionably and remove stale success reports.
- Final `./scripts/validate_se_bank_synth.sh`, Windows target `cargo check`, `cargo fmt`, and `./build.sh` all passed.
- Step 2 Task 1 TDD red: the host validator rejected the intentionally absent `src/services/song_rate/model.rs`.
- Step 2 Task 1 host validation: 31 tests passed (23 Step 1 + 8 cache-model tests); synthetic 75%/125% demos and stable report remained green.
- Step 2 Task 1 regression/build gates: Assist Tick validator, Windows target check, whole-crate format, and release build all passed.
- Step 2 Task 2 TDD red: the host validator failed on the intentionally absent `src/services/song_rate/cache.rs`.
- Step 2 Task 2 host validation: 38 tests passed; publication order, interruption recovery, valid/corrupt destination handling, LRU/tombstone isolation, bounded recovery diagnostics, and cache-root containment are covered.
- Step 2 Task 2 regression/build gates: song-rate validator, Assist Tick validator, Windows target check, whole-crate format, and release build all passed.
- Step 2 Task 3 TDD red: the host validator failed on the intentionally absent `src/services/song_rate/worker.rs`.
- Step 2 Task 3 host validation: 42 tests passed; concurrent duplicate sharing, one-heavy-worker serialization, queued/active cancellation, supersession, timeout, panic continuation, and lock discipline are covered with barriers/manual clocks.
- Step 2 Task 3 regression/build gates: song-rate validator, Assist Tick validator, Windows target check, whole-crate format, and release build all passed.
- Step 2 Task 4 TDD red: focused host compilation failed on the intentionally absent lease/admission/purge APIs.
- Step 2 Task 4 host validation: 46 tests passed; consuming leases, active/process pins, Evicting wait/retry, manifest-first deletion, LRU/free-space admission, quarantine invalidation, and purge refusal/success are covered.
- Step 2 Task 4 regression/build gates: song-rate validator, Assist Tick validator, Windows target check, whole-crate format, and release build all passed.
- Step 2 Task 5 TDD red: the completed validator run failed only on the newly required missing `cache` report section.
- Final Step 2 host validation: 46 focused tests passed and all ten cache-demo checks passed; report `overall_pass` and `cache.passed` are true.
- Cache demo evidence: one build served four concurrent callers, warm reuse performed zero transforms, cancellation/timeout/panic/stale recovery counts are nonzero, startup recovery removed two orphans, and lease-protected eviction/quarantine/purge checks passed.
- Stable cache metrics include full keys/digests, cold/warm latency, estimated/measured memory, build/share/cancel/timeout/panic/stale/recovery counts, committed/temp/limit/free-space bytes, lease/state/purge counts, eviction order, quarantine outcome, and per-check status without absolute paths.
- Final `./scripts/validate_se_bank_synth.sh`, Windows target `cargo check`, whole-crate `cargo fmt`, and release `./build.sh` all passed after the cache demo.
- Step 3 Task 1 TDD red: the host validator rejected the intentionally absent `src/core/memory_patch.rs`.
- Step 3 Task 1 host validation: 51 tests passed; checked patch success, expected-byte rejection, every primary failure rollback, rollback-failure reporting, rel32 bounds, and near-allocation failure are covered.
- Step 3 Task 1 cross-version RE: all clock/create/unregister patterns returned one match on each supported binary, with clock patch at anchor `+0x25` and literal bytes `44 8D 34 18 4C 8D 67 58`.
- Step 3 Task 1 regression/build gates: song-rate validator, Assist Tick validator, Windows target check, whole-crate format, and release build all passed.
- Step 3 Task 2 TDD red: the host validator rejected the intentionally absent `src/services/song_rate/clock_patch.rs`.
- Step 3 Task 2 host validation: 56 tests passed; signed identity/non-identity Q31 vectors, emitted layout/return/factor checks, checked install failures, seqlock stress, and deterministic deferred reset are covered.
- Step 3 Task 2 regression/build gates: song-rate validator, Assist Tick validator, Windows target check, whole-crate format, and release build all passed.
- Step 3 Task 3 TDD red: the host validator rejected the intentionally absent `src/services/song_rate/xact_runtime.rs`.
- Step 3 Task 3 host validation: 62 tests passed; nested/overflow TLS, exact slot recovery, four-slot concurrency, lease transitions, queue FIFO/saturation, and identity-state isolation are covered.
- Step 3 Task 3 regression/build gates: song-rate validator, Assist Tick validator, Windows target check, whole-crate format, and release build all passed.
- Step 3 Task 4 TDD red: the host validator rejected the intentionally absent `src/services/song_rate/wavebank_hook.rs`.
- Step 3 Task 4 host validation: 69 tests passed; exactly-once/nested/panic create/unregister behavior, full identity readiness combinations, and every five-hook rollback position are covered.
- Step 3 Task 4 regression/build gates: song-rate validator, Assist Tick validator, Windows target check, whole-crate format, and release build all passed.
- Step 3 Task 5 TDD red: the host validator failed on the intentionally absent `src/services/movie_policy.rs`.
- Step 3 Task 5 host validation: 71 focused tests and all seven stable identity-runtime report checks passed; `overall_pass`, `cache.passed`, and `identity_runtime.passed` are true.
- Step 3 Task 5 local gates: song-rate validator, Assist Tick validator, Windows target check, whole-crate format, and release build all passed.
- Agent deployment was skipped per maintainer direction; the maintainer manually ran the identity build under CrossOver. Step 3 remains unchecked pending the untainted score-save oracle below.
- CrossOver identity test (2026-08-06, game build 20260721): maintainer reported normal boot/play behavior; log confirms all three song-rate signatures, derived clock patch `+0x5CD6A`, shared movie hook, identity clock, transactional LayeredFS, identity wave hooks, and `Song-rate identity transaction ready` with no song-rate warning.
- CrossOver movie policy: Non-Native OS contributor enabled and `movie_policy` suppressed one DirectShow graph build; maintainer observed normal behavior/no crash.
- CrossOver gameplay lifecycle: log contains normal gameplay entry/exit across multiple songs and no hook crash/panic. `data_mods/_cache/song_playback_speed/` does not exist, confirming no runtime generation/redirection.
- Trusted score-save oracle remains pending: `mod-config.json` had `autoplay=1` for P1/P2, and the log correctly shows pre-existing `score_guard` stage-save suppression. This cannot prove ordinary identity score submission.
- Native-Windows movie-original behavior was not tested and remains explicitly unclaimed for later release validation.
- CrossOver trusted score-save retest: maintainer disabled Autoplay and completed a normal carded 100% song. Log shows `Autoplay: side=0 OFF`, normal GAMEPLAY -> STAGE_RESULT, `score_guard: side=0 savekind=2 save allowed (stage_taint=false, logout_taint=false)`, backend `playerdata_save` request, sender/receiver `ret=TRUE`, successful response, and `EssCallAndWaitBase3PlayerDataSave::wait() : Success`.
- Final Step 3 artifact check: `data_mods/_cache/song_playback_speed/` remains absent. CrossOver identity validation is accepted and Step 3 is closed.
- Step 4 Task 1 TDD red: the host validator rejected the intentionally absent `src/services/song_rate/lifecycle.rs`.
- Step 4 Task 1 host validation: 111 tests passed (71 pre-existing + 17 score_guard ledger/readiness/league + 23 lifecycle eligibility/transition/phase); full validator, cache demo, and identity-runtime report all green with no schema change.
- Step 4 Task 1 regression/build gates: song-rate validator, Assist Tick validator, Windows target check (0 warnings), whole-crate format, and release build all passed; re-verified after post-review edits. No deployment (prohibited for this task).
- Step 4 Task 2 TDD red: the host validator rejected the intentionally absent `src/services/song_rate/transaction.rs`.
- Step 4 Task 2 host validation: 138 tests passed (111 prior + 11 transaction + 16 conversion, including end-to-end prepare->create->commit->unload->idempotent-reload, quarantine-blocks-future-exposure, deferred-reset-beats-racing-commit, and reload-refusal clock reset); full validator, cache demo, and identity-runtime report all green with no schema change.
- Step 4 Task 2 regression/build gates: song-rate validator, Assist Tick validator, Windows target check (0 warnings), whole-crate format, and release build all passed. No deployment (prohibited for this task).
- Step 4 single deployment performed 2026-08-06 (maintainer-directed, ahead of Task 3's report-extension half): Task 2 DLL (md5 a6e070a5...) + diagnostic config (dev mode on, song 'abdt' 75% at ./data_mods/_diag/abdt-75.xwb) + generated bank installed on the CrossOver cabinet. Backups: mod-config.json.pre-step4-backup, ddr_world_hook.dll.step3-backup. p2.premium_free and p2.autoplay zeroed in the offline cache (premium free's frozen stage counter would collide the 100%-restoration save with the diagnostic's consumed tombstone).
- FIRST 75% CABINET RUN FAILED (2026-08-06 17:55, log preserved understanding): boot readiness all green; `generation 1 armed (75%, mask 0b01, stage 0)`; `generation 1 exposed 'sound/win/dance/abdt.xwb' -> 'C:\ddr_world\contents\./data_mods\_cache/song_playback_speed\dbea619e....xwb' (rate 1/1)`; then NO committed/late-fail/score_guard lines before the maintainer quit mid-song. Observed: chart/clock ran at the 75% timeline (arrows desynced exactly as if audio should have been slowed) while AUDIO stayed stock 100%.
- Root cause analysis: (a) the exposed path handed to AVS was the store's CANONICAL ABSOLUTE mixed-separator root — proven LayeredFS replacements pass relative forward-slash paths (./data_mods/...); the AVS conversion of the absolute path did not produce our artifact, and the stock bank was opened instead (most consistent hypothesis: reused caller dest buffer retained the stock native path). (b) DESIGN-CONFORMANCE DEFECT in Task 2: the seam exposed the token BEFORE running the original AVS conversion, violating design §LayeredFS Integration ('sets its exact-path token only after the original AVS conversion succeeds for the generated path') — so the silently failed redirect still committed non-identity Q31 against stock audio. (c) cosmetic: the exposed log prints the pre-commit snapshot rate ('rate 1/1') instead of the token's rate. (d) diagnostic gap: no 'gameplay started with committed' line appeared even though Q31 was live — the commit may land after the GAMEPLAY scene entry, so the gameplay-entry-only log can miss it.
- EXPOSE-AFTER-CONVERT-SUCCESS FIX COMPLETE (2026-08-06 18:46, deployed): all four root-cause legs closed. Host validation: 140 tests passed (138 prior + convert-ordering + convert-closure-failure); full validator, cache demo, identity-runtime report, Assist Tick validator, Windows target check (0 warnings), whole-crate format, and release build all green. Fixed DLL (md5 a1ea604b97e697fd7929ff601cfac446) copied over the cabinet's `ddr_world_hook.dll`; config + diagnostic bank unchanged from the 17:53 staging; the failed run's imported cache entry (key dbea619e...) left in place (next prepare is a warm hit; validation reruns per prepare). Awaiting maintainer cabinet re-run. Expected log lines: boot `Song-rate identity transaction ready`; abdt 75% → `generation N armed (75%, mask 0b01, ...)`, `generation N exposed 'sound/win/dance/abdt.xwb' -> './data_mods/_cache/song_playback_speed/dbea619e....xwb' (rate 6214401/8285824, convert status <rc>)`, `generation N committed (75%, rate 6214401/8285824, q31 ...)` (drain poll, ≤250 ms after commit) and/or `gameplay started with committed generation N`, gameplay-exit `completed at gameplay exit — identity reset first`, `savekind=2` stage save SUPPRESSED for the rate-tainted side; different song 100% → `save allowed`; card-out → `P1 records virginised` + logout save SANITISED; backend keeps the 100% score and has no abdt score. Oracle: slower music, SAME pitch, arrows in sync.
- SECOND 75% CABINET RUN ALSO FAILED (2026-08-06 19:02, fixed DLL confirmed running): armed 19:02:52; `exposed ... -> './data_mods/_cache/song_playback_speed/dbea619e....xwb' (rate 6214401/8285824, convert status 1)` 19:02:54; `committed (75%, rate 6214401/8285824, q31 1610621289)` 19:02:54 (drain poll). The transaction is now fully proven at the redirect level — relative forward-slash path, original AVS conversion succeeded into the real dest (dest verified to contain the cache-key hex), original create succeeded, commit ordered correctly — yet the audible music was STILL stock 100% with the clock at 75% (same audible symptom as run 1). Maintainer quit ~11 s after commit (no exit/unload lines; the `gameplay started with committed` entry line also did not appear — unexplained, secondary). No score lines (quit pre-save).
- Ghidra findings (20260721): `wavebank_create` +0x1AB050 converts the file-table row path (row+0x11, rows 0xa0 at `*(DAT_1806f2f48+0x28)`) via `XCnbrep7000046` = avs_fs_convert_path (our seam), then `CreateFileA(converted_dest)` and hands the handle to `IXACT2Engine` vtable+0x58 (streaming create) — so the engine bank created at 19:02:54 IS backed by our slowed file. Slot dispatcher `FUN_1801aa3c0` (+0x1AA3C0): slots {0,3,5} stream, {1,2} in-memory; `abdt`→5→always streaming. Create/xsb dispatcher `FUN_1801aa520` (xsb→sound-bank create +0x1AAFA0, else wave-bank create). File-table registration `FUN_1801fef30` is FNV-1a find-or-add + refcount ⇒ ONE file id per path string; wavebank_create's duplicate guard returns 0 when the file id already has a live bank ⇒ at most one `abdt` wave bank at a time, and our create succeeding proves no other instance existed at create time. CONTRADICTION: exactly one `abdt` bank existed (ours, slowed) yet music was stock ⇒ either the audible gameplay BGM does not come from `sound/win/dance/<code>.xwb` at all, or an unregister+recreate cycle we cannot see replaced our instance (a recreate would have re-entered the seam and logged — it did not).
- STATIC REPLACEMENT DISCRIMINATOR STAGED (2026-08-06 19:25, zero code changes; deployed DLL unchanged and correct for this test): cabinet `mod-config.json` diagnostic block disabled by renaming the key to `diagnostic_DISABLED_for_static_test` (nothing arms, clock stays identity, seam inert); the 75% bank copied to `data_mods/zz_rate_diag/sound/win/dance/abdt.xwb` (allowlist empty, dev mode on ⇒ folder is scanned). Every consumer of the file — preview, gameplay, any probe — now gets slowed data through the ordinary static path. Listening test: wheel preview slow? gameplay music slow? (bail mid-song; arrows will desync the OPPOSITE way — chart 100% vs audio 75% — expected and fine). Music slow ⇒ audible source IS this file (instrument the create/unregister timeline next); music stock ⇒ audible gameplay BGM is sourced elsewhere entirely (retarget the seam after RE). Revert after: delete `data_mods/zz_rate_diag/` and restore the `diagnostic` key name.
- STATIC REPLACEMENT TEST RESULT (2026-08-06 22:36 boot, log `diagnostic=false` confirms identity clock): wheel PREVIEW slowed ✓, GAMEPLAY music slowed with correct pitch ✓ — the transform is EAR-VALIDATED on-cabinet and the audible gameplay BGM IS `sound/win/dance/<code>.xwb`. Arrows desynced as expected for identity clock + slow audio (maintainer reported first arrow landing 4-5 s early — matches 0.333 × ~13.5 s of accumulated 100%-vs-75% drift; maintainer also perceived arrows "slower than 100%", unconfirmed — wall-driven clock at identity should be exactly 100%, re-observe next run).
- CLOCK MODEL PINNED (Ghidra 20260721, `FUN_18005CCE0` = the GamePlayActor update): `mc = [app_global(DAT_1806f2cf0)+0x1268] − [actor+0x16C](sound_offset) − [actor+0x160](start anchor) + Option-vcall(+0x248)`. `+0x1268` is written from `XCnbrep700002c()` (AVS system-time import) at input poll (`FUN_180023220`) and at input init (`FUN_180022f90`) — the clock is WALL-driven; the Option vcall (`FUN_1801e9020` → `ddr::player::Option`) contributes the per-player judgment-timing constant, not a position. Q31 patch semantics as designed.
- PREVIEW PLAYER FOUND (Ghidra): `FUN_18010eab0` plays the song-wheel preview from `data/sound/win/dance/<code>.xwb`, cue `<code>_s` (suffix literal at 0x18036eee8), via `FUN_1801ccd10(mgr+0xC8, 5, path, cue)` — the SAME file/slot-5 XACT machinery as gameplay.
- ZOMBIE-PREVIEW-BANK HYPOTHESIS (explains all three runs): the select-scene preview's STOCK `abdt` wave bank is still alive engine-side at gameplay load (the preview cue plays into the transition; XACT defers destroying a bank with a live cue). The gameplay sound bank resolves its wave bank BY INTERNAL NAME (`abdt`) and binds the older stock instance; our slowed bank is created successfully but never bound. Dynamic runs 1-2 ⇒ stock audio ✓; static run ⇒ both instances slowed ⇒ slow audio ✓. Discriminating test (zero code, cabinet restored to dynamic mode 2026-08-07: `zz_rate_diag/` removed, `diagnostic` key restored, fixed DLL + imported cache entry unchanged): at song select, hover a DIFFERENT song until its preview plays, then move to abdt and confirm QUICKLY (before abdt's own preview starts) — the leftover engine bank then carries the other song's name, gameplay `abdt` can only bind OUR bank. Expected: slow pitch-correct audio AND arrows in sync (Q31 75% + 75% audio). If confirmed ⇒ design the real fix (ensure the preview bank is fully destroyed/stopped before the gameplay create binds, or break the name collision per-generation via paired XSB+XWB rename).
- MAINTAINER VIDEO CORRECTION (2026-08-07): re-reviewing the static run against a stock-cabinet YouTube capture, the arrows scrolled at NORMAL (100%) speed — the earlier "slower than 100%" was misperception against slowed audio. The wall-driven clock model is therefore fully validated: identity Q31 ⇒ 100% arrows; the 4-5 s early first arrow = accumulated 100%-vs-75% drift. The ONLY remaining bug is the audible-instance binding.
- PREVIEW-AVOIDANCE TEST REJECTED by maintainer (the preview starts near-instantly and always plays before difficulty confirm — cannot be out-raced). Replaced with instrumentation.
- REFINED THEORY (fixes a hole in the zombie variant): the unregister path CLOSES THE FILE HANDLE synchronously (CloseHandle right after the engine Destroy vcall — Ghidra `+0x1AB3D0`), so a destroyed bank cannot stream a whole song. The surviving variant: the preview's row goes into a release state {0,7,8} at song confirm, making it INVISIBLE to the file-table lookup (`FUN_1801febd0` skips those states and compares NO path — the hash covers the basename only) while its bank + open handle stay fully alive; the gameplay loader then registers a NEW row (different file id) for the same path, the duplicate guard passes, our bank becomes the SECOND live `abdt`, and the engine's by-name bind (first match in the engine's append-at-tail wave-bank list, `FUN_00410f00` in xactengine2_10.dll) picks the older stock preview instance, which streams the whole song from its still-open handle.
- BANK-EVENT TIMELINE INSTRUMENTATION DEPLOYED (2026-08-07, md5 9de6432f8b4bf3aecf5602089e97de7e): `xact_runtime::BankTimeline` — 64-slot lock-free packed-AtomicU64 MPSC ring (record = two atomics + QPC read, detour-legal; overflow drops-and-counts); recorded from both wave-bank detours (create: file_id + returned status + compressed CreateOutcome path incl. Committed = our redirect; unregister: file_id), gated on `runtime::diagnostic_configured()`; drained by the 250 ms maintenance thread as `song_rate: bank timeline t+Nms CREATE/UNREGISTER ...` lines. Host validation: 143 tests (140 prior + 3 timeline: roundtrip/order, negative-id + all-path packing, overflow drop counting); all five gates green. Cabinet config unchanged (dynamic diagnostic armed).
- INSTRUMENTED 75% RUN — TIMELINE CAPTURED, ZOMBIE-PREVIEW THEORY DEAD (2026-08-06 23:42–23:44 boot, md5 9de6432f...): boot creates `t+1684/1685/1722/1972ms` file_id 97/96/1597/118 all `path=Stock`; `t+74236ms CREATE file_id=132 status=1 path=Stock` (song-select preview loads abdt.xwb); 23:43:51 `generation 1 armed (75%, mask 0b01, stage 0)` then `t+91000ms UNREGISTER file_id=133` + `UNREGISTER file_id=132` (song confirmed → preview XSB+XWB unregistered; unregister closes the file handle SYNCHRONOUSLY per Ghidra +0x1AB3D0); 23:43:53 `exposed 'sound/win/dance/abdt.xwb' -> './data_mods/_cache/song_playback_speed/dbea619e....xwb' (rate 6214401/8285824, convert status 1)`, `committed (75%, ..., q31 1610621289)`, `t+93580ms CREATE file_id=1638 status=1 path=Committed` (fresh file id). NO further bank events during audible gameplay; maintainer quit mid-song ~29 s later (log ends 23:44:22, no exit/save lines). AUDIO STILL STOCK 100%, full song, not silent; assist tick (ON) ticked at the normal 100% schedule matching the stock audio. Forced conclusion: the preview instance was fully destroyed BEFORE our create — during gameplay the only live `abdt` XACT wave bank was ours (slowed) — so the audible BGM either does not come from the streaming wave bank at all, or the stock file is read through a second, non-redirected channel. (The static test slows everything because `find_mod_replacement` redirects ALL opens/converts; the dynamic seam redirects ONLY the `fs_convert_path` nested inside `wavebank_create`.) Secondary note: `gameplay started with committed generation` line again absent despite commit preceding GAMEPLAY entry — LOW priority, the drain's `committed` line covers the oracle; don't chase while the audio bug is open.
- OPEN-HANDLE KILLER TEST (2026-08-07 00:43 boot, same DLL; `layeredfs.verbose: true` armed for the run; lsof watcher polling all wine processes at 0.5 s): timeline — STOCK `data/sound/win/dance/abdt.xwb` held open 00:46:05→00:46:46 only (the preview bank; matches `t+136683ms CREATE file_id=107 path=Stock` and its 00:46:46 unregister), then OUR `_cache/song_playback_speed/dbea619e....xwb` held open continuously 00:46:49→00:48:14 (the whole song; matches `t+180297ms CREATE file_id=1638 path=Committed`). During audible gameplay OUR slowed bank was the ONLY abdt-related open file — and the maintainer confirms the music was STILL stock 100%. Cache-file content re-verified by md5 (matches `_diag/abdt-75.xwb`, differs from stock). Conclusion forced: the audible BGM is served from MEMORY, not from any open file; the FileManager's whole-file load of the stock bank (~00:46:46–49, via un-redirected `avs_fs_open`/`avs_fs_read` — a fast read the 0.5 s lsof poll missed) is the only candidate source.
- ROOT CAUSE CONFIRMED IN THE BINARY (Ghidra, gamemdx 20260721): `wavebank_create` (+0x1AB050) has BOTH engine create branches — streaming vt+0x58 AND in-memory vt+0x50 — so every manager bank create passes our detour (no second create path); gamemdx has exactly ONE `CreateFileA` site (inside wavebank_create) and imports no other audio API (no DirectSound/XAudio2/MF) — XACT is the sole music output. For slot-5 (per-song dance) banks, wavebank_create ALSO inserts `{win32_handle → file_id}` into a sorted vector at manager+0x419*8 (the insert is gated on `slot==5` — exactly the "streams via FileManager, not via file" class). The engine's registered XACT file-read callback `FUN_1801aa250` (the binary's only other `ReadFile` user) services EVERY streaming read: it calls `FUN_1801aba70` (handle→file_id lookup in that vector); on miss it does a real `ReadFile`, on hit — always, for dance banks — it MEMCPYS from the FileManager RAM buffer (`[DAT_1806f2f48+8] + file_id*0x40 + 8`, size @+0x14) and fakes the OVERLAPPED result. The `FileManager` xwb callback (`FUN_1801ac650`) returns a no-read Task ONLY for streaming non-slot-5 files; for slot 5 it returns NULL ⇒ the FileManager reads the whole XWB into RAM through `avs_fs_open`/`avs_fs_read` (un-redirected in dynamic mode) before the bank is created. NET: our redirected CreateFileA handle is opened, held all song, and NEVER READ — it is only a stream-map lookup key; the audible bytes are the RAM copy. This explains every observation across all three dynamic runs, the static test (LayeredFS redirects `avs_fs_open` ⇒ RAM copy slowed), the lsof timeline, and the bank-event timeline. Additional World facts pinned: the gameplay loader issues the dance `FileManager` load for `sound/win/dance/<code>.xsb`/`.xwb` (category "sound") at song confirm — ~3 s BEFORE wavebank_create; BGM start is sound-bank cue Prepare→IsPrepared→Play (no retail seek path); the cue-name suffix variants never change the bank FILE name, so a path-keyed open redirect is unaffected.
- OPEN-REDIRECT FIX IMPLEMENTED + DEPLOYED (2026-08-07, maintainer-approved design deviation — the streaming wave bank is NOT the sole audio source): the pipeline now has TWO LayeredFS interceptions. (1) NEW open-time redirect: `conversion::prepare_open_redirect` (shared `validate_and_import` half: source read → Step 1 validation → diagnostic-bank validation → quarantine check → idempotent store import; Armed→Preparing→RedirectReady; every failure ⇒ EarlyFailed) called from `runtime::redirect_dance_bank_open` (generation-keyed `OPEN_REDIRECT` cache serves repeat lstat+open probes and the Quick-Restart reload; new `open-redirect` log line) wired into BOTH `fs_open_body` AND `fs_lstat_body` (lstat must report the generated bank's size or the FileManager under-sizes its RAM buffer) via `file_hooks::song_rate_open_redirect` (stock-native resolution through the convert-path ORIGINAL — no hook re-entry). (2) The exposure seam (`prepare_streaming_redirect`) now enters from RedirectReady via the new `lifecycle::begin_exposing` (RedirectReady→Preparing; the seam re-derives everything — warm store hit) and STRUCTURALLY REFUSES from Armed (new `RefuseReason::OpenNotRedirected` ⇒ EarlyFailed): if the FileManager copy was never redirected, the audible bytes are stock, so exposing can never commit a rate against stock audio (this converts the former silent-desync class into fail-open stock). Commit authority unchanged (wave-bank token transaction; Q31 LAST). Known rare failure mode (recorded): open redirected but create/commit later fails ⇒ slowed audio at identity clock for one song, side tainted — conservative direction preserved. Host validation: 147 tests (143 prior + open-redirect happy/structural-refusal/failure/phase-ownership; source-fault test now covers the seam-stage re-validation leg; wrong-rate/corrupt/wrong-song/quarantine now refuse at the OPEN stage with the seam standing down). All five gates green. Fixed DLL (md5 de04293af982d46a960030109315fe28) deployed to the cabinet; `layeredfs.verbose` restored to false; diagnostic config unchanged (abdt 75%, warm cache entry dbea619e...). Awaiting the maintainer's oracle run.
- ORACLE RUN PASSED — FIRST FULLY-SYNCED 75% (2026-08-07 01:57 boot, fixed DLL md5 de04293a...): maintainer played abdt at 75% multiple times in one session — MUSIC AT 75%, PITCH-CORRECT, ARROWS IN SYNC (Q31 + audible audio finally match). Log evidence, both generations: `generation 1 armed (75%, mask 0b01, stage 0)` 01:59:18 → NEW `generation 1 open-redirect 'sound/win/dance/abdt.xwb' -> './data_mods/_cache/song_playback_speed/dbea619e....xwb' (rate 6214401/8285824)` 01:59:21 (the audible-copy redirect firing ~0 s before the create — the FileManager load) → `exposed ... (convert status 1)` + `committed (75%, ..., q31 1610621289)` same second → gameplay → `completed at gameplay exit — identity reset first` → `unload released cache lease (slot 0)`; generation 2 identical at stage 1 (02:00:14). Score policy END-TO-END: `savekind=2 rate-tainted stage save SUPPRESSED (generation=1, stage=0)` + fail-closed Duplicate suppressions for the repeat saves + `savekind=2 save SUPPRESSED (stage_taint=true, logout_taint=true)` (early attempts had autoplay ON — suppressed under BOTH taints; the final attempt had autoplay OFF — suppressed under rate taint alone). The network `playerdata_save` xrpc calls seen after suppression lines are EXPECTED (maintainer-confirmed policy: saves are not blocked wholesale — score payloads are stripped/suppressed; `EssCallAndWaitBase3PlayerDataSave::wait() : Success`). MAINTAINER BACKEND CHECK: no score row was uploaded for ANY abdt attempt, including the clean autoplay-OFF one. The audible-source fix is validated; remaining Step 4 validation = the one-session matrix (75% suppressed / different-song 100% allowed / card-out sanitised + backend intact).
- STEP 4 MATRIX PASSED — CABINET ORACLE COMPLETE (2026-08-07 02:10 boot, fixed DLL md5 de04293a..., autoplay OFF throughout, maintainer-run + backend-verified): one session, three gameplays + card-out. (1) gen 1 abdt 75% (stage 0): `armed` → `open-redirect` → `exposed` → `committed (q31 1610621289)` → synced 75% playback → `completed at gameplay exit` → `savekind=2 rate-tainted stage save SUPPRESSED`. (2) gen 2 the DIFFERENT 100% song (stage 1): armed song-agnostically BUT `bank timeline CREATE file_id=1637 path=Stock` (NOT Committed) + NO open-redirect/exposed/committed → true stock 100% → `completed at gameplay exit` → `score_guard: side=0 savekind=2 save allowed (stage_taint=false, logout_taint=true)` (logout_taint set by gen 1's 75% commit — correct; per-stage saves gate only on stage_taint, so the clean song saved). (3) gen 3 abdt 75% (stage 2): identical to gen 1, suppressed. Card-out: `P1 logout save SANITISED — scores stripped (Removed), profile forwarded`. MAINTAINER BACKEND CHECK: the 100% middle song's score IS present, NO abdt score for any attempt, profile write-back intact. All five Step-4 acceptance oracles (75% end-to-end sync, competitive-data containment, literal-100% restoration, movie/lease/unload cleanliness, backend absence) PASS. Note the design assumption that the streaming wave bank is the sole audio source was wrong and required the open-redirect audio-source fix (documented above) — so Step 4 needed more than the planned single deployment, but the plan's Step-4 gate (the cabinet oracle) is fully met.

- Steps 5+6 task 01 host validation (2026-08-07, no deployment): TDD red — 2 new rate-domain tests failed on the old `75|100|125` predicate before widening; compile-red on the removed `DiagnosticSpec` APIs before the arm-model rewrite. Green: 153 tests (147 baseline + rate-domain/Q31-boundary + on-demand cold-75/cold-125/warm/cross-song/admission/corrupt-source/mislabeled-source + song-binding refusal; every Step-4 invariant test retained and passing — structural refusal, convert ordering, quarantine, reload idempotence, end-to-end commit/unload/late-fail). All five gates green (song-rate validator, Assist Tick validator, Windows target check with 0 warnings, whole-crate format, release build). Release DLL produced but NOT deployed (task 04 owns the single deployment).
- Steps 5+6 task 02 host validation (2026-08-07, no deployment): TDD red — availability tests compile-failed on the absent `available` field/`set_available` (harness mini-module of custom_options api+registry added to the validator); snap tests preceded `snap_rate_percent`. Green: 156 tests (153 + snap normalization + 2 registry-availability). Delivered: `SongPlaybackSpeedMod` (`song-playback-speed`) with the `song_speed` scalar row (25..=175, step 5, coarse 10, default 100, `PersistMode::Full`, snap load transform, on_change → `set_desired_percent`), the NEW framework APIs `set_option_available` (silent non-injection at the builder-hook per-open snapshot; open forms structurally immutable) + strict `row_injection_available()` (rows scalar-ready + builder + latched filter hook), `runtime::integration_ready()`, the committed `seop_item_song_speed.png` label (only-new-file diff), and mod-config.json examples (mods entry + row_order). All five gates green; nothing committed.
- Steps 5+6 task 03 backend validation (2026-08-08, bemani-buddy repo, no deployment): TDD red — 5 new handler tests compile-failed on the absent `mod_song_speed` wire field (E0560/E0609) before the model regen. Green: `cargo build` OK; `cargo test` 236 passed 0 failed (231 baseline + 5 new); `cargo clippy --workspace --all-targets` exit 0 with 29 warnings — count IDENTICAL on the pristine tree (stash-verified), zero introduced. `cargo fmt --check` fails on the PRISTINE tree (211 diffs; local rustfmt 1.9.0-stable vs the committed compact style; repo pins no toolchain) — `cargo fmt` NOT run to avoid ~100-file maintainer-code churn; new code hand-matched to the committed surrounding style. Migration 012 applied to local MySQL; `.sqlx/` regenerated (3 modified / 1 removed / 1 added query JSON). Live save→load round-trip through a real DB is task 04 cabinet matrix leg (i).
- Steps 5+6 task 04 agent-half validation (2026-08-08, no deployment): TDD red — the extended python schema check failed the completed validator run on ONLY the missing `on_demand` section. Green: full validator (156 tests; on_demand checks all PASS — cold 75 ratio 64/85 latency 23 ms builds 1, cold 125 ratio 64/51 latency 16 ms builds 1, warm 0 ms builds 0 both rates, seam transactions Exposed with convert status forwarded, rate + source key invalidation, platform latency), Assist Tick validator, Windows target check (0 warnings — covers the req-24 runtime.rs change), whole-crate format, release build (DLL md5 fce4d53859d40e66173fa600d1296026, built NOT deployed).

## Deviations & open questions

- **Task-01 design strengthening — generation-to-song binding.** The merged-delivery model made arming song-agnostic (correct for movies/policy: every armed song genuinely commits), but the task specs' claim that song specificity could live entirely in the path-derived code missed an AUDIO hole: with no binding, the exposure seam (or the runtime's generation-keyed open cache) could serve/expose a DIFFERENT dance bank than the one whose FileManager RAM copy was redirected — recreating the rate-against-stock-audio class the two-stage invariant exists to prevent. Fixed inside task 01: `LifecycleState::bind_song` (64-bit song digest, set at the FIRST successful open redirect, cleared on every arm); the seam and the cached-path branch refuse any other song as `OpenNotRedirected`. Regression-tested. Consumers in tasks 02–04 need no changes.

- **RESOLVED BY DESIGN (2026-08-07, merged Steps 5+6 model) — arm scope over-suppressed non-diagnostic movies.** Original gap: `classify_scene26` (lifecycle.rs:175) arms on SESSION eligibility only and never checked whether the loaded song was the rate-affected song, so while the DIAGNOSTIC (one hard-coded song) was configured, EVERY eligible song's scene-26 entry armed and tentatively suppressed its background movie — including songs that played at 100% (cabinet-observed 2026-08-07: gen 2, the 100% middle song, logged `movie tentatively suppressed` yet created a `path=Stock` bank and never committed). Invisible on Wine (movies globally suppressed by `non_native_os_support`); would be a real bug on native Windows. Resolution: the merged delivery has NO song-specific forced list — the session-selected rate applies to whatever song the player selects, so every armed song genuinely commits at that rate and song-agnostic arming becomes CORRECT; tentative suppression through a failed (EarlyFailed) attempt is accepted design behavior ("tentative movie suppression lasts through the attempt"). Residual native-Windows movie validation remains deferred release evidence (Step 8).
- **DEFERRED BY MAINTAINER (2026-08-07) — Step 7 competitive-field score audit.** The maintainer is confident in the existing sanitisation (Step 4 cabinet-proved: suppressed rate-tainted stage saves, sanitised logout, backend absence) and will personally perform a final audit once the feature is complete. The merged Steps 5+6 delivery re-verifies the Step 4 containment oracles in its cabinet pass but does not expand the sanitizer.
- Task 3 report-extension (its Technical Requirements 1–2: extend the `song-rate-validation/v1` report with diagnostic-transaction checks + record song/source/generated/module/platform digests and exact rate) was NOT implemented — the plan's Step-4 gate is the cabinet oracle (plan.md:178–180), which passed conclusively. The still-meaningful half (digest/exact-rate recording) is now folded into the merged-delivery validator extension (step05 task 04, Technical Requirement 1).
- The shared parser allows padding between the empty seek segment and entry-name segment while retaining XACT's fixed header/BANKDATA/entry-metadata anchors; the serializer emits the canonical compact layout.
- Steps 1-2 added only pure audio and host-side cache/worker services; no hooks, runtime arming, LayeredFS/XACT integration, UI, or score behavior were added.
- Task 2 review aligned `scale_i32` with the actual rounded Q31 stub, rejected empty candidate intersections instead of escaping the search radius, and strengthened identity/tie/interior-loop/error coverage.
- Task 3 review made the memory ceiling non-bypassable, moved cancellation before large allocations, validates every generated ADPCM entry, and reports the post-encode/decode seam rather than the pre-encode PCM seam.
- Task 4 development evidence is complete. Release-corpus mode intentionally remains unclaimed until external stock/custom banks covering both entry orders are run on actual native Windows and CrossOver with explicit `--platform` values; generated fixtures are rejected as release corpus.
- Step 2 Task 1 uses fixed tagged little-endian length-prefixed cache-key fields; mutable LRU timestamps are serialized separately and cannot alter immutable artifact identity.
- Cache publication uses a dedicated store publication mutex, not the future coordinator mutex; this serializes destination checks/renames without allowing later worker state locks to span filesystem I/O.
- Generation request construction parses/hashes the source and computes exact target frames before coordinator entry; no source read, DSP, store I/O, wait, or callback runs while the coordinator metadata mutex is held.
- The production coordinator uses `GetDiskFreeSpaceExW`; deterministic host tests inject capacity. Cache-limit input is hard-clamped to the 1024 GiB design ceiling, while tests may use smaller limits.
- The expected injected worker panic prints through Rust's panic hook even though `catch_unwind` contains it; the worker records the panic, wakes the waiter, and successfully processes the following job.
- Release-corpus/native-platform evidence remains intentionally unclaimed. The existing `--require-corpus` fail-closed contract still requires external stock/custom banks and explicit native-Windows/CrossOver platform selection.
- Maintainer-requested addendum task (add to the end of this feature, after Step 8): make the validation harnesses self-contained. `scripts/validate_song_playback_speed.sh` currently hard-dies without a sibling `ddr-chart-tools` checkout (and `validate_se_bank_synth.sh` shares the pattern), so no local gate runs on a fresh clone of this repository alone. Demote the sibling parity comparison to an optional evidence tier (like the release corpus): run the full core suite self-contained, and record a skipped/absent sibling-parity status in the stable report when no checkout is supplied.
- Step 4 Task 1 stage-identity decision: both the scene-26 arm and the save-time claim read the GameWork stage counter (decoded from `premium_free_stage_inc` disp8). An ESS save delayed across the WaitSequence stage bump would decode stage+1 and fail the exact claim — conservative direction (suppress without consuming; the side stays blanket-suppressed until consumed or card-in reset). Upgrade path if Task 3 observes a mismatch: scan the savedata `/result` slots for the single non-virgin record (needs new ess.dll RE for the in-entry mcode offset).
- Observed pre-existing latent issue (out of Step 4 scope, legacy autoplay model): `load_receiver_trampoline` calls `score_guard::reset_session()` for BOTH sides on ANY successful profile load, so a hypothetical mid-session second-player card-in would erase the first side's session taint. Rate state deliberately avoids this via per-side positive-match reset; flagged for the maintainer.
- League-strip removal-failure handling: the strip necessarily runs after the sender builds the request, so the only fail-closed lever is signalling sender failure (return 0). The failure leg is realistically unreachable (the node was just located; removal is a null-guarded unlink) and is exercised host-side through the pure tri-state policy only; live behavior of a 0 return from the sender is unverified by design.
- Step 4 Task 2 deviation: the XACT slot is claimed at expose time (conversion path) rather than the design's pre-original claim — recovery only depends on Exposed slots carrying owner/nonce/file-id, and this keeps every unrelated wave bank free of slot churn (recorded in the task context).
- Observed and fixed a fuzzy-match edit hazard: an edit whose oldString was written from memory silently rewrote `worker.rs::request` to call a nonexistent method; restored from the `request_superseding` pattern. Treat edit oldStrings as requiring a fresh read of the file.
- Corpus-metric calibration finding (2026-08-06, first REAL bank through the pipeline — stock `abdt.xwb`): the transform itself is clean (exact 6214401/8285824 at 75%, deterministic, identity preserved, zero clipping, zero stereo-lag change, preview seam delta unchanged, 75.7 MB peak < 128 MiB) but two corpus gates fail on real polyphonic music: `pitch_error_percent` reads 106% at 75% (single-window autocorrelation octave-jumps on a full mix; reads 2.4% at 125% on the same audio) and `codec_snr_db` reads 29.1 dB vs the 30 dB floor (identical at both rates — it measures the SOURCE's own ADPCM round-trip, and the design calibrated 30 dB for synthetic sine content). The corpus artifacts are still written and usable; the release-corpus gates need real-music-appropriate metrics before Step 8 release validation. Listening checklist + live diagnostic remain the authority for musical content.
- The Step 4 diagnostic artifact was generated from the maintainer's stock `abdt.xwb` (mcode 38896) via corpus mode and staged on the CrossOver install at `data_mods/_diag/abdt-75.xwb`; `song_code` is the 4-character bank name `abdt`, never the mcode. Config + DLL applied to the live install 2026-08-06 (see Deploy & test log).
- Expose-after-convert-success fix (landed 2026-08-06): `conversion.rs::prepare_inner` reorders every fallible step (lease → redirect-ready → slot claim) BEFORE the native conversion; the generated GAME-VISIBLE path (`<game_visible_root>/<key>.xwb`, relative forward-slash) is converted through the ORIGINAL AVS conversion via an injected `convert: FnOnce(&str) -> Option<i32>` closure; the token is exposed strictly AFTER that conversion succeeds; `PrepareOutcome::Exposed` carries `{generated_path, convert_result, effective_rate}` and `ConversionEnv` gained `game_visible_root`. `runtime.rs::convert_streaming_xwb` now returns `Option<i32>` (the original's convert status, forwarded verbatim by the seam), takes the convert closure, and its exposed log prints the outcome's effective_rate + convert status; `runtime::init` takes the raw LayeredFS `mod_folder` and derives BOTH `CACHE_ROOT` (filesystem PathBuf) and the new `GAME_VISIBLE_ROOT` (`<mod_folder>/_cache/song_playback_speed` string) from it so they can never diverge (`lib.rs` 5b updated). `file_hooks.rs` passes `convert_generated_into_dest`: pre-poisons dest[0], runs the original into the REAL dest, and returns `Some(rc)` only when rc >= 0 AND dest was written AND the written native path contains the cache-key hex stem (conservative success proof); on `None` the stock fall-through re-runs the original over dest, healing any partial write. Diagnostic gap (d) closed: the 250 ms maintenance drain polls `clock_patch::snapshot()` and logs each newly committed generation exactly once (`COMMIT_LOGGED_GENERATION` latch, drain-only — the gameplay-entry oracle line is unchanged and independent); detours still never log. New tests: convert-time ordering probe (slot Entered-not-Exposed at convert, path starts with the injected game-visible root, status forwarded verbatim) and convert-closure-failure (lease released, slot Free, no exposure, EarlyFailed).

## Key facts for a cold resume

- Supported song banks are strict XWB v43/header 42, flags `0x00090001`, two entries, 2048-byte alignment, and stereo MS-ADPCM with raw block alignment 48.
- Entry names must be `<code>` and `<code>_s` in either physical order; preserve that order and XSB-visible indices.
- Source tails may be exact, one byte short, or two bytes short only when complete-block count matches logical duration; generated payloads are exact whole blocks.
- Shared pure XACT code belongs in `src/core/xact/`; Assist Tick keeps its fixed XSB/container policy and migrates codec primitives only after byte-parity proof.
- Never commit game-derived XWB inputs, generated transformed banks, local corpus paths, or cabinet logs.
- Step 2 cache code is entirely host-side under `src/services/song_rate/`; it is registered as a module only and installs no runtime hooks or callbacks.
- The stable development report at `target/song-rate-validation/report.json` now carries both Step 1 audio evidence and Step 2 cache evidence; target/temp artifacts remain ignored and uncommitted.
- Step 4 policy/score state (Task 1): lifecycle in `song_rate/lifecycle.rs` (pure) + `song_rate/runtime.rs` (windows glue, permanent scene callback); pending rate-save ledger + full-sanitization readiness + league tri-state in `score_guard`; save-time rate election in `custom_options_persistence`; stage counter via `stage_records::stage_counter()`; diagnostic config `song_playback_speed.diagnostic` gated on `layeredfs.developer_mode`, 75% only. Nothing appends to the ledger and no generated path exists until Task 2 wires the transaction.
- Step 4 transaction (Task 2): `song_rate/transaction.rs` (exactly-once create protocol, injected `TransactionParts`) + `song_rate/conversion.rs` (prepare/expose pipeline, injected `ConversionEnv`); windows glue in `wavebank_hook.rs` (real detour body) + `runtime.rs` (lazy coordinator under `<mod_folder>/_cache/song_playback_speed`, 250 ms maintenance drain, `convert_streaming_xwb` seam) + `file_hooks.rs`. The diagnostic bank is validated against the live source and IMPORTED into the real cache store (warm runs hit the entry; leases/eviction/quarantine machinery is the real Step 2 code). Fault selector: `DDR_SONG_RATE_FAULT` env (dev mode only) with values pre-original/post-original/token-mismatch/xact-reject/source-read/validation/conversion/maintenance-saturation.
