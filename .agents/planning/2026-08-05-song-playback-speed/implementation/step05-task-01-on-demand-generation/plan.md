# Plan: Step 5 Task 1 — Wire On-Demand Generation Into the Live Transaction

Status: Approved 2026-08-07 (auto mode — verified upstream approval lineage: task
`Generated By: code-task-generator 2026-08-07` from the maintainer-approved merged
Steps 5+6 breakdown; plan + design `Status: Approved 2026-08-05`; scalar rate range
25..=175 step 5 is the maintainer's recorded 2026-08-07 amendment.)

## Implementation shape

Five coherent slices, each TDD'd (red → green) through the host validator harness:

### Slice A — rate math domain (pure)
`rate::target_for_percent`: replace `matches!(percent, 75 | 100 | 125)` with
`(25..=175).contains(&percent) && percent % 5 == 0`. Tests: 25/175 exact ratios,
30/95/105/170 accepted, 20/180/76/0 rejected, DurationOutOfRange near 2^28 source
frames at percent 25 (the extreme-slow-rate reachability leg).
`clock_patch_tests.rs`: Q31 reference vectors for factors at 25% (≈0.25·2^31) and
175% (≈1.75·2^31 > i32::MAX — proves the 64-bit slot path) against
`scale_music_count_q31`, boundary inputs ±i32::MAX.

### Slice B — arm model (pure lifecycle)
- Delete `DiagnosticSpec`/`validate_diagnostic`/`DiagnosticConfigError`; add
  `normalize_desired_percent(i32) -> Result<ValidPercent, PercentError>`-style pure
  validation: multiple of 5 in 25..=175 (`Identity` for 100).
- `EligibilityInputs`: `diagnostic: Option<&DiagnosticSpec>` → `desired: Option<[i32; 2]>`.
- `IdentityReason`: `NoDiagnostic` → `NoRateSource`; add `IdentityRate` (desired == 100)
  and `UnsupportedRate` (invalid value fail-closed).
- `classify_scene26`: services → desired-present → course → entered/side → validate
  `desired[side]` → stage → Arm{percent}.
- Tests: matrix per side, 100-identity, invalid-value fail-closed, boundary 25/175 arm.

### Slice C — conversion pipeline (pure + store/worker integration)
- `is_diagnostic_dance_path(path, code)` → `dance_bank_song_code(path) -> Option<&str>`
  (any `sound/**/dance/<code>.xwb`; returns the stem). Callers gate on armed phase +
  Some(code).
- `ConversionEnv`: drop `spec`; add `requested_percent: i32` (from the armed lifecycle).
- Add pure `effective_source_path(static_replacement, resolve_stock_native)` helper
  (hoisted from runtime.rs) — static replacement wins.
- `validate_and_import` → `resolve_generated_bank`: fault source_read → read source →
  fault validation (SourceValidation) → `GenerationRequest::from_source(source,
  code_from_path, percent, now)` → `coordinator.request()` (warm/cold; failures map to
  `RefuseReason::Generation(BuildFailureKind)`) → quarantine check from the RESULT
  manifest → Ok((cache_key, manifest)). `validate_diagnostic_bank` deleted.
- `RefuseReason`: remove `DiagnosticValidation`, add `Generation(BuildFailureKind)`.
- Tests (rewrite conversion_tests fixtures to drive the REAL worker on synthetic banks):
  cold-build success 75 + 125 through open redirect (assert generated artifact parses,
  exact rate, phase RedirectReady); warm reuse (second call `warm_hit`, no re-transform —
  observable via store mtime/coordinator diagnostics); seam-after-open warm + exposes;
  structural refusal from Armed unchanged; source-read/validation/conversion faults;
  Generation failure via 1-byte-limit coordinator (admission); quarantine refusal;
  reload idempotence; worker reuse across two songs/generations; unrelated/nested path
  isolation (`dance_bank_song_code` negatives); phase-ownership (Identity → NotOurs = the
  pure zero-footprint gate).

### Slice D — coordinator limit plumbing
`worker.rs`: `DiagnosticCoordinator::new_with_limit(store, cache_limit_bytes)` (same
components as `new`, injected limit; `new` delegates). `model.rs` normalize tests
(default/clamp/zero/max) if not already present.

### Slice E — windows glue + config (compile-checked via cargo check, not host-run)
- `config.rs`: `SongPlaybackSpeedConfig { diagnostic: parse-but-ignore, cache_limit_gib:
  Option<u64> }`.
- `lib.rs` 5b: drop diagnostic validation; keep dev-mode-gated fault selector; log one
  INFO when the retired `diagnostic` key is present; pass `cache_limit_gib` to
  `runtime::init`.
- `runtime.rs`: remove `DIAGNOSTIC`; add `DESIRED_PERCENT: [AtomicI32; 2]` + pub
  `set_desired_percent(side, percent)` (Task 2 hook point); phase-first fast gate in
  `redirect_dance_bank_open`/`convert_streaming_xwb` (one atomic load at Identity), then
  `dance_bank_song_code`; sticky `RATE_ARMED_THIS_BOOT` set on first Armed (spawns drain,
  gates timeline via renamed `rate_recording_active()`); `coordinator()` uses
  `new_with_limit` from the init-normalized limit (INFO for Missing, WARN for clamps);
  scene callback gathers `desired`, gates session reads + identity logging on
  non-identity desired.
- `wavebank_hook.rs:53`: `diagnostic_configured()` → `rate_recording_active()`.

## Test scenarios → acceptance criteria map

- AC1 (on-demand through open redirect, exact rate 75/125 + 25/175 math): Slice A + C tests.
- AC2 (zero footprint at identity): Slice C phase-ownership test (pure); runtime fast-gate
  is one atomic load (review); existing identity_runtime validator section stays green.
- AC3 (two-stage invariant): existing structural-refusal + ordering tests must stay green
  with the new pipeline (no test deletions of Step-4 invariant coverage).
- AC4 (early failures → stock): fault legs + Generation/admission + quarantine tests; all
  assert EarlyFailed phase, no exposure, slots/leases clean.
- AC5 (config + gates): Slice D/E + normalize tests; five gates green; no deployment.

## Risks / notes

- conversion_tests.rs currently builds a pre-generated 75% output bank fixture via the
  transform — those fixtures become the WORKER's job; tests simplify (source bank only).
- The retired `xwb_path` fixture code paths in tests are deleted, not preserved.
- `tests.rs` cache-demo and Step-2/3 suites must remain untouched and green.
- Log-line vocabulary consumed by the cabinet oracle (`armed`, `open-redirect`, `exposed`,
  `committed`, `completed`) keeps its shape; only the init lines change.
