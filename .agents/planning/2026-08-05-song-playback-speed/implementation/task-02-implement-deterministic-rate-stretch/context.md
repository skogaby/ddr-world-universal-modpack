# Task 2 Context: Deterministic Rate Stretch

## Approval

- Generated task: `.agents/tasks/song-playback-speed/step01/task-02-implement-deterministic-rate-stretch.code-task.md` (`Generated By: code-task-generator 2026-08-05`).
- Source plan: `implementation/plan.md`, `Status: Approved 2026-08-05`.
- Required design: `design/detailed-design.md`, `Status: Approved 2026-08-05`.
- Mode: automatic. The approved task/design authorize implementation without another approach-approval stop.

## Prior Art And Existing Foundation

- `src/core/xact/` now provides the host-testable Task 1 format and codec boundary.
- ITGmania's `RageSoundReader_SpeedChange.cpp` was read as algorithmic prior art only. The approved design replaces its floating-point, per-channel behavior with fixed integer arithmetic and one joint-channel match path.

## Requirements

1. Add reduced exact `RateRatio`, checked target-block/frame arithmetic, Q31 conversion, content-to-wall conversion, signed half-away rounding, and saturating i32 clock scaling.
2. Implement the approved deterministic window/hop/match/search parameters from sample rate.
3. Use a Q32 nominal source phase, inclusive bounded candidate search, joint-channel i64 SAD, and the specified deterministic tie-break.
4. Apply one selected source start to every channel and integer fixed-point overlap.
5. Anchor source frame zero and the final logical source frame while producing exactly the requested output frame count.
6. Support an explicit source/output half-open loop context with cyclic source reads near the mapped boundary and report seam-relevant data.
7. Reject malformed/interleaving/short inputs with typed errors; use checked arithmetic and fallible large allocations.
8. Add focused host tests for 75/100/125 math, pitch, clipping, stereo coherence, boundaries, determinism, and cyclic loops.

## Validation Commands

- Host tests: compile `src/core/xact/mod.rs` with `rustc --test`, then run the produced test binary.
- Regression: `./scripts/validate_se_bank_synth.sh`.
- Repository gates: `cargo check --target x86_64-pc-windows-msvc`, `cargo fmt`, and `./build.sh` from the repository root.

## Implementation Paths

- `src/core/xact/rate.rs`
- `src/core/xact/stretch.rs`
- `src/core/xact/mod.rs`
- `src/core/xact/tests.rs`

## Assumptions And Boundaries

- The production rates remain 75%, 100%, and 125%; 100% stretching exists for tests only.
- DSP operates on interleaved i16 frames and does not use floating point for any production decision.
- Loop context supplies both source and already-mapped output half-open ranges; Task 3 owns loop-boundary mapping.
- Source and output must each be long enough to place the approved synthesis profile and terminal window; short entries fail rather than select a fallback algorithm.
