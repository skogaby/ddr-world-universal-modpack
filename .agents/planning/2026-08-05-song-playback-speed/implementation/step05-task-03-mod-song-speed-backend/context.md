# Task 03 Context — `mod_song_speed` backend persistence (bemani-buddy)

Task spec: `.agents/tasks/song-playback-speed/step05/task-03-add-mod-song-speed-backend-persistence.code-task.md`
(modpack repo). Implementation happens in the SIBLING repo `bemani-buddy`; this working
record stays in the modpack planning dir per the feature convention.

## Approval lineage

- Task spec carries `Generated By: code-task-generator 2026-08-07` (maintainer-approved
  breakdown recorded in the canonical progress.md Done section).
- Source plan `implementation/plan.md` carries `Status: Approved 2026-08-05`; Step 6
  covers the backend field.
- Design `design/detailed-design.md` (Backend Persistence) approved 2026-08-05; the
  75/100/125 enum wording is superseded by the maintainer's 2026-08-07 scalar amendment.
- **MAINTAINER AMENDMENT (2026-08-08, in-session):** the server MUST NOT validate the
  range of the incoming `mod_song_speed` value. Store verbatim (only-when-present, like
  every other `mod_*` field). Rationale: widening/narrowing the acceptable range in the
  client DLL must not require a server change. This supersedes the task spec's Technical
  Requirement 5 (multiples-of-5-in-25..=175 boundary validation) and the invalid-value
  test legs in Requirement 7 / Acceptance Criterion 2. Malformed (non-numeric) values
  still skip naturally because `child_i32` parse failure yields `None`.

## Requirements (post-amendment)

1. `"mod_song_speed": "s32?"` in BOTH `PlayerdataLoadOption` and `PlayerdataSaveOption`
   in `models/ddr_world/playdata_3.json`, placed after `mod_assist_tick` (end of the
   PersistMode::Full block, before the SaveOnly `mod_customize_*` block in the save shape).
2. Regenerate via `cargo run -p codegen -- models/ddr_world/ crates/bemani-protocol/src/ddr_world/`;
   never hand-edit `@generated` output; expect only the two new optional fields in the diff.
3. Migration `migrations/012_ddr_world_song_speed.sql`:
   `ALTER TABLE ddr_world_profiles ADD COLUMN opt_mod_song_speed INT NULL DEFAULT NULL;`
   (nullable is load-bearing — no server-side default; pattern = 008/011).
4. Thread `opt_mod_song_speed: Option<i32>` through:
   - `crates/db/src/models/ddr_world/profile.rs` (Modpack options block, after
     `opt_mod_assist_tick`)
   - `crates/db/src/mysql/ddr_world/profile.rs` (`row_to_profile!` + UPDATE column list
     + params, keeping the existing grouping)
5. Save ingestion in `handle_save_profile`: one more only-when-present line in the
   modpack-options block — `if let Some(v) = child_i32(option, "mod_song_speed")
   { profile.opt_mod_song_speed = Some(v); }`. NO validation.
6. Load echo: `mod_song_speed: profile.opt_mod_song_speed` in the load `<option>` block;
   `mod_song_speed: None` in `build_new_player_response`.
7. Tests in the existing `#[cfg(test)] mod tests` following the workout-field precedent
   (`option_with` helper): present parses, absent None, malformed (non-numeric) None,
   and None-skip on serialization (un-hooked clients see no new element).
8. Regenerate `.sqlx/` (`sqlx migrate run --source migrations/` +
   `cargo sqlx prepare --workspace`, DATABASE_URL from config.toml).
9. Gates at bemani-buddy root: `cargo build`, `cargo test`,
   `cargo clippy --workspace --all-targets` clean, `cargo fmt` clean.
10. NEVER commit/push/deploy in either repo. bemani-buddy convention: `touch` + `git add`
    new files BEFORE writing content.

## Build/test commands

- bemani-buddy root: `cargo build` / `cargo test` / `cargo clippy --workspace --all-targets` / `cargo fmt --check`
- sqlx regen: `export DATABASE_URL="mysql://bemani:bemani_dev@localhost/bemani_buddy"`
  (from config.toml), `sqlx migrate run --source migrations/`, `cargo sqlx prepare --workspace`.
  MySQL confirmed listening on localhost:3306; `sqlx` CLI at `~/.cargo/bin/sqlx`.

## Patterns observed

- Save handler is raw (`handle_playerdata_save_raw`), dispatches savekind 1/2/3; ALL
  paths that carry `<option>` go through `handle_save_profile`, so only-when-present
  there covers every savekind automatically.
- Existing `mod_*` fields have NO server-side validation anywhere — the amendment makes
  `mod_song_speed` exactly consistent with the block it joins.
- Test precedent (playdata.rs tests module): pure-function tests over an `option_with`
  XML node; no DB, no handler-context mocking exists in the crate.
- Generated optional outputs get `#[serde(skip_serializing_if = "Option::is_none")]`,
  which is the un-hooked-client protection.

## Files to touch (bemani-buddy)

- `models/ddr_world/playdata_3.json`
- `crates/bemani-protocol/src/ddr_world/playdata_3.rs` (codegen output)
- `migrations/012_ddr_world_song_speed.sql` (new)
- `crates/db/src/models/ddr_world/profile.rs`
- `crates/db/src/mysql/ddr_world/profile.rs`
- `crates/game-server/src/handlers/ddr_world/playdata.rs`
- `.sqlx/` (regenerated)
