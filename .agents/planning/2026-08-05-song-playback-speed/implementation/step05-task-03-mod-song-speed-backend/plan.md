# Task 03 Plan — `mod_song_speed` backend persistence

Status: Approved 2026-08-08 (auto mode via verified upstream approval: task spec
`Generated By: code-task-generator 2026-08-07`, plan `Status: Approved 2026-08-05`;
the no-validation change is a direct in-session maintainer instruction recorded in
context.md)

## Test scenarios (TDD)

All in `crates/game-server/src/handlers/ddr_world/playdata.rs::tests`, using the
existing `option_with` helper. The parse tests exercise the same `child_i32` +
only-when-present mechanics `handle_save_profile` uses (the handler itself needs a
DB and has no test seam — precedent: the workout-field tests).

1. `song_speed_present_is_parsed` — `option_with(&[("mod_song_speed","75")])` →
   `child_i32(...) == Some(75)`. Also a representative above-identity value (125).
2. `song_speed_absent_is_none` — option node without the field → `None`
   (only-when-present ⇒ stored value untouched).
3. `song_speed_malformed_is_none` — `("mod_song_speed","fast")` → `None` (skip, not
   clobber; parse failure is the only "rejection" the server performs).
4. `song_speed_none_is_skipped_on_load` — serialize a `PlayerdataLoadOption` with
   `mod_song_speed: None` via `xml_serde::to_xml_element` and assert no
   `mod_song_speed` child exists; with `Some(75)` assert the child exists with text
   "75" (un-hooked-client protection + echo proof).

Compile-red first: tests reference `mod_song_speed` on `PlayerdataLoadOption`
before the model regen, so the suite fails to compile until the codegen step — that
is the red state for the wire-format half.

## Implementation steps

1. Write the four tests (red: compile failure on missing field / behavior).
2. Edit `models/ddr_world/playdata_3.json` (both shapes, after `mod_assist_tick`);
   run codegen; verify diff is exactly the two new optional fields.
3. New migration `migrations/012_ddr_world_song_speed.sql` (touch + git add first).
4. Thread `opt_mod_song_speed` through db model + mysql row map/UPDATE.
5. Handler: save ingestion line + load echo + new-player `None`.
6. sqlx migrate run + cargo sqlx prepare (regenerated `.sqlx/` left in tree).
7. Gates: build, test, clippy, fmt.

## Risks

- `.sqlx/` regen requires the live MySQL instance to be the schema the migrations
  produce; `sqlx migrate run` applies 012 idempotently on top.
- The UPDATE statement's positional params must stay aligned — insert the new
  column/param at the same position in both lists (after `opt_mod_assist_tick`).
