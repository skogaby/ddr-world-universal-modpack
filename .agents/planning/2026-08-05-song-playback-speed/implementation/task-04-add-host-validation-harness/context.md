# Task 4 Context: Host Validation Harness

## Approval

- Generated task: `.agents/tasks/song-playback-speed/step01/task-04-add-host-validation-harness.code-task.md` (`Generated By: code-task-generator 2026-08-05`).
- Source plan: `implementation/plan.md`, `Status: Approved 2026-08-05`.
- Required design: `design/detailed-design.md`, `Status: Approved 2026-08-05`.
- Mode: automatic. The approved task/design authorize implementation without another approach-approval stop.

## Existing Pattern And Foundation

- `scripts/validate_se_bank_synth.sh` is the established throwaway Cargo harness pattern and remains a separate regression gate.
- Tasks 1-3 now expose the complete pure pipeline under `src/core/xact/`; direct host tests pass independently of the Windows DLL.
- Sibling `ddr-chart-tools` remains a path dependency used only for independent parser/codec comparisons.

## Requirements

1. Add executable `scripts/validate_song_playback_speed.sh` with sibling discovery via `DDR_CHART_TOOLS_DIR` and optional corpus discovery via `DDR_SONG_RATE_CORPUS_DIR`.
2. Run all pure module tests and a release-mode validation/demo harness without linking the DLL.
3. Cross-check shared XWB output and mono/stereo ADPCM bytes against the sibling implementation.
4. Generate synthetic 75%/125% demo banks only under `target/song-rate-validation/`.
5. Measure/report exact ratios, pitch error, codec SNR, clipping, stereo lag, seam, deterministic bytes, identity, latency, and peak estimated memory.
6. Emit stable `song-rate-validation/v1` JSON at `target/song-rate-validation/report.json`; exit nonzero on any failed required check.
7. Permit ordinary synthetic-only runs; `--require-corpus` requires a local supported corpus with both entry orders and a custom-named bank.
8. Corpus reports record relative names and full input digests without copying source banks into the repository.
9. Unknown options, missing sibling source, missing required corpus, and malformed required corpus fail actionably.
10. Run Assist Tick and repository build gates; close only Step 1 after the final demo passes.

## Validation Commands

- `./scripts/validate_song_playback_speed.sh`
- CLI failure checks for unknown option, missing sibling, and missing required corpus.
- `./scripts/validate_se_bank_synth.sh`
- `cargo check --target x86_64-pc-windows-msvc`
- `cargo fmt`
- `./build.sh`

## Implementation Paths

- `scripts/validate_song_playback_speed.sh`
- `.agents/planning/2026-08-05-song-playback-speed/progress.md`
- `.agents/planning/2026-08-05-song-playback-speed/implementation/plan.md`

## Assumptions And Boundaries

- Synthetic validation is sufficient to complete the host-only Step 1 implementation; release-corpus mode remains a stricter later release gate.
- The corpus convention is recursive `.xwb` discovery; release-required custom coverage is identified by a relative filename containing `custom`.
- Report/demo artifacts under `target/` are ignored and never staged.
