# Task 3 Context: Song XWB Transformer

## Approval

- Generated task: `.agents/tasks/song-playback-speed/step01/task-03-build-song-xwb-transformer.code-task.md` (`Generated By: code-task-generator 2026-08-05`).
- Source plan: `implementation/plan.md`, `Status: Approved 2026-08-05`.
- Required design: `design/detailed-design.md`, `Status: Approved 2026-08-05`.
- Mode: automatic. The approved task/design authorize implementation without another approach-approval stop.

## Existing Foundation And Comparison Source

- Tasks 1-2 provide strict borrowed XWB parsing, direct block encoding, streaming serialization, exact rate math, and deterministic stretching under `src/core/xact/`.
- Sibling `ddr-chart-tools/src/job/mod.rs` was read for orchestration comparison only. Its whole-buffer encode/write path is not reused because this task requires strict profile validation, sequential processing, cancellation, bounded memory, and exact transformed metadata.

## Requirements

1. Add a pure transform API taking source XWB bytes, exact song code, percentage, a seekable output writer, and a cancellation/checkpoint callback.
2. Resolve main and preview by exact names while preserving physical entry order, names, packed formats, sample rates, flags, alignment, and indices.
3. Decode validated logical frames, compute exact block targets, stretch both entries independently, direct-encode blocks, and stream one rebuilt XWB.
4. Map half-open loops with checked half-up arithmetic and reject invalid/zero/reversed mapped loops; validate loop seam continuity.
5. Preflight a conservative sequential peak-memory estimate and reject above 128 MiB before large allocation.
6. Check cancellation during source hashing, decode blocks, stretch windows, encode blocks, output writes, output hashing, and postcondition validation.
7. Compute full MD5 source/output digests without depending on the sibling CLI crate.
8. Re-read and strictly reparse generated output before returning manifest-ready metadata.
9. Add synthetic tests for both orders, 75/125, stock tails, identity preservation, cancellation, deterministic output, memory rejection, loop mapping, and digest/report correctness.

## Validation Commands

- Host tests: compile `src/core/xact/mod.rs` with `rustc --test`, then run the produced test binary.
- Regression: `./scripts/validate_se_bank_synth.sh`.
- Repository gates: `cargo check --target x86_64-pc-windows-msvc`, `cargo fmt`, and `./build.sh` from the repository root.

## Implementation Paths

- `src/core/xact/digest.rs`
- `src/core/xact/transform.rs`
- `src/core/xact/adpcm.rs`
- `src/core/xact/stretch.rs`
- `src/core/xact/xwb.rs`
- `src/core/xact/mod.rs`
- `src/core/xact/tests.rs`

## Assumptions And Boundaries

- The output writer is newly created/truncated and implements `Read + Write + Seek`; that supports streaming generation followed by mandatory postcondition reparse without retaining a second compressed bank during DSP.
- Task 3 uses a fixed 128 MiB default limit; a lower internal test limit exercises admission failures.
- Source/output full digests are MD5 as specified by the approved cache design.
- No filesystem cache, worker thread, LayeredFS, XACT call, hook, UI, or score behavior is added.
