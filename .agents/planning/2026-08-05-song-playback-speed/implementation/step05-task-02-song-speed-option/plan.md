# Plan: Step 5 Task 2 — Add the Player-Facing SONG SPEED Option

Status: Approved 2026-08-07 (auto mode — verified upstream approval lineage, same as
task 01: `Generated By: code-task-generator 2026-08-07` from the maintainer-approved
merged Steps 5+6 breakdown; plan + design `Status: Approved 2026-08-05`; scalar
25..=175 step 5/coarse 10 is the maintainer's recorded 2026-08-07 amendment.)

## Slices (TDD where host-testable)

### A — pure normalization (host red/green)
`lifecycle::snap_rate_percent(i32) -> i32`: clamp to 25..=175, half-up snap to the
nearest multiple of 5. Tests: in-domain passthrough (incl. 25/100/175), snap
(77→75? no: 77→75 via (77+2)/5*5=75; 78→80), clamp (i32::MIN→25, 0→25, 20→25,
180→175, i32::MAX→175), snapped result always satisfies `is_supported_rate_percent`.

### B — framework availability (registry host red/green; glue cargo-checked)
- `registry.rs`: `available: bool = true` on `RegisteredOption` (mutated only under
  the STATE mutex); `set_available` on the state; expose to snapshot readers.
- `mod.rs`: `pub fn set_option_available(id, bool)` (unknown id → one WARN per call
  site occurrence, no-op), `pub fn row_injection_available()` (`is_available() &&
  rows::is_scalar_ready() && builder_hook::is_ready() && FILTER_READY`), latch
  `FILTER_READY` from `filter_hook::init`.
- `builder_hook.rs`: snapshot filter drops unavailable options (silent
  non-injection; open forms untouched by construction).
- New host tests `custom_options/availability_tests.rs` against a LOCAL
  `RegistryState` (no global pollution): default available; set false → excluded
  from the injection view; set true → included again with the SAME handle (no
  re-registration); unknown id no-op; values/persistence untouched by availability.
- Harness: add `services::custom_options` mini-module (api + registry + tests) to
  the validator's generated main.rs + file-existence list.

### C — the mod + runtime readiness (cargo-checked; cabinet legs in task 04)
- `runtime.rs`: `pub fn integration_ready() -> bool` = BOOT_READY &&
  `score_guard::is_full_sanitization_available()`.
- `src/mods/song_playback_speed.rs`: Mod impl (id `song-playback-speed`);
  `enable()` gates on `row_injection_available() && integration_ready()`
  (self-disable + WARN otherwise, `is_active` reports it), registers `song_speed`
  scalar (25, 175, step 5, Integer, coarse 10, default 100, `PersistMode::Full`
  default, load transform = snap, on_change = snap → `set_desired_percent`),
  treats `Duplicate` as success, reseeds via registry values on re-enable, then
  `set_option_available(true)`. `disable()`: `set_option_available(false)` +
  desired percents → 100; active attempt untouched (lifecycle owns it).
- Register in `src/mods/mod.rs` + the `lib.rs` instance list (step 7/8, before the
  atlas flush at step 10).
- No scene callback and no arm calls in the mod: task 01's permanent runtime
  callback already consumes the desired atomics.

### D — assets + config example
- `scripts/gen_option_labels.py`: add `("song_speed", "SONG SPEED")`; run;
  commit the generated `seop_item_song_speed.png`.
- `mod-config.json`: `"song-playback-speed": true` in mods; add `song_speed` to the
  `custom_options.row_order` example.

## AC map
- AC1 (registration/availability): Slice B host tests + builder-filter code review +
  task 04 cabinet leg (boot-disabled→enable flip).
- AC2 (per-side selection + normalization): Slice A tests + task 01's classifier
  tests (entered side selects) + task 04 round-trip.
- AC3 (eligibility fail-closed): task 01's lifecycle matrix (already green).
- AC4 (next-song-only/disable): structural (atomics read only at arm) + disable
  path review + task 04 live leg.
- AC5 (gates): five gates green, no deployment, on_change provably one atomic store.

## Risks
- The harness mini-module must not drag windows deps: api.rs + registry.rs only.
- `registry.rs` doc says entries "never mutated afterward" — amend the doc where
  the availability flag changes that contract (values already were mutable).
