# Task 1 Context: Shared XACT Format Codecs

## Approval

- Generated task: `.agents/tasks/song-playback-speed/step01/task-01-add-shared-xact-format-codecs.code-task.md` (`Generated By: code-task-generator 2026-08-05`).
- Source plan: `implementation/plan.md`, `Status: Approved 2026-08-05`.
- Required design: `design/detailed-design.md`, `Status: Approved 2026-08-05`.
- Mode: automatic. The approved task/design authorize implementation without another approach-approval stop.
- `CODEASSIST.md` is absent. `AGENTS.md`, `CLAUDE.md`, and the approved feature artifacts are authoritative.

## Source Baseline

- Focused port source: sibling `ddr-chart-tools` revision `fa3500bd65ca0350411ef5113070055340eb5a6d`.
- Source modules: `src/xwb/container.rs`, `src/xwb/adpcm/`, and `src/util/io.rs` in that repository.
- Intentional deltas: borrowed entry data, strict DDR song-bank profile validation, typed dependency-free errors, checked arithmetic, logical-duration tail validation, exact-block direct interleaved encoding, and no whole-song deinterleaved channel copies.

## Requirements

1. Add dependency-free shared modules under `src/core/xact/` and export them from `src/core/mod.rs`.
2. Parse only the approved two-entry streaming XWB v43/header 42 profile and return borrowed names/data.
3. Reject malformed segment framing, bank metadata, identity, format, data ranges, durations, loops, and stock-tail equations with typed errors.
4. Accept exact, one-byte-short, and two-byte-short stock tails only when complete blocks match logical duration; decode complete blocks and trim to logical frames.
5. Encode arbitrary-channel interleaved PCM deterministically without padding; input must contain complete frames and complete ADPCM blocks.
6. Serialize replacement payloads while preserving bank name, entry names/order, packed formats, sample rates, build time, flags, and XSB-visible indices.
7. Emit only complete generated ADPCM blocks with deterministic 2048-byte entry alignment.
8. Use synthetic fixtures only and document the sibling revision and deltas.
9. Migrate Assist Tick's compatible codec calls only after byte-parity tests pass; keep its XSB and fixed container policy local.

## Acceptance Evidence

- Host unit tests compiled directly from `src/core/xact/mod.rs` with `rustc --test`.
- `./scripts/validate_se_bank_synth.sh` proves Assist Tick byte identity, silence behavior, and container placement against the sibling implementation.
- Repository gates run from the repository root: `cargo check --target x86_64-pc-windows-msvc`, `cargo fmt`, and `./build.sh`.

## Implementation Paths

- `src/core/xact/mod.rs`
- `src/core/xact/xwb.rs`
- `src/core/xact/adpcm.rs`
- `src/core/mod.rs`
- `src/services/se_bank_synth/adpcm.rs`
- `scripts/validate_se_bank_synth.sh`

## Assumptions And Boundaries

- Song code is the non-empty, null-terminated bank name; the two entry names must be that code and `{code}_s` in either order.
- Source tail remainders are `0`, `block_align - 1`, or `block_align - 2`; other partial blocks are rejected.
- Serializer replacements carry generated duration/loop metadata but cannot change entry identity or packed format.
- No runtime hooks, cache, LayeredFS integration, UI, score policy, or game-derived fixtures are part of this task.
