# DDR SELECTION — progress

Updated: 2026-09-24
Status: Step 7 of 14 — DONE (cabinet-proven 2026-09-24, uncommitted); Step 8 not started
NEXT ACTION: start Step 8 (legacy life gauge — `implementation/plan.md` Step 8): RE first (research doc per the Step 5–7 pattern; `research/hud-actors.md` §1 is the starting point), then implementation. When `dance_gauge` turns legacy, the `gauge` marker moves with it automatically (`marker_keys` gate).
Resume protocol: read `implementation/plan.md` (checklist), `design/detailed-design.md`, then the research files (`research/hud-layout-stage-frame.md` for Step 7, `research/stage-panel.md` for Step 5, `research/end-banners-sel-movies.md` for Step 6); this file is the live state.

## Done

- PDD: register (D1–D29, Readiness Confirmed), design Approved, plan Approved (2026-09-22).
- Pre-implementation RE (2026-09-22): helper `FUN_18006b710` byte shape identical on 20250805/20260224/20260721/20260825/20260915 (prologue AOB unique on all five); `GameWork+0xA8` read site (DPS identity table) unique on all five, offset 0xA8 everywhere; no reader of `LayoutActor+0x190` other than `onInitialize`; record insert `FUN_18006ec30` and list push `FUN_18006db60` COPY their inputs (a mod-owned `std::string` view is safe); `FUN_1801aca30` dedupes by name; `LayoutActor` slot 5 releases every pushed name.

## In flight

- Step 7 (2026-09-24, uncommitted, cabinet-proven) — legacy element positions, legacy stage frame, danger 3–5, BPM / name hidden, per `research/hud-layout-stage-frame.md` (§11 = as built):
  - **`src/services/hud_layout_hooks.rs`** (new, lib.rs 4h2b): the ONE owner of the `LayoutActor` marker builder + setter detours, promoted from center_arrows_single (builder pre / post, setter pre subscribers; `set_marker` runs the setter subscribers then the original ⇒ center-arrows' lane shift applies after our overwrite). center_arrows_single now subscribes (builder pre = its pass state, setter pre = its shift) and its disable only turns the subscribers off; its song-info dark-card detour stays in its own file (Step 10 will need it shared — deviation noted). No center-arrows validator exists (the plan assumed one) — its regression check is the cabinet run.
  - Signatures: AOBs `hud_layout_side_loop`, `hud_layout_reverse_store`, `hud_layout_judge_pos_call`, `ddr_sel_stage_frame_export`, `ddr_sel_stage_frame_texture` (all unique on all five builds). `derive_hud_layout` → `hud_layout_builder_entry` (the old center-arrows prologue-AOB / cluster − 0x1DC logic, moved) + all-or-nothing `hud_layout_style_off` 0x84 / `hud_layout_reverse_off` 0xE4 / `hud_layout_judge_pos_vslot` 0x298 (`HudLayoutSites`, `hud_layout_sites()`); `derive_ddr_sel_stage_frame` (RTTI StageFrameActor slot 4 / 8, strings `dance_stage` / `dast_stage_`, imm 0xB) → `ddr_sel_stage_frame_export_lea` / `_texture_site` (`DdrSelStageFrameSites`). Sweep ALL GREEN; `shape_diff`: old builds diverge only in frame displacements / after the read fields.
  - `marker_keys.rs` (pure, 10 host tests; validator 86): key → A3 marker (nesting, `{r}` reverse difficulty, lane groups), gates (package-gated keys stay World's until their adapter lands), World-only keys, root names (skin 1 = `dance_common0000_v2`), World's coord / arrow math, hidden coord.
  - `markers.rs` (engine): builder POST subscriber — legacy root (pushed by the package helper onto the LayoutActor load list, no record) → transient `dance_root` layer → A3 markers read exactly like World's builder (incl. the two lane-content loads: judge group by `judge_position XOR reverse`, arrow group by `reverse`) → `set_marker` → layer destroyed; World-only `bpm` / `name` parked at (−4096, −4096) (the actors position them once at init).
  - `stage_frame.rs` (engine): checked patches of the StageFrameActor export LEA (→ near `"stage_frame"`) and the texture prefix assign (imm 11 → 22, → near `"stage_frame000N_stage_"`), applied right before the helper registers `dance_stage_frame000N` (patch failure ⇒ stock), restored on any stock `dance_stage` request, at disarm and disable. Chosen over two detours (documented).
  - `policy.rs`: `adapter_for`; `mod.rs::adapters()` gains `Markers` (post-pass installed) and `StageFrame` (patch sites ready ∧ Markers); `dance_danger` 3–5 also needs the skin's root arc.
  - Gates: `cargo check` clean, `cargo fmt`, `./build.sh` 0 warnings, validator 86 tests, signature sweep ALL GREEN.

- Step 6 (2026-09-24, uncommitted, cabinet-proven runs #1–#3) — legacy end banners + `_sel` movies, per `research/end-banners-sel-movies.md` §5 / §7 (as built):
  - Signatures: `derive_ddr_sel_panel` also publishes `ddr_sel_shutter_cleared_row` / `_failed_row` (stage row + 1 / + 2 strides, content-gated, optional) and `ddr_sel_shutter_cleared_kind` / `_failed_kind` (stage kind + 1 / + 2; `DdrSelPanelSites::banner_rows`). New AOBs `ddr_sel_sma_movie_gate` (SceneManageActor::onInitialize's movie gate) and `ddr_sel_movie_sel_test` (MovieActor init's `+0x149` test) → `derive_ddr_sel_movie` (all-or-nothing; `DdrSelMovieSites`, `ddr_sel_movie_sites()`). Sweep ALL GREEN on all five; `shape_diff` reviewed (gate diverges only at the VIDEO SIZE instruction, both shapes accepted; the `_sel` test is identical).
  - `banner_logic.rs` (pure, 13 tests) + `banner.rs` (engine; called from `panel.rs::update_hook`, no second detour): one-update CLEARED / FAILED row patch (`{"common_shutter000N", "shutter_clear"/"shutter_failed", "", "", ""}`), overlay `00_cleared` / `00_failed` / `00_prayforall` (Tohoku EVOLVED mcode 37789, skin 4 only — decided after run #1) from World's copy of the package (`lookup_unowned`, never a ticket), group 5 raw 98, parked at 1 → 2, `in` at 2 → 3, `out` at 5 → 8, destroyed at its own `end` (+ a pre-original safety net at state 8). The session follows World's states only (outlives the disarm). Hosted only for requests made in GAMEPLAY (the DPS step-8 request — ResultSequence's end-of-results kind-4 request stays World's: its package name would be re-requested during the first banner's deferred destroy). Refusals (World's banner, one WARN): package missing / still held by our tickets / already resident / row patch failed / a previous banner still live.
  - `sel_movie_logic.rs` (pure, 4 tests) + `movie_sel.rs` (engine): one `GenericDetour` on `SceneManageActor::onInitialize`. Armed ∧ `<name><suffix>_sel.wmv` exists (LayeredFS, then `data/`) ⇒ movie-less songs get the entry's `+0x141` movie byte set to 4 for that one call (restored post-original), then the new MovieActor (RTTI-verified at `+0xD8`) gets `+0x149 = 1`; a frame callback logs the file it opened. VIDEO SIZE OFF stays OFF.
  - `mod.rs`: `armed_mcode()` (latched at the arm), module wiring; `afp_route` also routes while `banner::live()`; `bm2d_package::held_by_tickets`. `panel::arm` refuses (World's stage panel, WARN `… is still World's (a legacy end banner)`) while World's banner copy of `common_shutter000N` is still registered — a ticket would only borrow it and World's refcount-less release would destroy it under the panel (the reverse of the banner's own `held_by_tickets` refusal).
  - Gates: `cargo check` clean, `cargo fmt`, `./build.sh` 0 warnings, validator 74 tests, signature sweep ALL GREEN.

- Step 6 RE (2026-09-24) — `research/end-banners-sel-movies.md`. Findings: (1) World's CLEARED / FAILED are ShutterActor kinds 4 / 5 (old builds 2 / 3), requested by DPS step 8, opened by ResultSequence (`0x1008`); World has NO code fill for them — only the kind row, which sits at stage row + 1 / + 2 strides on every build, so the Step 5 one-update row patch + detour extend to them with no new signature. A3 added a second overlay clip (`00_cleared` / `00_failed` / `00_prayforall` for Tohoku EVOLVED, raw priority 98) on top of `shutter_clear` / `shutter_failed`; World dropped ENJOY DDR / PRAY FOR ALL. PRAY FOR ALL art exists for skins 1 and 4 only. Every banner cue is in the `dsel` bank. (2) The in-lane game over is NOT a bug: GamePlayActor forwards gauge-empty `0x103a` → `0x103c` only on EXTRA stages (Option `+0x200`); A3's normal-play predicate is `return 0` — neither game shows it on a normal fail; nothing to do. (3) `_sel`: World's MovieActor init tries `<basename>_sel` first when `+0x149` is set (dead flag, identical on all five builds); `SceneManageActor::onInitialize` creates the MovieActor at `+0xD8` before its init runs ⇒ a post-original detour there can set it. Only 7 of the 18 `_sel` songs have a World movie; A3 created the actor for the other 11 and only for songs picked in the DDR SELECTION folder.

- Era Cut-In toggle (2026-09-23, uncommitted, cabinet-proven; maintainer request after run #2 — the cut-in adds a few seconds and may divide players): GLOBAL SETTINGS → DDR SELECTION → **"Era Cut-In"** ON / OFF (default ON = A3). New `settings.rs` (row `ddr_selection_era_cutin`, live atomic, `persist_section` writes the whole `ddr_selection` section) + `config::DdrSelectionConfig { era_cutin }` (+ the repo `mod-config.json` entry). `panel::arm` latches it per panel: OFF ⇒ the cut-in / cut-in-bg packages are not requested and the fill never starts a cut-in, so the machine's no-cut-in path runs (root plays `in` at World's swap — A3's own "cut-in not loaded" branch). START still skips a playing cut-in. Log: `armed … (skin N: … , era cut-in off)`, `filled (… , era cut-in off (setting))`, on edit `era cut-in OFF (next song)`; the enable line now reports `(era cut-in ON|OFF)`.

- Step 5 rev 2 (2026-09-23, uncommitted, cabinet-proven run #2) — fixes for cabinet run #1 (see Deploy & test log) + the 20250805 floor:
  - **Just-in-time hosting** (`panel.rs::update_hook`): pre-original on the update whose state 0 has `pending == stage kind` (the kind-art loader's only read of the table). During song select (`panel::on_scene_change`) it resolves the song right there (`mod.rs::stage_panel_request_skin` → the factored `resolve_song()`, the same inputs + mode policy as the play-edge `arm()`; the confirm already committed the song) and creates the session (`panel::arm(skin, origin)`, now idempotent, no row patch). The row is patched for THAT ONE update and restored post-original (`after_row_read` INFO names the shutter state/pending World left). A later request in the window gets World's own panel (INFO). The edge `arm()` reconciles: same skin ⇒ nothing (its INFO says `stage panel hosted at the song-select request`); stock / refused ⇒ `panel::drop_if_unadopted()` unless World already read the row (then A3's panel stays, WARN); different skin ⇒ the panel keeps its skin (WARN). Leaving the window disarms when armed OR hosted.
  - **Adoption gate (Bug B)**: new machine action `Adopt` at state 1 → 2 — the pending stage clip must have `choice_stage_usr2`, else abandon. Only then `ROOT_LIVE` (`panel::root_live()` / `handles_dismissal()`): World's stage voice (`code_se`, now gated on it), the intro's World-art dismissal and the READY? dwell seeding stand down. Cleared at the release, abandon and disarm. The disarm WARNs `never hosted -- N stage-panel request(s) seen, row read by World / never read, last shutter (state, pending, active)` when a session never adopted a root.
  - **Fill waits for the packages**: new phase `FillWait` (`Frame::art_ready` = all four era packages ready): the fill runs as soon as they are, at the latest at World's swap (partial), and is skipped if World drains first. 3 new host tests (validator 57).
  - **AFP route** also routes while `panel::root_live()` (the panel's embedded sounds now play in scene 25, before the edge arms the skin).
  - **Old builds hosted**: `derive_ddr_sel_panel` shape-checks the un-null-checked `LEA "jacket_usr"; MOV RCX,[RSI+slot]; CALL find; MOV DL,1; MOV RCX,RAX; CALL SetVisible` before the stage tail and publishes `ddr_sel_shutter_jacket_vis_call` (20250805 +0x342C6, 20260224 +0x33B36); new builds publish `ddr_sel_shutter_jacket_vis_guard` (informational; `report.py` ALT_GROUPS pair). `host_ok` = guard OR that site ⇒ 1 on all five. `panel::guard_jacket_call` runs pre-original on every update about to run state 2 for the stage kind and NOPs the 5-byte CALL (`0F 1F 44 00 00`, checked patch, stock bytes read at init) exactly when the pending stage clip has no `jacket_usr` (restored otherwise) — decided from the clip, not the session, so a root that outlives a disarm / disable is still covered. Old loader / update / msg RE'd against 20260825 (`research/stage-panel.md` §1.5). Sweep ALL GREEN; `shape_diff` identical on 20260721+ (old builds diverge in the cross-checked field displacements only).
  - RE: `research/stage-panel.md` §1.4 rewritten (who requests kind 3 — `FUN_1800bc120` is `ResultSequence::onUpdate`, `FUN_180154940` the dan `GradePreparePanel`; the versus confirm inlines the request after its commit), §1.4a (the named-package loader), §1.4b (World's fill on A3's root is harmless), §1.5 (old builds), §4 (mechanism rev 2).

- Step 5 (2026-09-23, uncommitted; cabinet run #1 failed → rev 2 above) — A3's legacy stage panel, era cut-in, stage call. RE: `research/stage-panel.md` (World ShutterActor kind-art loader / fill / states / voice / msg, A3 `FUN_1800306c0` / `FUN_180030d10` / `FUN_18002f5f0` / `FUN_1800328c0` / voices, labels of every legacy clip, the `CLayer::SetPriority` inversion, timing vs the 25→26 arm, the CrossOver "no label" log explained). New:
  - Signatures: AOBs `ddr_sel_shutter_swap`, `ddr_sel_shutter_stage_tail`, `ddr_sel_shutter_kind_table` (+`_v1` old layout); `derive_ddr_sel_panel` (all-or-nothing, RTTI `ShutterActor` slot 6 → `ddr_sel_shutter_update`, swap/tail inside it, kind offsets cross-checked vs `shutter_actor_layout`) → `ddr_sel_shutter_stage_voice`, `ddr_sel_stage_voice_jz` (the voice fn's mute `JZ`, skipped block holds the slot-3 play), `ddr_sel_shutter_stage_row` (+ stride), `ddr_sel_shutter_basename_off` / `_jacket_off`, `ddr_sel_panel_host_ok` (1 on 20260721/0825/0915, 0 on 20250805/20260224 — their state 2 SetVisibles `jacket_usr` without a null check, and the legacy root has none). `DdrSelPanelSites` + `ddr_sel_panel_sites()`. Sweep ALL GREEN on all five; `shape_diff` identical on 20260721+ (old builds diverge — derivation input only, host_ok 0).
  - `bm2d_api`: `mc_load_movie` (+`_available`; `afp_mc_load_movie` export, independent optional cell), `layer_id_is_valid`, `layer_set_priority_raw`. `game_audio::stop_cue_in_slot`. `shutter`: `snapshot_of(actor)`, `kind_clip(actor, kind)` → `KindClip {object, layer, mc}`. `package_helper::probe_arc`. `sound::{play_era_cue, stop_era_cue}` (moved out of `intro`).
  - `panel_logic.rs` (pure, 15 host tests; validator now 54): package names, A3 stage band / stage call / cut-in SE / jacket rules, `clayer_priority`, and the `Panel` machine (Waiting → Filled → CutinHold → Showing → Parked → Draining → Gone; cut-in skip window, voice once, cut-in destroyed at `data_release`, `frame_out` on state 6, READY dismiss from 4/5/6 only, World's own drain resumes a held root, stalled-drain unblock once, `abandon`).
  - `panel.rs` (engine): see the AGENTS.md row. Arm (25→26, before World requests kind 3) probes `common_choice_v2`, requests our 4 packages, patches the stage row; the detour does the rest; disarm restores the row, stops the cut-in SE, destroys our cut-in layer and queues the tickets (released once World's root layer id is invalid + 60 frames). A root without `choice_stage_usr2` is abandoned (World-art dismissal + World's stage voice come back).
  - `intro.rs`: `SKIP_WORLD_DWELL` retired — the dwell is seeded by `panel.rs` only while A3's panel owns the dismissal; the World-art dismissal stands down when `panel::handles_dismissal()`; **legacy READY / HERE clips now raw priority 95** (Step 4 used raw 5, i.e. behind everything — A3 draws READY over the panel's `out`).
  - `code_se`: third site `stage voice (vo_stage_*)` gated on `panel::handles_dismissal()`.
  - Not done (follow-ups, documented in `research/stage-panel.md` §4): the score sets (high score / rank / FC mark / dancer name / area / target — hidden for now), the SD root on machine types 0/1, SD scaling of the cut-in.


- Step 4 (2026-09-23, uncommitted, cabinet-proven) — legacy READY / HERE WE GO + World-intro suppression. RE: `research/ready-go.md` (A3 ReadyGoActor decompile, the `dance_message000N` clip labels/sounds, World's CMA cascade and ShutterActor intro states, the `shutter_play` labels). New:
  - `src/services/shutter.rs` — ShutterActor snapshot / bannerless `0x100c` dismiss / drain unblock, **promoted from `quick_restart_or_fail`** (QR now calls it; its log lines are unchanged). Init in `lib.rs` step 4h2a.
  - `song_reset::intro_cascade_step()` (highest CMA StackStep of the live DPS: 1 READY, 2 HERE, 3 OUT) + `intro_cascade_available()`.
  - `bm2d_package::lookup_unowned(name)` (no-ticket registry lookup of a package another owner keeps resident) and `bm2d_api::mc_current_frame` (u32 `0x1010`).
  - Signatures: AOB `ddr_sel_dps_ready_dwell` + `derive_ddr_sel_intro` → `ddr_sel_dps_ready_timer_off` (0x130 on all five; threshold 5.0 s checked, shutter global cross-checked); `derive_ddr_sel_vo_ready` (const pattern, exactly 2 hits in the ShutterActor update, both LEAs = `"vo_ingame_ready"`, JZ → lock release) → `ddr_sel_vo_ready_jz_0/1`. Sweep ALL GREEN on all five; `shape_diff` identical.
  - `intro_logic.rs` (pure, 9 host tests: the ReadyGo machine incl. several messages per frame / late clips skip / no-HERE, `here_voice`, A3 `is_final_stage`, `should_dismiss_world_panel`, `seeds_dwell`) — validator now 39 tests.
  - `intro.rs` (engine): `input_manager::on_frame` driver; `dance_message` turns legacy only when `intro::capable()` (policy adapter `ReadyGo`); raw AFP layers `00_ready` / `00_here` from the LayoutActor-owned `dance_message000N`, group 5 / prio 5, parked invisible; cascade → play / goto `out` (op `0xF09`); skin 1 HERE voice `ACT3_1` / `ACT4_2` from the era bank; World's panel dismissed once its `stage_out` reveal reaches `ready_loop` (label unreadable ⇒ at once; READY at the latest); DPS READY? dwell seeded (1000.0) during steps 1..=5; clips destroyed at the GAMEPLAY-exit scene change, on a package change, on a DPS change, or once both reached `end`.
  - `code_se` generalised (per-site gate fn, up to 2 bytes per site): `vo_ingame_ready` ×2 silenced while the legacy clips exist (`intro::legacy_intro_live`).
  - Not done (by design): lesson `00_howtoplay` (attract only — never armed), A3's SD-cabinet clip scaling, World's stage voice + panel (Step 5).

- Step 3 fix (2026-09-23, uncommitted): **full-combo SE doubling.** New AOB `ddr_sel_fullcombo_se_site` (FullcomboActor::onMessage+0xB7: the inlined `MOV RBP,[audio_mgr]; MOV RDI,[RBP+0x30]; TEST; JZ release; … LEA RDX,["se_game_fullcombo"]; … GetCueIndex; … Play; … MOV R8D,2; CALL register_handle` block, 107 bytes — the first 35 alone also hit the song-select `se_common_decide_a` play) + `derive_ddr_sel_code_se` (gates: LEA at +0x13 → `"se_game_fullcombo\0"`, match+0x0E is `JZ rel8` landing on `TEST ECX,ECX; JLE` = the AVS lock release, site within 0x200 of `fullcombo_actor_on_message`) → derived `ddr_sel_fullcombo_se_jz`. Unique + resolved on all five builds (JZ @ +0x66675 / +0x656C5 / +0x69D15 / +0x69CC5 / +0x6A4C5), sweep ALL GREEN, `shape_diff` identical through 0x90 on all builds. **Mechanism changed from the proposed disp32 +17 to a one-byte `JZ`→`JMP` (`74`→`EB`, rel8 kept)**: the null-bank branch is World's own "no se_normal bank" path — skips GetCueIndex/Play/handle registration, still releases the AVS lock — and a 1-byte opcode flip can never tear an in-flight decode (a disp32 rewrite is 4 bytes and keeps a pointless GetCueIndex("") call). New `sound/code_se.rs`: a site table (`cue`, `package`, `replacement`), `init` (re-checks the stock `74`), `sync()` via `memory::apply_checked_patch` (expected-byte-verified; a failure disables the site for the session); applied only when `legacy_package(package)` AND the replacement cue can actually play (`afp_route::is_installed()` ∧ `bank::slot()` ∧ `bank::lookup("XAC_full_combo2")`) — otherwise World's sound is the only one, so it stays. `sync()` runs from `note_legacy` (first time a package turns legacy — the package helper, game thread), arm, every armed scene change (catches a late bank registration) and disarm (restore — `disarm` now calls it before its early return). S-Marvelous' post-original detour on the same function is untouched (the patch is 0xC5 past its prologue; S-Marv reads no handler bytes). Log: `DDR SELECTION: World's se_game_fullcombo silenced for this song (the legacy dance_fullcombo clip plays XAC_full_combo2)` once per song.
- Step 3 (uncommitted): RE `research/afp-sound-routing.md` (World's `bm2d::SoundCallback::play` picks ONE slot by name — `vo_*`/25-name A3 table → 3, else 2 — and a miss is silent; A3 relied on the same code with `_n` banks in slots 2/3). New: `src/mods/ddr_selection/sound/{mod,cues,bank_build,bank,afp_route}.rs`; `bank_build` + `cues` pure (host tests + a real-bank leg in `validate_ddr_selection.sh` that extracts the `_n` banks from `$DDR_WORLD_INSTALL`: 74 cues, 207 waves, 18.0 MB, validator green). XSB writer copies sounds / complex cues / type-1 variation tables verbatim with remapped offsets (engine rules read from `xactengine2_10.dll` `FUN_0040e970`/`FUN_0040d810`: variation tables contiguous in complex-cue order from 0x32, hash table right after the last one); flags-0x04 sounds accepted only with World's own RPC tail `07 00 01 f8 00 00 00`. XWB: one in-memory bank, verbatim wave bytes/format. Registration at the first scene change after the build (slot 4 only — refuses unless 0..3 stock + 4 empty) + a `GetCueIndex` self-check of every cue. AFP route: signature `afp_sound_callback_play` (+ `derive_afp_sound_callback`: RTTI slot 1 + tail→`se_play`; sweep green, shape-identical on all five), pre-original detour, ARMED ONLY, lock-free (`game_audio::se_play_from_sound_callback`, `bank::lookup`), stock pan rule; counters drained into one INFO per window. `game_audio`: `BankHandle::slot`, `cue_index`, `se_play_from_sound_callback`.

- Step 2 remainder (2026-09-22, uncommitted): `scripts/ddr_selection/{import_a3_assets.sh,import_a3_assets.bat,a3_assets.manifest}` — simple copy (maintainer call: no hashing): `always` = `arc/bm2d/dance_combo0005_v0.arc`, `missing` = the other 89 files (copied only if World lacks them); World path defaults to the folder above the script; release archive ships them in `ddr_selection_import/` (bat → CRLF). Tested: bash (stock World: 1 file; empty World: 90), bottle `cmd` (same; Wine's cmd hands blank lines to `for /f` — guarded). `docs/ddr_selection_research.md` §12 corrections; README section + feature-table row.

- Step 1 (uncommitted — maintainer commits manually):
  - `src/core/signatures.rs`: AOBs `layout_package_helper`, `dps_skin_table_read`; `derive_ddr_selection` (all-or-nothing; identity gates `"dance_message"`, `"%04d"`, `"bm2d"`, GameWork global cross-checked against `stage_record_accessor`); `DdrSelectionSites` + `ddr_selection_sites()`.
  - `src/mods/ddr_selection/{mod,policy,package_helper}.rs`; registered in `src/lib.rs`, `src/mods/mod.rs`; `DEFAULT_OFF_MODS`.
  - S-Marv stand-down: `flash.rs` (word re-drive), `splash.rs` (patch closure + S-MFC re-drive), `afp_patches.rs` (judge patch closure) consult `ddr_selection::legacy_package`.
  - `scripts/validate_ddr_selection.sh` (policy tests, 10 pass).
  - AGENTS.md row.
- Front-loaded from Step 2 (maintainer request, 2026-09-22): pure `trigger.rs` (row values, AUTO table, governance incl. bot exclusion, dev-knob override; 10 tests), `options.rs` (Dynamic scalar row `ddr_selection` 0..=6, `PersistMode::Local`, overlay + in-game, `versus_mirror`), `derive_music_series_vslot` (publishes `music_series_vslot` from `flare_skill_classifier`+2: 0x88 old / 0xA0 new — sweep green), series read = `find_music_by_mcode(PlayerWork+0x54)` → entry vtable slot; label + preview textures (eng/jpn/kor) via `scripts/option_strings.py` (regeneration touched only the 6 new PNGs); `mod-config.json` `option_menu_settings` entry after `arrow_opacity`.
- Readiness gates: `cargo check` clean, `cargo fmt` clean, `./build.sh` clean, signature sweep ALL GREEN (all derivations resolve on 20250805/20260224/20260721/20260825/20260915), `shape_diff`: `layout_package_helper` identical through 0x198 on every build; `dps_skin_table_read` diverges only at +0x153 on 20250805/20260224 (consumer reads +39/+49 only).

## Cabinet test — Step 7 — PASSED 2026-09-24

Deploy the DLL only (no data changes: every legacy layout root / stage-frame / danger arc ships in World). DDR SELECTION on; also a run with Center Arrows (1P) ON.

Expect once per boot (sig log, 20260915): `[+] hud_layout_builder_entry (derived) @ +0x6C440`, `hud_layout_style_off (derived) = 0x84`, `hud_layout_reverse_off (derived) = 0xE4`, `hud_layout_judge_pos_vslot (derived) = 0x298`, `[+] ddr_sel_stage_frame_export_lea (derived) @ +0x7A248`, `[+] ddr_sel_stage_frame_texture_site (derived) @ +0x7A3F6`; then `HudLayoutHooks: builder @ …, setter @ …, side extras resolved`, `HudLayoutHooks: detours installed (…)` (once, from whichever mod enables first), `DDR SELECTION: legacy element positions ready (layout builder post-pass)` and (center arrows on) `CenterArrowsSingle: layout subscribers registered (shared HUD layout hooks)`.

Per legacy song (the first three in the helper's package order): `legacy layout root dance_common000N queued on the LayoutActor load list (skin N)` (skin 1: `dance_common0000_v2`), `stage frame -> A3 names (export stage_frame, textures stage_frame000N_stage_*)`, `dance_stage -> dance_stage_frame000N (skin N, side 2)`, skins 3–5 `dance_danger -> dance_danger000N`, then `legacy markers 1p: single lane, reverse no, judge-group lane normal` (one per laid-out side) and `legacy element positions (skin N, dance_common000N) -- moved [stage, 1p:danger_gauge, 1p:gameover, 1p:fullcombo, 1p:judge, 1p:fast_slow, 1p:filter, 1p:score_compare, 1p:arrow_raw, 1p:freeze_judge]; World's (package stock) [song_info, 1p:score, 1p:difficulty, 1p:gauge, 1p:combo]; missing []; hidden [1p:bpm, 1p:name]`.

Check on screen, every skin 1–5 (1P, 2P versus, reverse scroll, doubles):
1. **Stage frame**: A3's legacy "1st STAGE" / "2nd STAGE" / "FINAL STAGE" frame at the top centre instead of World's (a 3rd stage on skins other than 4 has no texture — A3 showed the frame without the number too; report what it looks like).
2. **Danger**: at low life, skins 3–5 flash at the life gauge (A3's `danger_gauge` spot), NOT at the top-left corner; skins 1–2 still centred.
3. **Positions**: judgement word, FAST/SLOW, full-combo splash, game over at A3's spots (judgement a few px higher than World's; FAST/SLOW to the right of the combo); World's pacemaker (score compare) sits where A3's was (a bit lower than World's — **maintainer: keep it there or back at World's spot?**); arrows / receptors unchanged. Score, difficulty, life gauge and combo are still World's art at World's positions (Steps 8–10).
4. **Hidden**: no BPM display and no player name on legacy songs; both back on a stock song.
5. **Reverse scroll**: the judgement / FAST/SLOW / pacemaker sit in their A3 reverse positions (below the receptors); with World's JUDGE POSITION option set they follow it, like World.
6. **Center Arrows (1P) ON, solo**: the whole lane incl. the moved judgement / FAST/SLOW / pacemaker / full-combo is centred; the stage frame and danger unaffected; stock songs still centre as before (regression check of the shared hooks); OFF: nothing centred.
7. Quick restart mid-song (positions and frame survive), quick fail, a stock song after a legacy one (World's frame, BPM, name, positions back).

Report any WARN containing `legacy layout root`, `layout builder`, `stage-frame`, `element positions`, `HUD layout`, `dance_root`, any `F:afpu-package` / crash, and anything drawn at the top-left corner.

## Cabinet test — Step 6

Deploy the DLL only (no data changes: every banner arc and all 18 `_sel` movies ship in World). DDR SELECTION on, era cut-in any.

Expect once per boot (sig log, 20260915): `[+] ddr_sel_shutter_cleared_row (derived) @ +0x35E160`, `[+] ddr_sel_shutter_failed_row (derived) @ +0x35E1A0`, `ddr_sel_shutter_cleared_kind (derived) = 0x4`, `…failed_kind (derived) = 0x5`, `[+] ddr_sel_sma_init (derived) @ +0x7D870`, `ddr_sel_movie_sel_flag_off (derived) = 0x149`; then `DDR SELECTION: legacy end banners ready (CLEARED kind 4, FAILED kind 5)` and `DDR SELECTION: SceneManageActor init detour installed (_sel movies)`.

**Banners** — play a clear and a fail on every skin (1–5). Per legacy song end, in order:
1. `legacy CLEARED banner hosted (skin N: common_shutter000N / shutter_clear + 00_cleared)` (FAILED: `shutter_failed + 00_failed`);
2. `World's kind-art loader read the CLEARED row (SE / voice blanked) -- shutter now state Some(1), pending kind Some(4); row restored` (FAILED: kind `Some(5)`);
3. `legacy banner overlay 00_cleared created (layer 0x…, end Some(114); root shutter_clear layer 0x…, release frame Some(642))`;
4. `legacy CLEARED banner in (skin N, 00_cleared over shutter_clear)`;
5. on the results: `legacy CLEARED banner out (World opened it)`, `legacy banner overlay 00_cleared destroyed (frame Some(114))`, `legacy CLEARED banner released by World`;
6. the `legacy clip sounds --` line lists the banner cues (skin 1 `STG_CLOSE01` + `2nd_BIG2` / `ACT9`; 2 `2nd_KANSEI_B` / `ext_failed` + `sn2_gov`; 3 `end_door` + `STG_APP03` / `STG_CLOSE01` + `sn2_gov`; 4–5 `STG_CLOSE01` + `STG_APP02` + `Plate_spin4_st`).

Tohoku EVOLVED on skin 4 (clear it): step 1 reads `… + 00_prayforall, PRAY FOR ALL (Tohoku EVOLVED))` and the PRAY FOR ALL art shows; on every other skin (incl. 1 — its `00_prayforall` has no art, decided after run #1): `… + 00_cleared -- Tohoku EVOLVED, but this era has no PRAY FOR ALL art)` and the era's CLEARED word. Also: quick fail mid-song (the fast path requests no banner — no `banner hosted` line; with SKIP RESULTS OFF, or when the fast path is refused, the natural fail shows the legacy FAILED banner), a versus song (one banner), a stock song (World's CLEARED / FAILED, its `se_game_clear` / `vo_stage_clear`, no DDR SELECTION banner line).

Check on screen/ear: A3's banner art (the era shutter closing + the CLEARED / FAILED word on top), no World banner, no World `se_game_clear` / `se_game_failed` / `vo_stage_clear` on top of the era sounds, the banner opens normally on the results.

**`_sel` movies** — with VIDEO SIZE ON or FULLSCREEN, on any legacy skin:
- a song WITH a World movie (bril / para / maxx / burn / ichi / bagg / radu): `_sel movie mdb_apx/movie/bril_sel.wmv found (bril has a World movie too) -- flagging the MovieActor`, `MovieActor 0x… will try mdb_apx/movie/bril_sel.wmv first (World's own actor, VIDEO SIZE n)`, `MovieActor opened data/mdb_apx/movie/bril_sel.wmv (_sel movie)` — and the retro A3 movie plays instead of World's `_w`;
- a song WITHOUT one (trip = TRIP MACHINE, afro, cand, stil, roll, …): `_sel movie mdb_apx/movie/trip_sel.wmv found (trip has no World movie: movie byte 0 -> 4 for this init, restored after)`, `MovieActor 0x… will try … first (actor created for a song without a World movie, VIDEO SIZE n)`, `MovieActor opened data/mdb_apx/movie/trip_sel.wmv (_sel movie)`;
- a `_sel` song with VIDEO SIZE OFF: `_sel movie … found …` then `no MovieActor for this song (VIDEO SIZE 3 = OFF) -- no movie` (and no movie on screen);
- any other legacy song: `no _sel movie for <basename> (mdb_apx/movie/<basename>_sel.wmv not found) -- World's movie rules`;
- Background Dancers on (STAGE SCREENS default) with a newly-movied song (trip): the `_sel` movie on the stage screens (or THUMBNAIL on a screen-less stage); FULLSCREEN (NO STAGE): the movie full-screen behind the dancers.

Report any WARN containing `banner`, `row patch`, `common_shutter`, `_sel`, `MovieActor`, `music-info`, any `F:afpu-package` / crash, and whether the `_sel` movies play in sync (movieoffset: trip 3584).

## Cabinet test — P0

Setup: deploy the release DLL AND the six new `seop_{item,image}_ddr_selection.png` under `data_mods/custom_options/select_music_option_lang_{eng,jpn,kor}_v3_ifs/tex/` AND the repo `mod-config.json`'s new `option_menu_settings` entry (placed after ARROW OPACITY); `"mods": {"ddr-selection": true}`. Cycle skins with the DDR SELECTION row (in-game OPTIONS, or the overlay's PLAYER SETTINGS tab; applies next song). The dev knob `DDR_SELECTION_FORCE=<1..5>` (+ `layeredfs.developer_mode`) still overrides the row.

Expect in the log: `DDR SELECTION: option row live …`, `P1 row = X (next song)` on edits, per song `armed skin N (…) source=option|AUTO series S governing=P1 mcode=…` (AUTO on a World-era song: `AUTO -- mcode … is a modern-era song, stock UI`), one `dance_judge -> dance_judge0001 (skin 1, side 0)` line per package (judge, fast_slow, fullcombo, game_over, danger), `disarmed skin 1 (left the song window; N legacy package(s) this window)` after results.

Check on screen (skin 1): 1st MIX judgement words (incl. freeze OK/NG), FAST/SLOW, full-combo splash, game over (fail a song), danger (low life) centred; song-info panel and option icons ABSENT (World's skin-1 gates). Also: quick restart and quick fail mid-song, a stock song after unsetting the env var (World art returns), versus, S-Marvelous enabled (legacy MARVELOUS word, no violet word, no WARN). Report any `movieclip is invalid` / NULL-deref / WARN lines.

## Cabinet test — Era Cut-In toggle — PASSED 2026-09-23

Deploy the DLL (no data changes; the repo `mod-config.json` gained `"ddr_selection": {"era_cutin": true}`, optional — a missing section means ON). In the 0-0-0 overlay, GLOBAL SETTINGS → DDR SELECTION → Era Cut-In: set OFF, play a legacy song, set ON, play another. Expect: the edit logs `DDR SELECTION: era cut-in OFF (next song)` and `mod-config.json` then holds `"era_cutin": false`; the OFF song logs `legacy stage panel armed … era cut-in off` and `filled (… , era cut-in off (setting))`, shows the stage panel straight after the confirm with no cut-in animation or `sele_*` SE, and everything after (stage call, frame_out, READY close) is as in run #2; the ON song is exactly run #2 (cut-in, START skip). Check the setting survives a relaunch (the enable line reads `DDR SELECTION: enabled (era cut-in OFF)`).

## Cabinet test — Step 5 (rev 2) — PASSED 2026-09-23 (run #2)

Deploy the DLL only (no data changes). Play songs on every skin (1–5) incl. one SuperNOVA 2 song on skin 3 (e.g. CAN'T STOP FALLIN' IN LOVE / cach, gate, hana, flow2, chao, fasc, vemb, fway, alth, sunk, arra, plur); a 2nd and a final stage; quick restart mid-song; quick fail in the READY window and mid-song; a versus song; a stock song (DDR SELECTION OFF on the row). If an old-build install is available (20250805 / 20260224), one legacy song there too.

Expect per legacy song, in order (all BEFORE the `Scene change hook: prev=25 … next=26` line): `stage panel requested at song select -- skin N (…) source=… governing=P1 mcode=…`, `legacy stage panel armed at the song-select stage-panel request (skin N: …)`, `World's kind-art loader read the stage row (common_choice_v2 / shutter_choice_hd_root) -- shutter now state Some(1), pending kind Some(3); row restored` (kind `Some(1)` on old builds), `A3's stage root adopted (skin N, layer 0x…, armed at the song-select stage-panel request)` (old builds add `; old layout: World's jacket_usr SetVisible NOPed at the swap`; no `jacket_usr` crash at the swap), `World's stage voice (vo_stage_*) silenced for this song`, `legacy stage panel filled (skin N, stage S -> scene_choice_stage000N_…, jacket …, voice …, loaded [choice_stage, choice_background(, choice_jacket)], era cut-in)`; then at the edge `armed skin N (…) … (stage panel hosted at the song-select request)`; (skins 2–5) `stage call <cue>`; `legacy stage panel frame_out (song ready)`, `READY (legacy clip)`, `legacy stage panel closed at READY (state 6 -> 7)`, `legacy stage panel released by World`, ~1 s later `stage-panel packages released […]`. The Step 4 `World's stage panel dismissed at frame …` line must NOT appear on hosted songs; it must appear (Step 4 behaviour) whenever a song logs `legacy stage panel skipped` or `never hosted`. `legacy clip sounds --` lists the panel's sounds (skin 1 `STG_CLOSE01`, …; skins 4–5 `Plate_spin4_st`, `STG_APP02`, `banner_in`).

Check on screen/ear: right after confirming the song, the era cut-in (the big skin number) with its `sele_*` SE over the closing song select, then A3's stage panel (legacy frame + stage band + background; jacket on skins 4–5, the SN2 banner on skin 3 SN2 songs, the song jacket on other skin-3 songs, none on 1–2); START during the cut-in (after ~1 s) skips it; the era stage call (skins 2–5; silent on 1) and NOT World's `vo_stage_*`; no World stage panel at all; the panel opens (frame_out) when the song starts, the legacy READY! draws OVER it, the panel closes at READY; **no World READY?**; no 5 s wait before the song. Stock song unchanged (World panel + its voice + dwell + World READY?). Report any WARN containing `stage panel`, `stage row`, `stage root`, `never hosted`, `resolves stock at the play edge`, `jacket_usr SetVisible`, `dismiss refused`, `drain stalled`, and any `F:afpu-package` / crash — and how long the panel sequence feels (A3 held the song until the panel's `loop`, ~4 s after the cut-in).

## Cabinet test — Step 5 (rev 1, superseded — kept for reference)

Deploy the DLL only (no data changes; every A3 panel arc ships in World). Play songs on every skin (1–5) incl. one SuperNOVA 2 song on skin 3 (e.g. CAN'T STOP FALLIN' IN LOVE / cach, gate, hana, flow2, chao, fasc, vemb, fway, alth, sunk, arra, plur), plus: quick restart mid-song, quick fail in the READY window and mid-song, a versus song, a stock song (DDR SELECTION off), a 2nd and a final stage.

Expect once per boot: `ddr_sel_shutter_update … (derived)` lines in the sig log, `ShutterActor update detour installed (legacy stage panel)`. Per legacy song, in order: `legacy stage panel armed (skin N: common_choice_v2 + common_choice000N, common_shutter000N, common_choice_cutin000N)`, `World's stage voice (vo_stage_*) silenced for this song (…)`, `legacy stage panel filled (skin N, stage S -> scene_choice_stage000N_<1st|2nd|final|extra>, jacket <Hidden|Song|Banner>, voice <cue|none>, loaded [choice_stage, choice_background(, choice_jacket)], era cut-in)`, (skin 3 SN2 song) `SuperNOVA 2 banner banner_sn2_<song> on the stage panel`, (skins 2–5) `stage call <cue>`, `legacy stage panel frame_out (song ready)`, `READY (legacy clip)`, `legacy stage panel closed at READY (state 6 -> 7)`, `legacy stage panel released by World`, ~1 s later `stage-panel packages released [...]`. The Step 4 `World's stage panel dismissed at frame …` line must NOT appear on hosted songs. The `legacy clip sounds --` summary lists the panel sounds (e.g. skin 1 `STG_CLOSE01`, `se_shutter_in`/`out`; skins 4–5 `Plate_spin4_st`, `STG_APP02`, `banner_in`).

Check on screen/ear: after song select, the era cut-in (the big skin number animation) with its `sele_*` SE, then A3's stage panel (legacy frame + stage band + background; jacket on skins 4–5, the SN2 banner on skin 3 SN2 songs, song jacket on other skin-3 songs, no jacket on 1–2); START during the cut-in (after ~1 s) skips it; the era stage call (skins 2–5; silent on 1) and NOT World's `vo_stage_*`; the panel opens (frame_out) when the song starts, the legacy READY! draws OVER it, the panel closes at READY; no World READY? panel; no 5 s wait before the song. Stock song unchanged (World panel + its voice + dwell). Report any WARN containing `stage panel`, `stage row`, `dismiss refused`, `drain stalled`, `not shutter_choice_hd_root`, and any `F:afpu-package` / crash. Also: does the cut-in draw over the panel as expected, and is anything missing from the panel (the score sets are deliberately hidden in this build)?

## Cabinet test — Step 4

Deploy the DLL only. Pick any era; play a few songs (at least one on skin 1 and one on skin 4 or 5), plus: quick restart mid-song, quick fail in the READY window (before the arrows), a versus song, one song with DDR SELECTION off.

Expect per legacy song: `dance_message -> dance_message000N`, `legacy READY / HERE clips ready (dance_message000N, HERE yes)`, `World's vo_ingame_ready silenced for this song (…)`, `World's stage panel dismissed at frame ~344 (…, panel elements gone, World READY? not shown, READY pending) -- drain …` (rev 2; the `World's READY? dwell skipped` line is gone — the dwell stays stock until Step 5), `READY (legacy clip)`, `HERE WE GO (legacy clip[, voice ACT3_1])`, `legacy READY / HERE WE GO finished`; the `legacy clip sounds --` line lists the READY cues (skin 1 `ACT2_1`+`2nd_BIG2`, 2 `sn2_mst09`+`2nd_KANSEI_B`, 3 `sn2_mst09`, 4–5 `vo_ingame_ready`) and skin 1's `ACT3_1` (the HERE voice goes through the same bank but is played from code — it shows in the HERE line, not the clip-sound counter).

Check on screen/ear: the World stage panel still appears after song select (Step 5 replaces it) but the song starts sooner (no 5 s wait); World's "READY?" / "HERE WE GO!" never show; the legacy READY! and HERE WE GO!! clips play centred before the first arrows with the era voice, and World's own READY voice does not play on top; restart / fail / versus behave; a stock song is unchanged (World READY? + voice + dwell). Report any `shutter read failed` / `dismiss refused` / `vanished from the package registry` / `could not create` WARN, and whether the clips draw above the lanes.

## Cabinet test — Step 3 (rev 3: + full-combo SE fix)

Deploy the new DLL only. On a legacy skin: (1) full combo — ONE full-combo sound (the era `XAC_full_combo2`, no World `se_game_fullcombo` on top), log `World's se_game_fullcombo silenced for this song (…)` then `legacy clip sounds -- played from the era bank: [XAC_full_combo2 x1]`; a stock song afterwards must play World's full-combo SE again; (2) fail a song — `Plate_spin3_st` in the `legacy clip sounds --` line. Report any `ddr_sel_fullcombo_se_jz … unresolved` / `branch patch failed` WARN.

Previous (rev 2) instructions, still valid:

Deploy the DLL (no data changes). `ddr-selection` on, pick any era. Expect once per boot: `era bank built in N ms -- 74 cues, 207 waves, 17.2 MB (…)`, then at the next scene change `GameAudio: … claiming free sound-bank slot 4`, `CreateInMemoryWaveBank('dsel', …) hr=0x00000000`, `CreateSoundBank('dsel', …) hr=0x00000000`, `era bank 'dsel' registered in slot 4 (74/74 cues resolve)`. Play a song on a legacy skin: the full-combo splash plays `XAC_full_combo2`, the in-lane game over plays `Plate_spin3_st` (fail a song); after the song `N legacy clip sound(s) played from the era bank`. Report any `hr=0x8AC7…` or `do not resolve` line. (Rev 2: the per-cue summary `legacy clip sounds -- played from the era bank: [XAC_full_combo2 x1, …]` is logged at EVERY scene change while armed.)

## Deploy & test log

- 2026-09-24 Step 7 cabinet run #1 (20260915, CrossOver): maintainer — everything looked right (all skins, 1P / 2P, reverse, doubles, Center Arrows on / off, quick restart / fail, stock song). Log: 15 legacy songs on skins 1–5, each `legacy layout root … queued`, `stage frame -> A3 names`, and `legacy element positions … moved [stage, Np:danger_gauge, gameover, fullcombo, judge, fast_slow, filter, score_compare, arrow_raw, freeze_judge]; World's (package stock) [song_info, score, difficulty, gauge, combo]; missing []; hidden [bpm, name]` (both sides in versus); no DDR SELECTION WARN, no `F:` lines. Pacemaker at A3's spot accepted (no change requested). **Step 7 ticked.**

- 2026-09-24 Step 6 cabinet run #3: Tohoku EVOLVED works as expected on every skin (PRAY FOR ALL on X, the era CLEARED banner elsewhere). **Step 6 ticked.**

- 2026-09-24 Step 6 cabinet run #2 (spam-fix DLL): MAX 300 replayed — no `afp_mc_get_param … is invalid` lines (FIX CONFIRMED). Maintainer decision on run #1's finding: PRAY FOR ALL only on the era whose clip has the art (skin 4, X); skin 1 now gets the normal CLEARED banner for Tohoku EVOLVED (`banner_logic::has_pray_for_all` = skin 4; host test updated, validator 74). Rebuilt; awaiting a skin-1 Tohoku EVOLVED check.

- 2026-09-24 Step 6 cabinet run #1 (20260915, CrossOver): CLEARED / FAILED banners correct on every skin for normal songs; stock + versus fine; `_sel` movies played (maxx, trip) incl. on the stage monitors (Background Dancers STAGE SCREENS). Two findings: (1) Tohoku EVOLVED on skin 1: shutter only, no word — log shows `00_prayforall` hosted, created, in, out, destroyed; the asset itself has NO art (0 place tags; sounds only; arc byte-identical to A3's — research §7.1). Skin 4 PRAY FOR ALL correct. **Open decision (maintainer): keep skin 1 authentic or fall back to `00_cleared`.** (2) `afp_mc_get_param afp_mc_id[…] is invalid` spam on every legacy song from the stage panel's release to the disarm (~2 lines/frame; 43 404 in the session) — a Step 5 bug (the released panel's MovieClip ids kept being read). Fixed in `panel.rs` (see research §7.1); needs a re-run to confirm the spam is gone.

- 2026-09-23 Era Cut-In toggle cabinet check: works as expected (maintainer). Log: `enabled (era cut-in ON)` at boot; each overlay edit `era cut-in OFF|ON (next song)`; OFF songs (skins 1, 2) `armed … era cut-in off` + `filled (… era cut-in off (setting))`, the ON song `filled (… era cut-in)`; the install's `mod-config.json` ends with `"ddr_selection": {"era_cutin": false}`. No DDR SELECTION WARN.

- 2026-09-23 Step 5 cabinet run #2 (rev 2 DLL, 20260915): **everything worked as expected** (maintainer). Log over 7 legacy songs on skins 2–5 (1st and 2nd stages): every song logs the full rev-2 chain in scene 25 — `stage panel requested at song select`, `armed at the song-select stage-panel request`, `kind-art loader read the stage row … state Some(1), pending kind Some(3); row restored`, `A3's stage root adopted`, `World's stage voice silenced`, `filled (… loaded [choice_stage, choice_background(, choice_jacket)], era cut-in)` (skins 3–5 with the jacket) — then `armed … (stage panel hosted at the song-select request)`, `frame_out`, `closed at READY (state 6 -> 7)`, `released by World`. `era cut-in skipped` ×3 (START works). No DDR SELECTION WARN, no `F:` lines. Maintainer follow-up: the cut-in can be divisive (time added before the song) ⇒ asked for an ON/OFF toggle under GLOBAL SETTINGS → DDR SELECTION (see In flight). **Step 5 ticked.**

- 2026-09-23 Step 5 cabinet run #1 (20260915, CrossOver, 5 skin-2 songs incl. quick fails): **the legacy panel never hosted.** Maintainer: World's stage panel seen very briefly, no legacy panel, and World's READY? showed together with the legacy READY (Step 4 had suppressed it). Log: every derivation resolved (`ddr_sel_shutter_update` +0x34540, `ddr_sel_stage_voice_jz` +0x33F6F, `ddr_sel_shutter_stage_row` +0x35E120, basename 0x340, jacket 0x368, stride 0x40, host_ok 1) and the detour installed; per song `legacy stage panel armed`, `World's stage voice (vo_stage_*) silenced`, `armed skin 2`, then READY / HERE WE GO, `disarmed`, `stage-panel packages released` — but never `filled`, `frame_out`, `closed at READY`, `released by World`. Diagnosis: **(A)** on EVERY song the AVS line `afp_mc_deep_goto_play_label no label[in] in stream[shutter_play]` (World's kind-3 art load / swap) precedes the `Scene change hook: prev=25 … next=26` line — World requests and loads the stage panel DURING song select (the solo / versus confirm: the versus confirm `FUN_180114a10` on 20260825 inlines `request(3)` right after the commit `FUN_1800fdc90`), so the arm at the 25 → 26 edge patched the row after it had been read, and `SelectMusicTerminateSequence` (scene 26) only requests kind 3 when the shutter is idle — it never was. (The `FUN_1800bc120` requesters in the handoff are `ResultSequence::onUpdate`; `FUN_180154940` / lambda8 / lambda10 / lambda12 are the dan `GradePreparePanel`.) **(B)** `handles_dismissal()` = `HOSTED && !FOREIGN` was true all song with nothing filled ⇒ the intro skipped its World-art dismissal (World's READY? doubled), `seed_dwell` skipped World's 5 s dwell (the "very brief" World panel) and `code_se` silenced World's stage voice for nothing. Fixed in Step 5 rev 2 (see In flight).

- 2026-09-23 Step 4 cabinet run #2 (rev 2 DLL): maintainer — stage panel and READY clip both right. Log: `World's stage panel dismissed at frame Some(344) (…, panel elements gone, World READY? not shown, READY pending)` on every legacy song; era READY cues routed on skins 2 (`sn2_mst09`+`2nd_KANSEI_B`), 3 (`sn2_mst09`), 4/5 (`vo_ingame_ready`); no DDR SELECTION WARN. **Step 4 ticked.**

- 2026-09-23 Step 4 cabinet run #1 (skins 1 + 2, a quick fail in the READY window, a stock song): legacy READY / HERE WE GO play with the era voices (skin 1 `ACT2_1`+`2nd_BIG2` routed, HERE `ACT3_1`; skin 2 `sn2_mst09`+`2nd_KANSEI_B`), `vo_ingame_ready` silenced, clips destroyed on quick fail, stock song unchanged, no WARN. Two maintainer findings: (1) with the dwell skipped, World's stage panel (the jacket screen) showed only ~0.25 s — awkward; (2) World's READY? still flashed ~1 s before the legacy READY. Cause of (2): the dismiss waited for `ready_loop` (frame 408), but `shutter_play` places its `ready` sprite at frame 350 (panel elements leave at 344 — timeline dump of common_shutter_v3). Fix (rev 2): dismiss when `jacket_usr` has left (seen earlier on the same layer), backstops = World's `ready` sprite placed / `ready_loop` reached / READY fired; the dwell skip is OFF (`intro::SKIP_WORLD_DWELL`) until the Step 5 legacy panel brings A3's own hold. Awaiting run #2.

- 2026-09-23 Step 3 cabinet run #3 (rev 3 DLL, full-combo SE fix): (1) skin-2 full combo — `World's se_game_fullcombo silenced …` + `[XAC_full_combo2 x1]` — FIX CONFIRMED; (2) stock song afterwards — no silenced line, no WARN (restore is unlogged; audible check is the maintainer's); (3) skin-2 natural fail (175 % song speed, twice, incl. a fresh session) — NO `legacy clip sounds` line at all (the AFP route saw zero calls in the window, not even a miss) and the maintainer saw NO in-lane clip before the stock STAGE FAILED banner. RE so far: World's DangerActor (vtable 0x1803616a8, init FUN_180068ce0, msg FUN_1800694b0 on 20260825; child of the GamePlayActor, created when `GameWork+0x58 == 0`) builds the `game_over` clip from the `dance_game_over` record and, on msg 0x103c (sent by GamePlayActor 0x103a on gauge death, payload = GPA only in course mode with carried life ≤ 0), sets rate 1.0 + visible and — payload null — plays from its frozen frame 0; the legacy clip's `Plate_spin3_st` is a frame-1 DoAction (labels in 0 / loop 32 / out 65), World's v3 clip has no sound. A3 is byte-for-byte the same logic (0x103f). Maintainer follow-up (same day, ddr-selection OFF): stock World never shows an in-lane game over before STAGE FAILED either ⇒ not a DDR SELECTION bug; the clip is hidden or never advanced in World's fail flow. Moved to Step 6 (plan guidance updated). **Step 3 ticked.**

- 2026-09-23 Step 3 cabinet run #2 (rev 2 DLL, 20260915 data): bank registered at the first NOW_LOADING scene change (74/74 resolve); skin-2 full combo logged `legacy clip sounds -- played from the era bank: [XAC_full_combo2 x1]; refused 0; World cues passed through 0` — ROUTING PROVEN. Maintainer could not tell by ear (doesn't know the stock sounds). Game-over test pending.

- 2026-09-22 Step 3 cabinet run #1: bank built in 93 ms (74 cues, 207 waves, 17.2 MB, 9 shared), registered in slot 4, 74/74 cues resolve, both engine creates hr=0. One skin-1 song (all-S-Marvelous full combo); the session was closed on the results screen, so the per-window routing summary (then logged only at disarm) never printed — no evidence either way that the clip sounds routed. Maintainer listened for announcer changes (Step 11, not Step 3). Fix: summary now per cue, drained at every in-window scene change.

- 2026-09-22 cabinet (CrossOver, 20260915 data, `log.txt`): row cycling OFF / AUTO / every era; 11 armed songs across skins 1–4 (+2 on a second mcode), every whole-package swap logged and visible (judge words, FAST/SLOW, full-combo splash, danger on skins 1–2; **CORRECTION 2026-09-23: the in-lane game over was never seen by the maintainer — it was assumed, not observed**), disarm after every window incl. quick-fail (28→24). No DDR SELECTION WARN. Observed: STAGE FAILED / CLEARED banners stock — expected (shutter kinds 4/5, Step 6). Row label blank: the mod was enabled LIVE ~70 s after boot, so the boot-time `flush_label_atlas` never saw the registration — a framework-wide limitation for every mod that registers rows in `enable()`; maintainer: leave it unless fixed for all rows. Noise fixed: `afp_patcher`'s per-decline WARN (5 per legacy song from S-Marv's stand-down) demoted to DEBUG — every patch fn WARNs its own real degradations. Pre-existing on this install, not ours: `afp_mc_goto_play_label no label[stage_out|ready_out] in stream[shutter_play]` once per song (the stock-data CrossOver `shutter_play` without labels, `docs/quick_restart_fail_speedup_research.md` §14.1) — matters for Step 5, which hosts the legacy panel in kind 3.

## Deviations & open questions

- **Step 6 `_sel` scope:** (a) DECIDED 2026-09-24 (maintainer): any song played with a legacy era gets its `_sel` movie — the option rows are the only trigger, no DDR SELECTION folder is planned (design R6; A3's folder-only rule is not ported). (b) DECIDED 2026-09-24 (maintainer): **all 18 play**, incl. the 11 `_sel` songs with no World movie (afro, afte, bfor, bom2, cand, drte, kaku, para2, roll, stil, trip) — World creates no MovieActor for them, so the flag reaches only the 7 others (bagg, bril, burn, ichi, maxx, para, radu) unless the mod makes World create one (A3 did; it also ignored VIDEO SIZE OFF). Covering the 11 = a scoped "has a movie" override of the music-info entry around `SceneManageActor::onInitialize`. What `_sel` movies are (for the decision): 1280×720 VC-1 retro-style backgrounds made for A3's DDR SELECTION (early-2000s look — pixel patterns, "DANCE" lettering, TRIP MACHINE's spiral/gears title art), vs World's `_w` movies (640×360, modern effects) for the 7 songs that have one.

- **Full-combo SE doubling — FIXED 2026-09-23 (see In flight; implemented as a 1-byte JZ→JMP instead of the disp32 +17 proposed below).** Original note: World's FullcomboActor msg handler (`fullcombo_actor_on_message`, FUN_180069c00 on 20260825; S-Marvelous already owns a post-original detour on it — one-detour rule) plays `se_game_fullcombo` FROM CODE (slot-2 bank `*(mgr+0x30)` vtable GetCueIndex/Play directly, NOT via `se_play`; the LEA of the string is at ~0x180069cca), then the legacy clip plays `XAC_full_combo2` from the dsel bank ⇒ both sound. A3 has NO `se_game_fullcombo` string (its FullcomboActor played no code SE; only the clip) ⇒ fidelity = suppress World's SE while `legacy_package("dance_fullcombo")`. Proposed seam (no new detour): a runtime-toggled disp32 patch on that `LEA RDX,[rip+"se_game_fullcombo"]` — disp += 17 (points at the string's own NUL ⇒ GetCueIndex("") = 0xFFFF ⇒ no play) at arm when dance_fullcombo turns legacy, restored at disarm; needs a new AOB around the LEA (sweep + shape_diff), game-thread only (arm/disarm run in scene callbacks; the handler runs on the game thread). Other World code SEs to check for the same doubling in later steps: `se_game_failed` / `se_game_clear` (refs from the shutter kind table 0x18035e190/0x18035e3d0 — Step 6 banners), `se_game_miss` (0x180071133).

- Step 5 deviation (design §4.5 said "detours on the kind-art loader and kind-3 fill" + "stage-voice detour"): ONE detour on `ShutterActor::onUpdate` plus a checked `.rdata` patch of the stage row (World's own named-package path then loads A3's root) and a `code_se` byte flip for World's stage voice — no detour on `FUN_180035420` / `FUN_180035f00` / `FUN_180033760`. World's kind-3 fill still runs on the legacy root and is harmless (every child lookup misses; its SpriteLayers hide anchor-less).
- ~~Step 5 limitation: 20250805 / 20260224 cannot host the legacy root~~ — lifted in rev 2: the un-null-checked `jacket_usr` SetVisible CALL is NOPed while A3's root is live (`ddr_sel_shutter_jacket_vis_call`); `host_ok` = 1 on all five.
- Step 5 rev 2 deviation: the row patch lives for ONE ShutterActor update (the kind-art loader's read) instead of the whole armed window, and the session is created at the song-select stage-panel request (design assumed the play-edge arm precedes every request — cabinet run #1 disproved it). The edge arm re-resolves and reconciles (§1.4 of `research/stage-panel.md`).
- Step 5 rev 2: the panel sequence now starts at the confirm, as in A3: World's `SelectMusicSequence` waits for the covered state (the legacy root's `loop`, ~4 s after the cut-in's `close`) before scene 26 — a longer song-select → gameplay transition than World's panel. Authentic; flag it to the maintainer if it feels slow.
- Step 5 rev 2 open: dan / galaxy-brave (`GameWork+0x1C` 10 / 9) use their own kind tables — the default row is never read there, so a song-select request in those modes (if it resolves legacy at all — courses are refused by the mode policy) ends in `legacy stage panel skipped` (World's panel). No mode-offset derivation was added for it.
- Step 5 deviation: the dwell skip lives in `panel.rs` (seeded from the ShutterActor update, only while A3's panel owns the READY dismissal) instead of an `intro::SKIP_WORLD_DWELL` flip — the dwell must stay stock whenever World's own panel is on screen (old builds, a foreign root, a missing package).
- Step 5 finding: `CLayer::SetPriority(p)` = raw `100 - p` (p ≤ 100), drawn ascending — Step 4's READY / HERE clips were at raw 5 (drawn first, i.e. BEHIND everything, incl. World's panel). They are raw 95 now (A3's order: READY over the stage panel). If a Step 4 run showed the READY clip hidden under something, this is why.
- Step 5 finding: the CrossOver `afp_mc_goto_play_label no label[stage_out]` lines come from World's DEEP SetFrameLabel reaching child clips; the `shutter_play` root has `stage_out` / `ready_loop` / `end` but no `out` / `out_end` (QR §14.1's stall is timing-dependent, not install-dependent). A3's root has `out` / `end`, so the legacy drain completes by itself (the stall fallback is kept anyway).
- Step 4 deviation (rev 2, after cabinet run #1): the READY? dwell skip is implemented but OFF (`intro::SKIP_WORLD_DWELL = false`) — with World's panel still on screen it cut the panel to ~0.25 s. Step 5 turns it on together with the legacy panel and A3's own hold. The READY / HERE triggers poll the CMA StackStep (≤ 1 frame after the message) instead of a sender tap — no new detour.
- World code SEs audited for the same doubling (20260825, direct `LEA` xrefs; A3's binary has NO `se_game_*` string at all — every one is World-only): `se_game_fullcombo` (fixed), `vo_ingame_ready` ×2 in `FUN_180033f60` (Step 4 — dance_message's `00_ready` brings the era READY voice), `se_game_failed` / `se_game_clear` (no code LEA — referenced from the shutter kind table data 0x18035e190/0x18035e3d0; Step 6 banners), `se_game_miss` @ 0x180071133 in `FUN_180070de0` = World's battery-gauge (LIFE4 `damage_%d_usr` cells) life-lost update — the legacy gauge clips embed no sound, so no doubling; whether a legacy LIFE4 gauge keeps World's miss SE is a Step 8 fidelity call (A3 played none). `se_game_shockarrow` (`FUN_18005f220`) is not skin-related.
- Step 3: `game_audio::is_cue_playing` (plan) deferred to Step 11, its only consumer (no orphaned code). The AFP route acts only while a legacy skin is armed (stock World clips never change); Step 5 must arm before the shutter's first clip sound (`se_shutter_in` fires at the start of the stage shutter).

- Plan D27 moved the era-bank infrastructure from P4 to P2 (design §2.3) — flagged to the maintainer with the design.

## Key facts for a cold resume

- World skin field: `GameWork+0xA8`; the skin reaches World actors only through the `LayoutActor` record (`+0x28` of the record value); the three `CMP [GameWork+0xA8],1` gates hide SongInfo / option icons.
- Record value layout: MSVC string (0x20 + 8 pad) then `i32 skin @+0x28`.
- Legacy fallback must be the unsuffixed World base — never `<base>0000`.
- A3 install: `$DDR_A3_INSTALL`; World: `$DDR_WORLD_INSTALL`; builds: `~/Desktop/ddr_modules`.
