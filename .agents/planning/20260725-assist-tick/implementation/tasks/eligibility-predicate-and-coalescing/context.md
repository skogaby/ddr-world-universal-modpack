# Context — Step 4 Task 01: Eligibility Predicate + Tick Coalescing

**Task file:** `.agents/tasks/20260725-assist-tick/step04/task-01-eligibility-predicate-and-coalescing.code-task.md`
**Mode:** auto. **Approval chain verified:** `Generated By: code-task-generator 2026-07-26`
(breakdown approved by the maintainer in-session, 2026-07-26); source plan and design both
`Status: Approved 2026-07-25`. Dependency: Step 3 complete.

## Requirements

1. FR-2 predicate as one pure function over `*mut GameNote` (research §"The tick predicate" is a
   complete reference implementation): `kind == 0` whitelist → shock test (all four of
   `state[0..=3] == TRG` OR all four of `state[4..=7] == TRG`) → any-non-zero-panel guard
   (`!= 0`) → `music_count >= 0`.
2. `length[]` deliberately not consulted; the `FREEZE ARROW: OFF` reasoning documented at the
   predicate.
3. Coalescing after sort+dedup: timestamps within `COALESCE_MS = 4` of the previously kept one
   collapse (keep the earlier). Constant commented provisional pending Step 6's TPS-150
   measurement.
4. Once-per-song build line extended with rejection counts (kind / shock / no-panel / negative)
   and coalesced-away count; arithmetic must reconcile:
   `results == kept + rejects + coalesced`.
5. Per-frame path untouched; predicate runs only in the once-per-song build.
6. Panic-free; no new dep/config/detour.

## Implementation surface

`src/mods/assist_tick.rs::build_tick_list` (walks via `types::game_note::{actor_results_range,
for_each_result}`; currently keeps every `music_count >= 0`). Constants come from
`types::game_note::{kind, state}` (`kind::ARROW == 0`, `state::TRG == 1`).

## Verification

- Agent: gates + log reconciliation on the same chart as Step 3 (Ace out CSP: Step 3 kept=437 of
  438 — new kept must drop, counts must reconcile exactly).
- Maintainer: the note-type listening rows (jumps/freezes/shocks/mines/CUT/FREEZE-OFF) — Step 4's
  §7.4 matrix, on their identified charts.
