# Context — task-01 retire-cache-model

Task file: `.agents/tasks/song-rate-streaming/step01/task-01-retire-cache-model.code-task.md`
Source plan: `.agents/planning/2026-08-08-song-rate-streaming/implementation/plan.md` — **Status: Approved 2026-08-08**
Design: `.agents/planning/2026-08-08-song-rate-streaming/design/detailed-design.md` — **Status: Approved 2026-08-08**
Generated By: code-task-generator 2026-08-08 (verified in the task file's Metadata)
→ Approved-planning chain verified; auto mode may proceed without a separate plan approval.

Mode: auto (per handoff instructions; run to completion, record decisions).
Working docs live here (planning dir, per repo convention — NEVER `.agents/scratchpad/`,
see AGENTS.md Custom Instructions; precedent: the retired feature's
`implementation/step04-task-01-*/` directories).

## Build / test commands (repo root)

Gates, in order, all required green per task:
1. `./scripts/validate_song_playback_speed.sh` (needs sibling `../ddr-chart-tools` checkout)
2. `./scripts/validate_se_bank_synth.sh`
3. `cargo check --target x86_64-pc-windows-msvc` — must be 0 warnings
4. `cargo fmt` — WHOLE crate, never file args
5. `./build.sh`

The crate has no host `cargo test` (cdylib, windows-only deps); host tests run
via the validator scripts' `#[path]`-mounting harnesses.

## Requirements digest (from the task file)

1. Delete `src/services/song_rate/{cache,worker}.rs` + their suites (`tests.rs` = the cache/worker/model suite).
2. Reduce/delete `model.rs` — compiler decides survivors.
3. `src/core/xact/transform.rs` → pure plan logic relocates to stub `src/core/xact/virtual_bank.rs` with tests; pipeline/admission/`TransformReport` die.
4. `conversion.rs` → `binding.rs`: keep `dance_bank_song_code`, `song_code_digest` (+tests); add always-refusing preflight (typed refusal → EarlyFailed); delete redirect/exposure/store/lease/quarantine/generated-path logic.
5. Remove song-rate seams from `avs_layeredfs/file_hooks.rs`.
6. Trim `runtime.rs`: keep scene callback, desired atomics, commit-visibility log poll, bank-timeline drain, `integration_ready()` (now false).
7. Trim `transaction.rs`/`xact_runtime.rs`: drop lease-id + cache-digest pass-throughs and lease-era maintenance kinds; keep exactly-once frame/slot/token protocol + tests green.
8. `DDR_SONG_RATE_FAULT`: drop legs whose injection sites die (source-read/validation/conversion); keep selector + transaction legs.
9. Drop `cache_limit_gib` from config struct + `mod-config.json` (no shim).
10. Validator updated IN PLACE: no `cache`/`on_demand` sections, no schema versioning, survivors green.
11. Keepers unchanged in behavior: `mods/song_playback_speed.rs`, `lifecycle.rs`, `clock_patch.rs`, `score_guard.rs`.

## Reference surface discovered (implementation paths)

- Crate: `src/services/song_rate/{mod,cache,worker,model,conversion,conversion_tests,tests,runtime,transaction,transaction_tests,xact_runtime,xact_runtime_tests,wavebank_hook,wavebank_hook_tests}.rs`,
  `src/core/xact/{mod,transform,tests}.rs`, `src/services/avs_layeredfs/file_hooks.rs`,
  `src/lib.rs` (init block ~L189–200, ~L344–398), `src/mods/config.rs` (~L231–246), `mod-config.json`.
- Validator: full structural map captured in plan.md §Validator (from an explore-subagent pass over
  `scripts/validate_song_playback_speed.sh`, 2366 lines).
- `validate_se_bank_synth.sh` does NOT mount song_rate/transform — unaffected.
- `runtime.rs` is `#[cfg(windows)]` — never compiled by the validator harness.
- KEPT validator sections `validate_rate`/`validate_corpus` currently call
  `transform::transform_song_xwb` — they must be reworked onto surviving primitives (see plan).

## Ambiguities resolved (auto-mode interpretations, auditable)

- **RedirectToken digest fields**: task names "lease-id and cache-digest pass-through fields".
  `cache_key_digest`/`output_digest` are the cache digests; `normalized_path_digest`/
  `generated_path_digest` are generated-path-era fields whose only producer
  (conversion.rs `prepare_inner`) is deleted and whose concept the design deletes
  ("the two-stage open-redirect invariant is deleted, not relocated"). All four go;
  the token keeps `{call_nonce, call_depth, generation, requested_percent,
  participant_mask, stage_index, effective_rate}` (the design's Step-4 payload minus
  `song_code_digest`, which arrives in Step 4).
- **MaintenanceKind**: `ReleaseLease` dies with its producer/consumer (lease drain).
  `Quarantine` is retained in task-01 because the transaction's late-fail leg still
  pushes it (the protocol + saturation-fault tests depend on the push); task-02
  renames it to `ReclaimBinding` per its req 2.
- **`XactSlotPhase::Quarantined`** (slot phase) is untouched — task-02 renames only
  lifecycle phases and the MaintenanceKind vocabulary.
- **`IdentityReadiness`**: the `conversion`/`source_read` legs (LayeredFS seams) are
  replaced by one `binding` leg read from `binding::integration_available()`
  (constant `false` until plan Step 4 installs the io-callback detour pair) — this is
  what makes `integration_ready()` structurally false (task AC 4, design req 40).
- **Validator synthetic/corpus survival**: the harness gains a local
  `transform_bank` helper composed from surviving crate primitives
  (`xwb::parse_song_bank` → `virtual_bank::plan_entry` → `adpcm::decode_interleaved`
  → `stretch::stretch_interleaved` → `adpcm::encode_interleaved` →
  `xwb::write_song_bank_streaming`), preserving the DSP checks (pitch/SNR/seam/
  determinism/identity/latency). The memory-admission fields
  (`peak_memory_bytes`, the two `*_max` thresholds tied to
  `transform::DEFAULT_MEMORY_LIMIT_BYTES`) die with the admission; measured
  working-set values stay informational.
- **`model.rs`**: expected to die entirely (FullDigest's remaining consumers are all
  in deleted code); compiler decides.
- **`mod-config.json`**: the `song_playback_speed` block contains only
  `cache_limit_gib` → the whole block is removed.
- **FaultSelector**: keep `pre_original_panic`, `post_original_panic`,
  `token_mismatch`, `xact_reject`, `maintenance_saturation`; drop `source_read`,
  `validation`, `conversion` (+ parse strings).
- **`runtime::init`** loses `mod_folder`, `module_header`, `cache_limit` params;
  keeps `identity_ready`, `retired_diagnostic_present`, `fault`.
- **`enqueue_release_for_file`** (wavebank unregister post) dies with the lease drain;
  the unregister post keeps only the timeline record. Slot release protocol
  (`begin_release`/`begin_release_by_file`/`finish_release`) survives, tested;
  `begin_release_by_file` return shrinks to `Result<usize, SlotError>`.

## Non-functional expectations

- No behavior change to keepers; detour-legality unchanged (no new allocation/logging
  in detour paths); one bounded WARN patterns retained where their paths survive.
- 0-warning windows check: deleting code must not orphan imports (crate-wide
  `#![allow(dead_code)]` covers unused survivors but NOT unused imports).
