# Plan — task-01 retire-cache-model

Status: Approved 2026-08-09 (auto mode — the verified upstream approvals stand in:
implementation plan + design both `Status: Approved 2026-08-08`, task file
`Generated By: code-task-generator`)

## Test scenarios (per acceptance criterion)

AC1 — cache machinery gone:
- `rg -i "lease|manifest|tombstone|quarantin|evict|cache_limit|DEFAULT_MEMORY_LIMIT|_cache/song_playback_speed|CacheStore|coordinator" src/` returns
  no hits outside (a) `avs_layeredfs`'s own unrelated `cache_hasher`/arc cache, (b) the
  retained `XactSlotPhase::Quarantined` slot phase + `MaintenanceKind::Quarantine`
  (task-02 renames), (c) comments describing surviving late-fail protocol. Verified by
  inspection of the residual hit list.

AC2 — tree green: all five gates pass; windows check 0 warnings.

AC3 — validator trimmed in place:
- `report.json` top level has NO `cache`/`on_demand` keys, `schema` stays
  `"song-rate-validation/v1"` (no discriminator added), `overall_pass: true`.

AC4 — identity-only runtime:
- New host test (wavebank_hook_tests): `readiness(true)` with clock/wave-hook legs
  forced true still yields `is_ready() == false` because the `binding` leg reads
  `binding::integration_available()` (false). Direct-construction case keeps the
  every-leg-required property test (now 5 legs).
- Clock (`clock_patch_tests`), scene machine (`lifecycle_tests`), score readiness
  (`score_guard_tests`) suites unchanged and green.

AC5 — keepers untouched: `lifecycle_tests` (22), `clock_patch_tests` (6),
`score_guard_tests` (19), `mods/song_playback_speed.rs` textually unchanged.

AC6 — pure plan logic preserved (new tests in `virtual_bank.rs`, ported vectors):
- `plan_entry` targets: 1024-frame stereo 8 kHz entries at 75/125 % produce the same
  `StreamedEntry` values the transformer produced (duration = whole-block target from
  `rate::target_for_percent`, `data_len = blocks × block_align`).
- Interior loop vector: duration 1024, loop 128+768 at 75 % → mapped loop_start 176,
  loop_length 1056 (the exact assertion from the deleted
  `transformer_maps_interior_loops_and_reports_encoded_seams`).
- `loop_length == 0` → `(0, 0, None)`.
- Degenerate mapped loop (`mapped_end <= mapped_start`) → `InvalidMappedLoop`.
- Half-up rounding + one-frame clamp rule at the output boundary (boundary maps to
  ≤ output_frames; overshoot > 1 frame refuses).
- 28-bit refusal: an entry whose stretched duration exceeds 2^28−1 refuses via the
  propagated `RateError` (rate::target_for_percent's ceiling).

## Implementation approach (outside-in, compiler-driven)

1. **core/xact**: add `virtual_bank.rs` (relocate `EntryPlan`, `build_entry_plan`→
   `plan_entry`, `map_loop`, `map_boundary`, new minimal `PlanError`; inline
   `#[cfg(test)] mod tests` with the AC6 vectors). Update `core/xact/mod.rs`
   (`transform`→`virtual_bank`). Delete `transform.rs`. Trim `core/xact/tests.rs`
   (transformer section L776–1103 + `transform` import).
2. **song_rate services**: delete `cache.rs`, `worker.rs`, `tests.rs`, `model.rs`
   (compiler-verified). Rewrite `conversion.rs` as `binding.rs` (pure helpers +
   `integration_available() -> bool` (false) + `prepare_binding` always-refusing stub
   returning typed `BindRefusal`); port pure tests to `binding_tests.rs`. Update
   `mod.rs`.
3. **xact_runtime.rs**: token loses 4 digest fields; slot loses `lease_id`; `expose`
   loses the lease param; `begin_release_by_file` → `Result<usize, _>`;
   `token_digest()` dies; `MaintenanceEvent` = `{kind, slot_index}`;
   `MaintenanceKind` = `{Quarantine}`. Mechanical test fallout in
   `xact_runtime_tests.rs` (drop `ReleaseLease` event leg).
4. **transaction.rs**: FaultSelector loses source_read/validation/conversion;
   `enqueue_quarantine` loses digest/lease plumbing. Mechanical fallout in
   `transaction_tests.rs`.
5. **wavebank_hook.rs**: `IdentityReadiness` legs → `{clock, wavebank_create,
   wavebank_unregister, binding, movie_policy}`; `readiness(movie_policy)` reads the
   binding leg from `binding::integration_available()`; delete
   `enqueue_release_for_file`. Tests: readiness property test reshaped + the AC4
   assertion.
6. **runtime.rs**: delete coordinator/cache statics, `OPEN_REDIRECT`,
   `redirect_dance_bank_open`, `convert_streaming_xwb`, `drain_maintenance_once`,
   `detect_platform`, `warn_refused_once`, `now_unix_ms`; `init` signature shrinks.
7. **file_hooks.rs**: remove `song_rate_open_redirect` + both call sites, the
   `convert_streaming_xwb` seam + `convert_generated_into_dest`; keep
   `resolve_native_via_original` only if still referenced (it will not be — delete).
8. **lib.rs / config.rs / mod-config.json**: readiness + init call sites; drop
   `cache_limit_gib` field; drop the `song_playback_speed` JSON block.
9. **Validator** (in place; full line map in the explore record):
   - Preconditions L90/L96: `transform.rs`→`virtual_bank.rs`; `conversion*`→
     `binding*`; drop `tests.rs`, `model.rs`, `cache/worker` expectations.
   - Imports L290–309: drop cache/worker/model/conversion uses; add
     `virtual_bank`, `binding::{dance_bank_song_code…}` if needed.
   - Report: drop `cache`/`on_demand` fields + structs (L334–409), `cache_dir`
     plumbing (arg 6; L497/509/614/616), `overall_pass` legs, assembly lines,
     memory thresholds tied to `transform::DEFAULT_MEMORY_LIMIT_BYTES`.
   - Add harness-local `transform_bank(source, code, percent)` on surviving
     primitives; rework `validate_rate`/`validate_corpus` onto it (same checks minus
     the dead admission legs); `RateResult`/`CorpusRateResult` lose
     `peak_memory_bytes`.
   - Delete cache backends (L1229–1335), `validate_cache` + demos (L1337–1798),
     on_demand block (L1899–2183); harness `cargo run` arg list shrinks (L2267);
     python heredoc: drop cache (L2277–2318) + on_demand (L2324–2364) blocks, keep
     identity_runtime.
10. Gates in order; fix fallout until green.

## Risks / notes

- Biggest risk: the validator rework (bash-heredoc Rust, ~200 lines touched). The
  harness `cargo test` phase gives fast feedback before the release run.
- `stretch_interleaved` minimum-input constraints (window+hop) bound the synthetic
  fixtures — the existing fixtures (8192/1024 frames @8 kHz) already satisfy them.
- Windows 0-warning gate: watch for orphaned imports after each deletion.
