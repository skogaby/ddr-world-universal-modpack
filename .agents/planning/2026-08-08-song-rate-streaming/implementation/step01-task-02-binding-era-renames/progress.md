# Context & Plan — task-02 binding-era-renames-and-identity-assertions

Status: Approved 2026-08-09 (auto mode — verified upstream approvals as task-01:
plan + design Approved 2026-08-08, task Generated By code-task-generator)

Builds on the green tree task-01 produced (see
`../step01-task-01-retire-cache-model/progress.md`). Same gates, same conventions.

## Requirements digest

1. lifecycle.rs: `Preparing`→`Binding`; delete `RedirectReady`, `begin_exposing`,
   `mark_redirect_ready`; `begin_preparing`→`begin_binding`. Machine:
   `Armed → Binding → XactInFlight → Committed/LateFailed`, `Binding → EarlyFailed`,
   `Committed → XactInFlight` (Quick-Restart re-create). CAS semantics unchanged.
2. xact_runtime.rs: `MaintenanceKind::Quarantine` → `ReclaimBinding` (task-01 left
   Quarantine as the sole survivor precisely for this rename); queue mechanics
   untouched.
3. Identity-base assertions:
   - wavebank_hook_tests: the `binding` readiness leg is false and forces
     `is_ready() == false` even with every other leg true (Step 4 must flip it
     deliberately via `binding::integration_available`).
   - availability_tests (the custom-options registry/api mini-module): model the
     `SongPlaybackSpeedMod::enable()` gate — readiness false ⇒ no `song_speed`
     row registered.
4. Vocabulary sweep (`open-redirect`, exposure-seam phrasing, `RedirectReady`,
   generated-path phrasing) in surviving song_rate sources' comments/log strings.
   Retained on purpose: `RedirectToken` (the design's Step-4 spec keeps the name),
   `XactSlotPhase::Exposed/Quarantined` (slot-phase vocabulary, not lifecycle),
   historical references in the BankTimeline investigation notes.
5. No behavior change; mechanical test updates only.

## Test scenarios

- AC1: `rg "RedirectReady|begin_exposing|mark_redirect_ready" src/` → no hits;
  lifecycle_tests exercise `Armed → Binding → XactInFlight` with the same CAS
  assertions (mechanically rewritten chains: `begin_binding` then `mark_exposed`).
- AC2: the two new host tests above fail if Step 4's flip happens accidentally
  (readiness leg) or if a row registers while unready.
- AC3: all five gates green; report still carries no cache/on-demand sections.

## Checklist

- [x] 1. lifecycle.rs renames + consumer/test fallout
- [x] 2. MaintenanceKind rename + fallout
- [x] 3. identity-base assertions (wavebank_hook_tests + availability_tests)
- [x] 4. vocabulary sweep
- [x] 5. gates green

## Record

- 2026-08-09: implemented on task-01's tree; all five gates green
  (validator 114/114 — 111 + the 2 identity-base assertions + 1 new
  `Binding → EarlyFailed` transition test; se-bank ALL CHECKS PASSED;
  windows check 0 warnings; fmt `--check` clean; release build OK).
  Final sweep: `RedirectReady`/`begin_exposing`/`mark_redirect_ready` — zero
  hits in `src/`; no cache/lease/deadline/admission references anywhere in
  song_rate or core/xact. The harness-side `QUICK_FAIL_TAINT` dead-static
  warning is pre-existing (present in task-01's green log too).

## Deviations

- **No commit** — same handoff rule as task-01 (maintainer commits personally).
- `mark_exposed`/`mark_reexposed` keep their names: the task's rename list is
  deliberate and exhaustive; the "exposed" vocabulary survives in the slot-phase
  domain (`XactSlotPhase::Exposed`), which the design's Step-4 transaction spec
  still uses. Their docs now describe the binding-era transitions.
- `wavebank_hook::identity_conversion_path` kept (the validator's
  `identity_no_dynamic_redirect` check consumes it); its doc now describes it as
  the pin on the DELETED LayeredFS seam.

Status: Complete (uncommitted — maintainer commits personally per handoff instruction)
