# Context — Step 3 task-03: The Validator's Replay Legs (Step 3 Demo)

Date: 2026-08-09. Mode: auto. Working records in the planning dir.

## Approval chain (verified — same as siblings)

Task carries `Generated By: code-task-generator 2026-08-09`; Source Plan and
design both `Status: Approved 2026-08-08`. Required reading done: design
Testing Strategy (streaming-section bullets + synthetic engine replay) and
the RE appendix; step02-task-03 progress (how the streaming section was
built in place).

## Requirements (task file, normalized)

R1. `StreamingReport` gains replay results (shape free; recommended
    `replays: Vec<…>` with percent, entry order, packet count, reassembled
    length, reparse / byte-equality-vs-oracle / decode-equality booleans,
    `passed`), each also emitted as `check("streaming_replay_{percent}", …)`.
R2. The release-run pump mirrors task-02's read pattern COMPACTLY (approved
    at breakdown: a re-derivation in main.rs, never a shared crate helper):
    0x1000 header read at 0, sequential block-align-rounded 64 KiB packets,
    EOF clamp, served through `plan_virtual_bank`/`resolve` with entry
    bytes from a `StretchState` + `encode_block` feed over `BlockCachePcm`.
R3. Legs: 50% and 175% on the existing synthetic fixture; at least one leg
    uses the preview-first (reverse) fixture `main()` already builds.
R4. Pass per leg: reassembled bytes byte-equal `transform_bank`'s output
    for the same source/percent; `parse_song_bank` accepts them; the main
    entry decodes equal to the oracle's; `streaming.passed` (→
    `overall_pass`) conjoins via the existing checks conjunction.
R5. No schema/version discriminator; surviving sections and Step-2
    streaming legs untouched; throughput stays informational.
R6. Python heredoc needs NO structural change (verified by reading it: the
    streaming leg already fails on any failing `streaming.checks[]` entry);
    spot-check with a mutated report COPY, never committed.

## Key harness facts (from reading the script)

- `validate_streaming(source)` currently takes only the main-first fixture;
  `main()` builds `reverse_order` too → signature grows a second parameter.
- `StreamingReport { passed, synthetic_frames_per_second, rates, checks }`;
  `passed = rates.all && checks.all`; `overall_pass` conjoins
  `streaming.passed`; python gate: section present + `passed` +
  every `checks[].passed`.
- Available pieces: `transform_bank` (the whole-buffer oracle),
  `drive_streaming` (StretchState-over-BlockCachePcm produce loop),
  `virtual_bank::plan_virtual_bank`/`resolve` (task-01), `check()` console
  helper. `adpcm::encode_block` reachable via the mounted module.
- **Heredoc hazard**: the `main.rs` heredoc is UNQUOTED — no backticks, no
  dollar signs in inserted Rust (bit Step 1 once).

## Interpretation & assumptions (auto mode)

- Compact feed: `drive_streaming` (whole stretched PCM through
  `StretchState` over `BlockCachePcm`, chunk 997) then `encode_block` per
  whole block into the entry's byte stream — satisfies R2's pipeline
  naming; the pull-driven on-demand variant is task-02's exhaustive proof.
- Leg assignment: 50% on the main-first fixture, 175% on the preview-first
  fixture (R3's "at least one").
- `decode_equality` checks BOTH entries (superset of R4's "main entry").
- `packet_count` counts data-packet reads (header + defensive EOF read
  excluded); pattern fidelity (header 0x1000 served in full, packets served
  in full, nothing past `virtual_size`, EOF read serves 0) folds into the
  leg's `passed`.
- `replays.iter().all(passed)` is ALSO conjoined into `StreamingReport::
  passed` (belt-and-braces alongside the checks conjunction).

## Files to touch

- `scripts/validate_song_playback_speed.sh` ONLY: `StreamingReplayResult`
  struct + `replays` field, `replay_virtual_bank` helper, two legs wired
  into `validate_streaming` (new second parameter), `main()` call site.

## Out of scope

Crate code and tests.rs (tasks 01–02 complete), report schema string,
python heredoc structure, surviving sections, Step-2 streaming legs,
throughput. No commits. After this task: tick plan Step 3 (all three task
records `Status: Complete` + demo green).
