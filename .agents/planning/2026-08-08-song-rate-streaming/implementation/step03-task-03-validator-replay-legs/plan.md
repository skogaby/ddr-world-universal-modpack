# Plan — Step 3 task-03: The Validator's Replay Legs (Step 3 Demo)

Status: Approved 2026-08-09 (auto mode — verified upstream approval chain:
task `Generated By: code-task-generator 2026-08-09`, breakdown
maintainer-approved 2026-08-09; Source Plan + design `Status: Approved
2026-08-08`)

All edits in `scripts/validate_song_playback_speed.sh` (the harness main.rs
heredoc — UNQUOTED: no backticks / dollar signs in inserted Rust).

## Edits

1. `StreamingReplayResult` struct (`percent`, `entry_order`,
   `packet_count`, `reassembled_len`, `reparsed`, `matches_reference`,
   `decode_equality`, `passed`) + `replays: Vec<StreamingReplayResult>` on
   `StreamingReport`.
2. `replay_virtual_bank(source, percent, entry_order) ->
   StreamingReplayResult` — the compact re-derivation composed from
   existing harness pieces: `parse_song_bank` → `plan_virtual_bank` →
   oracle via `transform_bank` → per-entry feed (`drive_streaming` =
   `StretchState` over `BlockCachePcm`, then `encode_block` per whole
   block; length asserted == plan `data_len`) → the RE-pinned read pattern
   through `resolve` (0x1000 header read; per entry sequential
   block-align-rounded 64 KiB packets bounded to the stream; defensive
   past-the-end read must serve 0) reassembling into a pre-zeroed
   `virtual_size` buffer → pass = pattern fidelity ∧ byte equality vs the
   oracle ∧ reparse ∧ both entries' decode equality.
3. `validate_streaming` gains a `reverse_order: &[u8]` parameter; after the
   throughput block, two legs: 50% on the main-first fixture, 175% on the
   preview-first fixture; each emits
   `check("streaming_replay_{percent}", …)` (console PASS/FAIL — the step
   demo statement) and pushes into `replays`; `passed` additionally
   conjoins `replays.iter().all(passed)`.
4. `main()`: `validate_streaming(&source, &reverse_order)`.

## Verification

- Full validator run: console shows PASS `streaming_replay_50` and
  `streaming_replay_175`; report inspected (replays present + passed,
  `overall_pass` true, schema `song-rate-validation/v1`, no cache/on_demand
  keys, Step-2 legs + throughput untouched).
- Python-gate spot-check on mutated report COPIES in /tmp (failing replay
  check → exit nonzero; not committed).
- Full standing gates.

## Risks / notes

- The python heredoc needs no change (already gates all
  `streaming.checks[]`) — R6 verified by reading it, then spot-checked.
- The oracle stretches both fixture entries per leg in the release build —
  cheap at the 8,192-frame fixture (the Step-2 matrix already runs six
  rates).
