# Context — Step 7 task-01: Documentation Rewrite and Leftover Sweep

Task file: `.agents/tasks/song-rate-streaming/step07/task-01-documentation-rewrite-and-sweep.code-task.md`
Mode: auto (code-assist). Approval chain verified: task `Generated By:
code-task-generator 2026-08-11` (breakdown approved by maintainer in-session
2026-08-11); Source Plan + design both `Status: Approved 2026-08-08`.

Docs-only change set (no `src/`/`scripts/` edits). Authoritative source =
the feature `progress.md` Done entries + the step05/step06 working records,
never the pre-pivot design text.

## Edit list (surveyed)

1. **README.md**
   - Row 44 (feature table): retired model (on-demand generation, 30 s
     bound, admission, disk cache, `cache_limit_gib` link) → streaming
     truth + preview passthrough + Step-6 mentions.
   - Embedded mod-config example (~line 169): remove the
     `"song_playback_speed": { "cache_limit_gib": 10 }` block.
   - §"Song Playback Speed cache (`song_playback_speed`)" (~245–262):
     remove; keep a small note somewhere sensible that the retired
     `cache_limit_gib`/`diagnostic` keys in old configs are ignored.
   - Check TOC/anchor links to `#song-playback-speed-cache-song_playback_speed`.
2. **AGENTS.md**
   - Row 57 (Song Playback Speed): full rewrite to the streaming
     architecture (modules, two-region serving + preview passthrough, Q31
     commit order, score containment, Step-6 integrations incl.
     `tick_domain`/`real_speed`/`csv_rate_cells`, validator sections now
     {synthetic, streaming, corpus, identity_runtime, replays}, no config
     keys).
   - Row 54 (Assist tick): `TICK_CAPACITY_MS` = 1200 s wall (~28.9 MB),
     rate-aware FR-3 formula via `song_rate::tick_domain` + the
     AwaitAnchor-latched snapshot, converted restart skips, scaffold gone.
   - Line ~186 custom instruction: song-rate movie suppression is LIVE.
3. **docs/xact_streaming_research.md**: append implementation-findings
   section (preview passthrough + ~2.4× realtime WSOLA measurement,
   two-region serving, parser-rule emission contract).
4. **docs/song_playback_speed.md**: supersession banner; targeted fixes
   where retired behavior is stated as current; append dated "Rate-aware
   Real Speed (2026-08-11)" RE section (from step06-task-02 context.md).
5. **Design**: dated inline amendment notes at "Both entries are stretched
   at the same percent" (~430) + any sibling found by searching
   "stretch"/"preview" (req 12–14 neighborhood).
6. **Sweep**: `grep -rn cache_limit_gib` excluding `.agents/` ⇒ 0;
   `mod-config.json` clean (verified pre-edit — assert only).

## Gates

Same five as every task; docs-only, so the code gates prove the tree still
builds unchanged.
