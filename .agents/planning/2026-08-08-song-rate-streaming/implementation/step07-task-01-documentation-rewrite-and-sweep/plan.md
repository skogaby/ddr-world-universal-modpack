# Plan — Step 7 task-01: Documentation Rewrite and Leftover Sweep

Status: Approved 2026-08-11 (auto — verified upstream chain: task generated by
code-task-generator from the Approved 2026-08-08 plan/design; Step-7 breakdown
maintainer-approved 2026-08-11 incl. the four documentation-style decisions)

Docs-only. The edit list and splice points live in context.md. "Test
scenarios" for a docs task = the acceptance greps:

1. `grep -rn cache_limit_gib --exclude-dir=.agents .` ⇒ 0 hits.
2. `grep -n "on demand\|30 s\|admission" README.md` row 44 ⇒ no
   retired-model claims.
3. `grep -n "300 s\|300_000" AGENTS.md` (assist-tick row) ⇒ updated to
   1200 s.
4. `grep -n "identity-only Step 3" AGENTS.md` ⇒ 0 hits.
5. Design: every "both entries stretched" statement carries a dated
   amendment note with the original text intact.
6. `mod-config.json` carries no `song_playback_speed` block (assert only —
   already clean).
7. All five standing gates green (docs-only ⇒ proves the tree unchanged).

Implementation order: README (row → example → config section) → AGENTS
(rows 57, 54, line 186) → docs/xact_streaming_research.md append →
docs/song_playback_speed.md banner + RE section → design amendments →
sweep greps → gates.
