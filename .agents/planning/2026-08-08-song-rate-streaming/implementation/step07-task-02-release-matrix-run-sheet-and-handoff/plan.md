# Context + Plan — Step 7 task-02: Release-Matrix Run Sheet and Final Build Handoff

Status: Approved 2026-08-11 (auto — verified upstream chain: task generated by
code-task-generator from the Approved 2026-08-08 plan/design; Step-7 breakdown
maintainer-approved 2026-08-11, incl. the settled handoff semantics: this task
completes at handoff; the plan Step 7 tick waits for the maintainer's matrix)

Single combined context+plan record (the task is a run-sheet composition +
gate re-run + build; there is no code design to separate).

## Inputs

- Design req 42 (release requirements) + plan Step 7's matrix guidance.
- Step-5 deploy log (feature progress.md): benchmark PASSED; Quick Restart,
  score containment, 100 % literal stock, and the fault-injection
  silence-fill already carry FINAL-BUILD live evidence → re-confirmation
  legs.
- Step-6 records: tick-alignment oracle terms (synthesis INFO `rate={}%`),
  Real Speed oracle (INFO `song_rate/real_speed: side {} multiplier {}
  (target {} core {} at {}%)`; velocity comparison both fix states), PUS
  CSV cells.

## Work

1. Append "Deploy #5 — release matrix (PENDING maintainer run)" to the
   feature progress.md Deploy & test log: one leg per line with setup,
   pass/fail oracle, and log evidence to capture; re-confirmation legs
   marked. Update NEXT ACTION to the maintainer's matrix run; state
   explicitly that plan Step 7 stays unticked until it passes.
2. Re-run all five gates on the final tree (task-01's doc edits included);
   logs in this task's `logs/`.
3. Handoff note in progress.md: DLL path
   `target/x86_64-pc-windows-msvc/release/ddr_world_hook.dll`, deployment
   route `scripts/deploy.sh` (maintainer-run; never deployed by the agent).

## Acceptance mapping

- AC-1 (executable by the maintainer alone): every leg self-contained.
- AC-2 (release-ready): five gates green in this task's logs.
- AC-3 (nothing over-claimed): Step 7 checkbox untouched; the record and
  the feature NEXT ACTION say why.
