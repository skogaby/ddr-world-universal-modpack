# Context — Step 2 task-03: The Validator's Streaming Report Section (First Legs)

Date: 2026-08-09. Mode: auto. Working records in the planning dir.

## Approval chain (verified — same as siblings)

Task carries `Generated By: code-task-generator 2026-08-09`; Source Plan and
design both `Status: Approved 2026-08-08`. Required reading done: design
Testing Strategy (in-place validator update, streaming-section bullets,
informational throughput); step01-task-01 progress.md Deviations (heredoc
gotcha, in-place report evolution).

## Requirements (task file)

R1. `streaming` field on the harness `Report`: `StreamingReport { passed,
    per-rate results across 25/50/75/100/125/175 (byte-equality vs reference,
    chunking-independence, checkpoint-restore, loop shape exercised),
    informational synthetic_frames_per_second, checks: Vec<Check> }`.
R2. Driven from the release-run `main()` with the crate's real mounted
    sources over the existing synthetic bank fixtures.
R3. `overall_pass` conjoins `streaming.passed`; throughput excluded from
    every pass criterion.
R4. Python schema heredoc gains a streaming presence + passed + all-checks
    leg (identity_runtime shape).
R5. No schema/version discriminator; surviving sections untouched.
R6. Console `PASS/FAIL name: detail` lines (the existing `check()` helper).

## Key facts

- The harness `main.rs` heredoc is UNQUOTED: no backticks, no dollar signs in
  inserted Rust (Step 1's documented gotcha).
- The synthetic fixture (`build_source`) is a strict-profile bank whose
  entries carry FULL-ENTRY loops (loop_start 0, loop_length = duration) —
  exactly the production shape. Task-02's discovery binds here: 25%/50% only
  succeed WITH such a loop (NoCandidate otherwise), so the release matrix
  exercises the production `full-entry` loop shape per rate, derived through
  the REAL planning path (`virtual_bank::plan_entry` → output frames + mapped
  LoopContext), with the source served through the REAL on-demand view
  (`adpcm::BlockCachePcm`).
- Guidance-vs-requirement conflict resolved in requirement's favor: the task's
  Implementation Approach says "one checkpoint-restore leg", but Technical
  Requirement 1 specifies checkpoint-restore PER RATE — cheap in a release
  build, so per rate it is.
- `measure_operation` returns latency in whole ms — the workload is repeated
  enough times to be measurable and the divisor clamps to ≥ 1 ms (also keeps
  the f64 finite for serde_json).
- Existing harness imports reach `adpcm`, `stretch`, `virtual_bank`, `xwb`;
  the `SourcePcm` trait must be brought into scope for `frames()` calls.

## Files to touch

- `scripts/validate_song_playback_speed.sh` ONLY (Report struct + new structs
  + `validate_streaming()` + `main()` wiring + python leg).

## Out of scope

Crate code (tasks 01/02 complete), report schema string, surviving sections.
No commits.
